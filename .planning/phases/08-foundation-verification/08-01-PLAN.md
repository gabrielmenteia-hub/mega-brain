---
phase: 08-foundation-verification
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - mis/tests/test_base_scraper.py
  - mis/tests/test_health_monitor.py
  - mis/base_scraper.py
  - mis/health_monitor.py
  - mis/config.yaml
  - mis/scanner.py
  - .planning/phases/01-foundation/VERIFICATION.md
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-03
  - FOUND-04

must_haves:
  truths:
    - "BaseScraper aceita proxy_list e seleciona aleatoriamente por request — _select_proxy() retorna None quando lista vazia, proxy diferente para requests diferentes quando lista tem 3+ proxies"
    - "run_schema_integrity_check(db_path) retorna True quando 5 tabelas existem, False quando alguma falta, nunca propaga exceção"
    - "VERIFICATION.md da Phase 1 existe atualizado com FOUND-02 e FOUND-04 marcados SATISFIED com evidência das novas implementações"
    - "148 testes existentes continuam GREEN — backward compatibility garantida (proxy_url ainda funciona)"
  artifacts:
    - path: "mis/base_scraper.py"
      provides: "BaseScraper com proxy_list + _select_proxy() + per-request rotation via temporary httpx.AsyncClient"
      exports: ["BaseScraper"]
    - path: "mis/health_monitor.py"
      provides: "run_schema_integrity_check(db_path) async function"
      exports: ["run_canary_check", "register_canary_job", "run_platform_canary", "run_schema_integrity_check"]
    - path: "mis/config.yaml"
      provides: "proxy_list: [] entry no settings block"
      contains: "proxy_list"
    - path: "mis/scanner.py"
      provides: "proxy_list passado para instanciação de scanners"
      contains: "proxy_list"
    - path: ".planning/phases/01-foundation/VERIFICATION.md"
      provides: "VERIFICATION.md re-verificado com FOUND-02 e FOUND-04 como SATISFIED"
      contains: "re_verification: true"
  key_links:
    - from: "mis/base_scraper.py BaseScraper.__init__"
      to: "proxy_list parameter"
      via: "Optional[list[str]] = None kwarg, backward compatible com proxy_url"
      pattern: "proxy_list.*Optional.*list"
    - from: "mis/base_scraper.py fetch()"
      to: "temporary httpx.AsyncClient per-request"
      via: "_select_proxy() + async with httpx.AsyncClient(proxy=proxy)"
      pattern: "_select_proxy"
    - from: "mis/health_monitor.py run_schema_integrity_check"
      to: "sqlite_master table check"
      via: "sqlite3.connect(db_path) + SELECT name FROM sqlite_master WHERE type='table'"
      pattern: "sqlite_master"
    - from: "mis/scanner.py run_all_scanners"
      to: "BaseScraper subclasses"
      via: "proxy_list=settings.get('proxy_list', [])"
      pattern: "proxy_list.*settings"
---

<objective>
Fechar os gaps FOUND-02 e FOUND-04 identificados no audit v1.0: implementar proxy rotation no BaseScraper e schema integrity canary no health_monitor, e re-verificar a Phase 1 com evidência atualizada.

Purpose: O audit v1.0 identificou que REQUIREMENTS.md marca FOUND-02 e FOUND-04 como incompletos. Este plano implementa o que falta e produz o VERIFICATION.md da Phase 1 com status real, fechando o gap retroativamente.

Output:
- BaseScraper com proxy_list + rotacao aleatoria por request
- run_schema_integrity_check(db_path) no health_monitor
- config.yaml com proxy_list: []
- scanner.py passando proxy_list na instanciacao
- .planning/phases/01-foundation/VERIFICATION.md re-escrito com evidencia
- 4 novos testes (2 proxy rotation + 2 schema integrity) GREEN
- 148 testes existentes ainda GREEN
</objective>

<execution_context>
@C:/Users/Gabriel/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Gabriel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Interfaces extraidas do codebase para o executor — nao requer exploracao adicional. -->

From mis/base_scraper.py (current signature to MODIFY):
```python
class BaseScraper:
    def __init__(self, proxy_url: Optional[str] = None) -> None:
        self._proxy = proxy_url
        self._client: Optional[httpx.AsyncClient] = None

    async def __aenter__(self) -> "BaseScraper":
        self._client = httpx.AsyncClient(
            http2=True, follow_redirects=True,
            timeout=httpx.Timeout(30.0), proxy=self._proxy
        )

    async def fetch(self, url: str) -> str:
        domain = httpx.URL(url).host
        delay = DOMAIN_DELAYS.get(domain, DEFAULT_DELAY)
        @retry(stop=stop_after_attempt(3), wait=wait_exponential(...), reraise=True)
        async def _do_fetch() -> str:
            async with self._get_semaphore(domain):
                headers = self._build_headers()
                response = await self._client.get(url, headers=headers)
                ...
```

From mis/health_monitor.py (current state — run_platform_canary pattern to COPY for new function):
```python
async def run_platform_canary(db_path: str, platform_id: int, ...) -> bool:
    import sqlite3
    try:
        with sqlite3.connect(db_path) as conn:
            row = conn.execute("SELECT MAX(updated_at) ...").fetchone()
        ...
    except Exception as exc:
        log.error("...", alert="platform_data_stale", error=str(exc))
        return False
```

From mis/scanner.py (lines 190-205 — where proxy_url is read from config):
```python
proxy_url: Optional[str] = settings.get("proxy_url") or None
# ...
async with scanner_cls(proxy_url=proxy_url) as scanner:
```

From mis/config.yaml settings block (current, to ADD proxy_list):
```yaml
settings:
  proxy_url: ""
  request_delay_s: 2.0
  # ... outros campos
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — Escrever 4 testes RED antes da implementacao</name>
  <files>mis/tests/test_base_scraper.py, mis/tests/test_health_monitor.py</files>
  <behavior>
    - test_proxy_rotation_selects_from_list: instancia BaseScraper(proxy_list=["http://p1:8080", "http://p2:8080", "http://p3:8080"]), chama _select_proxy() 20x, coleta resultados em set(), verifica len(selected) > 1 (distribuicao aleatoria com 3 proxies em 20 draws)
    - test_proxy_rotation_no_proxy_returns_none: instancia BaseScraper() sem argumentos, chama _select_proxy(), verifica retorno is None
    - test_schema_integrity_check_ok: cria DB temporario com as 5 tabelas (products, platforms, niches, pains, dossiers) via sqlite3, chama await run_schema_integrity_check(db_path), verifica result is True
    - test_schema_integrity_check_missing_table: cria DB com apenas tabela "products", chama await run_schema_integrity_check(db_path), verifica result is False
  </behavior>
  <action>
    Adicionar os 4 testes ao final dos respectivos arquivos de teste existentes.

    Para test_base_scraper.py, adicionar no final (apos test_client_closed_on_exit):
    ```python
    @pytest.mark.asyncio
    async def test_proxy_rotation_selects_from_list():
        """Verifica que proxy_list com 3 proxies resulta em selecao aleatoria diversa."""
        proxies = ["http://proxy1:8080", "http://proxy2:8080", "http://proxy3:8080"]
        scraper = BaseScraper(proxy_list=proxies)
        selected = set()
        for _ in range(20):
            p = scraper._select_proxy()
            selected.add(p)
        assert len(selected) > 1, "Esperava mais de 1 proxy unico em 20 draws"

    @pytest.mark.asyncio
    async def test_proxy_rotation_no_proxy_returns_none():
        """Verifica que sem proxy_list, _select_proxy retorna None."""
        scraper = BaseScraper()
        assert scraper._select_proxy() is None
    ```

    Para test_health_monitor.py, adicionar import de sqlite3 e run_schema_integrity_check no topo, depois adicionar no final:
    ```python
    import sqlite3
    from mis.health_monitor import run_schema_integrity_check

    @pytest.mark.asyncio
    async def test_schema_integrity_check_ok(tmp_path):
        """Verifica retorno True quando todas as 5 tabelas existem."""
        db_path = str(tmp_path / "mis.db")
        with sqlite3.connect(db_path) as conn:
            for table in ["products", "platforms", "niches", "pains", "dossiers"]:
                conn.execute(f"CREATE TABLE {table} (id INTEGER PRIMARY KEY)")
        result = await run_schema_integrity_check(db_path)
        assert result is True

    @pytest.mark.asyncio
    async def test_schema_integrity_check_missing_table(tmp_path):
        """Verifica retorno False quando alguma tabela esta ausente."""
        db_path = str(tmp_path / "mis.db")
        with sqlite3.connect(db_path) as conn:
            conn.execute("CREATE TABLE products (id INTEGER PRIMARY KEY)")
            # Ausentes: platforms, niches, pains, dossiers
        result = await run_schema_integrity_check(db_path)
        assert result is False
    ```

    VERIFICAR que os testes FALHAM (RED) antes de prosseguir para a Task 2. Executar:
    `cd /c/Users/Gabriel/MEGABRAIN && python -m pytest mis/tests/test_base_scraper.py::test_proxy_rotation_selects_from_list mis/tests/test_base_scraper.py::test_proxy_rotation_no_proxy_returns_none mis/tests/test_health_monitor.py::test_schema_integrity_check_ok mis/tests/test_health_monitor.py::test_schema_integrity_check_missing_table -v --timeout=10 2>&1 | tail -20`

    Os 4 testes devem falhar com AttributeError ou ImportError confirmando RED.
  </action>
  <verify>
    <automated>cd /c/Users/Gabriel/MEGABRAIN && python -m pytest mis/tests/test_base_scraper.py::test_proxy_rotation_selects_from_list mis/tests/test_base_scraper.py::test_proxy_rotation_no_proxy_returns_none mis/tests/test_health_monitor.py::test_schema_integrity_check_ok mis/tests/test_health_monitor.py::test_schema_integrity_check_missing_table -v --timeout=10 2>&1 | tail -20</automated>
  </verify>
  <done>4 novos testes existem nos arquivos e FALHAM — AttributeError em _select_proxy e ImportError/AttributeError em run_schema_integrity_check confirmam estado RED</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wave 1 — Implementar proxy rotation no BaseScraper e schema integrity no health_monitor</name>
  <files>mis/base_scraper.py, mis/health_monitor.py, mis/config.yaml, mis/scanner.py</files>
  <behavior>
    GREEN criteria:
    - BaseScraper(proxy_list=["p1","p2","p3"])._select_proxy() retorna proxy da lista
    - BaseScraper()._select_proxy() retorna None
    - run_schema_integrity_check(db_path) com 5 tabelas -> True
    - run_schema_integrity_check(db_path) com tabela faltando -> False
    - Todos os 6 testes existentes de base_scraper continuam GREEN (backward compat)
    - Todos os 3 testes existentes de health_monitor continuam GREEN
  </behavior>
  <action>
    **1. mis/base_scraper.py — Adicionar proxy_list + _select_proxy() + per-request rotation**

    Modificar __init__ para aceitar proxy_list:
    ```python
    def __init__(
        self,
        proxy_url: Optional[str] = None,
        proxy_list: Optional[list[str]] = None,
    ) -> None:
        # proxy_list takes precedence over proxy_url
        self._proxy_list: list[str] = proxy_list if proxy_list else (
            [proxy_url] if proxy_url else []
        )
        # Preserve single-proxy backward compat for __aenter__ client
        self._proxy: Optional[str] = proxy_url if not proxy_list else None
        self._client: Optional[httpx.AsyncClient] = None
    ```

    Adicionar metodo _select_proxy() apos __aexit__:
    ```python
    def _select_proxy(self) -> Optional[str]:
        """Seleciona aleatoriamente um proxy da lista. Retorna None se lista vazia."""
        import random
        if not self._proxy_list:
            return None
        return random.choice(self._proxy_list)
    ```

    Modificar _do_fetch() dentro de fetch() para usar temporary client quando proxy_list:
    ```python
    async def _do_fetch() -> str:
        async with self._get_semaphore(domain):
            headers = self._build_headers()
            t0 = time.monotonic()
            if self._proxy_list:
                proxy = self._select_proxy()
                async with httpx.AsyncClient(
                    http2=True,
                    follow_redirects=True,
                    timeout=httpx.Timeout(30.0),
                    proxy=proxy,
                ) as temp_client:
                    response = await temp_client.get(url, headers=headers)
                    response.raise_for_status()
                    duration_ms = int((time.monotonic() - t0) * 1000)
                    log.info("fetch.ok", url=url, status_code=response.status_code,
                             duration_ms=duration_ms, domain=domain)
                    await asyncio.sleep(delay)
                    return response.text
            else:
                response = await self._client.get(url, headers=headers)
                response.raise_for_status()
                duration_ms = int((time.monotonic() - t0) * 1000)
                log.info("fetch.ok", url=url, status_code=response.status_code,
                         duration_ms=duration_ms, domain=domain)
                await asyncio.sleep(delay)
                return response.text
    ```

    IMPORTANTE: Manter o except do try/except externo IDENTICO — apenas o _do_fetch() muda.

    **2. mis/health_monitor.py — Adicionar run_schema_integrity_check()**

    Adicionar a funcao apos run_platform_canary (antes do fim do arquivo):
    ```python
    async def run_schema_integrity_check(db_path: str) -> bool:
        """Verifica integridade do schema — todas as 5 tabelas da Phase 1 existem.

        Retorna True se schema integro, False se alguma tabela esta ausente.
        Emite alert='SCHEMA_INTEGRITY_FAILED' via structlog.
        Nunca propaga excecoes.

        Args:
            db_path: Caminho para o arquivo SQLite.
        """
        import sqlite3

        REQUIRED_TABLES = {"products", "platforms", "niches", "pains", "dossiers"}
        try:
            with sqlite3.connect(db_path) as conn:
                rows = conn.execute(
                    "SELECT name FROM sqlite_master WHERE type='table'"
                ).fetchall()
            existing = {row[0] for row in rows}
            missing = REQUIRED_TABLES - existing
            if missing:
                log.error(
                    "health.schema_integrity.failed",
                    alert="SCHEMA_INTEGRITY_FAILED",
                    missing_tables=sorted(missing),
                )
                return False
            log.info("health.schema_integrity.ok", tables=sorted(REQUIRED_TABLES))
            return True
        except Exception as exc:
            log.error(
                "health.schema_integrity.error",
                alert="SCHEMA_INTEGRITY_FAILED",
                error=str(exc),
            )
            return False
    ```

    **3. mis/config.yaml — Adicionar proxy_list ao settings block**

    No bloco settings, adicionar `proxy_list: []` logo apos `proxy_url: ""`:
    ```yaml
    settings:
      proxy_url: ""
      proxy_list: []
      request_delay_s: 2.0
      ...
    ```

    **4. mis/scanner.py — Passar proxy_list na instanciacao**

    Localizar a linha (aprox. 195):
    ```python
    proxy_url: Optional[str] = settings.get("proxy_url") or None
    ```

    Adicionar leitura de proxy_list logo abaixo:
    ```python
    proxy_url: Optional[str] = settings.get("proxy_url") or None
    proxy_list: list[str] = settings.get("proxy_list") or []
    ```

    Localizar a linha (aprox. 205):
    ```python
    async with scanner_cls(proxy_url=proxy_url) as scanner:
    ```

    Modificar para:
    ```python
    async with scanner_cls(proxy_url=proxy_url, proxy_list=proxy_list) as scanner:
    ```

    Apos todas as modificacoes, verificar GREEN:
    `cd /c/Users/Gabriel/MEGABRAIN && python -m pytest mis/tests/test_base_scraper.py mis/tests/test_health_monitor.py -v --timeout=10`

    10 testes (6 existentes + 2 novos proxy + 2 novos schema) devem PASSAR.
  </action>
  <verify>
    <automated>cd /c/Users/Gabriel/MEGABRAIN && python -m pytest mis/tests/test_base_scraper.py mis/tests/test_health_monitor.py -v --timeout=10</automated>
  </verify>
  <done>10 testes passam (6 base_scraper + 4 novos: 2 proxy rotation + 2 schema integrity). Suite completa: cd /c/Users/Gabriel/MEGABRAIN && python -m pytest mis/tests/ -v --timeout=30 retorna 152 GREEN (148 existentes + 4 novos)</done>
</task>

<task type="auto">
  <name>Task 3: Wave 2 — Re-escrever VERIFICATION.md da Phase 1 com evidencia atualizada</name>
  <files>.planning/phases/01-foundation/VERIFICATION.md</files>
  <action>
    LER o arquivo existente em .planning/phases/01-foundation/VERIFICATION.md para preservar secoes corretas (artifacts, key links, test list).

    SOBRESCREVER o arquivo com versao atualizada que:
    1. Atualiza frontmatter: verified=hoje, re_verification=true, gaps=[]
    2. Mantém os 4 Observable Truths da tabela de Goal Achievement
    3. Atualiza Test Suite Results para refletir 152 testes (148 + 4 novos)
    4. Adiciona os 4 novos testes ao bloco de Test Suite Results
    5. Atualiza Required Artifacts adicionando evidencia de proxy_list em base_scraper.py e run_schema_integrity_check em health_monitor.py
    6. Atualiza Requirements Coverage:
       - FOUND-02: "SATISFIED — BaseScraper implementa proxy_list com _select_proxy() aleatorio por request via temporary httpx.AsyncClient; config.yaml tem proxy_list: []; scanner.py passa proxy_list; 8/8 testes passam"
       - FOUND-04: "SATISFIED — run_canary_check() cobre liveness (httpbin.org/get); run_schema_integrity_check(db_path) cobre schema integrity (5 tabelas); 5/5 testes passam"
    7. Adiciona secao Human Verification Required com bloco para VERIFICATION.md + proxy rotation real
    8. Atualiza Verdict para "PASSED (re-verified 2026-03-15)"

    O documento final deve ter:
    - Frontmatter com re_verification: true, gaps: [], verified: 2026-03-15T00:00:00Z
    - Secao Goal Achievement com 4 truths
    - Test Suite Results com 152 testes
    - Required Artifacts (11 originais + 2 evidencias novas inline)
    - Key Link Verification preservada do original + 1 nova linha para schema integrity
    - Requirements Coverage FOUND-01 a FOUND-04 todos SATISFIED
    - Verdict: Phase 1 PASSED (re-verificada)
  </action>
  <verify>
    <automated>cd /c/Users/Gabriel/MEGABRAIN && python -m pytest mis/tests/ -v --timeout=30 2>&1 | tail -5</automated>
  </verify>
  <done>
    .planning/phases/01-foundation/VERIFICATION.md existe com:
    - re_verification: true no frontmatter
    - gaps: [] no frontmatter
    - FOUND-02 e FOUND-04 marcados SATISFIED com evidencia de proxy_list e run_schema_integrity_check
    - Test Suite Results mostrando 152 testes GREEN
    - Verdict: PASSED
  </done>
</task>

</tasks>

<verification>
Suite completa GREEN apos todas as tasks:
```
cd /c/Users/Gabriel/MEGABRAIN && python -m pytest mis/tests/ -v --timeout=30
```
Esperado: 152 passed (148 existentes + 4 novos), 0 failed.

Backward compatibility:
- BaseScraper(proxy_url="http://single:8080") ainda funciona (proxy_list=[proxy_url] internamente)
- BaseScraper() sem argumentos ainda funciona
- Todos os scanners (HotmartScanner, ClickBankScanner, KiwifyScanner) continuam funcionando

VERIFICATION.md atualizado:
- re_verification: true
- FOUND-02 SATISFIED com evidencia de proxy_list
- FOUND-04 SATISFIED com evidencia de run_schema_integrity_check
</verification>

<success_criteria>
1. 4 novos testes existem e passam: test_proxy_rotation_selects_from_list, test_proxy_rotation_no_proxy_returns_none, test_schema_integrity_check_ok, test_schema_integrity_check_missing_table
2. 148 testes existentes continuam GREEN — nenhuma regressao
3. BaseScraper aceita proxy_list=[...] e _select_proxy() retorna proxy aleatorio da lista
4. run_schema_integrity_check(db_path) retorna True/False conforme tabelas presentes
5. config.yaml tem proxy_list: [] no settings block
6. scanner.py passa proxy_list=settings.get("proxy_list", []) na instanciacao
7. .planning/phases/01-foundation/VERIFICATION.md sobrescrito com re_verification: true e todos 4 requirements SATISFIED
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/08-foundation-verification/08-01-SUMMARY.md` com:
- O que foi implementado (proxy rotation, schema integrity)
- Evidencia de testes (152 GREEN)
- Decisoes tomadas (temporary httpx.AsyncClient por request, sqlite_master query pattern)
- Link para VERIFICATION.md atualizado
</output>
