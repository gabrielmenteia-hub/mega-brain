---
phase: 20-niche-data-model
plan: 02
type: execute
wave: 2
depends_on:
  - 20-01
files_modified:
  - mis/migrations/_008_niche_v3.py
  - mis/niche_repository.py
  - mis/db.py
autonomous: true
requirements:
  - NICHE-01
  - NICHE-02
must_haves:
  truths:
    - "Banco contém exatamente 4 nichos (Relacionamento, Saúde, Finanças, Renda Extra) após run_migrations"
    - "Banco contém >= 42 subnichos com IDs fixos distribuídos pelos 4 nichos"
    - "Cada subnicho tem slug de busca mapeado por plataforma (pelo menos Hotmart, ClickBank, Kiwify, Braip, Udemy, Gumroad, AppSumo) via subniche_platform_slugs"
    - "get_platform_slug(db_path, 201, 'clickbank') retorna 'health'"
    - "Migration é idempotente — segunda execução não duplica dados nem levanta exceção"
    - "Dados v1/v2 (products, pains com niche_id legado) intactos após migration"
    - "run_migrations() em db.py inclui _008 na cadeia"
  artifacts:
    - path: "mis/migrations/_008_niche_v3.py"
      provides: "Migration com 3 tabelas + seed data completo (42 subnichos, slugs por plataforma)"
      exports: ["run_migration_008"]
    - path: "mis/niche_repository.py"
      provides: "Query layer: list_niches, list_subniches, get_platform_slug"
      exports: ["list_niches", "list_subniches", "get_platform_slug"]
    - path: "mis/db.py"
      provides: "run_migrations() atualizado com _008 na cadeia"
      exports: ["run_migrations", "get_db"]
  key_links:
    - from: "mis/db.py"
      to: "mis/migrations/_008_niche_v3.py"
      via: "import + chamada em run_migrations()"
      pattern: "_run_008"
    - from: "mis/niche_repository.py"
      to: "subniche_platform_slugs JOIN platforms"
      via: "query SQL com JOIN"
      pattern: "subniche_platform_slugs"
---

<objective>
Implementar a migration _008 com seed data completo (42 subnichos + slugs por plataforma), o niche_repository.py com as 3 funções de query, e wiring em db.py — até todos os testes RED do Plan 01 virarem GREEN.

Purpose: Este é o entregável central da Phase 20. Após este plano, o banco estará populado com a hierarquia nicho/subnicho que Phase 21 (Manual Search Engine) vai consumir.
Output: mis/migrations/_008_niche_v3.py, mis/niche_repository.py, mis/db.py atualizado; 15 testes GREEN.
</objective>

<execution_context>
@C:/Users/Gabriel/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Gabriel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-niche-data-model/20-RESEARCH.md
@.planning/phases/20-niche-data-model/20-01-SUMMARY.md

<interfaces>
<!-- Padrão de migration existente (mis/migrations/_006_v2_platforms.py) -->
```python
import sqlite_utils

def run_migration_006(db_path: str) -> None:
    db = sqlite_utils.Database(db_path)
    # ... criação idempotente de colunas ...
    for pid, name, slug, base_url, rank_type in _PLATFORMS:
        db.execute(
            "INSERT OR IGNORE INTO platforms (id, name, slug, base_url, rank_type, created_at) "
            "VALUES (?, ?, ?, ?, ?, ?)",
            (pid, name, slug, base_url, rank_type, created_at),
        )
    db.conn.commit()
```

<!-- Padrão de db.py existente — cadeia de migrations -->
```python
from .migrations._007_is_stale import run_migration_007 as _run_007

def run_migrations(db_path: str) -> None:
    _run_001(db_path)
    _run_002(db_path)
    _run_003(db_path)
    _run_004(db_path)
    _run_005(db_path)
    _run_006(db_path)
    _run_007(db_path)
    # _run_008(db_path)  <- adicionar aqui
```

<!-- Platform IDs fixos (mis/platform_ids.py) -->
```python
HOTMART_PLATFORM_ID = 1      # hotmart
CLICKBANK_PLATFORM_ID = 2    # clickbank
KIWIFY_PLATFORM_ID = 3       # kiwify
EDUZZ_PLATFORM_ID = 4        # eduzz (sem slug — checkout only)
MONETIZZE_PLATFORM_ID = 5    # monetizze (sem slug — 403 sem login)
PERFECTPAY_PLATFORM_ID = 6   # perfectpay (sem slug — checkout only)
BRAIP_PLATFORM_ID = 7        # braip
PRODUCT_HUNT_PLATFORM_ID = 8 # product_hunt (usa 'trending' para todos)
UDEMY_PLATFORM_ID = 9        # udemy
JVZOO_PLATFORM_ID = 10       # jvzoo (usa category ID numerico "84" para todos)
GUMROAD_PLATFORM_ID = 11     # gumroad
APPSUMO_PLATFORM_ID = 12     # appsumo
```

<!-- Contratos exigidos pelos testes (mis/tests/test_niche_repository.py) -->
```python
def list_niches(db_path: str) -> list[dict]:
    # Retorna: [{"id": int, "name": str, "slug": str}, ...]

def list_subniches(db_path: str, niche_slug: str) -> list[dict]:
    # Retorna: [{"id": int, "name": str, "slug": str}, ...]
    # Retorna [] se niche_slug não existir

def get_platform_slug(db_path: str, subniche_id: int, platform_slug: str) -> str | None:
    # Retorna: str se mapeamento existe, None caso contrário
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration _008 com seed data completo</name>
  <files>mis/migrations/_008_niche_v3.py</files>
  <behavior>
    - run_migration_008(db_path) cria niches_v3, subniches e subniche_platform_slugs se não existirem
    - Seed insere exatamente 4 nichos com IDs fixos (1-4)
    - Seed insere exatamente 42 subnichos com IDs fixos (101-110 Relacionamento, 201-212 Saúde, 301-310 Finanças, 401-412 Renda Extra)
    - Seed insere subniche_platform_slugs apenas para plataformas com marketplace público (hotmart, clickbank, kiwify, braip, udemy, gumroad, appsumo, product_hunt, jvzoo)
    - PerfectPay (id=6), Eduzz (id=4) e Monetizze (id=5) NAO têm entradas na tabela de mapeamento
    - Segunda chamada a run_migration_008 não duplica dados (INSERT OR IGNORE) e não levanta exceção
    - db.execute("PRAGMA foreign_keys=ON") no início da função (SQLite não enforça FK por default)
  </behavior>
  <action>
    Criar mis/migrations/_008_niche_v3.py seguindo o padrão de _006_v2_platforms.py.

    **Estrutura da migration:**

    1. `PRAGMA foreign_keys=ON` no início
    2. Criar tabela `niches_v3`: id (int PK), name (str NOT NULL), slug (str NOT NULL), com índice único em slug — dentro de `if "niches_v3" not in db.table_names()`
    3. Criar tabela `subniches`: id (int PK), niche_id (int NOT NULL FK->niches_v3.id), name (str NOT NULL), slug (str NOT NULL), com índice único em (niche_id, slug) — dentro de `if "subniches" not in db.table_names()`
    4. Criar tabela `subniche_platform_slugs`: PK composta (subniche_id, platform_id), FK->subniches.id e FK->platforms.id, search_slug (str NOT NULL) — dentro de `if "subniche_platform_slugs" not in db.table_names()`
    5. INSERT OR IGNORE para os 4 nichos
    6. INSERT OR IGNORE para os 42 subnichos
    7. INSERT OR IGNORE para os slugs por plataforma
    8. db.conn.commit() no final

    **Seed data completo (_NICHES_V3) — IDs fixos:**
    ```python
    _NICHES_V3 = [
        (1, "Relacionamento", "relacionamento"),
        (2, "Saúde",          "saude"),
        (3, "Finanças",       "financas"),
        (4, "Renda Extra",    "renda-extra"),
    ]
    ```

    **Seed data completo (_SUBNICHES) — 42 subnichos com IDs fixos:**
    ```python
    _SUBNICHES = [
        # Relacionamento (niche_id=1) — 10 subnichos
        (101, 1, "Reconquista",                 "reconquista"),
        (102, 1, "Sedução masculina",            "seducao-masculina"),
        (103, 1, "Relacionamento feminino",      "relacionamento-feminino"),
        (104, 1, "Casamento e família",          "casamento-e-familia"),
        (105, 1, "Autoestima e confiança",       "autoestima-e-confianca"),
        (106, 1, "Comunicação e influência",     "comunicacao-e-influencia"),
        (107, 1, "Traição e ciúmes",             "traicao-e-ciumes"),
        (108, 1, "Divórcio e recomeço",          "divorcio-e-recomeco"),
        (109, 1, "Relacionamento à distância",   "relacionamento-a-distancia"),
        (110, 1, "Atração e linguagem corporal", "atracao-e-linguagem-corporal"),
        # Saúde (niche_id=2) — 12 subnichos
        (201, 2, "Emagrecimento",                "emagrecimento"),
        (202, 2, "Diabetes e glicemia",          "diabetes-e-glicemia"),
        (203, 2, "Ansiedade e sono",             "ansiedade-e-sono"),
        (204, 2, "Ganho de massa",               "ganho-de-massa"),
        (205, 2, "Saúde feminina",               "saude-feminina"),
        (206, 2, "Cabelo e pele",                "cabelo-e-pele"),
        (207, 2, "Dor crônica e coluna",         "dor-cronica-e-coluna"),
        (208, 2, "Tireoide e hormônios",         "tireoide-e-hormonios"),
        (209, 2, "Detox e alimentação saudável", "detox-e-alimentacao-saudavel"),
        (210, 2, "Saúde masculina",              "saude-masculina"),
        (211, 2, "Visão e audição",              "visao-e-audicao"),
        (212, 2, "Pressão alta e colesterol",    "pressao-alta-e-colesterol"),
        # Finanças (niche_id=3) — 10 subnichos
        (301, 3, "Renda extra online",           "renda-extra-online"),
        (302, 3, "Investimentos para iniciantes","investimentos-para-iniciantes"),
        (303, 3, "Sair das dívidas",             "sair-das-dividas"),
        (304, 3, "Day trade e cripto",           "day-trade-e-cripto"),
        (305, 3, "Empreendedorismo",             "empreendedorismo"),
        (306, 3, "Imóveis e renda passiva",      "imoveis-e-renda-passiva"),
        (307, 3, "Finanças para MEI",            "financas-para-mei"),
        (308, 3, "Aposentadoria e independência","aposentadoria-e-independencia"),
        (309, 3, "Concursos públicos",           "concursos-publicos"),
        (310, 3, "Importação e revenda",         "importacao-e-revenda"),
        # Renda Extra (niche_id=4) — 12 subnichos
        (401, 4, "Tráfego pago",                 "trafego-pago"),
        (402, 4, "Afiliados",                    "afiliados"),
        (403, 4, "Marketing de conteúdo",        "marketing-de-conteudo"),
        (404, 4, "Dropshipping",                 "dropshipping"),
        (405, 4, "Serviços freelancer",          "servicos-freelancer"),
        (406, 4, "Social media",                 "social-media"),
        (407, 4, "Vendas pelo WhatsApp",         "vendas-pelo-whatsapp"),
        (408, 4, "Edição de vídeo e design",     "edicao-de-video-e-design"),
        (409, 4, "Copywriting",                  "copywriting"),
        (410, 4, "Consultoria e mentoria online","consultoria-e-mentoria-online"),
        (411, 4, "YouTube e criação de conteúdo","youtube-e-criacao-de-conteudo"),
        (412, 4, "Precificação e vendas locais", "precificacao-e-vendas-locais"),
    ]
    ```

    **Seed data de slugs por plataforma (_PLATFORM_SLUGS):**
    Formato: (subniche_id, platform_id, search_slug)

    Plataformas BR com marketplace público: hotmart(1), kiwify(3), braip(7)
    Plataformas internacionais com marketplace público: clickbank(2), udemy(9), gumroad(11), appsumo(12)
    Plataformas com slug genérico fixo: product_hunt(8) usa "trending" para todos, jvzoo(10) usa "84" para todos
    Plataformas SEM mapeamento (checkout-only/403): eduzz(4), monetizze(5), perfectpay(6)

    Mapeamentos completos — usar as categorias mais próximas disponíveis por plataforma:

    **Relacionamento** (ids 101-110):
    - hotmart: "relacionamentos"
    - clickbank: "self-help"
    - kiwify: "relacionamentos"
    - braip: "cursos-online"
    - udemy: "Personal Development"
    - gumroad: "self-improvement"
    - appsumo: "productivity"
    - product_hunt: "trending"
    - jvzoo: "84"

    **Saúde** (ids 201-212):
    - hotmart: "saude-e-fitness"
    - clickbank: "health"
    - kiwify: "saude"
    - braip: "encapsulados"
    - udemy: "Health & Fitness"
    - gumroad: "health"
    - appsumo: "health-fitness"
    - product_hunt: "trending"
    - jvzoo: "84"

    **Finanças** (ids 301-310):
    - hotmart: "financas"
    - clickbank: "investing"
    - kiwify: "financas"
    - braip: "cursos-online"
    - udemy: "Finance & Accounting"
    - gumroad: "finance"
    - appsumo: "finance"
    - product_hunt: "trending"
    - jvzoo: "84"

    **Renda Extra** (ids 401-412):
    - hotmart: "marketing"
    - clickbank: "marketing"
    - kiwify: "marketing"
    - braip: "cursos-online"
    - udemy: "Marketing"
    - gumroad: "marketing"
    - appsumo: "marketing-sales"
    - product_hunt: "trending"
    - jvzoo: "84"

    Gerar a lista _PLATFORM_SLUGS iterando subniche_ids por nicho e aplicando o mapeamento correspondente.

    NAO alterar a tabela `niches` legada. NAO alterar config.yaml.
  </action>
  <verify>
    <automated>cd /c/Users/Gabriel/MEGABRAIN && python -m pytest mis/tests/test_migration_008.py -x -q</automated>
  </verify>
  <done>Todos os 7 testes de test_migration_008.py passam GREEN. python -c "import sqlite_utils; db=sqlite_utils.Database('/tmp/test_008.db'); from mis.db import run_migrations; run_migrations('/tmp/test_008.db'); print(list(db.execute('SELECT COUNT(*) FROM niches_v3')))" imprime [(4,)]</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: niche_repository.py + wiring em db.py</name>
  <files>mis/niche_repository.py, mis/db.py</files>
  <behavior>
    - list_niches(db_path) retorna lista de 4 dicts com chaves id, name, slug ordenados por id
    - list_subniches(db_path, 'saude') retorna lista com len == 12 (subnichos 201-212)
    - list_subniches(db_path, 'nicho-inexistente') retorna [] sem exceção
    - get_platform_slug(db_path, 201, 'hotmart') retorna 'saude-e-fitness'
    - get_platform_slug(db_path, 201, 'clickbank') retorna 'health'
    - get_platform_slug(db_path, 201, 'perfectpay') retorna None (plataforma sem mapeamento)
    - get_platform_slug(db_path, 9999, 'hotmart') retorna None (subnicho inexistente)
    - run_migrations() em db.py chama _run_008 depois de _run_007
  </behavior>
  <action>
    **1. Criar mis/niche_repository.py** seguindo o padrão de product_repository.py (funções puras que aceitam db_path, abrem conexão, retornam list[dict]):

    ```python
    """Query functions for the v3 niche/subniche hierarchy."""
    import sqlite_utils


    def list_niches(db_path: str) -> list[dict]:
        """Return all v3 niches ordered by id."""
        db = sqlite_utils.Database(db_path)
        rows = db.execute("SELECT id, name, slug FROM niches_v3 ORDER BY id").fetchall()
        return [{"id": r[0], "name": r[1], "slug": r[2]} for r in rows]


    def list_subniches(db_path: str, niche_slug: str) -> list[dict]:
        """Return all subniches for a given niche slug. Returns [] if niche not found."""
        db = sqlite_utils.Database(db_path)
        rows = db.execute(
            """SELECT s.id, s.name, s.slug
                 FROM subniches s
                 JOIN niches_v3 n ON n.id = s.niche_id
                WHERE n.slug = ?
                ORDER BY s.id""",
            [niche_slug],
        ).fetchall()
        return [{"id": r[0], "name": r[1], "slug": r[2]} for r in rows]


    def get_platform_slug(db_path: str, subniche_id: int, platform_slug: str) -> str | None:
        """Return the search slug for a subniche on a specific platform, or None."""
        db = sqlite_utils.Database(db_path)
        row = db.execute(
            """SELECT sps.search_slug
                 FROM subniche_platform_slugs sps
                 JOIN platforms pl ON pl.id = sps.platform_id
                WHERE sps.subniche_id = ? AND pl.slug = ?""",
            [subniche_id, platform_slug],
        ).fetchone()
        return row[0] if row else None
    ```

    **2. Atualizar mis/db.py** — adicionar import e chamada de _008:
    - Adicionar import: `from .migrations._008_niche_v3 import run_migration_008 as _run_008`
    - Adicionar ao final de run_migrations(): `_run_008(db_path)`
    - Atualizar docstring de run_migrations() para incluir _008

    NAO alterar get_db(). NAO alterar nenhuma outra função.
  </action>
  <verify>
    <automated>cd /c/Users/Gabriel/MEGABRAIN && python -m pytest mis/tests/test_migration_008.py mis/tests/test_niche_repository.py -x -q</automated>
  </verify>
  <done>Todos os 15 testes (7 migration + 8 repository) passam GREEN. python -m pytest mis/tests/ -x -q também passa (suite completa, sem regressões)</done>
</task>

</tasks>

<verification>
Suite completa GREEN sem regressão:

```bash
cd /c/Users/Gabriel/MEGABRAIN && python -m pytest mis/tests/ -x -q
```

Verificação de contagem no banco:
```bash
cd /c/Users/Gabriel/MEGABRAIN && python -c "
import tempfile, os
from mis.db import run_migrations
import sqlite_utils
with tempfile.NamedTemporaryFile(suffix='.db', delete=False) as f:
    db_path = f.name
run_migrations(db_path)
db = sqlite_utils.Database(db_path)
print('nichos:', next(db.execute('SELECT COUNT(*) FROM niches_v3'))[0])
print('subnichos:', next(db.execute('SELECT COUNT(*) FROM subniches'))[0])
print('slugs mapeados:', next(db.execute('SELECT COUNT(*) FROM subniche_platform_slugs'))[0])
os.unlink(db_path)
"
```
Esperado: nichos=4, subnichos=42, slugs mapeados=378 (42 * 9 plataformas com marketplace público).
</verification>

<success_criteria>
- mis/migrations/_008_niche_v3.py existe com 42 subnichos e slugs por plataforma completos
- mis/niche_repository.py existe com list_niches, list_subniches, get_platform_slug
- mis/db.py chama _run_008 no final de run_migrations()
- python -m pytest mis/tests/test_migration_008.py mis/tests/test_niche_repository.py -q mostra 15 passed
- python -m pytest mis/tests/ -q passa (zero regressões na suite completa)
- Tabela `niches` legada e produtos v1/v2 intactos
</success_criteria>

<output>
Após conclusão, criar .planning/phases/20-niche-data-model/20-02-SUMMARY.md
</output>
