---
phase: 21-manual-search-engine
plan: 02
type: tdd
wave: 2
depends_on:
  - 21-01
files_modified:
  - mis/migrations/_009_search_sessions.py
  - mis/search_repository.py
  - mis/search_orchestrator.py
  - mis/db.py
  - mis/web/app.py
autonomous: true
requirements:
  - SEARCH-01
  - SEARCH-03

must_haves:
  truths:
    - "Migration _009 cria search_sessions e search_session_products de forma idempotente"
    - "search_repository exporta todas as 7 funções com comportamentos corretos"
    - "run_manual_search é coroutine que atualiza status running/done/timeout no banco"
    - "create_app(db_path, start_scheduler=False) não inicia APScheduler"
    - "Startup hook marca sessões running como timeout antes do yield"
  artifacts:
    - path: "mis/migrations/_009_search_sessions.py"
      provides: "DDL idempotente para search_sessions + search_session_products"
      exports: ["run_migration_009"]
    - path: "mis/search_repository.py"
      provides: "CRUD de sessões de pesquisa com JSON desserializado"
      exports: ["create_session", "get_session", "update_session_status", "list_recent_sessions", "list_session_products", "delete_session", "mark_stale_running_sessions"]
    - path: "mis/search_orchestrator.py"
      provides: "run_manual_search + _TASK_REGISTRY para cancelamento"
      exports: ["run_manual_search", "register_task", "cancel_task"]
    - path: "mis/db.py"
      provides: "run_migrations encadeado com _run_009"
    - path: "mis/web/app.py"
      provides: "create_app com parâmetro start_scheduler=True (default) e startup hook de timeout"
  key_links:
    - from: "mis/web/app.py"
      to: "mis/search_repository.py"
      via: "mark_stale_running_sessions no lifespan startup"
      pattern: "mark_stale_running_sessions"
    - from: "mis/search_orchestrator.py"
      to: "mis/search_repository.py"
      via: "update_session_status chamada dentro de run_manual_search"
      pattern: "update_session_status"
    - from: "mis/search_orchestrator.py"
      to: "mis/niche_repository.get_platform_slug"
      via: "slug lookup para cada platform no SCANNER_MAP"
      pattern: "get_platform_slug"
    - from: "mis/db.py"
      to: "mis/migrations/_009_search_sessions.run_migration_009"
      via: "chamada sequencial em run_migrations()"
      pattern: "_run_009"
---

<objective>
Implementar os módulos de backend que fazem os testes RED do Plan 21-01 virarem GREEN.

Purpose: TDD GREEN — construir migration _009, search_repository, search_orchestrator e wiring no app.py/db.py contra os contratos já definidos nos testes.
Output: 5 arquivos Python implementados. Todos os 11 testes do Plan 21-01 passam.
</objective>

<execution_context>
@C:/Users/Gabriel/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Gabriel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-manual-search-engine/21-CONTEXT.md
@.planning/phases/21-manual-search-engine/21-RESEARCH.md
@.planning/phases/21-manual-search-engine/21-01-SUMMARY.md

<interfaces>
<!-- Contratos que a implementação deve satisfazer. -->

From mis/db.py (a ser modificado):
```python
def run_migrations(db_path: str) -> None:  # adicionar _run_009 no final
    _run_001(db_path)
    # ... _002 a _008 ...
    _run_009(db_path)   # NOVO

def get_db(db_path: str) -> sqlite_utils.Database:
    # já executa PRAGMA foreign_keys=ON — usar get_db() em delete_session
```

From mis/niche_repository.py:
```python
def get_platform_slug(db_path: str, subniche_id: int, platform_slug: str) -> str | None:
    # retorna None para Eduzz/Monetizze/PerfectPay (sem mapeamento em _008)
```

From mis/product_repository.py:
```python
def upsert_product(db: sqlite_utils.Database, product: "Product") -> None:
    # upsert por external_id + platform_id; retorna None; db é sqlite_utils.Database
```

From mis/scanner.py (SCANNER_MAP — adaptar, não importar diretamente):
```python
SCANNER_MAP = {
    "kiwify": KiwifyScanner,
    "hotmart": HotmartScanner,
    "clickbank": ClickBankScanner,
    "eduzz": EduzzScanner,        # get_platform_slug retorna None → skip
    "monetizze": MonetizzeScanner, # get_platform_slug retorna None → skip
    "perfectpay": PerfectPayScanner, # get_platform_slug retorna None → skip
    "braip": BraipScanner,
    "product_hunt": ProductHuntScanner,
    "udemy": UdemyScanner,
    "jvzoo": JVZooScanner,
    "gumroad": GumroadScanner,
    "appsumo": AppSumoScanner,
}
```

PITFALL CRÍTICO — niche_id legado:
```
products.niche_id é FK para tabela 'niches' (legada, v1/v2)
niches_v3 é tabela separada com mesmos IDs 1-4 (verificar durante implementação)
run_manual_search() deve construir Product com niche_id da tabela 'niches' legada
Solução: buscar niches.id via niches.id == subniches.niche_id.niches_v3_id não existe
         usar db.execute("SELECT id FROM niches WHERE id = ?", [niches_v3_id]).fetchone()
         se IDs 1-4 coincidem (provável), niche_id de niches_v3 é diretamente usável
         documentar no código: # niche_id: uses legacy niches table (same IDs 1-4 as niches_v3)
```

Schema das tabelas _009 (a ser criado):
```sql
CREATE TABLE search_sessions (
    id                INTEGER PRIMARY KEY AUTOINCREMENT,
    subniche_id       INTEGER NOT NULL REFERENCES subniches(id),
    status            TEXT    NOT NULL DEFAULT 'pending',
    platform_statuses TEXT,       -- JSON: {"hotmart": "done", "clickbank": "error"}
    started_at        TEXT,
    finished_at       TEXT,
    product_count     INTEGER DEFAULT 0
);

CREATE TABLE search_session_products (
    session_id    INTEGER NOT NULL REFERENCES search_sessions(id) ON DELETE CASCADE,
    product_id    INTEGER NOT NULL REFERENCES products(id),
    rank_at_scan  INTEGER,
    platform_slug TEXT,
    PRIMARY KEY (session_id, product_id)
);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration _009 + search_repository</name>
  <files>mis/migrations/_009_search_sessions.py, mis/search_repository.py, mis/db.py</files>
  <behavior>
    - run_migration_009: cria search_sessions + search_session_products com IF NOT EXISTS (idempotente)
    - create_session: INSERT com status='pending', started_at=now ISO, retorna id (int)
    - get_session: SELECT + json.loads(platform_statuses or '{}') → retorna dict com platform_statuses como dict
    - update_session_status: UPDATE search_sessions SET status, platform_statuses (json.dumps), product_count, finished_at
    - list_recent_sessions: SELECT ... ORDER BY started_at DESC LIMIT limit; inclui subniche_name via JOIN subniches
    - list_session_products: JOIN products + platforms; suporta platform_filter; paginação via LIMIT/OFFSET
    - delete_session: usa get_db() (PRAGMA FK=ON ativo); DELETE search_sessions WHERE id = ?; CASCADE remove search_session_products
    - mark_stale_running_sessions: UPDATE search_sessions SET status='timeout' WHERE status='running'; retorna rowcount
    - db.py: importar _run_009 e adicionar chamada no final de run_migrations()
  </behavior>
  <action>
    1. Criar mis/migrations/_009_search_sessions.py seguindo o padrão de _008_niche_v3.py:
       - Função run_migration_009(db_path: str) -> None
       - Usar sqlite3 direto (como _008) com `db.execute("PRAGMA foreign_keys=ON")`
       - Checar `table_names()` antes de CREATE TABLE
       - Não usar sqlite_utils DDL — usar `db.execute()` com SQL literal (padrão do projeto)
       - Chamar `db.conn.commit()` no final

    2. Criar mis/search_repository.py seguindo padrão de niche_repository.py:
       - Imports: `import json`, `from datetime import datetime, timezone`, `from .db import get_db`
       - `create_session`: usa get_db(); INSERT; retorna `db.execute("SELECT last_insert_rowid()").fetchone()[0]`
       - `get_session`: SELECT * FROM search_sessions WHERE id = ?; deserializar platform_statuses com json.loads
       - `update_session_status`: UPDATE com platform_statuses=json.dumps(platform_statuses); finished_at=now() se status in ("done", "timeout", "cancelled")
       - `list_recent_sessions`: JOIN subniches para ter subniche_name disponível no template; retorna dicts
       - `list_session_products`: SELECT ssp.rank_at_scan, ssp.platform_slug, p.title, p.url, p.price, p.commission_pct, p.thumbnail_url, pl.name as platform_name, pl.slug as platform_slug FROM search_session_products ssp JOIN products p ON p.id = ssp.product_id JOIN platforms pl ON pl.id = p.platform_id WHERE ssp.session_id = ?; aplicar platform_filter e LIMIT/OFFSET
       - `delete_session`: usa get_db() (garante FK CASCADE); `db.execute("DELETE FROM search_sessions WHERE id = ?", [session_id])`
       - `mark_stale_running_sessions`: UPDATE search_sessions SET status='timeout', finished_at=? WHERE status='running'; retorna `db.conn.total_changes`

    3. Modificar mis/db.py:
       - Adicionar import: `from .migrations._009_search_sessions import run_migration_009 as _run_009`
       - Adicionar no final de run_migrations(): `_run_009(db_path)`

    Rodar testes do repositório para verificar GREEN:
    `python -m pytest mis/tests/test_search_repository.py -x -q`
  </action>
  <verify>
    <automated>cd C:/Users/Gabriel/MEGABRAIN && python -m pytest mis/tests/test_search_repository.py -x -q 2>&1 | tail -10</automated>
  </verify>
  <done>Todos os 6 testes de test_search_repository.py passam (GREEN). run_migrations() inclui _009 sem erros. Arquivo _009 é idempotente (chamar duas vezes não falha).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: search_orchestrator + wiring app.py</name>
  <files>mis/search_orchestrator.py, mis/web/app.py</files>
  <behavior>
    - run_manual_search: coroutine assíncrona que chama update_session_status("running"), lança asyncio.gather com timeout 120s, persiste produtos, marca done/timeout
    - register_task / cancel_task: _TASK_REGISTRY dict em módulo; cancel_task retorna False para id desconhecido
    - create_app(db_path, start_scheduler=True): start_scheduler=False suprime todo o bloco de registro de jobs + get_scheduler().start(); startup hook mark_stale_running_sessions roda independente do valor de start_scheduler
  </behavior>
  <action>
    1. Criar mis/search_orchestrator.py:

    ```python
    """Manual search orchestrator for v3.0.

    Provides run_manual_search() that scans all platforms with a mapped slug
    for a given subniche, persisting results to search_sessions +
    search_session_products tables.

    _TASK_REGISTRY holds asyncio.Task references for cancellation via
    DELETE /search/{id}. Registry is in-memory only; server restart clears it.
    The startup hook in web/app.py marks any 'running' session as 'timeout'.
    """
    import asyncio
    import json
    import structlog
    from datetime import datetime, timezone

    _log = structlog.get_logger(__name__)
    _TASK_REGISTRY: dict[int, asyncio.Task] = {}


    def register_task(session_id: int, task: asyncio.Task) -> None:
        _TASK_REGISTRY[session_id] = task


    def cancel_task(session_id: int) -> bool:
        task = _TASK_REGISTRY.pop(session_id, None)
        if task and not task.done():
            task.cancel()
            return True
        return False


    async def run_manual_search(session_id: int, subniche_id: int, db_path: str) -> None:
        ...
    ```

    Implementar run_manual_search():
    - Importar SCANNER_MAP lazy (inside function) para evitar circular imports: `from mis.scanner import get_scanner_map; SCANNER_MAP = get_scanner_map()` — ou copiar o dict de scanner.py para o orchestrator diretamente.
    - Buscar slugs: para cada platform_slug em SCANNER_MAP, chamar `get_platform_slug(db_path, subniche_id, platform_slug)`. Skip se None.
    - Buscar niche_id legado: `db.execute("SELECT niche_id FROM subniches WHERE id = ?", [subniche_id]).fetchone()` retorna niche_id FK para niches_v3. Verificar que mesmo ID existe em tabela niches legada: `db.execute("SELECT id FROM niches WHERE id = ?", [niche_id_v3]).fetchone()`. Usar id diretamente se existe, caso contrário usar niche_id=1 como fallback com log.warning.
    - update_session_status(db_path, session_id, "running", {}, 0)
    - asyncio.gather com return_exceptions=True, wrapped em asyncio.wait_for(timeout=120.0)
    - Tratar asyncio.TimeoutError: cancelar sub-tasks explicitamente, update_session_status("timeout", ...)
    - Tratar asyncio.CancelledError: update_session_status("cancelled", ...) e re-raise
    - Após gather: iterar resultados; para cada lista de Product: upsert_product + INSERT OR IGNORE em search_session_products; acumular platform_statuses
    - update_session_status(db_path, session_id, "done", platform_statuses, total_product_count)

    IMPORTANTE: Se scanner.py não expõe `get_scanner_map()`, importar a dict diretamente via lazy import dentro da função para evitar circular:
    ```python
    async def run_manual_search(session_id, subniche_id, db_path):
        from mis.scanner import SCANNER_MAP  # lazy import — evita circular
        ...
    ```
    Verificar no scanner.py se SCANNER_MAP é acessível diretamente (está dentro de run_all_scanners como local var). Se sim, replicar o dict no orchestrator como constante local. Se não, criar módulo separado.

    2. Modificar mis/web/app.py:
    - Adicionar parâmetro `start_scheduler: bool = True` em `create_app(db_path: str, start_scheduler: bool = True)`
    - Dentro do lifespan: envolver todos os blocos de register_scan_and_spy_job, register_radar_jobs, register_canary_job, register_platform_canary_jobs, get_scheduler().start() em `if start_scheduler:`
    - Teardown: envolver get_scheduler().shutdown() em `if start_scheduler:`
    - Adicionar import: `from mis.search_repository import mark_stale_running_sessions`
    - Adicionar startup hook ANTES do yield (e FORA do if start_scheduler): `mark_stale_running_sessions(db_path)`

    Verificar GREEN dos testes de orchestrator e app:
    `python -m pytest mis/tests/test_search_orchestrator.py mis/tests/web/test_web_search.py::test_no_scheduler_on_startup -x -q`
  </action>
  <verify>
    <automated>cd C:/Users/Gabriel/MEGABRAIN && python -m pytest mis/tests/test_search_repository.py mis/tests/test_search_orchestrator.py mis/tests/web/test_web_search.py::test_no_scheduler_on_startup -x -q 2>&1 | tail -10</automated>
  </verify>
  <done>Todos os 10 testes (6 repositório + 3 orchestrator + 1 app) passam GREEN. run_manual_search é coroutine assíncrona. create_app aceita start_scheduler=False sem erro. mark_stale_running_sessions é chamado no lifespan startup.</done>
</task>

</tasks>

<verification>
Suite GREEN após Plan 21-02:
```bash
cd C:/Users/Gabriel/MEGABRAIN && python -m pytest mis/tests/test_search_repository.py mis/tests/test_search_orchestrator.py mis/tests/web/test_web_search.py::test_no_scheduler_on_startup -v 2>&1 | tail -15
```
Esperado: 10 passed.

Suite completa não deve regredir:
```bash
cd C:/Users/Gabriel/MEGABRAIN && python -m pytest mis/tests/ -x -q --ignore=mis/tests/web/test_web_search.py 2>&1 | tail -5
```
</verification>

<success_criteria>
- migration _009 existe e é idempotente
- search_repository.py exporta 7 funções; todos os 6 testes de repositório GREEN
- search_orchestrator.py exporta run_manual_search (coroutine), register_task, cancel_task
- create_app aceita start_scheduler=False; lifespan chama mark_stale_running_sessions independente do parâmetro
- Suite existente (phases 1-20) não regrediu
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/21-manual-search-engine/21-02-SUMMARY.md`
</output>
