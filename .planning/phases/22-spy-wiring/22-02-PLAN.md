---
phase: 22-spy-wiring
plan: 02
type: execute
wave: 2
depends_on:
  - "22-01"
files_modified:
  - mis/spy_orchestrator.py
  - mis/search_orchestrator.py
  - mis/search_repository.py
autonomous: true
requirements:
  - SPY-V3-01
  - SPY-V3-02

must_haves:
  truths:
    - "run_manual_search() chama run_spy_batch() com top-5 por plataforma após scan concluir"
    - "session.status transiciona: running → done → spying → spy_done"
    - "spy_done é tratado como terminal status (finished_at gravado)"
    - "Plataformas fallback-only (eduzz, monetizze, perfectpay) não disparam spy"
    - "list_session_products() retorna product_id e dossier_status por produto"
    - "Testes test_spy_triggered_after_scan, test_session_status_transitions, test_fallback_platforms_excluded, test_list_session_products_with_dossier passam (GREEN)"
  artifacts:
    - path: "mis/spy_orchestrator.py"
      provides: "Constante SPY_V3_TOP_N = 5"
      contains: "SPY_V3_TOP_N"
    - path: "mis/search_orchestrator.py"
      provides: "Spy batch trigger ao final de run_manual_search + helper get_top_products_for_spy()"
      contains: "run_spy_batch"
    - path: "mis/search_repository.py"
      provides: "list_session_products com product_id + dossier_status, spy_done em terminal_statuses"
      contains: "dossier_status"
  key_links:
    - from: "mis/search_orchestrator.py:run_manual_search"
      to: "mis/spy_orchestrator.py:run_spy_batch"
      via: "await run_spy_batch(products_to_spy, max_concurrent=SPY_V3_TOP_N)"
      pattern: "run_spy_batch"
    - from: "mis/search_repository.py:list_session_products"
      to: "dossiers table"
      via: "LEFT JOIN dossiers d ON d.product_id = ssp.product_id"
      pattern: "LEFT JOIN dossiers"
---

<objective>
Implementar o wiring de backend para espionagem automática: constante SPY_V3_TOP_N, trigger do spy batch ao final de run_manual_search(), transições de status 'spying'/'spy_done', e extensão de list_session_products() com product_id + dossier_status.

Purpose: Habilitar que a pesquisa manual dispare automaticamente a espionagem dos top produtos, sem nova infraestrutura — apenas conectando run_spy_batch() ao fluxo existente.
Output: Backend completo para SPY-V3-01 e parte de SPY-V3-02 (product_id + dossier_status no repositório).
</objective>

<execution_context>
@C:/Users/Gabriel/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Gabriel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-spy-wiring/22-CONTEXT.md
@.planning/phases/22-spy-wiring/22-RESEARCH.md
@mis/spy_orchestrator.py
@mis/search_orchestrator.py
@mis/search_repository.py
@mis/tests/test_spy_wiring.py

<interfaces>
<!-- Interfaces existentes que este plano usa e estende -->

De mis/spy_orchestrator.py:
```python
SPY_TOP_N = 10  # legado — NÃO alterar
async def run_spy(product_id: int, force: bool = False) -> None: ...
async def run_spy_batch(products: list[dict], max_concurrent: int) -> None: ...
# products: lista de dicts com chave 'id' (product_id interno do banco)
```

De mis/search_orchestrator.py:
```python
_TASK_REGISTRY: dict[int, asyncio.Task] = {}
def register_task(session_id: int, task: asyncio.Task) -> None: ...
def cancel_task(session_id: int) -> bool: ...
async def run_manual_search(session_id: int, subniche_id: int, db_path: str) -> None:
    # Termina com: update_session_status(db_path, session_id, "done", platform_statuses, total_products)
```

De mis/search_repository.py:
```python
def update_session_status(db_path, session_id, status, platform_statuses, product_count) -> None:
    terminal_statuses = {"done", "timeout", "cancelled"}  # adicionar "spy_done"

def list_session_products(db_path, session_id, platform_filter=None, limit=100, offset=0) -> list[dict]:
    # Retorna: rank_at_scan, platform_slug, title, url, price, commission_pct, thumbnail_url, platform_name
    # ADICIONAR: product_id, dossier_status (LEFT JOIN dossiers)
```

Plataformas fallback-only (sem slug mapping): eduzz, monetizze, perfectpay
— Já filtradas em run_manual_search() via `get_platform_slug()` retornando None.
— Ao calcular top-5 para spy, usar apenas products do search_session_products (que já são produtos com slug válido).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: spy_orchestrator.py — adicionar SPY_V3_TOP_N e helper get_top_products_for_spy()</name>
  <files>mis/spy_orchestrator.py</files>
  <behavior>
    - SPY_V3_TOP_N = 5 existe como constante separada de SPY_TOP_N = 10
    - get_top_products_for_spy(db_path, session_id) retorna lista de dicts {"id": product_id, "rank": rank_at_scan} com top-5 por plataforma
    - Usa Python puro (defaultdict) sobre query ORDER BY platform_slug, rank_at_scan ASC — não SQL complexo
    - Plataformas com 0 produtos na sessão retornam lista vazia (sem erro)
    - Retorno máximo: SPY_V3_TOP_N * (número de plataformas com produtos) = até 25 itens
  </behavior>
  <action>
    Adicionar no início do arquivo (após SPY_TOP_N = 10):

    ```python
    # v3.0: top-5 per platform per search session — separate from legacy SPY_TOP_N = 10
    SPY_V3_TOP_N = 5
    ```

    Adicionar função pública get_top_products_for_spy():

    ```python
    def get_top_products_for_spy(db_path: str, session_id: int) -> list[dict]:
        """Return top-SPY_V3_TOP_N products per platform for a search session.

        Uses in-memory grouping (defaultdict) over a single ordered query.
        Plataformas fallback-only (eduzz, monetizze, perfectpay) are naturally
        excluded because they have no slug mapping and produce 0 rows in
        search_session_products.

        Args:
            db_path:    Path to the SQLite database file.
            session_id: The search session to query.

        Returns:
            List of dicts with keys 'id' (product_id) and 'rank' (rank_at_scan).
            Up to SPY_V3_TOP_N * num_platforms items, ordered by platform then rank.
        """
        from collections import defaultdict
        db = get_db(db_path)
        rows = db.execute(
            "SELECT product_id, platform_slug, rank_at_scan "
            "FROM search_session_products "
            "WHERE session_id = ? "
            "ORDER BY platform_slug, rank_at_scan ASC",
            [session_id],
        ).fetchall()
        top_per_platform: dict[str, list] = defaultdict(list)
        for product_id, platform_slug, rank_at_scan in rows:
            if len(top_per_platform[platform_slug]) < SPY_V3_TOP_N:
                top_per_platform[platform_slug].append(
                    {"id": product_id, "rank": rank_at_scan if rank_at_scan is not None else 999}
                )
        return [p for ps in top_per_platform.values() for p in ps]
    ```

    NÃO alterar SPY_TOP_N = 10, run_spy(), run_spy_batch() — API existente permanece intacta.
  </action>
  <verify>
    <automated>pytest mis/tests/test_spy_wiring.py::test_fallback_platforms_excluded -xvs 2>&1 | tail -15</automated>
  </verify>
  <done>SPY_V3_TOP_N = 5 exportado; get_top_products_for_spy() existe e retorna top-5 por plataforma; test_fallback_platforms_excluded passa GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: search_orchestrator.py — trigger spy batch ao final de run_manual_search()</name>
  <files>mis/search_orchestrator.py</files>
  <behavior>
    - Após update_session_status(..., "done", ...), run_manual_search() chama update_session_status(..., "spying", ...)
    - Calcula top-5 via get_top_products_for_spy(db_path, session_id)
    - Se products_to_spy não vazio: await run_spy_batch(products_to_spy, max_concurrent=SPY_V3_TOP_N)
    - Após run_spy_batch concluir: update_session_status(..., "spy_done", ...)
    - Se products_to_spy vazio (scan retornou 0 produtos): vai direto para "spy_done" sem chamar run_spy_batch
    - Em caso de CancelledError durante spy: session fica em 'spying' (sem mudança — startup cleanup é deferred)
    - Sequência exata de status: running → done → spying → spy_done
  </behavior>
  <action>
    Adicionar imports no topo do arquivo:
    ```python
    from .spy_orchestrator import run_spy_batch, get_top_products_for_spy, SPY_V3_TOP_N
    ```

    Substituir o bloco final de run_manual_search() (após o update_session_status "done") pelo seguinte:

    ```python
    update_session_status(
        db_path, session_id, "done", platform_statuses, total_products
    )
    _log.info(
        "run_manual_search.complete",
        session_id=session_id,
        subniche_id=subniche_id,
        total_products=total_products,
    )

    # --- SPY WIRING (v3.0) ---
    # Transition to 'spying' and run spy batch on top products
    update_session_status(db_path, session_id, "spying", platform_statuses, total_products)
    products_to_spy = get_top_products_for_spy(db_path, session_id)
    _log.info(
        "run_manual_search.spy_start",
        session_id=session_id,
        products_to_spy=len(products_to_spy),
    )
    if products_to_spy:
        await run_spy_batch(products_to_spy, max_concurrent=SPY_V3_TOP_N)
    update_session_status(db_path, session_id, "spy_done", platform_statuses, total_products)
    _log.info("run_manual_search.spy_done", session_id=session_id)
    ```

    ATENÇÃO: Não duplicar o _log.info "run_manual_search.complete" — ele fica antes do bloco spy.
    O bloco spy é adicionado DEPOIS do log existente, dentro do mesmo try/except.
    O except asyncio.CancelledError existente já captura cancelamento durante scan — não alterar.
  </action>
  <verify>
    <automated>pytest mis/tests/test_spy_wiring.py::test_spy_triggered_after_scan mis/tests/test_spy_wiring.py::test_session_status_transitions -xvs 2>&1 | tail -20</automated>
  </verify>
  <done>run_manual_search() chama run_spy_batch() com produtos corretos; status transiciona running→done→spying→spy_done; os dois testes passam GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: search_repository.py — spy_done terminal + product_id/dossier_status em list_session_products()</name>
  <files>mis/search_repository.py</files>
  <behavior>
    - update_session_status() trata 'spy_done' como terminal status (finished_at gravado)
    - list_session_products() retorna product_id (ssp.product_id) em cada dict
    - list_session_products() retorna dossier_status (d.status, NULL se sem dossier) em cada dict
    - list_session_products() retorna dossier_id (d.id) em cada dict
    - Mudança é aditiva — todas as chaves anteriores permanecem; nenhum caller quebra
    - Produto sem dossier: dossier_status = None, dossier_id = None
  </behavior>
  <action>
    Em update_session_status(), alterar a linha de terminal_statuses:
    ```python
    terminal_statuses = {"done", "timeout", "cancelled", "spy_done"}
    ```

    Em list_session_products(), adicionar LEFT JOIN com dossiers e as novas colunas ao SELECT:

    Alterar o SELECT de:
    ```sql
    SELECT ssp.rank_at_scan,
           ssp.platform_slug,
           p.title, p.url, p.price, p.commission_pct, p.thumbnail_url,
           pl.name AS platform_name
      FROM search_session_products ssp
      JOIN products  p  ON p.id  = ssp.product_id
      JOIN platforms pl ON pl.id = p.platform_id
     WHERE ssp.session_id = ?
    ```

    Para:
    ```sql
    SELECT ssp.rank_at_scan,
           ssp.platform_slug,
           p.title, p.url, p.price, p.commission_pct, p.thumbnail_url,
           pl.name AS platform_name,
           ssp.product_id,
           d.id   AS dossier_id,
           d.status AS dossier_status
      FROM search_session_products ssp
      JOIN products  p  ON p.id  = ssp.product_id
      JOIN platforms pl ON pl.id = p.platform_id
      LEFT JOIN dossiers d ON d.product_id = ssp.product_id
     WHERE ssp.session_id = ?
    ```

    Atualizar o dict de retorno para incluir os novos campos (índices r[8], r[9], r[10]):
    ```python
    {
        "rank_at_scan": r[0],
        "platform_slug": r[1],
        "title": r[2],
        "url": r[3],
        "price": r[4],
        "commission_pct": r[5],
        "thumbnail_url": r[6],
        "platform_name": r[7],
        "product_id": r[8],
        "dossier_id": r[9],
        "dossier_status": r[10],
    }
    ```
  </action>
  <verify>
    <automated>pytest mis/tests/test_search_repository.py::test_list_session_products_with_dossier mis/tests/test_spy_wiring.py -xvs 2>&1 | tail -20</automated>
  </verify>
  <done>spy_done é terminal (finished_at gravado); list_session_products() retorna product_id, dossier_id, dossier_status; test_list_session_products_with_dossier e todos os testes em test_spy_wiring.py passam GREEN.</done>
</task>

</tasks>

<verification>
Suite completa para confirmar que nenhum teste existente quebrou:
`pytest mis/tests/ -x 2>&1 | tail -15`

Verificar os testes específicos deste plano:
`pytest mis/tests/test_spy_wiring.py mis/tests/test_search_repository.py::test_list_session_products_with_dossier -v 2>&1 | tail -20`

Resultado esperado: test_spy_triggered_after_scan, test_session_status_transitions, test_fallback_platforms_excluded, test_cancel_session_cancels_spy, test_list_session_products_with_dossier passam GREEN (5+ testes).
</verification>

<success_criteria>
- SPY_V3_TOP_N = 5 definido em spy_orchestrator.py (separado de SPY_TOP_N = 10)
- get_top_products_for_spy() retorna top-5 por plataforma via Python grouping
- run_manual_search() termina com status 'spy_done' após run_spy_batch()
- update_session_status() trata 'spy_done' como terminal
- list_session_products() retorna product_id + dossier_id + dossier_status
- Testes RED do plano 01 relevantes a este plano passam GREEN
- `pytest mis/tests/ -x` verde
</success_criteria>

<output>
After completion, create `.planning/phases/22-spy-wiring/22-02-SUMMARY.md`
</output>
