---
phase: 05-dashboard
plan: "03"
type: execute
wave: 3
depends_on:
  - "05-02"
files_modified:
  - mis/web/routes/dossier.py
  - mis/web/routes/feed.py
  - mis/web/templates/dossier.html
  - mis/web/templates/dossier_tab_visao_geral.html
  - mis/web/templates/dossier_tab_copy.html
  - mis/web/templates/dossier_tab_anuncios.html
  - mis/web/templates/dossier_tab_reviews.html
  - mis/web/templates/dossier_tab_template.html
  - mis/web/templates/feed.html
  - mis/web/templates/feed_report.html
  - mis/web/app.py
autonomous: true
requirements:
  - DASH-02
  - DASH-03

must_haves:
  truths:
    - "GET /dossier/{id} retorna 200 para produto existente com dossiê"
    - "GET /dossier/{id} retorna 404 para id inexistente"
    - "GET /dossier/{id}/tab/reviews com HX-Request:true retorna fragment sem <nav>"
    - "GET /feed retorna 200 com conteúdo das abas por nicho"
    - "GET /feed/niche/{slug} com HX-Request:true retorna fragment sem <nav>"
    - "Tabs do dossiê carregam via HTMX sem reload de página"
    - "Feed exibe relatório mais recente por nicho com timestamp e contador de sinais"
  artifacts:
    - path: "mis/web/routes/dossier.py"
      provides: "GET /dossier/{id} + GET /dossier/{id}/tab/{tab_name}"
    - path: "mis/web/routes/feed.py"
      provides: "GET /feed + GET /feed/niche/{slug}"
    - path: "mis/web/templates/dossier.html"
      provides: "página de dossiê com header fixo e tabs"
    - path: "mis/web/templates/dossier_tab_visao_geral.html"
      provides: "fragment da tab Visão Geral (análise IA)"
    - path: "mis/web/templates/dossier_tab_reviews.html"
      provides: "fragment da tab Reviews (síntese + lista)"
    - path: "mis/web/templates/feed.html"
      provides: "página de feed com abas por nicho"
    - path: "mis/web/templates/feed_report.html"
      provides: "fragment do relatório de um nicho"
  key_links:
    - from: "mis/web/routes/dossier.py"
      to: "mis/dossier_repository.py"
      via: "get_dossier_by_product_id(db_path, product_id)"
      pattern: "dossier_repository.get_dossier_by_product_id"
    - from: "mis/web/routes/feed.py"
      to: "mis/pain_repository.py"
      via: "get_latest_report(db_path, niche_slug)"
      pattern: "pain_repository.get_latest_report"
    - from: "mis/web/app.py"
      to: "mis/web/routes/dossier.py"
      via: "app.include_router(dossier.router)"
      pattern: "include_router"
---

<objective>
Implementar a página de dossiê individual com tabs HTMX (Visão Geral, Copy, Anúncios, Reviews, Template) e o feed de dores do mercado com abas por nicho e relatórios históricos.

Purpose: Entrega DASH-02 (página core do sistema — por que o produto vende) e DASH-03 (inteligência de mercado em tempo real).
Output: /dossier/{id} com 5 tabs navegáveis; /feed com abas por nicho e selector de histórico.
</objective>

<execution_context>
@C:/Users/Gabriel/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Gabriel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-dashboard/05-CONTEXT.md
@.planning/phases/05-dashboard/05-RESEARCH.md
@.planning/phases/05-dashboard/05-02-SUMMARY.md
@mis/web/app.py
@mis/dossier_repository.py
@mis/pain_repository.py

<interfaces>
<!-- Contratos dos repositórios criados em 05-02 -->

dossier_repository.py:
```python
def get_dossier_by_product_id(db_path: str, product_id: int) -> dict | None:
    # Retorna dict com campos:
    # id, product_id, confidence_score, opportunity_score, gate_passed
    # "dossier": dict (json.loads de dossier_json) — campos: analysis, success_factors, pains, template, opportunity_score
    # "ads": list (json.loads de ads_json)
    # "copy": dict (json.loads de copy_json) — campos: headlines, arguments, cta, narrative
    # "reviews": dict (json.loads de reviews_json) — campos: synthesis, positive, negative, total_count
    # Retorna None se não encontrado

def list_dossiers_by_rank(...) -> list[dict]:
    # Cada dict: id, title, url, platform_name, platform_slug, niche_name,
    #            niche_slug, rank, opportunity_score, confidence_score, has_dossier
```

pain_repository.py:
```python
def get_latest_report(db_path: str, niche_slug: str) -> dict | None:
    # Retorna dict com campos:
    # id, niche_id, cycle_at, created_at, signal_count
    # "report": dict (json.loads de report_json)
    #   — campos esperados: pains: [{description, interest_level, sources: [url1, url2]}]
    # Retorna None se nenhum relatório encontrado

def get_historical_reports(db_path: str, niche_slug: str, limit: int = 48) -> list[dict]:
    # Retorna lista de dicts: {id, cycle_at} sem report_json
```

app.state (injetado em create_app):
```python
request.app.state.db_path  # str — caminho do banco
request.app.state.templates  # Jinja2Templates
```

HTMX detection helper (já em ranking.py):
```python
def is_htmx(request: Request) -> bool:
    return request.headers.get("HX-Request") == "true"
```

Tabs do dossiê (slugs):
  "visao-geral", "copy", "anuncios", "reviews", "template"

Template Jinja2 para tabs (RESEARCH.md Pattern 5):
```html
<button hx-get="/dossier/{{ product_id }}/tab/{{ tab }}"
        hx-target="#tab-content" hx-swap="innerHTML">
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Página de dossiê individual com tabs HTMX</name>
  <files>
    mis/web/routes/dossier.py
    mis/web/templates/dossier.html
    mis/web/templates/dossier_tab_visao_geral.html
    mis/web/templates/dossier_tab_copy.html
    mis/web/templates/dossier_tab_anuncios.html
    mis/web/templates/dossier_tab_reviews.html
    mis/web/templates/dossier_tab_template.html
    mis/web/app.py
  </files>
  <behavior>
    - GET /dossier/9999 → status 404
    - GET /dossier/{id} com produto existente → status 200, body contém o título do produto
    - GET /dossier/{id}/tab/reviews com HX-Request:true → status 200, sem "<nav" no body
    - GET /dossier/{id}/tab/visao-geral com HX-Request:true → status 200
    - Navegação anterior/próximo: GET /dossier/{id} inclui links prev/next baseados no rank
  </behavior>
  <action>
    1. Criar mis/web/routes/dossier.py:
       - APIRouter
       - Helper is_htmx() (ou importar de ranking.py via from .ranking import is_htmx)

       GET /dossier/{product_id:int}:
       - Buscar produto: list_dossiers_by_rank com filtro de id (ou query direta: SELECT * FROM products WHERE id=?)
       - Buscar dossiê: dossier_repository.get_dossier_by_product_id(db_path, product_id)
       - Se produto não existe → raise HTTPException(404)
       - Buscar prev/next para navegação: SELECT id, rank FROM products WHERE rank < produto.rank ORDER BY rank DESC LIMIT 1 (prev) e rank > produto.rank ORDER BY rank ASC LIMIT 1 (next)
       - active_tab = query param "tab" default "visao-geral"
       - Retornar dossier.html com contexto: product, dossier, active_tab, prev_id, next_id

       GET /dossier/{product_id:int}/tab/{tab_name:str}:
       - tab_name deve estar em {"visao-geral", "copy", "anuncios", "reviews", "template"}
       - Se inválido → HTTPException(400)
       - Buscar dossiê via dossier_repository
       - Se não encontrado: retornar template informando "Análise em andamento"
       - Retornar dossier_tab_{tab_name.replace("-", "_")}.html como partial
         (ex: tab "visao-geral" → "dossier_tab_visao_geral.html")

    2. Criar mis/web/templates/dossier.html (extends base.html):
       - Header fixo: nome do produto, plataforma, posição no ranking
       - Confidence score: "Oportunidade: {{ dossier.opportunity_score or '—' }} | Confiabilidade: {{ (dossier.confidence_score * 100)|int if dossier else 0 }}%"
       - Se produto sem dossiê: badge "Análise em andamento" no header
       - Navegação anterior/próximo no header: setas com links /dossier/{{ prev_id }} e /dossier/{{ next_id }}
       - Tabs horizontais (5 botões com hx-get + hx-target="#tab-content"):
         Visão Geral | Copy | Anúncios | Reviews | Template
         Tab ativa destacada com border-b-2 border-blue-400
       - <div id="tab-content"> com include do template da tab ativa (active_tab)
       - Link "← Voltar ao Ranking" no topo

    3. Criar 5 templates de tab (todos PARTIAL — SEM extends base.html):
       - dossier_tab_visao_geral.html:
         - Seção "Por que está vendendo" com dossier.dossier.analysis
         - Seção "Dores endereçadas" com lista de dossier.dossier.pains
         - Seção "Fatores de sucesso" com lista de dossier.dossier.success_factors
         - Se sem dossiê: "Análise em andamento — dados sendo processados"

       - dossier_tab_copy.html:
         - Headlines em destaque (dossier.copy.headlines se disponível)
         - Argumentos (dossier.copy.arguments)
         - CTA identificados (dossier.copy.cta)
         - Se sem copy: "Copy ainda não extraída"

       - dossier_tab_anuncios.html:
         - {% for ad in dossier.ads %} → card com copy do anúncio, data
         - Se sem anúncios: "Nenhum anúncio coletado"

       - dossier_tab_reviews.html:
         - Síntese IA no topo (dossier.reviews.synthesis)
         - Lista de reviews positivos (dossier.reviews.positive) com rating ⭐
         - Lista de reviews negativos (dossier.reviews.negative) com rating ⭐
         - Se sem reviews: "Reviews ainda não coletados"

       - dossier_tab_template.html:
         - Texto formatado do template de modelagem (dossier.dossier.template)
         - Botão "Copiar para clipboard" com data-clipboard="{{ dossier.dossier.template }}"
         - Se sem template: "Template ainda não gerado"

    4. Atualizar mis/web/app.py para registrar o router de dossier:
       - from .routes import dossier
       - app.include_router(dossier.router)
  </action>
  <verify>
    <automated>cd mis && python -m pytest tests/web/test_web_dossier.py -x -q</automated>
  </verify>
  <done>Todos os 3 testes de test_web_dossier.py GREEN. 404 para ID inexistente. 200 para dossiê existente. Tab partial sem navbar. app.py registra o router de dossier.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Feed de dores do mercado com abas por nicho e histórico</name>
  <files>
    mis/web/routes/feed.py
    mis/web/templates/feed.html
    mis/web/templates/feed_report.html
    mis/web/app.py
  </files>
  <behavior>
    - GET /feed → status 200, body contém "Feed de Dores" ou nomes dos nichos configurados
    - GET /feed/niche/emagrecimento com HX-Request:true → status 200, sem "<nav" no body
    - GET /feed sem reports disponíveis → status 200, sem erro (empty state gracioso)
    - GET /feed?niche=emagrecimento&report_id=5 → carrega relatório específico do histórico
  </behavior>
  <action>
    1. Criar mis/web/routes/feed.py:
       - APIRouter

       GET /feed:
       - Buscar nichos configurados: config.settings.niches (de mis.config import load_config)
         OU fallback: SELECT DISTINCT niche_slug FROM pain_reports via pain_repository
       - Para cada nicho: buscar relatório mais recente via pain_repository.get_latest_report(db_path, niche.slug)
       - Buscar histórico de cada nicho via pain_repository.get_historical_reports(db_path, niche.slug, limit=48)
       - active_niche = query param "niche", default = primeiro nicho configurado
       - active_report_id = query param "report_id", default = None (usa o mais recente)
       - Retornar feed.html

       GET /feed/niche/{niche_slug}:
       - Buscar relatório mais recente do nicho via pain_repository.get_latest_report
       - report_id = query param "report_id" opcional (para histórico)
       - Se report_id fornecido: buscar relatório específico por id
       - Se is_htmx → retornar feed_report.html partial
       - Senão → retornar feed.html completo com esse nicho ativo

    2. Criar mis/web/templates/feed.html (extends base.html):
       - Abas horizontais — uma por nicho configurado
         Tab ativa destacada com border-b-2 border-blue-400
         Cada tab: hx-get="/feed/niche/{{ niche.slug }}", hx-target="#feed-report-wrapper", hx-swap="innerHTML"
       - Selector de histórico: <select hx-get="/feed/niche/{{ active_niche }}" hx-include="this" hx-target="#feed-report-wrapper">
         {% for hist in historical_reports %}<option value="{{ hist.id }}">{{ hist.cycle_at[:16] }}</option>{% endfor %}
       - <div id="feed-report-wrapper">{% include "feed_report.html" %}</div>

    3. Criar mis/web/templates/feed_report.html (PARTIAL — SEM extends base.html):
       - Timestamp do relatório: data/hora e tempo relativo ("há N horas")
         Cálculo em Python no route handler: timedelta entre cycle_at e agora → "há X horas" ou "há X minutos"
         Passar como contexto: time_ago = "há 2 horas"
       - Contador de sinais: "Baseado em {{ report.signal_count }} sinais coletados"
       - Filtros de nível de interesse: checkboxes Alto/Médio/Baixo (via HTMX para re-filtrar a lista client-side seria JS — usar server-side com query params para manter pureza HTMX)
         Busca textual: input de texto com hx-get="/feed/niche/{{ niche_slug }}" hx-trigger="keyup changed delay:300ms" hx-target="#pains-list" hx-include="[name='interest_level']"
       - Lista de dores (id="pains-list"):
         {% for pain in report.report.pains %}
           - Título em destaque + nível de interesse (badge alto/médio/baixo com cores)
           - Descrição
           - Fontes: {% for url in pain.sources[:3] %}<a href="{{ url }}" target="_blank" class="text-blue-400 text-xs">fonte</a>{% endfor %}
         {% else %}
           "Nenhuma dor identificada neste ciclo"
         {% endfor %}
       - Empty state se report = None: "Nenhum relatório disponível para este nicho ainda."

       Calcular time_ago no route handler:
       ```python
       from datetime import datetime, timezone
       now = datetime.now(timezone.utc)
       cycle_dt = datetime.fromisoformat(report["cycle_at"].replace("Z", "+00:00"))
       delta = now - cycle_dt
       hours = int(delta.total_seconds() // 3600)
       minutes = int((delta.total_seconds() % 3600) // 60)
       time_ago = f"há {hours}h {minutes}min" if hours > 0 else f"há {minutes} minutos"
       ```

    4. Atualizar mis/web/app.py:
       - from .routes import feed
       - app.include_router(feed.router)
  </action>
  <verify>
    <automated>cd mis && python -m pytest tests/web/test_web_feed.py -x -q</automated>
  </verify>
  <done>Todos os testes de test_web_feed.py GREEN. GET /feed retorna 200. Partial de nicho sem navbar. Empty state sem erro quando sem reports no banco.</done>
</task>

</tasks>

<verification>
Suite completa de dossier e feed passa: `cd mis && python -m pytest tests/web/test_web_dossier.py tests/web/test_web_feed.py -x -q`

Todos os routers estão registrados em app.py: ranking, dossier, feed, health.
Templates partiais (dossier_tab_*.html, feed_report.html) NÃO contêm `{% extends "base.html" %}`.
</verification>

<success_criteria>
Testes de dossier e feed GREEN. Importar create_app e GET /dossier/1 (com produto no banco) retorna HTML com tabs. GET /feed sem dados retorna 200 com empty state.
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/05-dashboard/05-03-SUMMARY.md`
</output>
