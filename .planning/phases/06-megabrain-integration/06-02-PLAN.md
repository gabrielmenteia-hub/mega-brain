---
phase: 06-megabrain-integration
plan: "02"
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - mis/__main__.py
  - .claude/skills/mis-briefing/SKILL.md
  - .claude/CLAUDE.md
autonomous: true
requirements:
  - INT-02

must_haves:
  truths:
    - "Usuário pode invocar /mis-briefing no MEGABRAIN e receber briefing visual JARVIS-style"
    - "python -m mis export exporta dossiês e pain reports para knowledge/mis/ com resumo no terminal"
    - "python -m mis export --dest /outro/caminho respeita o destino alternativo"
    - "SKILL.md auto-detectado pelo skill_indexer.py via keywords declaradas"
    - "CLAUDE.md do MEGABRAIN documenta o bridge com assinaturas das funções e exemplo de import"
  artifacts:
    - path: "mis/__main__.py"
      provides: "Subcomando export adicionado ao CLI MIS"
      contains: "subparsers.add_parser('export'"
    - path: ".claude/skills/mis-briefing/SKILL.md"
      provides: "Skill standalone /mis-briefing com formato JARVIS"
      contains: "mis-briefing"
    - path: ".claude/CLAUDE.md"
      provides: "Seção MIS Integration com assinaturas e exemplo de import"
      contains: "MIS Integration"
  key_links:
    - from: ".claude/skills/mis-briefing/SKILL.md"
      to: "mis/mis_agent.py"
      via: "python -c 'from mis.mis_agent import get_briefing_data'"
      pattern: "get_briefing_data"
    - from: "mis/__main__.py"
      to: "mis/mis_agent.py"
      via: "export_to_megabrain(dest=args.dest)"
      pattern: "export_to_megabrain"
---

<objective>
Expor o bridge MIS para o ecossistema MEGABRAIN em três camadas: (1) CLI `python -m mis export` para exportação manual, (2) skill standalone `/mis-briefing` com formato JARVIS-style, e (3) documentação no CLAUDE.md do MEGABRAIN.

Purpose: INT-02 requer que o usuário possa invocar análise do MIS via comando dentro do MEGABRAIN, e que dossiês sejam exportáveis para o pipeline de conhecimento.

Output: `mis/__main__.py` atualizado, `.claude/skills/mis-briefing/SKILL.md` criado, `.claude/CLAUDE.md` atualizado com seção MIS Integration.
</objective>

<execution_context>
@C:/Users/Gabriel/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Gabriel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/Gabriel/MEGABRAIN/.planning/ROADMAP.md
@C:/Users/Gabriel/MEGABRAIN/.planning/phases/06-megabrain-integration/06-CONTEXT.md
@C:/Users/Gabriel/MEGABRAIN/.planning/phases/06-megabrain-integration/06-01-SUMMARY.md

<interfaces>
<!-- Contratos do mis_agent.py criado no Plan 01 -->

From mis/mis_agent.py (criado no Plan 01):
```python
def get_briefing_data() -> dict:
    # Retorna:
    # {
    #   'status': 'ok' | 'error',
    #   'products': list[dict],      # top-10, campos: name, platform_slug, niche_slug, rank, score
    #   'pains_by_niche': dict,      # {niche_slug: {'pains': list[dict], 'language': str}}
    #   'unseen_alerts': int,
    #   'health': {'score': int, 'scrapers': dict, 'last_cycle': str|None, 'data_stale': bool},
    #   'last_cycle': str | None,    # ISO timestamp
    #   'data_stale': bool,
    #   'db_path': str,
    #   # se status='error': 'message': str, 'setup_hint': str
    # }

def export_to_megabrain(dest: str | None = None) -> dict:
    # Retorna:
    # {
    #   'status': 'ok' | 'error',
    #   'exported': int,    # novos arquivos escritos
    #   'skipped': int,     # arquivos sem mudança (hash igual)
    #   'dest': str,        # caminho absoluto do destino
    # }
```

Padrão visual JARVIS (de .claude/skills/jarvis-briefing/SKILL.md):
- Largura: 120 caracteres
- Containers externos: ╔═══╗
- Containers internos: ┌───┐
- Barras de progresso: ████████░░░░░░░░ (16 blocos = 100%)
- Health Score: 0-100 pontos com barra visual
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar subcomando 'export' ao CLI mis/__main__.py</name>
  <files>mis/__main__.py</files>
  <action>
Adicionar subcomando `export` ao argparse existente em `mis/__main__.py`, seguindo o padrão dos subcomandos `spy`, `radar` e `dashboard` já presentes.

**No bloco de subparsers (após dashboard_parser):**
```python
# ── export subcommand ────────────────────────────────────────────────────
export_parser = subparsers.add_parser(
    "export",
    help="Export dossiers and pain reports to MEGABRAIN knowledge pipeline",
)
export_parser.add_argument(
    "--dest",
    default=None,
    metavar="PATH",
    help="Destination directory (default: MEGABRAIN_PATH/knowledge/mis/)",
)
```

**No dispatcher (após elif dashboard):**
```python
elif args.command == "export":
    _handle_export(args)
```

**Nova função _handle_export:**
```python
def _handle_export(args) -> None:
    """Handle the export subcommand."""
    from mis.mis_agent import export_to_megabrain

    result = export_to_megabrain(dest=args.dest)
    if result["status"] == "ok":
        print(
            f"Exported: {result['exported']} dossiers/reports, "
            f"{result['skipped']} skipped to {result['dest']}"
        )
    else:
        print(f"Export failed: {result['message']}")
        print(f"Hint: {result.get('setup_hint', '')}")
        sys.exit(1)
```

Verificar que `python -m mis --help` exibe `export` na lista de subcomandos.
  </action>
  <verify>
    <automated>cd C:/Users/Gabriel/MEGABRAIN && python -m mis export --help 2>&1 | head -10</automated>
  </verify>
  <done>python -m mis export --help mostra usage com flag --dest. python -m mis --help lista 'export' nos subcomandos disponíveis.</done>
</task>

<task type="auto">
  <name>Task 2: Criar SKILL.md do /mis-briefing e atualizar CLAUDE.md do MEGABRAIN</name>
  <files>.claude/skills/mis-briefing/SKILL.md, .claude/CLAUDE.md</files>
  <action>
**A. Criar `.claude/skills/mis-briefing/SKILL.md`**

Seguir o padrão visual do `.claude/skills/jarvis-briefing/SKILL.md` (já lido no contexto). O skill não é um agente — é um script de execução que Claude roda via Bash tool.

Conteúdo do SKILL.md:

```markdown
# MIS BRIEFING — Market Intelligence Status

> **Auto-Trigger:** "mis briefing", "produtos campeões", "radar de mercado"
> **Keywords:** mis briefing, produtos campeões, radar de mercado, market intelligence
> **Prioridade:** MÉDIA

---

## Conceito

Gera briefing visual dos dados do Market Intelligence System (MIS): produtos campeões, radar de dores e saúde dos scrapers.

---

## Trigger

`/mis-briefing`

## Pré-requisitos

- `mis/` instalado (repo MIS acessível)
- `MIS_PATH` configurado no `.env` do MEGABRAIN (caminho absoluto ao repo MIS)
- `MIS_DB_PATH` configurado no `.env` do MEGABRAIN (caminho absoluto ao `mis.db`)
- Pelo menos 1 ciclo de scanner executado (`python -m mis spy --url <URL>`)

---

## Execução

### 1. Carregar Dados do MIS

```bash
python -c "
import sys, os
sys.path.insert(0, os.environ['MIS_PATH'])
from mis.mis_agent import get_briefing_data
import json
data = get_briefing_data()
print(json.dumps(data, default=str, ensure_ascii=False))
"
```

Se retornar `status='error'`: exibir mensagem de erro + setup_hint. Não prosseguir.

Se MIS_PATH não estiver no ambiente: exibir `"MIS não encontrado. Configure MIS_PATH no .env e execute python -m mis para inicializar."`

### 2. Calcular e Formatar o Briefing

Usar os dados retornados para montar o briefing visual com containers JARVIS-style (largura 120 chars):

```
╔══════════════════════════════════════════════════════════════════════════════════════════════════════════════════════╗
║  MIS — MARKET INTELLIGENCE BRIEFING                                                              {ISO_TIMESTAMP}    ║
╠══════════════════════════════════════════════════════════════════════════════════════════════════════════════════════╣
║  HEALTH SCORE: {score}/100  [{barra_16_blocos}]                                                                     ║
║  {✅ OK / ⚠ FALHA} por scraper  |  Último ciclo: {last_cycle ou "N/A"}  {⚠ "Dados antigos" se data_stale}          ║
╠══════════════════════════════════════════════════════════════════════════════════════════════════════════════════════╣
║  TOP-10 PRODUTOS CAMPEÕES                                                                                            ║
╠══════════════════════════════════════════════════════════════════════════════════════════════════════════════════════╣
║  #1  {nome}  [{plataforma}]  [{nicho}]  Score: {score}                                                              ║
║  ...                                                                                                                 ║
╠══════════════════════════════════════════════════════════════════════════════════════════════════════════════════════╣
║  PAIN RADAR — TOP-5 DORES POR NICHO                                                                                 ║
╠══════════════════════════════════════════════════════════════════════════════════════════════════════════════════════╣
║  [{nicho}]                                                                                                           ║
║    1. [{Alto/Médio/Baixo}] {descrição da dor}                                                                       ║
║  ...                                                                                                                 ║
[SEÇÃO ALERTAS — apenas se unseen_alerts > 0]
╠══════════════════════════════════════════════════════════════════════════════════════════════════════════════════════╣
║  NOVOS CAMPEÕES ({N} alertas não vistos)                                                                            ║
║    • {produto} | {nicho} | #{posição} | {data}                                                                      ║
╠══════════════════════════════════════════════════════════════════════════════════════════════════════════════════════╣
║  Dashboard: http://localhost:8000  |  DB: {db_path}                                                                 ║
╚══════════════════════════════════════════════════════════════════════════════════════════════════════════════════════╝
```

**Regras de formatação:**
- Barra de progresso do Health Score: 16 blocos — `int(score/100 * 16)` blocos `█`, restante `░`
- Lista vazia de produtos: exibir `║  Nenhum produto encontrado. Execute os scanners primeiro.`
- Lista vazia de dores: exibir `║  Nenhum relatório disponível para este nicho.`
- Seção Alertas: omitida completamente se `unseen_alerts == 0`
- Estado de erro: substituir o container por mensagem simples — `"MIS não encontrado. Execute python -m mis para inicializar."`

### 3. Exibir

Imprimir o briefing diretamente no chat. Não salvar em arquivo (diferente do jarvis-briefing).

---

## Saída Esperada

Briefing visual completo com:
- Health Score e status dos scrapers
- Top-10 produtos campeões com score de oportunidade
- Top-5 dores por nicho com nível de interesse
- Alertas de novos campeões (se houver)
- Link para dashboard e caminho do DB
```

**B. Atualizar `.claude/CLAUDE.md`**

Ler o arquivo `.claude/CLAUDE.md` completo antes de editar. Adicionar nova seção **"## MIS Integration"** antes da seção `## Security` (ou ao final se não houver seção Security).

Conteúdo da seção a adicionar:

```markdown
## MIS Integration

O MIS (Market Intelligence System) é um módulo independente que coleta e analisa produtos campeões e dores de mercado. O único ponto de integração é `mis/mis_agent.py`.

**Localização:** `{MIS_PATH}/mis/` (configurado via env var `MIS_PATH`)

### Como importar

```python
import sys, os
sys.path.insert(0, os.environ['MIS_PATH'])
from mis.mis_agent import get_briefing_data, export_to_megabrain
```

### Funções disponíveis

```python
get_briefing_data() -> dict
# Retorna: {'status': 'ok', 'products': list, 'pains_by_niche': dict,
#           'unseen_alerts': int, 'health': dict, 'last_cycle': str|None,
#           'data_stale': bool, 'db_path': str}
# Em erro: {'status': 'error', 'message': str, 'setup_hint': str}

export_to_megabrain(dest: str | None = None) -> dict
# Exporta dossiês (status=complete) e pain reports (últimos 7 dias)
# para dest ou MEGABRAIN_PATH/knowledge/mis/ (env var)
# Retorna: {'status': 'ok', 'exported': int, 'skipped': int, 'dest': str}
```

### Variáveis de ambiente necessárias (.env do MEGABRAIN)

```
MIS_PATH=/caminho/absoluto/para/raiz/do/mis
MIS_DB_PATH=/caminho/absoluto/para/mis.db
MEGABRAIN_PATH=/caminho/absoluto/para/megabrain  # para export resolver knowledge/mis/
```

### Comando disponível

`/mis-briefing` — briefing visual dos produtos campeões e radar de dores
```
  </action>
  <verify>
    <automated>ls C:/Users/Gabriel/MEGABRAIN/.claude/skills/mis-briefing/SKILL.md && grep "MIS Integration" C:/Users/Gabriel/MEGABRAIN/.claude/CLAUDE.md</automated>
  </verify>
  <done>SKILL.md existe em .claude/skills/mis-briefing/SKILL.md com keywords e formato correto. CLAUDE.md contém seção "MIS Integration" com assinaturas das funções e exemplo de import.</done>
</task>

</tasks>

<verification>
```bash
# CLI export registrado
cd C:/Users/Gabriel/MEGABRAIN && python -m mis --help | grep export

# SKILL.md existe e tem keywords corretas
grep "mis briefing\|produtos campeões\|radar de mercado" C:/Users/Gabriel/MEGABRAIN/.claude/skills/mis-briefing/SKILL.md

# CLAUDE.md atualizado
grep "MIS Integration\|get_briefing_data\|export_to_megabrain" C:/Users/Gabriel/MEGABRAIN/.claude/CLAUDE.md
```
</verification>

<success_criteria>
- python -m mis export --help funciona (subcomando registrado)
- .claude/skills/mis-briefing/SKILL.md existe com auto-trigger keywords corretas
- CLAUDE.md do MEGABRAIN tem seção MIS Integration com assinaturas e exemplo
- Suite completa de testes continua GREEN: python -m pytest mis/ -v
</success_criteria>

<output>
After completion, create `.planning/phases/06-megabrain-integration/06-02-SUMMARY.md`
</output>
