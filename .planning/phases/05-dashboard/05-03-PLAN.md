---
phase: 05-dashboard
plan: "03"
type: execute
wave: 3
depends_on:
  - "05-02"
files_modified:
  - mis/web/__init__.py
  - mis/web/app.py
  - mis/web/routes/__init__.py
  - mis/web/routes/ranking.py
  - mis/web/routes/health.py
  - mis/web/templates/base.html
  - mis/web/templates/ranking.html
  - mis/web/templates/ranking_table.html
  - mis/web/templates/health.html
  - mis/web/static/app.js
  - mis/__main__.py
autonomous: true
requirements:
  - DASH-01
  - SCAN-05

must_haves:
  truths:
    - "GET /ranking retorna 200 com tabela de produtos (pode estar vazia)"
    - "GET /ranking?platform=hotmart filtra por plataforma via query param"
    - "GET /ranking/table com HX-Request:true retorna fragment sem <nav>"
    - "python -m mis dashboard --port 8000 inicia o servidor uvicorn"
    - "TestClient(create_app(db_path=...)) não levanta exceção"
  artifacts:
    - path: "mis/web/app.py"
      provides: "FastAPI app factory"
      exports: ["create_app"]
    - path: "mis/web/routes/ranking.py"
      provides: "rotas GET /ranking e GET /ranking/table"
    - path: "mis/web/templates/base.html"
      provides: "layout base com navbar, Tailwind CDN, HTMX CDN"
    - path: "mis/web/templates/ranking.html"
      provides: "página de ranking com filtros"
    - path: "mis/web/templates/ranking_table.html"
      provides: "fragment de tabela (SEM extends base.html — partial HTMX)"
  key_links:
    - from: "mis/__main__.py"
      to: "mis/web/app.py"
      via: "uvicorn.run(create_app(db_path=...))"
      pattern: "from mis.web.app import create_app"
    - from: "mis/web/routes/ranking.py"
      to: "mis/dossier_repository.py"
      via: "list_dossiers_by_rank(db_path, ...) para listar produtos"
      pattern: "dossier_repository.list_dossiers_by_rank"
    - from: "mis/web/app.py"
      to: "mis/db.py"
      via: "run_migrations(db_path) chamado em create_app"
      pattern: "run_migrations"
---

<objective>
Criar o web layer do dashboard: FastAPI app factory, página de ranking com filtros HTMX, subcomando `dashboard` no CLI.

Purpose: Desbloqueia todos os planos de UI subsequentes (05-04, 05-05). Entrega DASH-01 + SCAN-05 — a página mais importante para validação imediata.
Output: Servidor funcional em http://localhost:8000/ranking com tabela filtrável por plataforma e nicho.
</objective>

<execution_context>
@C:/Users/Gabriel/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Gabriel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-dashboard/05-CONTEXT.md
@.planning/phases/05-dashboard/05-RESEARCH.md
@.planning/phases/05-dashboard/05-02-SUMMARY.md
@mis/db.py
@mis/product_repository.py
@mis/__main__.py

<interfaces>
<!-- Repositórios criados em 05-02 — contratos para o web layer -->

dossier_repository.py (criado em 05-02):
```python
def list_dossiers_by_rank(
    db_path: str,
    platform: str | None = None,
    niche: str | None = None,
    order_by: str = "rank",
    order_dir: str = "asc",
    per_page: int = 20,
    page: int = 1
) -> list[dict]:
    # Retorna: [{id, title, url, platform_name, platform_slug, niche_name,
    #            niche_slug, rank, opportunity_score, confidence_score, has_dossier}]
```

Padrão de template TemplateResponse (FastAPI >= 0.108):
```python
return templates.TemplateResponse(request=request, name="ranking.html", context={...})
```
NÃO usar forma antiga: TemplateResponse("name.html", {"request": request, ...})

db_path injetado via app.state:
```python
db_path = request.app.state.db_path
templates = request.app.state.templates
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FastAPI app factory + Ranking page + subcomando dashboard CLI</name>
  <files>
    mis/web/__init__.py
    mis/web/app.py
    mis/web/routes/__init__.py
    mis/web/routes/ranking.py
    mis/web/routes/health.py
    mis/web/templates/base.html
    mis/web/templates/ranking.html
    mis/web/templates/ranking_table.html
    mis/web/templates/health.html
    mis/web/static/app.js
    mis/__main__.py
  </files>
  <behavior>
    - GET /ranking retorna 200, body contém "Ranking"
    - GET /ranking?platform=hotmart retorna 200 (filtragem por platform slug)
    - GET /ranking?niche=emagrecimento retorna 200 (filtragem por niche slug)
    - GET /ranking/table com header HX-Request:true retorna 200, body NÃO contém "<nav"
    - GET /ranking?per_page=10&page=1 retorna 200
    - GET /health retorna 200
    - TestClient(create_app(db_path=...)) não levanta exceção
  </behavior>
  <action>
    1. Instalar dependências web se não disponíveis:
       `pip install "fastapi[standard]" uvicorn jinja2 aiofiles`
       Verificar com `python -c "import fastapi, uvicorn, jinja2, aiofiles"`.

    2. Criar mis/web/__init__.py vazio (marcador de pacote).

    3. Criar mis/web/routes/__init__.py vazio.

    4. Criar mis/web/static/app.js:
       Mínimo necessário — clipboard copy handler:
       ```javascript
       document.addEventListener('click', function(e) {
         if (e.target.matches('[data-clipboard]')) {
           navigator.clipboard.writeText(e.target.dataset.clipboard);
           e.target.textContent = 'Copiado!';
           setTimeout(() => e.target.textContent = 'Copiar para clipboard', 2000);
         }
       });
       ```

    5. Criar mis/web/templates/base.html:
       - DOCTYPE html, lang="pt-BR", class="dark"
       - Tailwind Play CDN: <script src="https://cdn.tailwindcss.com"></script>
       - tailwind.config = { darkMode: 'class' }
       - HTMX CDN: <script src="https://cdn.jsdelivr.net/npm/htmx.org@2.0.8/dist/htmx.min.js"></script>
       - <body class="bg-gray-950 text-gray-100 min-h-screen">
       - Navbar com 3 links: /ranking (Ranking) | /feed (Feed de Dores) | /alerts (Alertas)
         Badge polling no link Alertas:
         <span id="alerts-badge" hx-get="/alerts/badge" hx-trigger="every 30s" hx-swap="outerHTML">
       - <main class="px-6 py-6">{% block content %}{% endblock %}</main>
       - Footer com link /health discreto: <a href="/health" class="hover:text-gray-400">system health</a>
       - {% block scripts %}{% endblock %} antes de </body>

    6. Criar mis/web/templates/ranking_table.html (PARTIAL — SEM extends base.html):
       - Tabela HTML com colunas: # | Produto | Plataforma | Nicho | Score
       - {% for product in products %} loop → linha com td para cada campo
       - Badge "Pendente" para produto sem dossiê:
         {% if not product.has_dossier %}<span class="bg-yellow-600 text-xs px-1 rounded">Pendente</span>{% endif %}
       - Link em título do produto: <a href="/dossier/{{ product.id }}">{{ product.title }}</a>
       - Linha no rodapé: "Última atualização: {{ last_updated }}" (ou "Sem dados ainda" se vazio)
       - CONFIRMAR: este é um fragment HTMX sem lógica Python — apenas Jinja2 tags de iteração e condicionais simples.

    7. Criar mis/web/templates/ranking.html (extends base.html):
       - {% extends "base.html" %}{% block content %}
       - Filtros no topo: dois <select> para Plataforma e Nicho
         hx-get="/ranking/table", hx-target="#ranking-table-wrapper", hx-swap="innerHTML"
         hx-include="[name='niche'],[name='platform'],[name='per_page'],[name='page']"
       - Select de per_page: opções 10/20/50
       - <div id="ranking-table-wrapper">{% include "ranking_table.html" %}</div>

    8. Criar mis/web/templates/health.html (extends base.html):
       - Mensagem "Sistema operacional" + lista de jobs se disponível via request.app.state
       - Read-only, sem formulários

    9. Criar mis/web/routes/health.py:
       - APIRouter
       - Helper is_htmx(request): return request.headers.get("HX-Request") == "true"
       - GET /health → health.html com contexto mínimo
       - Tentar importar get_scheduler() de mis.scheduler para exibir jobs; se ImportError/Exception, contexto vazio

    10. Criar mis/web/routes/ranking.py:
        Helper is_htmx(request) → request.headers.get("HX-Request") == "true"

        GET /ranking:
        - Query params: platform, niche, order_by="rank", order_dir="asc", per_page=20, page=1
        - Chamar dossier_repository.list_dossiers_by_rank(db_path, ...)
        - Buscar plataformas: SELECT DISTINCT slug, name FROM platforms ORDER BY name
        - Buscar nichos: SELECT DISTINCT slug, name FROM niches ORDER BY name
        - last_updated = SELECT MAX(updated_at) FROM products
        - Se is_htmx → retornar ranking_table.html partial
        - Senão → retornar ranking.html completo

        GET /ranking/table (partial dedicado):
        - Mesmos params que GET /ranking
        - Sempre retorna ranking_table.html (sem base)

    11. Criar mis/web/app.py:
        - create_app(db_path: str) -> FastAPI
        - FastAPI(title="MIS Dashboard", docs_url=None, redoc_url=None)
        - StaticFiles montado em /static
        - app.state.db_path = db_path
        - app.state.templates = Jinja2Templates(TEMPLATES_DIR) — TEMPLATES_DIR = Path(__file__).parent / "templates"
        - GET "/" redireciona para "/ranking" via RedirectResponse(status_code=302)
        - include_router para ranking e health
        - Chamar run_migrations(db_path) ao inicializar

    12. Atualizar mis/__main__.py:
        - Adicionar subparser "dashboard" com --port (default 8000) e --host (default "127.0.0.1")
        - Adicionar elif args.command == "dashboard": _handle_dashboard(args)
        - Implementar _handle_dashboard:
          ```python
          def _handle_dashboard(args) -> None:
              import uvicorn, os
              from mis.web.app import create_app
              db_path = os.environ.get("MIS_DB_PATH", "data/mis.db")
              app = create_app(db_path=db_path)
              uvicorn.run(app, host=args.host, port=args.port)
          ```
  </action>
  <verify>
    <automated>cd /c/Users/Gabriel/MEGABRAIN/mis && python -m pytest tests/web/test_web_ranking.py -x -q</automated>
  </verify>
  <done>Todos os 5 testes de test_web_ranking.py GREEN. GET /ranking retorna 200. Partial /ranking/table sem navbar. Servidor sobe via create_app(). __main__.py aceita subcomando dashboard sem erro de parse.</done>
</task>

</tasks>

<verification>
Testes de ranking GREEN: `cd mis && python -m pytest tests/web/test_web_ranking.py tests/web/test_alert_repository.py tests/web/test_dossier_repository.py tests/web/test_pain_repository.py -x -q`

App factory importável: `python -c "from mis.web.app import create_app; app = create_app(':memory:'); print('OK')"`

Migration _005 idempotente: rodar run_migrations() duas vezes no mesmo banco não levanta exceção.

CLI: `python -m mis dashboard --help` mostra o subcomando.
</verification>

<success_criteria>
Testes de ranking e repositórios todos GREEN. `python -m mis dashboard --help` mostra o subcomando. FastAPI app factory importável sem erro. ranking_table.html NÃO contém `{% extends %}`.
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/05-dashboard/05-03-SUMMARY.md`
</output>
