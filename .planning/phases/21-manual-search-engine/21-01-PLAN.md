---
phase: 21-manual-search-engine
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - mis/tests/test_search_repository.py
  - mis/tests/test_search_orchestrator.py
  - mis/tests/web/test_web_search.py
autonomous: true
requirements:
  - SEARCH-01
  - SEARCH-02
  - SEARCH-03

must_haves:
  truths:
    - "Todos os arquivos de teste existem e são coletados por pytest sem ImportError"
    - "Cada teste falha com ImportError ou NameError (RED confirmado) — nenhum teste passa"
    - "Os stubs cobrem migration _009, search_repository, search_orchestrator e rotas web"
  artifacts:
    - path: "mis/tests/test_search_repository.py"
      provides: "Testes RED para SEARCH-01 (create/list session), SEARCH-02 (list_session_products), SEARCH-03 (startup timeout)"
      min_lines: 60
    - path: "mis/tests/test_search_orchestrator.py"
      provides: "Testes RED para run_manual_search: timeout, resultado parcial, isolamento de erro por plataforma"
      min_lines: 40
    - path: "mis/tests/web/test_web_search.py"
      provides: "Testes RED para rotas FastAPI: GET /pesquisar, POST /search/run, SEARCH-03 sem scheduler"
      min_lines: 50
  key_links:
    - from: "mis/tests/test_search_repository.py"
      to: "mis/search_repository.py"
      via: "import direto"
      pattern: "from mis.search_repository import"
    - from: "mis/tests/test_search_orchestrator.py"
      to: "mis/search_orchestrator.py"
      via: "import direto"
      pattern: "from mis.search_orchestrator import"
    - from: "mis/tests/web/test_web_search.py"
      to: "mis/web/routes/search.py"
      via: "fixture app_client + create_app"
      pattern: "from mis.web.routes.search import"
---

<objective>
Escrever todos os testes RED da Fase 21 antes de qualquer implementação.

Purpose: TDD RED — definir o contrato observável de cada módulo antes de construí-lo. Os testes devem falhar com ImportError ou NameError porque os módulos ainda não existem.
Output: Três arquivos de teste com 11 testes cobrindo SEARCH-01, SEARCH-02, SEARCH-03 — todos vermelhos.
</objective>

<execution_context>
@C:/Users/Gabriel/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Gabriel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/21-manual-search-engine/21-CONTEXT.md
@.planning/phases/21-manual-search-engine/21-RESEARCH.md
@.planning/phases/21-manual-search-engine/21-VALIDATION.md

<interfaces>
<!-- Contratos que os testes devem verificar. Extraídos do codebase. -->

From mis/db.py:
```python
def run_migrations(db_path: str) -> None: ...
def get_db(db_path: str) -> sqlite_utils.Database: ...
```

From mis/niche_repository.py:
```python
def list_niches(db_path: str) -> list[dict]: ...           # retorna [{"id", "name", "slug"}]
def list_subniches(db_path: str, niche_slug: str) -> list[dict]: ...
def get_platform_slug(db_path: str, subniche_id: int, platform_slug: str) -> str | None: ...
```

From mis/scanner.py (Product dataclass):
```python
@dataclass
class Product:
    external_id: str
    title: str
    url: str
    platform_id: int
    niche_id: int        # FK para tabela 'niches' LEGADA (não niches_v3)
    rank: int
    price: Optional[float] = None
    commission_pct: Optional[float] = None
    rating: Optional[float] = None
    thumbnail_url: Optional[str] = None
```

Contratos a verificar (módulos que AINDA NÃO EXISTEM — causam RED):

mis/search_repository.py deve exportar:
    create_session(db_path: str, subniche_id: int) -> int
    get_session(db_path: str, session_id: int) -> dict
    update_session_status(db_path: str, session_id: int, status: str,
                          platform_statuses: dict, product_count: int) -> None
    list_recent_sessions(db_path: str, limit: int = 10) -> list[dict]
    list_session_products(db_path: str, session_id: int,
                          platform_filter: str | None = None,
                          per_page: int = 20, page: int = 1) -> list[dict]
    delete_session(db_path: str, session_id: int) -> None
    mark_stale_running_sessions(db_path: str) -> int  # retorna contagem de sessões marcadas

mis/search_orchestrator.py deve exportar:
    run_manual_search(session_id: int, subniche_id: int, db_path: str) -> None  # coroutine
    register_task(session_id: int, task: asyncio.Task) -> None
    cancel_task(session_id: int) -> bool

mis/migrations/_009_search_sessions.py deve exportar:
    run_migration_009(db_path: str) -> None

mis/web/routes/search.py deve exportar:
    router (APIRouter)

Fixture app_client (mis/tests/web/conftest.py — já existe):
```python
@pytest.fixture
def app_client(db_path):  # TestClient com DB efêmero + create_app(db_path)
    ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Testes RED para search_repository e migration _009</name>
  <files>mis/tests/test_search_repository.py</files>
  <behavior>
    - test_migration_009_creates_tables: run_migrations(db_path) cria "search_sessions" e "search_session_products" em db.table_names()
    - test_create_and_list_session: create_session(db_path, subniche_id=1) retorna int; list_recent_sessions(db_path) retorna lista com 1 item; item contém chave "subniche_id" == 1
    - test_get_session_returns_dict: get_session(db_path, session_id) retorna dict com chaves "id", "status", "subniche_id", "platform_statuses" (dict, não string)
    - test_list_session_products: insert produto via upsert_product + linkar via update_session_status; list_session_products(db_path, session_id) retorna lista; cada item tem "rank_at_scan" e "platform_slug"
    - test_startup_marks_running_as_timeout: inserir sessão com status="running"; chamar mark_stale_running_sessions(db_path); get_session() retorna status="timeout"
    - test_delete_session_cascades: create_session; delete_session; list_recent_sessions retorna lista vazia
  </behavior>
  <action>
    Criar mis/tests/test_search_repository.py com 6 testes. Todos devem importar de módulos que NÃO EXISTEM:
    - from mis.search_repository import create_session, get_session, update_session_status, list_recent_sessions, list_session_products, delete_session, mark_stale_running_sessions
    - from mis.migrations._009_search_sessions import run_migration_009
    - from mis.db import run_migrations (já existe — import não falha)

    Usar conftest.py padrão do diretório mis/tests/ que já fornece fixture `db_path` via `tmp_path`.

    Verificar que conftest.py em mis/tests/ existe e tem fixture db_path. Se não tiver, criar fixture mínima no próprio arquivo de teste:
    ```python
    @pytest.fixture
    def db_path(tmp_path):
        return str(tmp_path / "test.db")
    ```

    Os testes test_list_session_products e test_get_session_returns_dict dependem de migration_009 + create_session funcionando. Estruturá-los para que falhem com ImportError (não com AssertionError) no estado RED atual.

    PITFALL: platform_statuses é JSON em TEXT no SQLite. get_session() deve deserializar — testar isso em test_get_session_returns_dict verificando `isinstance(session["platform_statuses"], dict)`.

    PITFALL: delete_session usa ON DELETE CASCADE — só funciona com get_db() que executa PRAGMA foreign_keys=ON. Documentar no teste com comentário.
  </action>
  <verify>
    <automated>cd C:/Users/Gabriel/MEGABRAIN && python -m pytest mis/tests/test_search_repository.py -x -q 2>&1 | head -20</automated>
  </verify>
  <done>Arquivo existe com 6 testes. Todos falham com ImportError (ou ModuleNotFoundError). Nenhum falha com SyntaxError. Saída de pytest mostra "6 failed" ou "ERROR" em todos.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Testes RED para search_orchestrator e rotas web</name>
  <files>mis/tests/test_search_orchestrator.py, mis/tests/web/test_web_search.py</files>
  <behavior>
    test_search_orchestrator.py:
    - test_run_manual_search_is_coroutine: inspect.iscoroutinefunction(run_manual_search) == True
    - test_cancel_task_returns_false_for_unknown: cancel_task(session_id=9999) == False
    - test_register_and_cancel_task: criar asyncio.Task mock; register_task(1, task); cancel_task(1) == True

    test_web_search.py:
    - test_pesquisar_page_returns_200: GET /pesquisar retorna 200
    - test_pesquisar_subniches_returns_options: GET /pesquisar/subniches?niche_slug=saude retorna 200 com "<option" no body
    - test_post_search_run_creates_session: POST /search/run com subniche_id válido retorna 302 e Location contém "/search/"
    - test_no_scheduler_on_startup: create_app(db_path, start_scheduler=False) não lança exceção; app.state.db_path == db_path
    - test_delete_search_redirects: DELETE /search/999 retorna 302 para /pesquisar
  </behavior>
  <action>
    Criar mis/tests/test_search_orchestrator.py com 3 testes importando de mis.search_orchestrator (não existe — RED).

    Usar asyncio para test_register_and_cancel_task:
    ```python
    import asyncio

    async def _dummy():
        await asyncio.sleep(10)

    def test_register_and_cancel_task():
        loop = asyncio.new_event_loop()
        task = loop.create_task(_dummy())
        register_task(1, task)
        assert cancel_task(1) is True
        loop.close()
    ```

    Criar mis/tests/web/test_web_search.py com 5 testes. Os testes de rota usam fixture app_client de mis/tests/web/conftest.py (já existe). test_no_scheduler_on_startup usa create_app diretamente:
    ```python
    from mis.web.app import create_app
    def test_no_scheduler_on_startup(db_path):
        app = create_app(db_path, start_scheduler=False)  # novo parâmetro — RED
        assert app.state.db_path == db_path
    ```

    test_post_search_run_creates_session usa subniche_id=1 (primeira subniche após migration). Verificar redirect sem follow_redirects:
    ```python
    def test_post_search_run_creates_session(app_client):
        resp = app_client.post("/search/run", data={"subniche_id": "1"}, follow_redirects=False)
        assert resp.status_code in (302, 303)
        assert "/search/" in resp.headers["location"]
    ```

    IMPORTANTE: test_pesquisar_subniches_returns_options e test_post_search_run_creates_session só funcionam quando as rotas existirem (Plan 21-03). No estado RED atual, devem falhar com 404 ou ImportError — ambos são aceitáveis como RED.
  </action>
  <verify>
    <automated>cd C:/Users/Gabriel/MEGABRAIN && python -m pytest mis/tests/test_search_orchestrator.py mis/tests/web/test_web_search.py -x -q 2>&1 | head -30</automated>
  </verify>
  <done>Dois arquivos existem. test_search_orchestrator.py tem 3 testes (todos RED por ImportError). test_web_search.py tem 5 testes (todos RED por ImportError ou 404). Nenhum teste passa ainda.</done>
</task>

</tasks>

<verification>
RED state confirmado:
```bash
cd C:/Users/Gabriel/MEGABRAIN && python -m pytest mis/tests/test_search_repository.py mis/tests/test_search_orchestrator.py mis/tests/web/test_web_search.py -q 2>&1 | tail -5
```
Saída esperada: "11 failed" ou mistura de "failed" e "error". Zero testes passando.
</verification>

<success_criteria>
- Três arquivos de teste criados com 11 testes no total
- Todos os 11 testes falham (RED confirmado)
- Nenhum SyntaxError nos arquivos de teste
- Contratos corretos verificados: import paths, assinaturas de função, comportamentos esperados
- test_no_scheduler_on_startup testa create_app(db_path, start_scheduler=False) — novo parâmetro que ainda não existe
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/21-manual-search-engine/21-01-SUMMARY.md`
</output>
