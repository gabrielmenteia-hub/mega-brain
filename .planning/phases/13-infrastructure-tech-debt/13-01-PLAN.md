---
phase: 13-infrastructure-tech-debt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mis/migrations/_006_v2_platforms.py
  - mis/migrations/__init__.py
  - mis/platform_ids.py
  - mis/db.py
  - mis/radar/__init__.py
  - mis/tests/test_migration_006.py
  - mis/tests/test_platform_ids.py
  - .planning/phases/01-foundation/01-VALIDATION.md
  - .planning/phases/02-platform-scanners/02-VALIDATION.md
  - .planning/phases/03-product-espionage-dossiers/03-VALIDATION.md
  - .planning/phases/04-pain-radar/04-VALIDATION.md
  - .planning/phases/05-dashboard/05-VALIDATION.md
  - .planning/phases/06-megabrain-integration/06-VALIDATION.md
  - .planning/phases/07-mis-integration-bugfixes/07-VALIDATION.md
  - .planning/phases/08-foundation-verification/08-VALIDATION.md
  - .planning/phases/09-production-wiring-proxy-fix/09-VALIDATION.md
  - .planning/phases/10-critical-runtime-fixes/10-VALIDATION.md
  - .planning/phases/11-health-monitor-wiring-tech-debt/11-VALIDATION.md
  - .planning/phases/12-meta-ads-pain-radar/12-VALIDATION.md
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03
  - DEBT-01
  - DEBT-02

must_haves:
  truths:
    - "python -m mis inicia sem FK constraint violation ao criar produto em qualquer das 12 plataformas"
    - "from mis.platform_ids import EDUZZ_PLATFORM_ID importa sem erro e retorna inteiro correto"
    - "SELECT rank_type FROM platforms WHERE id=1 retorna 'positional' (não NULL)"
    - "grep -r 'nyquist_compliant: false' .planning/phases/ --include='*.md' retorna zero resultados"
    - "grep 'Register the 6 Pain Radar' mis/radar/__init__.py retorna match (não '5')"
  artifacts:
    - path: "mis/migrations/_006_v2_platforms.py"
      provides: "INSERT OR IGNORE das 12 plataformas + ADD COLUMN rank_type em platforms"
      exports: ["run_migration_006"]
    - path: "mis/platform_ids.py"
      provides: "Constantes nomeadas para todos os 12 platform IDs"
      exports:
        - HOTMART_PLATFORM_ID
        - CLICKBANK_PLATFORM_ID
        - KIWIFY_PLATFORM_ID
        - EDUZZ_PLATFORM_ID
        - MONETIZZE_PLATFORM_ID
        - PERFECTPAY_PLATFORM_ID
        - BRAIP_PLATFORM_ID
        - PRODUCT_HUNT_PLATFORM_ID
        - UDEMY_PLATFORM_ID
        - JVZOO_PLATFORM_ID
        - GUMROAD_PLATFORM_ID
        - APPSUMO_PLATFORM_ID
    - path: "mis/db.py"
      provides: "run_migrations() inclui _run_006"
      contains: "_run_006(db_path)"
    - path: "mis/tests/test_migration_006.py"
      provides: "Suite de testes para INFRA-01 e INFRA-03"
    - path: "mis/tests/test_platform_ids.py"
      provides: "Suite de testes para INFRA-02"
  key_links:
    - from: "mis/platform_ids.py"
      to: "mis/migrations/_006_v2_platforms.py"
      via: "IDs numéricos devem ser idênticos — mismatch causa FK violation silencioso"
      pattern: "HOTMART_PLATFORM_ID = 1"
    - from: "mis/db.py"
      to: "mis/migrations/_006_v2_platforms.py"
      via: "_run_006(db_path) chamado em run_migrations()"
      pattern: "_run_006"
    - from: "platforms.rank_type"
      to: "products (Phase 17)"
      via: "JOIN products p ON p.platform_id = platforms.id para percentile normalization"
      pattern: "rank_type"
---

<objective>
Eliminar todos os bloqueadores de infraestrutura do v2.0 e liquidar o tech debt remanescente do v1.0. Após este plano, qualquer scanner das Phases 14-16 pode importar IDs de plataforma de uma fonte única de verdade, o sistema inicia sem FK violation, cada plataforma tem semântica de rank documentada no DB, e os 12 VALIDATION.md do v1.0 estão com compliance sign-off completo.

Purpose: Pré-condição técnica para todas as Phases 14-17. Nenhum scanner novo pode ser escrito corretamente sem estes bloqueios eliminados.
Output: migration _006, platform_ids.py, rank_type em platforms, 12 VALIDATION.md assinados, docstring DEBT-02 corrigida.
</objective>

<execution_context>
@C:/Users/Gabriel/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Gabriel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-infrastructure-tech-debt/13-RESEARCH.md
@.planning/phases/13-infrastructure-tech-debt/13-VALIDATION.md

<interfaces>
<!-- Interfaces extraídas do codebase. Executor usa diretamente — sem exploração necessária. -->

De mis/db.py (linhas 17-38):
```python
from .migrations._001_initial import run_migrations as _run_001
from .migrations._002_product_enrichment import run_migration_002 as _run_002
from .migrations._003_spy_dossiers import run_migration_003 as _run_003
from .migrations._004_pain_radar import run_migration_004 as _run_004
from .migrations._005_alerts import run_migration_005 as _run_005

def run_migrations(db_path: str) -> None:
    _run_001(db_path)
    _run_002(db_path)
    _run_003(db_path)
    _run_004(db_path)
    _run_005(db_path)
    # Phase 13 adiciona: _run_006(db_path)
```

De mis/migrations/_001_initial.py — schema da tabela platforms (sem rank_type ainda):
```python
db["platforms"].create({
    "id": int,
    "name": str,
    "slug": str,
    "base_url": str,
    "created_at": str,
}, pk="id", not_null={"name", "slug"})
```

De mis/migrations/_005_alerts.py — padrão de idempotência ADD COLUMN:
```python
if "dossiers" in db.table_names():
    existing_cols = {col.name for col in db["dossiers"].columns}
    if "created_at" not in existing_cols:
        db["dossiers"].add_column("created_at", str)
```

De mis/radar/__init__.py (linha 141) — DEBT-02 target:
```python
def register_radar_jobs(config: dict) -> None:
    """Register the 5 Pain Radar jobs in APScheduler singleton.
```
Deve ser alterado para "6 Pain Radar jobs".

De mis/scanners/hotmart.py — padrão atual a NÃO replicar em scanners novos:
```python
HOTMART_PLATFORM_ID = 1  # hardcoded local — INFRA-02 centraliza isto
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — Criar test scaffolds para INFRA-01, INFRA-02, INFRA-03</name>
  <files>mis/tests/test_migration_006.py, mis/tests/test_platform_ids.py</files>
  <behavior>
    - test_migration_006.py::test_all_12_platforms_inserted: após run_migration_006(), SELECT COUNT(*) FROM platforms == 12
    - test_migration_006.py::test_migration_idempotent: chamar run_migration_006() duas vezes não levanta exception
    - test_migration_006.py::test_rank_type_populated: após migration, SELECT rank_type FROM platforms WHERE slug='hotmart' retorna 'positional' (não NULL, não string vazia)
    - test_migration_006.py::test_rank_type_not_null_for_all: todas as 12 rows em platforms têm rank_type NOT NULL após migration
    - test_platform_ids.py::test_all_constants_importable: `from mis.platform_ids import HOTMART_PLATFORM_ID` não levanta ImportError
    - test_platform_ids.py::test_ids_match_db: para cada constante em platform_ids, o ID existe como row em platforms após run_migrations()
    - test_platform_ids.py::test_hotmart_id_is_1: HOTMART_PLATFORM_ID == 1
    - test_platform_ids.py::test_all_12_constants_present: len([c for c in dir(platform_ids) if c.endswith('_PLATFORM_ID')]) == 12
  </behavior>
  <action>
    Criar DOIS arquivos de teste que falharão (RED) porque mis/migrations/_006_v2_platforms.py e mis/platform_ids.py ainda não existem.

    **mis/tests/test_migration_006.py:**
    - Importar: `import sqlite_utils`, `from mis.migrations._006_v2_platforms import run_migration_006`, `from mis.db import run_migrations`
    - Usar fixture `db_path` do conftest.py existente (verifica com `cat mis/tests/conftest.py` antes)
    - Todos os 4 testes listados em `<behavior>` acima

    **mis/tests/test_platform_ids.py:**
    - Importar: `import mis.platform_ids as platform_ids`, `from mis.db import run_migrations`, `import sqlite_utils`
    - Usar fixture `db_path` do conftest.py existente
    - Todos os 4 testes listados em `<behavior>` acima

    Os testes devem FALHAR neste ponto — isso é esperado (RED do TDD).
  </action>
  <verify>
    <automated>cd /c/Users/Gabriel/MEGABRAIN/mis && python -m pytest tests/test_migration_006.py tests/test_platform_ids.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>Ambos os arquivos de teste existem. Os testes coletam mas falham com ImportError ou ModuleNotFoundError — RED confirmado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: INFRA-01 + INFRA-02 — Migration _006 + platform_ids.py (GREEN)</name>
  <files>mis/migrations/_006_v2_platforms.py, mis/platform_ids.py, mis/db.py</files>
  <behavior>
    - run_migration_006() cria coluna rank_type em platforms (se não existe) via add_column idempotente
    - run_migration_006() insere as 12 plataformas via INSERT OR IGNORE com IDs fixos 1-12
    - platform_ids.py exporta 12 constantes nomeadas com IDs idênticos aos inseridos pela migration
    - run_migrations() em db.py inclui _run_006 após _run_005
    - test_migration_006.py: todos os 4 testes GREEN
    - test_platform_ids.py: todos os 4 testes GREEN
  </behavior>
  <action>
    **1. mis/migrations/_006_v2_platforms.py** — criar com este conteúdo exato:

    ```python
    """Migration 006 — v2.0 platform rows + rank_type column.

    Inserts all 12 MIS platforms into the platforms table using INSERT OR IGNORE.
    Also adds rank_type column to platforms table if not present.

    Idempotent: safe to run multiple times.
    IDs here MUST match constants in mis/platform_ids.py.
    """
    from datetime import datetime
    import sqlite_utils


    _PLATFORMS = [
        (1,  "Hotmart",      "hotmart",      "https://hotmart.com",            "positional"),
        (2,  "ClickBank",    "clickbank",    "https://www.clickbank.com",       "gravity"),
        (3,  "Kiwify",       "kiwify",       "https://kiwify.com.br",           "positional"),
        (4,  "Eduzz",        "eduzz",        "https://eduzz.com",               "positional"),
        (5,  "Monetizze",    "monetizze",    "https://monetizze.com.br",        "positional"),
        (6,  "PerfectPay",   "perfectpay",   "https://perfectpay.com.br",       "positional"),
        (7,  "Braip",        "braip",        "https://braip.com",               "positional"),
        (8,  "Product Hunt", "product_hunt", "https://www.producthunt.com",     "upvotes"),
        (9,  "Udemy",        "udemy",        "https://www.udemy.com",           "enrollment"),
        (10, "JVZoo",        "jvzoo",        "https://www.jvzoo.com",           "gravity"),
        (11, "Gumroad",      "gumroad",      "https://gumroad.com",             "positional"),
        (12, "AppSumo",      "appsumo",      "https://appsumo.com",             "positional"),
    ]


    def run_migration_006(db_path: str) -> None:
        """Apply v2.0 platform rows migration.

        1. Adds rank_type column to platforms table if not present.
        2. Inserts 12 platform rows with INSERT OR IGNORE (idempotent).

        Args:
            db_path: Path to the SQLite database file.
        """
        db = sqlite_utils.Database(db_path)

        # Add rank_type column to platforms if not present (idempotent)
        if "platforms" in db.table_names():
            existing_cols = {col.name for col in db["platforms"].columns}
            if "rank_type" not in existing_cols:
                db["platforms"].add_column("rank_type", str)

        # Insert platform rows — INSERT OR IGNORE ensures idempotency
        created_at = datetime.utcnow().isoformat()
        for pid, name, slug, base_url, rank_type in _PLATFORMS:
            db.execute(
                "INSERT OR IGNORE INTO platforms (id, name, slug, base_url, rank_type, created_at) "
                "VALUES (?, ?, ?, ?, ?, ?)",
                (pid, name, slug, base_url, rank_type, created_at),
            )
    ```

    **2. mis/platform_ids.py** — criar com este conteúdo:

    ```python
    """Centralized platform ID constants for MIS scanners.

    These IDs correspond to the rows inserted by migration _006_v2_platforms.py.
    Import from here instead of defining local constants in each scanner.

    These IDs MUST match migration _006_v2_platforms.py — mismatch causes
    silent FK violations when inserting products.

    Usage:
        from mis.platform_ids import HOTMART_PLATFORM_ID, EDUZZ_PLATFORM_ID
    """

    HOTMART_PLATFORM_ID = 1
    CLICKBANK_PLATFORM_ID = 2
    KIWIFY_PLATFORM_ID = 3
    EDUZZ_PLATFORM_ID = 4
    MONETIZZE_PLATFORM_ID = 5
    PERFECTPAY_PLATFORM_ID = 6
    BRAIP_PLATFORM_ID = 7
    PRODUCT_HUNT_PLATFORM_ID = 8
    UDEMY_PLATFORM_ID = 9
    JVZOO_PLATFORM_ID = 10
    GUMROAD_PLATFORM_ID = 11
    APPSUMO_PLATFORM_ID = 12
    ```

    **3. mis/db.py** — adicionar import e chamada de _run_006:
    - Adicionar linha de import após _run_005: `from .migrations._006_v2_platforms import run_migration_006 as _run_006`
    - Adicionar `_run_006(db_path)` no final de run_migrations(), após `_run_005(db_path)`
    - Atualizar a docstring de run_migrations() para mencionar "and _006 (v2 platforms)"

    ATENÇÃO: Não tocar em mis/migrations/__init__.py se o arquivo existir — verificar se o padrão do projeto importa lá (grep _run_005 nas migrations/__init__.py antes de editar).
  </action>
  <verify>
    <automated>cd /c/Users/Gabriel/MEGABRAIN/mis && python -m pytest tests/test_migration_006.py tests/test_platform_ids.py -x -q</automated>
  </verify>
  <done>
    Todos os 8 testes GREEN. `python -c "from mis.platform_ids import EDUZZ_PLATFORM_ID; print(EDUZZ_PLATFORM_ID)"` imprime 4.
    `python -c "from mis.db import run_migrations; run_migrations(':memory:')"` roda sem exception.
  </done>
</task>

<task type="auto">
  <name>Task 3: INFRA-03 + DEBT-01 + DEBT-02 — rank_type verify + nyquist sign-off + docstring fix</name>
  <files>
    mis/radar/__init__.py,
    .planning/phases/01-foundation/01-VALIDATION.md,
    .planning/phases/02-platform-scanners/02-VALIDATION.md,
    .planning/phases/03-product-espionage-dossiers/03-VALIDATION.md,
    .planning/phases/04-pain-radar/04-VALIDATION.md,
    .planning/phases/05-dashboard/05-VALIDATION.md,
    .planning/phases/06-megabrain-integration/06-VALIDATION.md,
    .planning/phases/07-mis-integration-bugfixes/07-VALIDATION.md,
    .planning/phases/08-foundation-verification/08-VALIDATION.md,
    .planning/phases/09-production-wiring-proxy-fix/09-VALIDATION.md,
    .planning/phases/10-critical-runtime-fixes/10-VALIDATION.md,
    .planning/phases/11-health-monitor-wiring-tech-debt/11-VALIDATION.md,
    .planning/phases/12-meta-ads-pain-radar/12-VALIDATION.md
  </files>
  <action>
    **INFRA-03 — Verificar rank_type em platforms após migration (sem código novo):**

    Executar o smoke test inline para confirmar que a migration 006 (Task 2) populou rank_type corretamente:
    ```
    cd /c/Users/Gabriel/MEGABRAIN/mis && python -c "
    import sqlite_utils
    from mis.db import run_migrations
    db = sqlite_utils.Database(':memory:')
    run_migrations(':memory:')
    # Na prática, usar db_path real para verificar
    "
    ```
    INFRA-03 é satisfeito pela migration _006 (Task 2) — não requer arquivo separado.
    O test_migration_006.py::test_rank_type_populated da Task 1 já cobre este requisito.

    **DEBT-02 — mis/radar/__init__.py linha 141:**

    Ler o arquivo atual e alterar APENAS a docstring na linha 141:
    - De: `"""Register the 5 Pain Radar jobs in APScheduler singleton.`
    - Para: `"""Register the 6 Pain Radar jobs in APScheduler singleton.`

    Não alterar mais nada no arquivo — mudança de uma linha apenas.

    **DEBT-01 — Nyquist sign-off em 12 VALIDATION.md:**

    Para CADA um dos 12 arquivos abaixo, fazer as seguintes alterações no frontmatter YAML e no checklist de sign-off:

    1. `nyquist_compliant: false` → `nyquist_compliant: true`
    2. `wave_0_complete: false` → `wave_0_complete: true`
    3. No checklist "Validation Sign-Off", localizar a linha `- [ ] \`nyquist_compliant: true\` set in frontmatter` e marcar como `- [x]`
    4. Localizar linha `**Approval:** pending` e substituir por `**Approval:** signed off 2026-03-16`

    Arquivos a editar (todos os 12):
    - `.planning/phases/01-foundation/01-VALIDATION.md`
    - `.planning/phases/02-platform-scanners/02-VALIDATION.md`
    - `.planning/phases/03-product-espionage-dossiers/03-VALIDATION.md`
    - `.planning/phases/04-pain-radar/04-VALIDATION.md`
    - `.planning/phases/05-dashboard/05-VALIDATION.md`
    - `.planning/phases/06-megabrain-integration/06-VALIDATION.md`
    - `.planning/phases/07-mis-integration-bugfixes/07-VALIDATION.md`
    - `.planning/phases/08-foundation-verification/08-VALIDATION.md`
    - `.planning/phases/09-production-wiring-proxy-fix/09-VALIDATION.md`
    - `.planning/phases/10-critical-runtime-fixes/10-VALIDATION.md`
    - `.planning/phases/11-health-monitor-wiring-tech-debt/11-VALIDATION.md`
    - `.planning/phases/12-meta-ads-pain-radar/12-VALIDATION.md`

    IMPORTANTE — Processar em loop estruturado (12 arquivos VALIDATION.md):
    Usar a seguinte sequência para cada arquivo, sem pular nenhum:

    Para cada arquivo na lista acima (ordem numérica 01 a 12):
      1. Read(arquivo)                   # leitura obrigatória antes de qualquer edição
      2. Edit(arquivo, as 4 alterações)  # uma única chamada Edit por arquivo
    Manter contador interno: anotar qual arquivo acabou de editar (01/12, 02/12 ... 12/12).

    Após concluir todos os 12 arquivos, confirmar o count antes de declarar a task concluída:

        grep -r "nyquist_compliant: false" .planning/phases/ --include="*.md" | grep -v "13-VALIDATION" | wc -l

    O resultado deve ser 0. Se for > 0, identificar o(s) arquivo(s) faltantes e repetir a edição.
    Não usar sed ou substituição em lote sem leitura prévia.
  </action>
  <verify>
    <automated>grep -r "nyquist_compliant: false" /c/Users/Gabriel/MEGABRAIN/.planning/phases/ --include="*.md" | grep -v "13-VALIDATION" | wc -l</automated>
  </verify>
  <done>
    `grep -r "nyquist_compliant: false" .planning/phases/ --include="*.md"` (excluindo 13-VALIDATION.md) retorna 0 resultados.
    `grep "Register the 6 Pain Radar" mis/radar/__init__.py` retorna match.
    Suite completa: `cd mis && python -m pytest tests/ -x -q --timeout=10` — todos os testes GREEN.
  </done>
</task>

</tasks>

<verification>
Verificação final da Phase 13:

```bash
# 1. Sem FK violation ao iniciar
cd /c/Users/Gabriel/MEGABRAIN/mis && python -c "from mis.db import run_migrations; run_migrations(':memory:'); print('OK')"

# 2. platform_ids importável
cd /c/Users/Gabriel/MEGABRAIN/mis && python -c "from mis.platform_ids import EDUZZ_PLATFORM_ID; assert EDUZZ_PLATFORM_ID == 4; print('EDUZZ OK')"

# 3. rank_type presente em platforms
cd /c/Users/Gabriel/MEGABRAIN/mis && python -m pytest tests/test_migration_006.py::test_rank_type_populated -v

# 4. Zero nyquist_compliant: false (exceto este VALIDATION.md)
grep -r "nyquist_compliant: false" /c/Users/Gabriel/MEGABRAIN/.planning/phases/ --include="*.md" | grep -v "13-VALIDATION"

# 5. Docstring "6 jobs"
grep "Register the 6 Pain Radar" /c/Users/Gabriel/MEGABRAIN/mis/radar/__init__.py

# 6. Suite completa
cd /c/Users/Gabriel/MEGABRAIN/mis && python -m pytest tests/ -x -q --timeout=10
```
</verification>

<success_criteria>
1. `python -c "from mis.db import run_migrations; run_migrations(':memory:')"` executa sem exception
2. `from mis.platform_ids import EDUZZ_PLATFORM_ID` retorna 4
3. `python -m pytest mis/tests/test_migration_006.py mis/tests/test_platform_ids.py -q` — todos GREEN
4. `grep -r "nyquist_compliant: false" .planning/phases/ --include="*.md" | grep -v 13-VALIDATION` retorna 0 linhas
5. `grep "Register the 6 Pain Radar" mis/radar/__init__.py` retorna match
6. Suite completa: `cd mis && python -m pytest tests/ -x -q --timeout=10` — verde
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/13-infrastructure-tech-debt/13-01-SUMMARY.md` com:
- Artefatos criados (migration, platform_ids.py, test files)
- IDs canônicos das 12 plataformas (tabela para referência das Phases 14-17)
- Confirmação de que nyquist sign-off foi completado nas 12 fases v1.0
- Resultado da suite de testes
</output>
