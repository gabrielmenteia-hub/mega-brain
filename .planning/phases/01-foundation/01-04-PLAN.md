---
phase: 01-foundation
plan: "04"
type: execute
wave: 2
depends_on:
  - "01-01"
  - "01-02"
  - "01-03"
files_modified:
  - mis/health_monitor.py
  - mis/tests/test_health_monitor.py
autonomous: true
requirements:
  - FOUND-04

must_haves:
  truths:
    - "run_canary_check() retorna True quando fetch retorna conteudo > 100 bytes"
    - "run_canary_check() retorna False e loga SCRAPER_RETURNING_EMPTY_RESPONSE quando conteudo < 100 bytes"
    - "run_canary_check() retorna False e loga SCRAPER_BROKEN_CANARY_FAILED quando ScraperError e levantada"
    - "canary job esta registrado no APScheduler com intervalo de 15 minutos"
    - "alertas sao emitidos via structlog JSON (legivel por ferramentas externas)"
  artifacts:
    - path: "mis/health_monitor.py"
      provides: "run_canary_check() async, CANARY_URL, CANARY_MIN_LENGTH, register_canary_job()"
      exports: ["run_canary_check", "register_canary_job", "CANARY_URL"]
    - path: "mis/tests/test_health_monitor.py"
      provides: "3 testes cobrindo FOUND-04: healthy, empty, scraper_error"
  key_links:
    - from: "mis/health_monitor.py"
      to: "mis/base_scraper.py"
      via: "async with BaseScraper() as scraper: content = await scraper.fetch(CANARY_URL)"
      pattern: "BaseScraper"
    - from: "mis/health_monitor.py"
      to: "mis/scheduler.py"
      via: "register_canary_job() chama get_scheduler().add_job()"
      pattern: "get_scheduler"
    - from: "mis/tests/test_health_monitor.py"
      to: "mis/health_monitor.py"
      via: "unittest.mock.patch('mis.health_monitor.BaseScraper') mocka o contexto inteiro"
      pattern: "patch.*BaseScraper"
---

<objective>
Implementar o health monitor com canary check: uma funcao async que usa BaseScraper para buscar uma URL de referencia (httpbin.org/get), verifica que a resposta tem conteudo substancial, e emite alertas estruturados via structlog quando o scraper falha ou retorna conteudo vazio. Registrar o job canary no APScheduler para execucao a cada 15 minutos.

Purpose: FOUND-04 exige que scrapers quebrados sejam detectados sem intervencao manual. O health monitor e o mecanismo de auto-deteccao — sem ele, falhas de scraper ficam silenciosas ate o usuario notar dados faltando.

Output: mis/health_monitor.py com run_canary_check() e register_canary_job(). 3 testes unitarios passando (sem chamadas reais de rede).
</objective>

<execution_context>
@C:/Users/Gabriel/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Gabriel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
@.planning/phases/01-foundation/01-03-SUMMARY.md

<interfaces>
<!-- Contratos disponiveis para este plano — criados nos planos anteriores -->

De mis/exceptions.py:
```python
class ScraperError(Exception):
    url: str
    attempts: int
    cause: Exception
```

De mis/base_scraper.py:
```python
class BaseScraper:
    async def __aenter__(self) -> "BaseScraper": ...
    async def __aexit__(self, *_) -> None: ...
    async def fetch(self, url: str) -> str: ...
    # Levanta ScraperError se todas as tentativas falharem
    # NUNCA retorna None
```

De mis/scheduler.py:
```python
def get_scheduler() -> AsyncIOScheduler: ...
def start_scheduler() -> None: ...
def stop_scheduler() -> None: ...
# Uso: get_scheduler().add_job(coro_func, "interval", minutes=15, id="job_id")
```

De mis/config.py:
```python
def load_config(config_path=None) -> dict: ...
# cfg["niches"] = lista de dicts com name, slug, keywords
# cfg["settings"]["proxy_url"] = str (vazio se sem proxy)
```

Pattern de mock para BaseScraper (da VALIDATION.md):
```python
from unittest.mock import AsyncMock, patch

async def test_canary_empty_response():
    with patch("mis.health_monitor.BaseScraper") as MockScraper:
        instance = MockScraper.return_value.__aenter__.return_value
        instance.fetch = AsyncMock(return_value="short")  # < 100 chars
        result = await run_canary_check()
    assert result is False
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: run_canary_check() com alertas structlog</name>
  <files>
    mis/health_monitor.py,
    mis/tests/test_health_monitor.py
  </files>
  <behavior>
    - test_canary_healthy: mock BaseScraper.fetch retorna string com 200+ chars; run_canary_check() retorna True; nenhum log de erro emitido
    - test_canary_empty_response: mock retorna string com 50 chars (< 100); retorna False; structlog capturado tem event="health.canary.empty" e alert="SCRAPER_RETURNING_EMPTY_RESPONSE"
    - test_canary_scraper_error: mock levanta ScraperError("url", 3, Exception("timeout")); retorna False; structlog capturado tem event="health.canary.failed" e alert="SCRAPER_BROKEN_CANARY_FAILED"
    - CANARY_URL = "https://httpbin.org/get" — constante exportada
    - CANARY_MIN_LENGTH = 100 — bytes minimos esperados na resposta valida
    - run_canary_check() nunca propaga excecao — captura tudo internamente e retorna bool
  </behavior>
  <action>
    RED: Substituir stubs de test_health_monitor.py pelos testes reais.

    1. Escrever mis/tests/test_health_monitor.py:
       - Usar `unittest.mock.patch("mis.health_monitor.BaseScraper")` para mockar o contexto manager
       - Para capturar logs structlog nos testes, usar `structlog.testing.capture_logs()` context manager (disponivel em structlog>=21.5)
       - test_canary_healthy: mock fetch retorna "x" * 200; assert result is True
       - test_canary_empty_response: mock fetch retorna "x" * 50; assert result is False; com capture_logs() verificar que um log tem alert="SCRAPER_RETURNING_EMPTY_RESPONSE"
       - test_canary_scraper_error: mock levanta ScraperError; assert result is False; verificar log com alert="SCRAPER_BROKEN_CANARY_FAILED"

    Confirmar RED: pytest mis/tests/test_health_monitor.py -x falha.

    GREEN: Implementar.

    2. Criar mis/health_monitor.py:
       ```python
       import structlog
       from .base_scraper import BaseScraper
       from .exceptions import ScraperError

       log = structlog.get_logger()

       CANARY_URL = "https://httpbin.org/get"
       CANARY_MIN_LENGTH = 100


       async def run_canary_check() -> bool:
           """Run one canary check against a known-good URL.
           Returns True if healthy, False if degraded. Never propagates exceptions.
           Emits structured alert logs on failure for external parsing.
           """
           async with BaseScraper() as scraper:
               try:
                   content = await scraper.fetch(CANARY_URL)
                   if len(content) < CANARY_MIN_LENGTH:
                       log.warning(
                           "health.canary.empty",
                           url=CANARY_URL,
                           content_length=len(content),
                           alert="SCRAPER_RETURNING_EMPTY_RESPONSE",
                       )
                       return False
                   log.info("health.canary.ok", url=CANARY_URL, content_length=len(content))
                   return True
               except ScraperError as exc:
                   log.error(
                       "health.canary.failed",
                       url=CANARY_URL,
                       attempts=exc.attempts,
                       cause=str(exc.cause),
                       alert="SCRAPER_BROKEN_CANARY_FAILED",
                   )
                   return False
               except Exception as exc:
                   # Unexpected error — still return False, do not propagate
                   log.error(
                       "health.canary.unexpected_error",
                       url=CANARY_URL,
                       error=str(exc),
                       alert="SCRAPER_BROKEN_CANARY_FAILED",
                   )
                   return False


       def register_canary_job() -> None:
           """Register the canary health check job in the global APScheduler.
           Runs every 15 minutes. Safe to call multiple times (job is replaced if exists).
           """
           from .scheduler import get_scheduler
           scheduler = get_scheduler()
           scheduler.add_job(
               run_canary_check,
               trigger="interval",
               minutes=15,
               id="health_canary",
               replace_existing=True,
           )
           log.info("health.canary.registered", interval_minutes=15)
       ```

    NOTA: `structlog.testing.capture_logs()` captura logs emitidos por structlog nos testes. Se nao disponivel na versao instalada, usar `structlog.contextvars.clear_contextvars()` + output redirect.
  </action>
  <verify>
    <automated>cd /c/Users/Gabriel/MEGABRAIN && python -m pytest mis/tests/test_health_monitor.py -v --timeout=10</automated>
  </verify>
  <done>3 testes test_health_monitor.py passando: test_canary_healthy, test_canary_empty_response, test_canary_scraper_error. Nenhuma chamada real de rede nos testes.</done>
</task>

<task type="auto">
  <name>Task 2: Suite completa e verificacao final da Phase 1</name>
  <files>
    mis/tests/test_db.py,
    mis/tests/test_base_scraper.py,
    mis/tests/test_config.py,
    mis/tests/test_health_monitor.py
  </files>
  <action>
    Rodar a suite completa e corrigir qualquer falha residual antes de declarar Phase 1 completa.

    1. Rodar: `pytest mis/tests/ -v --timeout=30`
    2. Se algum teste falhar:
       - Diagnosticar a causa (ImportError, fixture problem, assertion error)
       - Corrigir no arquivo correto — nao pular testes com pytest.mark.skip
       - Re-rodar ate todos GREEN
    3. Verificar que os stubs que foram pre-criados em 01-01 e estavam RED agora estao GREEN (todos os 15 testes do VALIDATION.md devem passar)
    4. Verificar import chain completo sem erros:
       ```
       python -c "from mis.db import run_migrations, get_db; from mis.base_scraper import BaseScraper; from mis.config import load_config; from mis.health_monitor import run_canary_check, register_canary_job; from mis.scheduler import get_scheduler; print('all imports OK')"
       ```
    5. Verificar que register_canary_job() registra no scheduler sem erro:
       ```
       python -c "
       from mis.health_monitor import register_canary_job
       from mis.scheduler import get_scheduler
       register_canary_job()
       jobs = get_scheduler().get_jobs()
       print('jobs registered:', [j.id for j in jobs])
       assert any(j.id == 'health_canary' for j in jobs), 'health_canary job not found'
       print('canary job OK')
       "
       ```
  </action>
  <verify>
    <automated>cd /c/Users/Gabriel/MEGABRAIN && python -m pytest mis/tests/ -v --timeout=30</automated>
  </verify>
  <done>Todos os 15 testes (test_db: 3, test_base_scraper: 6, test_config: 3, test_health_monitor: 3) passando com status GREEN. Import chain completo sem erro. register_canary_job() registra job com id="health_canary".</done>
</task>

</tasks>

<verification>
Suite final completa da Phase 1:

```
cd /c/Users/Gabriel/MEGABRAIN && python -m pytest mis/tests/ -v --timeout=30
```

Contagem esperada: 15 passed, 0 failed, 0 error.

Verificar chain de imports:
```
python -c "
from mis.db import run_migrations, get_db
from mis.base_scraper import BaseScraper
from mis.config import load_config
from mis.health_monitor import run_canary_check, register_canary_job, CANARY_URL
from mis.scheduler import get_scheduler, start_scheduler, stop_scheduler
from mis.exceptions import ScraperError
print('ALL IMPORTS OK')
print('CANARY_URL:', CANARY_URL)
"
```

Verificar canary registrado no scheduler:
```
python -c "
from mis.health_monitor import register_canary_job
from mis.scheduler import get_scheduler
register_canary_job()
jobs = get_scheduler().get_jobs()
print('jobs:', [j.id for j in jobs])
"
```
</verification>

<success_criteria>
- mis/health_monitor.py exporta run_canary_check(), register_canary_job(), CANARY_URL, CANARY_MIN_LENGTH
- run_canary_check() retorna bool, nunca propaga excecao, emite alertas JSON via structlog
- alert="SCRAPER_RETURNING_EMPTY_RESPONSE" quando conteudo < CANARY_MIN_LENGTH
- alert="SCRAPER_BROKEN_CANARY_FAILED" quando ScraperError levantada
- register_canary_job() registra job com id="health_canary", intervalo=15 min, replace_existing=True
- 3 testes test_health_monitor.py passando (sem rede real)
- Suite completa: 15/15 testes passando (todos os 4 arquivos de teste)
- Todos os modulos importam sem erro
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/01-foundation/01-04-SUMMARY.md` com:
- Assinatura de run_canary_check() e run_canary_check() e seus alertas
- CANARY_URL e CANARY_MIN_LENGTH configurados
- Como o job e registrado no scheduler (pattern para Fase 2 adicionar mais jobs)
- Contagem final da suite: X/15 testes GREEN
- Confirmacao de que Phase 1 esta completa e Phase 2 pode comecar
</output>
