---
phase: 09-production-wiring-proxy-fix
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - mis/tests/test_proxy_forwarding.py
  - mis/tests/test_lifespan.py
  - mis/scanner.py
  - mis/scanners/hotmart.py
  - mis/scanners/kiwify.py
  - mis/scanners/clickbank.py
  - mis/web/app.py
autonomous: true
requirements:
  - SCAN-04
  - FOUND-02
gap_closure: true

must_haves:
  truths:
    - "python -m mis dashboard inicia o servidor E inicia o APScheduler com jobs registrados"
    - "Configurar proxy_list nao-vazio em config.yaml nao levanta TypeError em nenhum dos 3 scanners"
    - "PlatformScanner forwards proxy_list para BaseScraper._proxy_list corretamente"
    - "scheduler.shutdown() e chamado quando o servidor para"
  artifacts:
    - path: "mis/tests/test_proxy_forwarding.py"
      provides: "Testes RED para proxy_list forwarding (5 casos)"
      exports: ["test_hotmart_accepts_proxy_list", "test_kiwify_accepts_proxy_list", "test_clickbank_accepts_proxy_list", "test_proxy_list_reaches_base_scraper", "test_run_all_scanners_proxy_list_no_typeerror"]
    - path: "mis/tests/test_lifespan.py"
      provides: "Testes RED para lifespan hook (3 casos)"
      exports: ["test_lifespan_registers_jobs_on_startup", "test_lifespan_shutdown_on_exit", "test_lifespan_scheduler_has_jobs"]
    - path: "mis/scanner.py"
      provides: "PlatformScanner com proxy_list aceito e propagado"
      contains: "proxy_list: Optional[list[str]] = None"
    - path: "mis/web/app.py"
      provides: "create_app com lifespan hook que registra e inicia scheduler"
      contains: "asynccontextmanager"
  key_links:
    - from: "mis/web/app.py create_app()"
      to: "mis.scheduler.register_scan_and_spy_job + register_radar_jobs + register_canary_job"
      via: "asynccontextmanager lifespan -> get_scheduler().start()"
      pattern: "lifespan.*register_scan_and_spy_job"
    - from: "mis/scanner.py run_all_scanners()"
      to: "mis/scanners/hotmart.py + kiwify.py + clickbank.py"
      via: "scanner_cls(proxy_url=proxy_url, proxy_list=proxy_list)"
      pattern: "proxy_list.*BaseScraper"
---

<objective>
Fechar os 2 gaps de integração identificados no audit v1.0:

1. SCAN-04: FastAPI `create_app()` nao tem lifespan hook — APScheduler nunca inicia quando `python -m mis dashboard` sobe. Jobs de scan, spy e radar nao disparam automaticamente.

2. FOUND-02: `PlatformScanner.__init__` nao aceita `proxy_list` — `run_all_scanners()` passa `proxy_list=proxy_list` mas `PlatformScanner` so aceita `proxy_url`, causando `TypeError` em runtime sempre que `proxy_list` esta configurado em `config.yaml`.

Ambos os fixes sao cirurgicos: todos os componentes existem e funcionam em isolamento, o problema e apenas de wiring.

Purpose: Fechar os ultimos 2 gaps bloqueantes para o milestone v1.0 — sem novos features, sem mudancas em assinaturas publicas.
Output: 2 arquivos de teste novos + 5 arquivos de producao editados, todos testes GREEN.
</objective>

<execution_context>
@C:/Users/Gabriel/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Gabriel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-production-wiring-proxy-fix/09-CONTEXT.md
@.planning/phases/09-production-wiring-proxy-fix/09-RESEARCH.md
@.planning/phases/09-production-wiring-proxy-fix/09-VALIDATION.md
@.planning/v1.0-MILESTONE-AUDIT.md

<interfaces>
<!-- Interfaces existentes que o executor vai usar — extraidas do codebase -->

From mis/scanner.py (linha 63 — estado ANTES do fix):
```python
class PlatformScanner:
    def __init__(self, proxy_url: Optional[str] = None) -> None:
        self._base = BaseScraper(proxy_url=proxy_url)
```

From mis/scanner.py (linha 195-206 — run_all_scanners ja passa proxy_list):
```python
proxy_url: Optional[str] = settings.get("proxy_url") or None
proxy_list: list[str] = settings.get("proxy_list") or []
# ...
async with scanner_cls(proxy_url=proxy_url, proxy_list=proxy_list) as scanner:
    # TypeError aqui se PlatformScanner nao aceitar proxy_list
```

From mis/scanners/hotmart.py (linha 116-122 — estado ANTES do fix):
```python
def __init__(
    self,
    proxy_url: Optional[str] = None,
    niche_id: int = 0,
) -> None:
    super().__init__(proxy_url=proxy_url)
    self._default_niche_id = niche_id
```

From mis/scanners/kiwify.py (linha 227-232 — estado ANTES do fix):
```python
def __init__(
    self,
    proxy_url: Optional[str] = None,
    niche_id: int = 0,
) -> None:
    super().__init__(proxy_url=proxy_url)
```

From mis/scanners/clickbank.py (linha 146-151 — estado ANTES do fix):
```python
def __init__(
    self,
    proxy_url: Optional[str] = None,
    niche_id: int = 0,
) -> None:
    super().__init__(proxy_url=proxy_url)
```

From mis/web/app.py (linha 20 — estado ANTES do fix):
```python
def create_app(db_path: str) -> FastAPI:
    app = FastAPI(
        title="MIS Dashboard",
        docs_url=None,
        redoc_url=None,
    )
    # sem lifespan hook
```

From mis/scheduler.py (funcoes existentes):
```python
def get_scheduler() -> AsyncIOScheduler: ...  # singleton
def start_scheduler() -> None: ...             # guard: if not scheduler.running
def stop_scheduler() -> None: ...              # sets _scheduler = None
def register_scan_and_spy_job(config: dict) -> None: ...
```

From mis/radar/__init__.py:
```python
def register_radar_jobs(config: dict) -> None: ...
```

From mis/health_monitor.py:
```python
def register_canary_job() -> None: ...
```

From mis/base_scraper.py (confirmado Phase 8):
```python
class BaseScraper:
    def __init__(
        self,
        proxy_url: Optional[str] = None,
        proxy_list: Optional[list[str]] = None,
        ...
    ) -> None:
        self._proxy_list = list(proxy_list or [])
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — Criar test stubs RED para ambos os gaps</name>
  <files>mis/tests/test_proxy_forwarding.py, mis/tests/test_lifespan.py</files>
  <behavior>
    test_proxy_forwarding.py (5 testes, todos devem falhar RED antes do fix):
    - test_hotmart_accepts_proxy_list: HotmartScanner(proxy_url=None, proxy_list=["http://p:8080"]) nao levanta TypeError
    - test_kiwify_accepts_proxy_list: KiwifyScanner(proxy_url=None, proxy_list=["http://p:8080"]) nao levanta TypeError
    - test_clickbank_accepts_proxy_list: ClickBankScanner(proxy_url=None, proxy_list=["http://p:8080"]) nao levanta TypeError
    - test_proxy_list_reaches_base_scraper: HotmartScanner(proxy_list=["http://p:8080"])._base._proxy_list == ["http://p:8080"]
    - test_run_all_scanners_proxy_list_no_typeerror: chamar run_all_scanners com mock db e proxy_list nao-vazio nao levanta TypeError

    test_lifespan.py (3 testes, todos devem falhar RED antes do fix):
    - test_lifespan_registers_jobs_on_startup: TestClient com app criado via create_app() dispara lifespan; mock_scan, mock_radar, mock_canary foram chamados uma vez cada
    - test_lifespan_shutdown_on_exit: mock_scheduler.shutdown foi chamado com wait=False ao sair do TestClient
    - test_lifespan_scheduler_has_jobs: TestClient com scheduler real (stop_scheduler() antes e depois) — scheduler.running == True durante o bloco with

    Stubs devem ter imports no nivel de modulo para garantir falha RED na coleta do pytest (nao apenas em tempo de execucao).
  </behavior>
  <action>
    Criar mis/tests/test_proxy_forwarding.py com os 5 testes descritos. Importar HotmartScanner, KiwifyScanner, ClickBankScanner no topo do arquivo. Os testes com proxy_list devem usar pytest.raises(TypeError) inverted — ou seja, assert que NAO levanta — para confirmar RED antes do fix.

    Padrao RED para proxy tests: testar `scanner.__class__.__init__.__code__.co_varnames` para confirmar que 'proxy_list' esta nos parametros (vai falhar porque nao esta ainda), OU simplesmente instanciar com proxy_list= e capturar TypeError. O mais simples: tentar instanciar e deixar falhar naturalmente.

    Criar mis/tests/test_lifespan.py com os 3 testes descritos. Usar unittest.mock.patch para os unit tests (mock approach). Importar create_app no topo. Para test_lifespan_scheduler_has_jobs: chamar stop_scheduler() em setup e teardown via try/finally para limpar singleton.

    Patching correto: patch nos paths `mis.web.app.register_scan_and_spy_job`, `mis.web.app.register_radar_jobs`, `mis.web.app.register_canary_job`, `mis.web.app.get_scheduler` — NAO no modulo de origem.

    Confirmar que os testes FALHAM antes de implementar qualquer fix (RED obrigatorio).
  </action>
  <verify>
    <automated>cd C:/Users/Gabriel/MEGABRAIN && python -m pytest mis/tests/test_proxy_forwarding.py mis/tests/test_lifespan.py -x 2>&1 | tail -20</automated>
  </verify>
  <done>
    Os 8 testes existem e FALHAM (RED). pytest coletou os 8 testes sem erros de importacao. Saida mostra FAILED (nao ERROR de sintaxe).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Proxy forwarding fix — PlatformScanner e 3 subclasses (FOUND-02)</name>
  <files>mis/scanner.py, mis/scanners/hotmart.py, mis/scanners/kiwify.py, mis/scanners/clickbank.py</files>
  <behavior>
    - PlatformScanner.__init__ aceita proxy_list e propaga para BaseScraper
    - HotmartScanner.__init__ aceita proxy_list e passa via super()
    - KiwifyScanner.__init__ aceita proxy_list e passa via super()
    - ClickBankScanner.__init__ aceita proxy_list e passa via super()
    - Callers existentes (keyword args) continuam funcionando sem alteracao
    - scanner._base._proxy_list == proxy_list quando proxy_list e passado
  </behavior>
  <action>
    Em mis/scanner.py linha 63, alterar PlatformScanner.__init__:
    - ANTES: `def __init__(self, proxy_url: Optional[str] = None) -> None:`
    - DEPOIS: `def __init__(self, proxy_url: Optional[str] = None, proxy_list: Optional[list[str]] = None) -> None:`
    - E alterar linha 68: `self._base = BaseScraper(proxy_url=proxy_url, proxy_list=proxy_list)`

    Em mis/scanners/hotmart.py linha 116-122, alterar HotmartScanner.__init__:
    - Adicionar `proxy_list: Optional[list[str]] = None,` ENTRE proxy_url e niche_id
    - Alterar super() call: `super().__init__(proxy_url=proxy_url, proxy_list=proxy_list)`
    - Manter `self._default_niche_id = niche_id` sem alteracao

    Aplicar o mesmo padrao em mis/scanners/kiwify.py (linha 227) e mis/scanners/clickbank.py (linha 146).

    Verificar que Optional esta importado em cada arquivo (ja deve estar). Nenhuma outra alteracao — fix cirurgico de 2 linhas por arquivo.

    NAO modificar `run_all_scanners()` em scanner.py — ja passa proxy_list corretamente (linha 206).
  </action>
  <verify>
    <automated>cd C:/Users/Gabriel/MEGABRAIN && python -m pytest mis/tests/test_proxy_forwarding.py -x -v 2>&1 | tail -20</automated>
  </verify>
  <done>
    5/5 testes em test_proxy_forwarding.py passam (GREEN). HotmartScanner, KiwifyScanner e ClickBankScanner aceitam proxy_list sem TypeError. proxy_list chega em BaseScraper._proxy_list. Suite completa continua verde: `pytest mis/tests/ -x` passa.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Lifespan hook — FastAPI create_app com scheduler startup (SCAN-04)</name>
  <files>mis/web/app.py</files>
  <behavior>
    - create_app() registra register_scan_and_spy_job, register_radar_jobs, register_canary_job no startup
    - NAO registra register_scanner_jobs (seria double-scan redundante — decisao locked)
    - get_scheduler().start() e chamado apos registro dos jobs
    - Se load_config() falhar: logar warning, continuar com config={} (nao falhar hard)
    - Se qualquer register_* falhar: logar warning, continuar (nao falhar hard)
    - get_scheduler().shutdown(wait=False) e chamado no teardown
    - Assinatura de create_app(db_path: str) nao muda
    - Nenhum caller existente de create_app() precisa ser alterado
  </behavior>
  <action>
    Ler mis/web/app.py completo antes de editar.

    Adicionar imports no topo do arquivo:
    ```python
    from contextlib import asynccontextmanager
    import structlog
    ```

    Dentro de create_app(db_path: str), ANTES de `app = FastAPI(...)`, definir o lifespan como closure:
    ```python
    @asynccontextmanager
    async def lifespan(app: FastAPI):
        from mis.config import load_config
        from mis.scheduler import get_scheduler, register_scan_and_spy_job
        from mis.radar import register_radar_jobs
        from mis.health_monitor import register_canary_job

        log = structlog.get_logger(__name__)

        try:
            config = load_config()
        except Exception as exc:
            log.warning("lifespan.config_error", error=str(exc))
            config = {}

        for name, fn, args in [
            ("register_scan_and_spy_job", register_scan_and_spy_job, [config]),
            ("register_radar_jobs", register_radar_jobs, [config]),
            ("register_canary_job", register_canary_job, []),
        ]:
            try:
                fn(*args)
            except Exception as exc:
                log.warning("lifespan.register_failed", job=name, error=str(exc))

        try:
            get_scheduler().start()
            log.info("lifespan.scheduler_started")
        except Exception as exc:
            log.warning("lifespan.scheduler_start_failed", error=str(exc))

        yield

        try:
            get_scheduler().shutdown(wait=False)
            log.info("lifespan.scheduler_stopped")
        except Exception as exc:
            log.warning("lifespan.scheduler_stop_failed", error=str(exc))
    ```

    Alterar a criacao do FastAPI para incluir lifespan:
    - ANTES: `app = FastAPI(title="MIS Dashboard", docs_url=None, redoc_url=None)`
    - DEPOIS: `app = FastAPI(title="MIS Dashboard", docs_url=None, redoc_url=None, lifespan=lifespan)`

    Manter todo o restante de create_app() identico (registro de routers, state, etc).

    ATENCAO: Imports lazy (dentro do lifespan) sao intencionais para evitar circular imports e para permitir que o test patch `mis.web.app.register_scan_and_spy_job` funcione corretamente. Alternativamente, se imports no topo do modulo nao causam circulares, prefer imports no topo. Verificar se ha circular imports antes de decidir. Se nao ha circulares, mover imports para o topo e ajustar paths de patch nos testes para `mis.web.app.X`.
  </action>
  <verify>
    <automated>cd C:/Users/Gabriel/MEGABRAIN && python -m pytest mis/tests/test_lifespan.py -x -v 2>&1 | tail -20</automated>
  </verify>
  <done>
    3/3 testes em test_lifespan.py passam (GREEN). register_scan_and_spy_job, register_radar_jobs e register_canary_job foram chamados no startup. scheduler.shutdown(wait=False) foi chamado no teardown. scheduler.running == True durante o bloco with TestClient. Suite completa verde: `pytest mis/tests/ -x` passa com todos os testes anteriores intactos.
  </done>
</task>

</tasks>

<verification>
Verificacao final da fase — executar em ordem:

1. Testes especificos da fase:
   `pytest mis/tests/test_proxy_forwarding.py mis/tests/test_lifespan.py -v`
   Esperado: 8/8 GREEN

2. Suite completa:
   `pytest mis/tests/ -x`
   Esperado: todos os testes anteriores + 8 novos GREEN, nenhuma regressao

3. Smoke test de imports:
   `python -c "from mis.web.app import create_app; from mis.scanner import PlatformScanner; print('OK')"`
   Esperado: OK sem erros

4. Verificacao de assinatura (confirmar backward compat):
   `python -c "from mis.scanners.hotmart import HotmartScanner; h = HotmartScanner(proxy_url=None); print('backward compat OK')"`
   Esperado: OK (sem TypeError — callers sem proxy_list continuam funcionando)
</verification>

<success_criteria>
- [ ] test_proxy_forwarding.py: 5/5 testes GREEN (HotmartScanner, KiwifyScanner, ClickBankScanner aceitam proxy_list, proxy_list chega em _base._proxy_list, run_all_scanners nao levanta TypeError)
- [ ] test_lifespan.py: 3/3 testes GREEN (jobs registrados, shutdown chamado, scheduler running)
- [ ] Suite completa `pytest mis/tests/ -x` GREEN sem regressoes
- [ ] SCAN-04 satisfeito: `python -m mis dashboard` inicia servidor E APScheduler com jobs registrados
- [ ] FOUND-02 satisfeito: proxy_list propaga por toda a cadeia PlatformScanner -> BaseScraper
- [ ] Assinatura de create_app(db_path) inalterada
- [ ] Callers existentes de PlatformScanner(proxy_url=X) continuam funcionando
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/09-production-wiring-proxy-fix/09-01-SUMMARY.md` com:
- Frontmatter: phase, plan, requirements_completed: [SCAN-04, FOUND-02], files_modified
- Resumo dos fixes implementados
- Decisoes tomadas durante execucao (ex: lazy imports vs top-level)
- Contagem de testes: X tests GREEN
</output>
