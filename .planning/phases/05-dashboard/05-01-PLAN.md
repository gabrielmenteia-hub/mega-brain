---
phase: 05-dashboard
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - mis/tests/web/__init__.py
  - mis/tests/web/conftest.py
  - mis/tests/web/test_web_ranking.py
  - mis/tests/web/test_web_dossier.py
  - mis/tests/web/test_web_feed.py
  - mis/tests/web/test_web_alerts.py
  - mis/tests/web/test_alert_repository.py
  - mis/tests/web/test_dossier_repository.py
  - mis/tests/web/test_pain_repository.py
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04
  - SCAN-05

must_haves:
  truths:
    - "pytest coleta todos os arquivos em mis/tests/web/ sem ImportError"
    - "Cada test file tem pelo menos 2 testes failing (RED) que definem contratos de comportamento"
    - "conftest.py fornece fixture app_client com TestClient + banco SQLite temporário"
  artifacts:
    - path: "mis/tests/web/__init__.py"
      provides: "marcador de pacote Python"
    - path: "mis/tests/web/conftest.py"
      provides: "fixtures TestClient, db_path, app_client"
    - path: "mis/tests/web/test_web_ranking.py"
      provides: "contratos de teste para DASH-01 e SCAN-05"
    - path: "mis/tests/web/test_web_dossier.py"
      provides: "contratos de teste para DASH-02"
    - path: "mis/tests/web/test_web_feed.py"
      provides: "contratos de teste para DASH-03"
    - path: "mis/tests/web/test_web_alerts.py"
      provides: "contratos de teste para DASH-04"
    - path: "mis/tests/web/test_alert_repository.py"
      provides: "contratos de teste para alert_repository"
    - path: "mis/tests/web/test_dossier_repository.py"
      provides: "contratos de teste para dossier_repository"
    - path: "mis/tests/web/test_pain_repository.py"
      provides: "contratos de teste para pain_repository"
  key_links:
    - from: "mis/tests/web/conftest.py"
      to: "mis/web/app.py"
      via: "create_app(db_path) importado no conftest"
      pattern: "from mis.web.app import create_app"
    - from: "mis/tests/web/conftest.py"
      to: "mis/db.py"
      via: "run_migrations(db_path) para preparar schema"
      pattern: "from mis.db import run_migrations"
---

<objective>
Criar scaffolds de teste (Wave 0) com testes failing que definem os contratos de comportamento de todas as rotas e repositórios do dashboard antes de qualquer implementação.

Purpose: Nyquist compliance — cada task de implementação dos planos 05-02..05-04 terá testes RED prontos para virar GREEN.
Output: 9 arquivos em mis/tests/web/ com ~30 testes failing coletáveis.
</objective>

<execution_context>
@C:/Users/Gabriel/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Gabriel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-dashboard/05-CONTEXT.md
@.planning/phases/05-dashboard/05-RESEARCH.md
@.planning/phases/05-dashboard/05-VALIDATION.md
@mis/tests/conftest.py
@mis/db.py
@mis/product_repository.py

<interfaces>
<!-- Contratos que os testes vão usar — importados quando os módulos existirem -->

Fixture existente (mis/tests/conftest.py):
```python
@pytest.fixture
def db_path(tmp_path):
    return str(tmp_path / "mis.db")
```

Módulos a serem criados (os testes IMPORTAM mas ainda não existem — pytest.importorskip NÃO usar, deixar ImportError como RED):
```python
from mis.web.app import create_app          # → FastAPI app factory
from mis.dossier_repository import get_dossier_by_product_id, list_dossiers_by_rank
from mis.pain_repository import get_latest_report, get_historical_reports
from mis.alert_repository import create_alert, get_unseen_count, mark_seen, expire_old_alerts
```

TestClient (fastapi.testclient.TestClient) — incluído no fastapi[standard].
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Instalar dependências web e criar conftest.py + test stubs de repositórios</name>
  <files>
    mis/tests/web/__init__.py
    mis/tests/web/conftest.py
    mis/tests/web/test_alert_repository.py
    mis/tests/web/test_dossier_repository.py
    mis/tests/web/test_pain_repository.py
  </files>
  <behavior>
    - test_alert_repository:
      - test_create_alert_inserts_row: criar alerta com product_id=1, position=5 → row existe na tabela alerts
      - test_get_unseen_count_returns_zero_initially: banco vazio → count == 0
      - test_mark_seen_updates_flag: criar alerta, mark_seen(id) → seen == 1
      - test_expire_old_alerts_removes_expired: criar alerta com expires_at no passado → expire_old_alerts() → row removida
    - test_dossier_repository:
      - test_get_dossier_by_product_id_returns_none_when_missing: produto sem dossiê → None
      - test_list_dossiers_by_rank_returns_ordered: 3 produtos com ranks diferentes → lista ordenada ASC por rank
    - test_pain_repository:
      - test_get_latest_report_returns_none_when_empty: banco vazio → None
      - test_get_historical_reports_returns_list: 2 reports para mesmo nicho → lista de 2
  </behavior>
  <action>
    1. Instalar dependências web se não disponíveis:
       `pip install "fastapi[standard]" uvicorn jinja2 aiofiles`
       Verificar se já instaladas com `python -c "import fastapi, uvicorn, jinja2, aiofiles"`.

    2. Criar mis/tests/web/__init__.py vazio (marcador de pacote).

    3. Criar mis/tests/web/conftest.py:
       - Importar TestClient de fastapi.testclient
       - Importar create_app de mis.web.app (vai falhar até plan 05-02 — OK, é RED intencional)
       - Importar run_migrations de mis.db
       - Fixture `db_path(tmp_path)` → str(tmp_path / "web_mis.db")
       - Fixture `app_client(db_path)` → run_migrations(db_path), TestClient(create_app(db_path=db_path))
       - Usar `conftest.py` em mis/tests/web/ (escopo local ao pacote web tests)

    4. Criar mis/tests/web/test_alert_repository.py com os 4 testes descritos em <behavior>.
       - Importar funções de mis.alert_repository (módulo ainda não existe — RED)
       - Usar fixture db_path do conftest pai (mis/tests/conftest.py) — não do web/conftest.py
       - Cada teste: run_migrations(db_path) primeiro para criar tabela alerts (_005)

    5. Criar mis/tests/web/test_dossier_repository.py com os 2 testes descritos.
       - Importar de mis.dossier_repository
       - run_migrations(db_path) + inserir rows direto via get_db() para setup

    6. Criar mis/tests/web/test_pain_repository.py com os 2 testes descritos.
       - Importar de mis.pain_repository
       - run_migrations(db_path) + inserir rows direto via get_db()

    Todos os testes devem FALHAR com ImportError ou ModuleNotFoundError (RED confirmado).
  </action>
  <verify>
    <automated>cd mis && python -m pytest tests/web/test_alert_repository.py tests/web/test_dossier_repository.py tests/web/test_pain_repository.py --co -q 2>&1 | head -30</automated>
  </verify>
  <done>pytest coleta os 3 arquivos de repositório (8 testes visíveis). Todos falham com ImportError/ModuleNotFoundError — RED confirmado. conftest.py existe sem erro de sintaxe.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Criar test stubs de rotas web (ranking, dossier, feed, alerts)</name>
  <files>
    mis/tests/web/test_web_ranking.py
    mis/tests/web/test_web_dossier.py
    mis/tests/web/test_web_feed.py
    mis/tests/web/test_web_alerts.py
  </files>
  <behavior>
    - test_web_ranking:
      - test_ranking_page_returns_200: GET /ranking → status 200, "Ranking" no body
      - test_ranking_filter_by_platform: GET /ranking?platform=hotmart → status 200
      - test_ranking_filter_by_niche: GET /ranking?niche=emagrecimento → status 200
      - test_ranking_table_partial_htmx: GET /ranking/table com header HX-Request:true → status 200, sem "<nav" no body
      - test_ranking_pagination_per_page: GET /ranking?per_page=10&page=1 → status 200
    - test_web_dossier:
      - test_dossier_page_returns_404_for_missing: GET /dossier/9999 → status 404
      - test_dossier_tab_overview: GET /dossier/{id}/tab/visao-geral → status 200 com HX-Request header
      - test_dossier_tab_reviews: GET /dossier/{id}/tab/reviews → status 200 com HX-Request header
    - test_web_feed:
      - test_feed_page_returns_200: GET /feed → status 200
      - test_feed_niche_partial: GET /feed/niche/emagrecimento com HX-Request:true → status 200, sem "<nav"
    - test_web_alerts:
      - test_alerts_page_returns_200: GET /alerts → status 200
      - test_alerts_badge_returns_fragment: GET /alerts/badge com HX-Request:true → status 200
      - test_alerts_mark_seen: POST /alerts/1/mark-seen → status 200 ou 404
  </behavior>
  <action>
    Criar os 4 arquivos de teste de rotas.

    Padrão para cada teste:
    ```python
    def test_ranking_page_returns_200(app_client):
        response = app_client.get("/ranking")
        assert response.status_code == 200
        assert "Ranking" in response.text
    ```

    Para testes HTMX partial — passar header:
    ```python
    response = app_client.get("/ranking/table", headers={"HX-Request": "true"})
    assert "<nav" not in response.text
    ```

    Para test_dossier_tab_overview e test_dossier_tab_reviews — precisam de produto+dossiê no DB.
    Inserir diretamente via get_db() no corpo do teste (ou fixture local).

    Todos os testes FALHAM porque create_app não existe ainda — RED confirmado.
    Usar fixture app_client do mis/tests/web/conftest.py.
  </action>
  <verify>
    <automated>cd mis && python -m pytest tests/web/ --co -q 2>&1 | tail -20</automated>
  </verify>
  <done>pytest coleta todos os 9 arquivos em tests/web/ — mínimo 21 testes visíveis. Todos falham com ImportError ou CollectionError pelo create_app ausente. Estrutura de testes completa para as 4 requirements (DASH-01..04, SCAN-05).</done>
</task>

</tasks>

<verification>
Todos os arquivos de test scaffold existem em mis/tests/web/. pytest coleta sem erro de sintaxe. Todos os testes falham RED por ImportError — não por erro de lógica. O conftest.py usa a assinatura correta de create_app(db_path=...).
</verification>

<success_criteria>
`cd mis && python -m pytest tests/web/ --co -q` mostra 21+ testes coletados, todos marcados para falhar. Nenhum SyntaxError nos arquivos.
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/05-dashboard/05-01-SUMMARY.md`
</output>
