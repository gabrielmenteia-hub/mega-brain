---
phase: 05-dashboard
plan: "05"
type: execute
wave: 5
depends_on:
  - "05-04"
files_modified:
  - mis/web/routes/alerts.py
  - mis/web/templates/alerts.html
  - mis/web/templates/alerts_badge.html
  - mis/web/app.py
  - mis/scanner.py
  - mis/scanners/hotmart.py
  - mis/scanners/clickbank.py
  - mis/scanners/kiwify.py
autonomous: true
requirements:
  - DASH-04

must_haves:
  truths:
    - "GET /alerts retorna 200 com lista de alertas (pode estar vazia)"
    - "GET /alerts/badge com HX-Request:true retorna fragment com contagem de não vistos"
    - "POST /alerts/{id}/mark-seen marca alerta como visto"
    - "Alerta é criado automaticamente quando produto entra no top 20 pela primeira vez"
    - "Alertas expiram automaticamente após 7 dias via expire_old_alerts()"
    - "Badge no navbar atualiza a cada 30 segundos via HTMX polling"
  artifacts:
    - path: "mis/web/routes/alerts.py"
      provides: "GET /alerts, GET /alerts/badge, POST /alerts/{id}/mark-seen"
    - path: "mis/web/templates/alerts.html"
      provides: "página de alertas com lista seen/unseen"
    - path: "mis/web/templates/alerts_badge.html"
      provides: "fragment badge do navbar (SEM extends base — partial HTMX)"
    - path: "mis/scanner.py"
      provides: "save_batch_with_alerts() — wrapper com lógica top-20"
    - path: "mis/scanners/hotmart.py"
      provides: "chamada trocada de save_batch para save_batch_with_alerts"
    - path: "mis/scanners/clickbank.py"
      provides: "chamada trocada de save_batch para save_batch_with_alerts"
    - path: "mis/scanners/kiwify.py"
      provides: "chamada trocada de save_batch para save_batch_with_alerts"
  key_links:
    - from: "mis/web/templates/base.html"
      to: "mis/web/routes/alerts.py"
      via: "HTMX polling: hx-get=/alerts/badge hx-trigger=every 30s"
      pattern: "/alerts/badge"
    - from: "mis/scanners/hotmart.py"
      to: "mis/scanner.py"
      via: "save_batch_with_alerts(db, db_path, products) — linha 220 do arquivo atual"
      pattern: "save_batch_with_alerts"
    - from: "mis/scanners/clickbank.py"
      to: "mis/scanner.py"
      via: "save_batch_with_alerts(db, db_path, products) — linha 331 do arquivo atual"
      pattern: "save_batch_with_alerts"
    - from: "mis/scanners/kiwify.py"
      to: "mis/scanner.py"
      via: "save_batch_with_alerts(db, db_path, products) — linha 332 do arquivo atual"
      pattern: "save_batch_with_alerts"
    - from: "mis/web/app.py"
      to: "mis/web/routes/alerts.py"
      via: "app.include_router(alerts.router)"
      pattern: "include_router"
---

<objective>
Implementar o sistema de alertas completo: página /alerts, badge no navbar com polling HTMX, e trigger automático de criação de alertas quando produto entra no top 20 do scanner.

Purpose: Fecha DASH-04 — o usuário é notificado de novos campeões sem precisar monitorar ativamente. Resolve também o wiring entre save_batch_with_alerts e os 3 scanners individuais que chamavam save_batch diretamente.
Output: Página /alerts operacional; badge atualiza automaticamente; novos campeões geram alertas nos scanners hotmart, clickbank e kiwify.
</objective>

<execution_context>
@C:/Users/Gabriel/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Gabriel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-dashboard/05-CONTEXT.md
@.planning/phases/05-dashboard/05-RESEARCH.md
@.planning/phases/05-dashboard/05-04-SUMMARY.md
@mis/alert_repository.py
@mis/web/app.py
@mis/web/templates/base.html
@mis/scanner.py
@mis/scanners/hotmart.py
@mis/scanners/clickbank.py
@mis/scanners/kiwify.py

<interfaces>
<!-- Contratos existentes que esta task usa e estende -->

alert_repository.py (criado em 05-02):
```python
def create_alert(db_path: str, product_id: int, platform_slug: str,
                 niche_slug: str, position: int) -> int:
    # Retorna id do alerta criado (idempotente: retorna id existente se alerta recente já existe)

def get_unseen_count(db_path: str) -> int:
def mark_seen(db_path: str, alert_id: int) -> bool:
def expire_old_alerts(db_path: str) -> int:
```

Padrão atual dos 3 scanners (ANTES desta task):
```python
# mis/scanners/hotmart.py linha 201-220 (mesmo padrão em clickbank.py e kiwify.py)
from mis.product_repository import save_batch
db_path = config.get("settings", {}).get("db_path", "mis.db")
...
db = get_db(db_path)
save_batch(db, products)  # ← TROCAR por save_batch_with_alerts(db, db_path, products)
```

Os scanners JÁ TÊM db_path disponível como variável local — não é necessário refatorar a assinatura da função run_job, apenas trocar a chamada.

HTMX polling base.html (criado em 05-03):
```html
<span id="alerts-badge"
      hx-get="/alerts/badge"
      hx-trigger="every 30s"
      hx-swap="outerHTML">
</span>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rotas e templates de alertas (página + badge polling)</name>
  <files>
    mis/web/routes/alerts.py
    mis/web/templates/alerts.html
    mis/web/templates/alerts_badge.html
    mis/web/app.py
  </files>
  <behavior>
    - GET /alerts → status 200
    - GET /alerts com banco vazio → status 200, sem erro (empty state)
    - GET /alerts/badge → status 200, body é fragment HTML com contagem
    - GET /alerts/badge com HX-Request:true → status 200, sem "<nav" no body
    - POST /alerts/1/mark-seen → status 200 se alerta existe; status 404 se não existe
    - Alertas não vistos aparecem destacados; vistos ficam esmaecidos
  </behavior>
  <action>
    1. Criar mis/web/routes/alerts.py:
       - APIRouter
       - Importar alert_repository de mis.alert_repository
       - Importar get_db de mis.db

       GET /alerts:
       - expire_old_alerts(db_path) para limpar expirados antes de listar
       - Query via get_db:
         SELECT a.*, p.title as product_title, p.url as product_url
         FROM alerts a JOIN products p ON p.id = a.product_id
         ORDER BY a.created_at DESC
       - Retornar alerts.html com: alerts (lista), unseen_count = get_unseen_count(db_path)

       GET /alerts/badge:
       - unseen_count = get_unseen_count(db_path)
       - Retornar alerts_badge.html (sempre fragment, com ou sem HX-Request)

       POST /alerts/{alert_id:int}/mark-seen:
       - mark_seen(db_path, alert_id) → bool
       - Se False → HTTPException(404)
       - Retornar RedirectResponse("/alerts", status_code=303)

    2. Criar mis/web/templates/alerts.html (extends base.html):
       - Título "Alertas" com badge "{{ unseen_count }} novos"
       - {% for alert in alerts %}
           <div class="... {% if alert.seen %}opacity-50{% endif %}">
             - Produto: {{ alert.product_title }} (link /dossier/{{ alert.product_id }})
             - Nicho: {{ alert.niche_slug }} | Plataforma: {{ alert.platform_slug }}
             - Posição alcançada: #{{ alert.position }}
             - Data: {{ alert.created_at[:16] }}
             - Botão "Marcar como visto" (form POST /alerts/{{ alert.id }}/mark-seen) — apenas se alert.seen == 0
           </div>
         {% else %}
           "Nenhum alerta recente."
         {% endfor %}

    3. Criar mis/web/templates/alerts_badge.html (PARTIAL — SEM extends base.html):
       <!-- Fragment trocado via HTMX polling com hx-swap="outerHTML" -->
       <span id="alerts-badge"
             hx-get="/alerts/badge"
             hx-trigger="every 30s"
             hx-swap="outerHTML">
         {% if unseen_count > 0 %}
         <span class="bg-red-500 text-white text-xs font-bold rounded-full px-2 py-0.5 ml-1">
           {{ unseen_count }}
         </span>
         {% endif %}
       </span>
       IMPORTANTE: o span externo com id="alerts-badge" deve estar no template para que
       outerHTML swap funcione corretamente (substitui o span inteiro).

    4. Atualizar mis/web/app.py:
       - from .routes import alerts
       - app.include_router(alerts.router)
  </action>
  <verify>
    <automated>cd /c/Users/Gabriel/MEGABRAIN/mis && python -m pytest tests/web/test_web_alerts.py -x -q</automated>
  </verify>
  <done>Todos os testes de test_web_alerts.py GREEN. GET /alerts retorna 200. Badge fragment não contém navbar. POST /alerts/1/mark-seen retorna 200 ou 404 conforme existência. app.py registra todos os 5 routers (ranking, dossier, feed, alerts, health).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Trigger automático de alertas nos 3 scanners</name>
  <files>
    mis/scanner.py
    mis/scanners/hotmart.py
    mis/scanners/clickbank.py
    mis/scanners/kiwify.py
  </files>
  <behavior>
    - Quando produto com rank_anterior > 20 tem novo rank <= 20 → create_alert() é chamado
    - Quando produto novo (não estava no banco) com rank <= 20 → create_alert() é chamado
    - Quando produto já estava no top 20 (rank_anterior <= 20) → create_alert() NÃO é chamado
    - Quando produto sai do top 20 → nenhum alerta
    - test_alert_created_on_top20_entry: run_migrations, inserir produto com rank=25, chamar save_batch_with_alerts com rank novo=5 → alert existe no banco
  </behavior>
  <action>
    Ler os 4 arquivos (mis/scanner.py, mis/scanners/hotmart.py, mis/scanners/clickbank.py, mis/scanners/kiwify.py) antes de editar.

    1. Adicionar save_batch_with_alerts em mis/scanner.py:

    ```python
    def save_batch_with_alerts(
        db: sqlite_utils.Database,
        db_path: str,
        products: list,
    ) -> None:
        """Upsert products and create top-20 entry alerts for new entrants."""
        from mis.product_repository import save_batch
        from mis.alert_repository import create_alert

        THRESHOLD = 20

        # 1. Capturar ranks atuais ANTES do upsert
        old_ranks: dict[tuple, int | None] = {}
        for p in products:
            rows = list(db.execute(
                "SELECT rank FROM products WHERE platform_id=? AND external_id=?",
                [p.platform_id, p.external_id]
            ))
            old_ranks[(p.platform_id, p.external_id)] = rows[0][0] if rows else None

        # 2. Upsert
        save_batch(db, products)

        # 3. Criar alertas para novas entradas no top 20
        for p in products:
            if p.rank > THRESHOLD:
                continue
            old_rank = old_ranks.get((p.platform_id, p.external_id))
            if old_rank is not None and old_rank <= THRESHOLD:
                continue  # Já estava no top 20 — não é nova entrada

            # Nova entrada no top 20: buscar products.id real pós-upsert
            rows = list(db.execute(
                "SELECT id FROM products WHERE platform_id=? AND external_id=?",
                [p.platform_id, p.external_id]
            ))
            if not rows:
                continue
            real_id = rows[0][0]

            platforms = list(db.execute("SELECT slug FROM platforms WHERE id=?", [p.platform_id]))
            platform_slug = platforms[0][0] if platforms else str(p.platform_id)

            niches = list(db.execute("SELECT slug FROM niches WHERE id=?", [p.niche_id]))
            niche_slug = niches[0][0] if niches else str(p.niche_id)

            create_alert(
                db_path=db_path,
                product_id=real_id,
                platform_slug=platform_slug,
                niche_slug=niche_slug,
                position=p.rank,
            )
    ```

    2. Atualizar mis/scanners/hotmart.py:
       - Linha 201: substituir `from mis.product_repository import save_batch` por `from mis.scanner import save_batch_with_alerts`
       - Linha 220: substituir `save_batch(db, products)` por `save_batch_with_alerts(db, db_path, products)`
       - db_path já está disponível como variável local no mesmo escopo (linha 203)

    3. Atualizar mis/scanners/clickbank.py:
       - Linha 311: substituir `from mis.product_repository import save_batch` por `from mis.scanner import save_batch_with_alerts`
       - Linha 331: substituir `save_batch(db, products)` por `save_batch_with_alerts(db, db_path, products)`
       - db_path já está disponível como variável local no mesmo escopo (linha 314)

    4. Atualizar mis/scanners/kiwify.py:
       - Linha 312: substituir `from mis.product_repository import save_batch` por `from mis.scanner import save_batch_with_alerts`
       - Linha 332: substituir `save_batch(db, products)` por `save_batch_with_alerts(db, db_path, products)`
       - db_path já está disponível como variável local no mesmo escopo (linha 315)

    Nota: Os números de linha são referência — verificar o contexto real após leitura dos arquivos. O padrão de substituição é idêntico nos 3 scanners.
  </action>
  <verify>
    <automated>cd /c/Users/Gabriel/MEGABRAIN/mis && python -m pytest tests/web/ -x -q</automated>
  </verify>
  <done>Full suite tests/web/ GREEN. save_batch_with_alerts existe em scanner.py com lógica top-20. Os 3 arquivos de scanner importam save_batch_with_alerts (não save_batch de product_repository). Todos os 8 testes de repositório continuam GREEN.</done>
</task>

</tasks>

<verification>
Suite completa do web module passa: `cd mis && python -m pytest tests/web/ -x -q`

Full suite do projeto não quebra: `cd mis && python -m pytest tests/ -x -q`

Badge template contém span externo com id="alerts-badge" para HTMX outerHTML swap.

Todos os 5 routers registrados em app.py (ranking, dossier, feed, alerts, health).

Scanners não importam mais save_batch de product_repository diretamente:
`grep -n "from mis.product_repository import save_batch" mis/scanners/*.py` → sem resultados.
</verification>

<success_criteria>
Todos os testes em tests/web/ GREEN. Full suite tests/ GREEN. create_app importável e servidor sobe via `python -m mis dashboard`. Página /alerts exibe lista vazia sem erro. save_batch_with_alerts em scanner.py com lógica top-20 correta. Os 3 scanners (hotmart, clickbank, kiwify) chamam save_batch_with_alerts.
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/05-dashboard/05-05-SUMMARY.md`
</output>
