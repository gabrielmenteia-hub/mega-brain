---
phase: 16-international-high-friction
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - mis/base_scraper.py
  - mis/scanners/jvzoo.py
  - mis/tests/test_jvzoo_scanner.py
  - mis/tests/fixtures/jvzoo/listings_category84.html
  - mis/tests/fixtures/jvzoo/incapsula_block.html
autonomous: true
requirements:
  - SCAN-INTL-03
must_haves:
  truths:
    - "JVZooScanner.scan_niche() retorna lista de Product com rank, title, url preenchidos quando HTML SSR válido"
    - "JVZooScanner detecta bloqueio Incapsula via status 403/503 e retorna [] com alert='bot_detected'"
    - "JVZooScanner detecta bloqueio Incapsula via corpo HTML com 'incapsula' e retorna [] com alert='bot_detected'"
    - "PLAYWRIGHT_SEMAPHORE = asyncio.Semaphore(3) existe em mis/base_scraper.py"
    - "fetch_spa() adquire PLAYWRIGHT_SEMAPHORE antes do semaphore por domínio"
    - "6 testes GREEN para JVZooScanner"
  artifacts:
    - path: "mis/scanners/jvzoo.py"
      provides: "JVZooScanner class com scan_niche() + _parse_listings()"
      exports: ["JVZooScanner"]
    - path: "mis/tests/test_jvzoo_scanner.py"
      provides: "6 testes para SCAN-INTL-03"
      min_lines: 100
    - path: "mis/tests/fixtures/jvzoo/listings_category84.html"
      provides: "HTML sintético com 2 produtos JVZoo para mocking"
    - path: "mis/tests/fixtures/jvzoo/incapsula_block.html"
      provides: "HTML sintético de página de bloqueio Incapsula"
    - path: "mis/base_scraper.py"
      provides: "PLAYWRIGHT_SEMAPHORE global + fetch_spa() modificado"
      contains: "PLAYWRIGHT_SEMAPHORE = asyncio.Semaphore(3)"
  key_links:
    - from: "mis/scanners/jvzoo.py"
      to: "mis/base_scraper.py"
      via: "PlatformScanner.fetch() herda via self._base"
      pattern: "await self\\.fetch\\("
    - from: "mis/scanners/jvzoo.py"
      to: "mis/platform_ids.py"
      via: "import JVZOO_PLATFORM_ID"
      pattern: "from mis\\.platform_ids import JVZOO_PLATFORM_ID"
    - from: "mis/base_scraper.py"
      to: "PLAYWRIGHT_SEMAPHORE"
      via: "async with PLAYWRIGHT_SEMAPHORE antes de async_playwright()"
      pattern: "async with PLAYWRIGHT_SEMAPHORE"
---

<objective>
Implementar PLAYWRIGHT_SEMAPHORE global em base_scraper.py e criar JVZooScanner completo seguindo TDD (RED -> GREEN). JVZoo é SSR-only (fetch() sem Playwright) com detecção de bloqueio Incapsula.

Purpose: PLAYWRIGHT_SEMAPHORE é pré-requisito crítico para Gumroad e AppSumo (plano 16-02). JVZooScanner isolado no plano 16-01 reduz risco — bot detection é a maior incerteza desta fase.
Output: mis/base_scraper.py modificado + mis/scanners/jvzoo.py + testes GREEN.
</objective>

<execution_context>
@C:/Users/Gabriel/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Gabriel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@mis/base_scraper.py
@mis/scanner.py
@mis/platform_ids.py
@mis/scanners/product_hunt.py
@mis/tests/test_product_hunt_scanner.py
</context>

<interfaces>
<!-- Contratos que o executor precisa. Extraídos diretamente do codebase. -->

De mis/platform_ids.py:
```python
JVZOO_PLATFORM_ID = 10
GUMROAD_PLATFORM_ID = 11
APPSUMO_PLATFORM_ID = 12
```

De mis/scanner.py:
```python
@dataclass
class Product:
    external_id: str
    title: str
    url: str
    platform_id: int
    niche_id: int
    rank: int
    price: Optional[float] = None
    commission_pct: Optional[float] = None
    rating: Optional[float] = None
    thumbnail_url: Optional[str] = None

class PlatformScanner(ABC):
    def __init__(self, proxy_url=None, proxy_list=None): ...
    async def __aenter__(self) -> "PlatformScanner": ...
    async def __aexit__(self, *args): ...
    async def fetch(self, url: str) -> str: ...
    async def fetch_spa(self, url: str) -> str: ...
    @abstractmethod
    async def scan_niche(self, niche_slug: str, platform_slug: str) -> list[Product]: ...
```

De mis/base_scraper.py (estado atual — será modificado):
```python
# Linha ~48: semaphore por domínio (MANTER intacto)
_SEMAPHORE: dict[str, asyncio.Semaphore] = {}

# fetch_spa() atual (SEM PLAYWRIGHT_SEMAPHORE):
async def fetch_spa(self, url: str) -> str:
    domain = httpx.URL(url).host
    delay = DOMAIN_DELAYS.get(domain, DEFAULT_DELAY)
    async with self._get_semaphore(domain):          # per-domain rate limit
        async with async_playwright() as pw:
            ...

# MODIFICAÇÃO NECESSÁRIA — adicionar após _SEMAPHORE dict:
PLAYWRIGHT_SEMAPHORE = asyncio.Semaphore(3)

# fetch_spa() modificado — ordem: PLAYWRIGHT_SEMAPHORE -> domínio (anti-deadlock):
async def fetch_spa(self, url: str) -> str:
    domain = httpx.URL(url).host
    delay = DOMAIN_DELAYS.get(domain, DEFAULT_DELAY)
    async with PLAYWRIGHT_SEMAPHORE:                 # NOVO: global OOM prevention
        async with self._get_semaphore(domain):      # existente: per-domain rate limit
            async with async_playwright() as pw:
                ...
```

De mis/exceptions.py — ScraperError é levantado por fetch() após 3 tentativas:
```python
class ScraperError(Exception): ...
```
</interfaces>

<feature>
  <name>PLAYWRIGHT_SEMAPHORE + JVZooScanner</name>
  <files>
    mis/base_scraper.py,
    mis/scanners/jvzoo.py,
    mis/tests/test_jvzoo_scanner.py,
    mis/tests/fixtures/jvzoo/listings_category84.html,
    mis/tests/fixtures/jvzoo/incapsula_block.html
  </files>
  <behavior>
    RED (falha até implementação completa):
    - test_happy_path: respx mock retorna listings_category84.html -> result >= 1 produto, rank=1, platform_id=JVZOO_PLATFORM_ID
    - test_field_types: rank é int, price é float ou None, external_id é str não-vazio, url contém "jvzoomarket.com"

    GREEN (passam desde Wave 0):
    - test_bot_detected_http_403: respx mock retorna status 403 -> result == [], stdout contém "bot_detected"
    - test_bot_detected_incapsula_body: respx mock retorna status 200 com HTML incapsula_block.html -> result == [], stdout contém "bot_detected"
    - test_empty_results: respx retorna HTML sem produtos -> result == []
    - test_upsert_no_duplicates: upsert produto -> reinserir mesmo external_id com rank=2 -> 1 row no DB, rank atualizado

    Separado (test para PLAYWRIGHT_SEMAPHORE — sempre GREEN):
    - test_playwright_semaphore_exists: importar PLAYWRIGHT_SEMAPHORE de mis.base_scraper -> isinstance(PLAYWRIGHT_SEMAPHORE, asyncio.Semaphore) e PLAYWRIGHT_SEMAPHORE._value == 3
  </behavior>
  <implementation>
    FASE 1 — Wave 0 (RED/GREEN mix):

    1. Criar mis/tests/fixtures/jvzoo/ (mkdir)
    2. Criar mis/tests/fixtures/jvzoo/listings_category84.html — HTML sintético com 2 produtos:
       - produto 1: a[href*="/productlibrary/marketframe?pid=12345"] "Email Marketing Pro 2026", span.price "$47.00", span.sales "280 Sold"
       - produto 2: a[href*="/productlibrary/marketframe?pid=67890"] "Traffic Dominator Suite", span.price "$97.00", span.sales "142 Sold"
    3. Criar mis/tests/fixtures/jvzoo/incapsula_block.html — HTML com "incapsula" e "incident id" no corpo
    4. Criar mis/tests/test_jvzoo_scanner.py com os 7 testes descritos acima (6 scanner + 1 semaphore)
       - test_happy_path e test_field_types: anotados como RED (scan retorna [] no stub)
       - demais: GREEN desde o stub
    5. Criar mis/scanners/jvzoo.py STUB mínimo:
       - class JVZooScanner(PlatformScanner) com scan_niche() que retorna [] (para testes RED falharem)
       - Implementar apenas: verificação bot_detected via ScraperError + verificação "incapsula" no corpo
       - _parse_listings() retorna [] (stub)
    6. Executar pytest — confirmar RED em test_happy_path, test_field_types; GREEN nos demais

    FASE 2 — GREEN cycle (implementação completa):

    1. Modificar mis/base_scraper.py:
       - Adicionar após _SEMAPHORE dict: `PLAYWRIGHT_SEMAPHORE = asyncio.Semaphore(3)`
       - Modificar fetch_spa() — mover _get_semaphore() para DENTRO do PLAYWRIGHT_SEMAPHORE:
         `async with PLAYWRIGHT_SEMAPHORE: async with self._get_semaphore(domain): async with async_playwright() as pw:`
       - CRÍTICO: ordem PLAYWRIGHT_SEMAPHORE -> domínio (anti-deadlock conforme RESEARCH.md Pitfall 3)

    2. Implementar mis/scanners/jvzoo.py completo:
       - JVZOO_BASE_URL = "https://www.jvzoomarket.com"
       - scan_niche() usa URL: `f"{JVZOO_BASE_URL}/listings?category={platform_slug}&sort=sales"`
       - Detecção bot: ScraperError -> log.warning(alert="bot_detected") -> return []
       - Detecção Incapsula soft-block: if "incapsula" in html.lower() or "incident id" in html.lower() -> log.warning(alert="bot_detected") -> return []
       - _parse_listings(html, niche_id): BeautifulSoup, seletores do fixture:
         - produtos: div.product-listing
         - URL: a[href*="marketframe?pid="]
         - external_id: extrair pid= do href (re.search(r'pid=(\d+)', href))
         - title: texto do link
         - price: texto de span.price, float(text.replace('$',''))
         - rank: posição 1-based na lista
       - Adicionar DOMAIN_DELAYS["www.jvzoomarket.com"] = 2.0 em base_scraper.py

    3. Executar pytest — confirmar todos os 7 testes GREEN

    FASE 3 — REFACTOR (se necessário):
    - Mover _parse_listings() para função privada pura (sem self) para facilitar testes unitários diretos
    - Adicionar docstring à classe com URL padrão e semântica do rank
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — fixtures + stubs + testes RED/GREEN</name>
  <files>
    mis/tests/test_jvzoo_scanner.py,
    mis/tests/fixtures/jvzoo/listings_category84.html,
    mis/tests/fixtures/jvzoo/incapsula_block.html,
    mis/scanners/jvzoo.py
  </files>
  <behavior>
    RED (devem falhar até Task 2):
    - test_happy_path: fixture HTML -> >= 1 produto com rank=1, platform_id=JVZOO_PLATFORM_ID
    - test_field_types: rank é int, price é float ou None, external_id é str não-vazio

    GREEN (devem passar já neste stub):
    - test_bot_detected_http_403: status 403 -> result == [], "bot_detected" em stdout
    - test_bot_detected_incapsula_body: HTML com "incapsula" -> result == [], "bot_detected" em stdout
    - test_empty_results: HTML sem produtos -> result == []
    - test_upsert_no_duplicates: DB upsert -> reinserir -> 1 row, rank atualizado
    - test_playwright_semaphore_exists: PLAYWRIGHT_SEMAPHORE em base_scraper tem _value == 3
  </behavior>
  <action>
    1. Criar diretório mis/tests/fixtures/jvzoo/

    2. Criar mis/tests/fixtures/jvzoo/listings_category84.html:
    HTML sintético com 2 produtos. Estrutura exata:
    ```html
    <!DOCTYPE html>
    <html><body>
      <div class="product-listing">
        <a href="/productlibrary/marketframe?pid=12345">Email Marketing Pro 2026</a>
        <span class="price">$47.00</span>
        <span class="sales">280 Sold</span>
      </div>
      <div class="product-listing">
        <a href="/productlibrary/marketframe?pid=67890">Traffic Dominator Suite</a>
        <span class="price">$97.00</span>
        <span class="sales">142 Sold</span>
      </div>
    </body></html>
    ```

    3. Criar mis/tests/fixtures/jvzoo/incapsula_block.html:
    ```html
    <!DOCTYPE html>
    <html><head><title>Access Denied</title></head>
    <body>
      <p>Request unsuccessful. Incapsula incident id: INC-XXXXX-YYYYY</p>
      <p>Your access to this site has been limited by the site owner.</p>
    </body></html>
    ```

    4. Criar mis/scanners/jvzoo.py STUB:
    ```python
    """JVZoo marketplace scanner (SSR-only, sem Playwright).
    Detects Incapsula bot detection via HTTP status ou corpo HTML.
    Platform ID: 10 (JVZOO_PLATFORM_ID de mis.platform_ids).
    """
    from __future__ import annotations
    import re
    from typing import Optional
    import structlog
    from mis.scanner import PlatformScanner, Product
    from mis.platform_ids import JVZOO_PLATFORM_ID
    from mis.exceptions import ScraperError

    log = structlog.get_logger(__name__)
    JVZOO_BASE_URL = "https://www.jvzoomarket.com"

    class JVZooScanner(PlatformScanner):
        async def scan_niche(self, niche_slug: str, platform_slug: str, niche_id: int = 0) -> list[Product]:
            url = f"{JVZOO_BASE_URL}/listings?category={platform_slug}&sort=sales"
            try:
                html = await self.fetch(url)
            except ScraperError:
                log.warning("jvzoo_scanner.bot_detected", alert="bot_detected",
                            niche=niche_slug, reason="http_error")
                return []
            if "incapsula" in html.lower() or "incident id" in html.lower():
                log.warning("jvzoo_scanner.bot_detected", alert="bot_detected",
                            niche=niche_slug, reason="incapsula_challenge_page")
                return []
            # STUB: parsing não implementado ainda
            return []

        def _parse_listings(self, html: str, niche_id: int) -> list[Product]:
            return []  # STUB
    ```

    5. Criar mis/tests/test_jvzoo_scanner.py com todos os 7 testes:
    - Importar respx, Path, Response de httpx
    - FIXTURE_PATH = Path(__file__).parent / "fixtures/jvzoo/listings_category84.html"
    - INCAPSULA_PATH = Path(__file__).parent / "fixtures/jvzoo/incapsula_block.html"
    - JVZoo URL base para respx: "https://www.jvzoomarket.com/listings"
    - test_happy_path: respx.get mock com FIXTURE_PATH -> assert len >= 1, rank == 1, platform_id == JVZOO_PLATFORM_ID — marcado RED no docstring
    - test_field_types: respx.get mock com FIXTURE_PATH -> assert isinstance(rank, int), isinstance(external_id, str) — marcado RED
    - test_bot_detected_http_403: respx.get mock retorna Response(403) -> result == [], "bot_detected" em capsys.readouterr().out
    - test_bot_detected_incapsula_body: respx.get mock retorna Response(200, text=INCAPSULA_PATH.read_text()) -> result == [], "bot_detected" em stdout
    - test_empty_results: respx.get mock retorna HTML sem div.product-listing -> result == []
    - test_upsert_no_duplicates: DB com platform id=10, upsert -> reinserir rank=2 -> 1 row, rank=2
    - test_playwright_semaphore_exists: from mis.base_scraper import PLAYWRIGHT_SEMAPHORE -> isinstance check + _value == 3

    NOTA: test_playwright_semaphore_exists vai FALHAR enquanto PLAYWRIGHT_SEMAPHORE não existir em base_scraper.py — isso é esperado no Wave 0.
  </action>
  <verify>
    <automated>cd /c/Users/Gabriel/MEGABRAIN && python -m pytest mis/tests/test_jvzoo_scanner.py -x --tb=short 2>&1 | head -50</automated>
    Resultado esperado: test_bot_detected_http_403, test_bot_detected_incapsula_body, test_empty_results, test_upsert_no_duplicates passam GREEN. test_happy_path, test_field_types, test_playwright_semaphore_exists FALHAM RED.
  </verify>
  <done>
    3 testes GREEN, 3 testes RED confirmados. Stubs criados e funcionando conforme esperado.
    Fixtures HTML existem em mis/tests/fixtures/jvzoo/.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — PLAYWRIGHT_SEMAPHORE + JVZooScanner completo</name>
  <files>
    mis/base_scraper.py,
    mis/scanners/jvzoo.py
  </files>
  <behavior>
    Todos os 7 testes devem passar GREEN após esta task:
    - test_happy_path: fixture HTML -> 2 produtos, rank=1 e rank=2, platform_id=10, external_id="12345"
    - test_field_types: rank é int, external_id é str não-vazio, price é float ou None
    - test_playwright_semaphore_exists: PLAYWRIGHT_SEMAPHORE em base_scraper com _value == 3
    - test_bot_detected_http_403: GREEN (já passava)
    - test_bot_detected_incapsula_body: GREEN (já passava)
    - test_empty_results: GREEN (já passava)
    - test_upsert_no_duplicates: GREEN (já passava)
  </behavior>
  <action>
    1. Modificar mis/base_scraper.py:
       a) Após a linha `_SEMAPHORE: dict[str, asyncio.Semaphore] = {}` (~linha 48), adicionar:
          ```python
          # Global Playwright concurrency limit — prevents OOM with 5+ parallel niches.
          # Each Playwright context uses ~50-100MB RAM. Limit=3 is safe for 8GB machines.
          # CRITICAL: acquire PLAYWRIGHT_SEMAPHORE BEFORE per-domain semaphore to prevent deadlock.
          PLAYWRIGHT_SEMAPHORE = asyncio.Semaphore(3)
          ```
       b) Modificar fetch_spa() — trocar a ordem de aquisição de semaphore:
          ANTES:
          ```python
          async with self._get_semaphore(domain):
              async with async_playwright() as pw:
          ```
          DEPOIS:
          ```python
          async with PLAYWRIGHT_SEMAPHORE:                 # OOM prevention (global)
              async with self._get_semaphore(domain):      # rate limiting (per-domain)
                  async with async_playwright() as pw:
          ```
          Mover o `await asyncio.sleep(delay)` para FORA do bloco PLAYWRIGHT_SEMAPHORE mas dentro do método (após o bloco).
       c) Adicionar em DOMAIN_DELAYS:
          ```python
          "www.jvzoomarket.com": 2.0,
          ```

    2. Implementar mis/scanners/jvzoo.py completo:
       - Substituir o STUB de _parse_listings() pela implementação real com BeautifulSoup:
         ```python
         def _parse_listings(self, html: str, niche_id: int) -> list[Product]:
             from bs4 import BeautifulSoup
             soup = BeautifulSoup(html, "html.parser")
             listings = soup.find_all("div", class_="product-listing")
             products = []
             for rank, listing in enumerate(listings, start=1):
                 link = listing.find("a", href=lambda h: h and "marketframe?pid=" in h)
                 if not link:
                     continue
                 href = link.get("href", "")
                 pid_match = re.search(r'pid=(\d+)', href)
                 if not pid_match:
                     continue
                 external_id = pid_match.group(1)
                 title = link.get_text(strip=True) or external_id
                 price_tag = listing.find("span", class_="price")
                 price = None
                 if price_tag:
                     try:
                         price = float(price_tag.get_text(strip=True).replace("$", "").replace(",", ""))
                     except (ValueError, AttributeError):
                         price = None
                 products.append(Product(
                     external_id=external_id,
                     title=title,
                     url=f"https://www.jvzoomarket.com{href}",
                     platform_id=JVZOO_PLATFORM_ID,
                     niche_id=niche_id,
                     rank=rank,
                     price=price,
                 ))
             return products
         ```
       - Atualizar scan_niche() para chamar _parse_listings() em vez de retornar []:
         ```python
         return self._parse_listings(html, niche_id)
         ```

    3. Executar pytest para confirmar todos 7 GREEN.
  </action>
  <verify>
    <automated>cd /c/Users/Gabriel/MEGABRAIN && python -m pytest mis/tests/test_jvzoo_scanner.py -v 2>&1 | tail -20</automated>
    Resultado esperado: 7 passed. Nenhum FAILED.
    Suite completa: `python -m pytest mis/tests/ -x 2>&1 | tail -10` — nenhuma regressão.
  </verify>
  <done>
    7/7 testes GREEN. PLAYWRIGHT_SEMAPHORE existe em mis/base_scraper.py com Semaphore(3).
    fetch_spa() adquire PLAYWRIGHT_SEMAPHORE antes do semaphore por domínio.
    JVZooScanner parseia fixture HTML e retorna produtos com rank, external_id, price corretos.
    Suite completa sem regressões.
  </done>
</task>

</tasks>

<verification>
Verificação final da fase 16-01:

```bash
# 1. Todos os testes JVZoo passando
cd /c/Users/Gabriel/MEGABRAIN && python -m pytest mis/tests/test_jvzoo_scanner.py -v

# 2. PLAYWRIGHT_SEMAPHORE existe com valor correto
python -c "from mis.base_scraper import PLAYWRIGHT_SEMAPHORE; import asyncio; assert isinstance(PLAYWRIGHT_SEMAPHORE, asyncio.Semaphore); print('OK:', PLAYWRIGHT_SEMAPHORE._value)"

# 3. fetch_spa() tem PLAYWRIGHT_SEMAPHORE na ordem correta (anti-deadlock)
grep -A 5 "async def fetch_spa" mis/base_scraper.py

# 4. Suite completa sem regressões
python -m pytest mis/tests/ -x --tb=short 2>&1 | tail -15
```
</verification>

<success_criteria>
- PLAYWRIGHT_SEMAPHORE = asyncio.Semaphore(3) em mis/base_scraper.py, adquirido ANTES do semaphore por domínio em fetch_spa()
- JVZooScanner em mis/scanners/jvzoo.py: fetch() SSR-only, detecção Incapsula via status E corpo, _parse_listings() com BeautifulSoup
- 7 testes em test_jvzoo_scanner.py todos GREEN
- Suite completa (mis/tests/) sem regressões
- Requisito SCAN-INTL-03 coberto: scanner retorna produtos OU degrada com alert='bot_detected'
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/16-international-high-friction/16-01-SUMMARY.md`
</output>
