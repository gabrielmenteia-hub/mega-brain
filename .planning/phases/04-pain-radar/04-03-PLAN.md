---
phase: 04-pain-radar
plan: "03"
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - mis/radar/youtube_collector.py
  - mis/tests/test_youtube_collector.py
autonomous: true
requirements:
  - RADAR-03
  - RADAR-05

must_haves:
  truths:
    - "collect_youtube_signals() retorna lista com url, title, top_comments, source='youtube', niche_slug"
    - "Quota guard: ao atingir youtube_quota_daily_limit, job retorna [] e emite alert='youtube_quota_exhausted'"
    - "Quota é persistida em youtube_quota_log no banco (sobrevive a restarts)"
    - "Quota reseta à meia-noite PT (07:00 UTC) — get_quota_used_today() usa este reset_hour"
    - "view_count e like_count coletados via videos().list(part='statistics') — não search.list"
  artifacts:
    - path: "mis/radar/youtube_collector.py"
      provides: "collect_youtube_signals(), get_quota_used_today(), log_quota_usage()"
      exports: ["collect_youtube_signals", "get_quota_used_today", "log_quota_usage"]
  key_links:
    - from: "mis/radar/youtube_collector.py"
      to: "mis/migrations/_004_pain_radar.py"
      via: "youtube_quota_log e pain_signals upsert"
      pattern: "youtube_quota_log.*insert|log_quota_usage"
---

<objective>
Implementar o coletor YouTube Data API v3 com quota management persistido no banco.

Purpose: YouTube é a fonte de maior riqueza (títulos + comentários) mas tem quota diária rígida. O quota guard persistido em banco garante que o sistema não estoure os 10.000 units mesmo após restarts, e desabilita automaticamente o job até o reset de meia-noite PT.

Output: mis/radar/youtube_collector.py funcional com quota guard, 4+ testes GREEN.
</objective>

<execution_context>
@C:/Users/Gabriel/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Gabriel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-pain-radar/04-CONTEXT.md
@.planning/phases/04-pain-radar/04-RESEARCH.md
@.planning/phases/04-pain-radar/04-01-SUMMARY.md

@mis/migrations/_004_pain_radar.py
@mis/config.yaml
@mis/tests/conftest.py
@mis/tests/test_youtube_collector.py
@mis/tests/fixtures/youtube_search_response.json

<interfaces>
<!-- Contratos da migration _004 necessários aqui -->

Tabela youtube_quota_log:
```python
# id: int (PK), units: int, operation: str, logged_at: str (ISO-8601)
# INDEX: idx_youtube_quota_log_at em (logged_at)
# Reset diário: 07:00 UTC = meia-noite PT
```

Tabela pain_signals (upsert pattern):
```python
# url_hash (UNIQUE), url, title, source='youtube', niche_slug,
# score=view_count, extra_json=JSON(top_comments, like_count), collected_at
```

YouTube API v3 (google-api-python-client):
```python
from googleapiclient.discovery import build
youtube = build("youtube", "v3", developerKey=api_key)

# search.list = 100 units
search_resp = youtube.search().list(q=keyword, part="snippet",
    maxResults=10, relevanceLanguage=lang, type="video").execute()

# videos.list (statistics) = 1 unit per request com múltiplos IDs
stats_resp = youtube.videos().list(
    id=",".join(video_ids), part="statistics").execute()

# commentThreads.list = 1 unit
comments_resp = youtube.commentThreads().list(
    videoId=vid_id, part="snippet", maxResults=10, order="relevance").execute()
```

Custo por keyword: 100 (search) + 1 (videos.statistics para 5 IDs) + 5 (1u × 5 commentThreads) = 106 units
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: YouTubeCollector com quota guard persistido</name>
  <files>
    mis/radar/youtube_collector.py,
    mis/tests/test_youtube_collector.py
  </files>
  <behavior>
    - test_collect_youtube_signals_returns_list: mockar googleapiclient.discovery.build; usar fixture youtube_search_response.json; assert lista retornada tem url, title, source=='youtube', niche_slug
    - test_quota_guard_disables_when_limit_reached: popular youtube_quota_log com 9000+ units desde hoje; assert collect_youtube_signals retorna []; assert structlog tem alert='youtube_quota_exhausted'
    - test_quota_log_persisted_in_db: chamar log_quota_usage(db_path, 100, 'search.list'); assert COUNT(*) em youtube_quota_log == 1; assert units == 100
    - test_get_quota_used_today_resets_at_midnight: inserir registro com logged_at antes de 07:00 UTC de hoje; assert get_quota_used_today() não contabiliza registro antigo
    - test_youtube_signals_have_required_fields: cada sinal tem url, title, score (int = view_count), extra_json (tem 'top_comments'), collected_at
  </behavior>
  <action>
Criar `mis/radar/youtube_collector.py`:

Funções de quota:

```python
def get_quota_used_today(db_path: str, reset_hour_utc: int = 7) -> int:
    """Soma units usados desde o último reset (07:00 UTC = meia-noite PT)."""
    db = sqlite_utils.Database(db_path)
    today = datetime.utcnow()
    reset_dt = today.replace(hour=reset_hour_utc, minute=0, second=0, microsecond=0)
    if today.hour < reset_hour_utc:
        reset_dt -= timedelta(days=1)  # reset foi ontem
    rows = list(db["youtube_quota_log"].rows_where(
        "logged_at > ?", [reset_dt.isoformat()]
    ))
    return sum(r["units"] for r in rows)

def log_quota_usage(db_path: str, units: int, operation: str) -> None:
    """Persiste uso de quota no banco."""
    db = sqlite_utils.Database(db_path)
    db["youtube_quota_log"].insert({
        "units": units,
        "operation": operation,
        "logged_at": datetime.utcnow().isoformat(),
    })
```

Função de coleta síncrona (wrappada em executor):

```python
def _sync_collect_youtube(keyword: str, lang: str, api_key: str) -> tuple[list[dict], int]:
    """Retorna (sinais, units_used). Nunca propaga exceção."""
    try:
        youtube = build("youtube", "v3", developerKey=api_key)

        # search.list = 100 units
        search_resp = youtube.search().list(
            q=keyword, part="snippet", maxResults=10,
            relevanceLanguage=lang, type="video"
        ).execute()
        units_used = 100

        videos = search_resp.get("items", [])[:5]
        if not videos:
            return [], units_used

        # videos.list statistics = 1 unit para múltiplos IDs
        video_ids = [v["id"]["videoId"] for v in videos]
        stats_resp = youtube.videos().list(
            id=",".join(video_ids), part="statistics"
        ).execute()
        units_used += 1
        stats_map = {
            s["id"]: s.get("statistics", {})
            for s in stats_resp.get("items", [])
        }

        signals = []
        for video in videos:
            vid_id = video["id"]["videoId"]
            title = video["snippet"]["title"]
            stats = stats_map.get(vid_id, {})
            view_count = int(stats.get("viewCount", 0))
            like_count = int(stats.get("likeCount", 0))

            # commentThreads.list = 1 unit por vídeo
            try:
                comments_resp = youtube.commentThreads().list(
                    videoId=vid_id, part="snippet",
                    maxResults=10, order="relevance"
                ).execute()
                top_comments = [
                    c["snippet"]["topLevelComment"]["snippet"]["textDisplay"]
                    for c in comments_resp.get("items", [])
                ]
                units_used += 1
            except Exception:
                top_comments = []

            signals.append({
                "url": f"https://youtube.com/watch?v={vid_id}",
                "title": title,
                "source": "youtube",
                "score": view_count,
                "extra_json": json.dumps({"top_comments": top_comments, "like_count": like_count}),
                "collected_at": datetime.utcnow().isoformat(),
            })

        return signals, units_used
    except Exception as e:
        log.error("radar.youtube.failed", keyword=keyword, error=str(e),
                  alert="radar_collector_failed")
        return [], 0
```

Função async principal:

```python
async def collect_youtube_signals(niche: dict, db_path: str, config: dict) -> list[dict]:
    """Coleta sinais do YouTube para um nicho. Quota guard via banco."""
    api_key = os.environ.get("YOUTUBE_DATA_API_KEY", "")
    if not api_key:
        log.warning("radar.youtube.no_api_key", alert="radar_collector_failed")
        return []

    quota_limit = config.get("settings", {}).get("youtube_quota_daily_limit", 9000)
    quota_used = get_quota_used_today(db_path)
    if quota_used >= quota_limit:
        log.warning("radar.youtube.quota_exhausted", quota_used=quota_used,
                    alert="youtube_quota_exhausted")
        return []

    lang = niche.get("radar", {}).get("relevance_language", "pt")
    all_signals = []
    loop = asyncio.get_event_loop()

    db = sqlite_utils.Database(db_path)
    for keyword in niche.get("keywords", []):
        # Verificar quota antes de cada keyword
        if get_quota_used_today(db_path) >= quota_limit:
            log.warning("radar.youtube.quota_exhausted_mid_niche",
                        alert="youtube_quota_exhausted")
            break

        signals, units_used = await loop.run_in_executor(
            None, _sync_collect_youtube, keyword, lang, api_key
        )

        if units_used > 0:
            log_quota_usage(db_path, units_used, "search+stats+comments")

        for sig in signals:
            url_hash = hashlib.sha256(sig["url"].encode()).hexdigest()
            sig["niche_slug"] = niche["slug"]
            db["pain_signals"].upsert(
                {**sig, "url_hash": url_hash},
                pk="url_hash",
                alter=True,
            )
            all_signals.append(sig)

    return all_signals
```

Atualizar `mis/tests/test_youtube_collector.py` com testes reais (substituir stubs por asserts). Usar monkeypatch para mockar `googleapiclient.discovery.build` e `youtube_quota_log` via tmp_path DB real (padrão conftest.py).
</action>
  <verify>
    <automated>cd /c/Users/Gabriel/MEGABRAIN/mis && python -m pytest tests/test_youtube_collector.py -x -q</automated>
  </verify>
  <done>5 testes GREEN em test_youtube_collector.py; quota guard funciona — retorna [] e emite alert quando limite atingido; log_quota_usage persiste no banco</done>
</task>

</tasks>

<verification>
Após conclusão:
1. `cd /c/Users/Gabriel/MEGABRAIN/mis && python -m pytest tests/test_youtube_collector.py -v` — todos GREEN
2. `cd /c/Users/Gabriel/MEGABRAIN/mis && python -m pytest tests/ -q` — suite completa GREEN
3. `python -c "from mis.radar.youtube_collector import collect_youtube_signals, get_quota_used_today, log_quota_usage; print('OK')"` — importa sem erro
</verification>

<success_criteria>
- mis/radar/youtube_collector.py existe e importa sem erro
- 5 testes GREEN cobrindo happy path, quota guard, persistência de log, reset de quota
- Quota persistida em banco (não em variável de módulo)
- PRAW-análogo: googleapiclient síncrono wrappado em run_in_executor
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/04-pain-radar/04-03-SUMMARY.md`
</output>
