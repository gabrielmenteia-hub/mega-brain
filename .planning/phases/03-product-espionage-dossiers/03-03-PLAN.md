---
phase: 03-product-espionage-dossiers
plan: "03"
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - mis/spies/completeness_gate.py
  - mis/tests/test_completeness_gate.py
autonomous: true
requirements:
  - SPY-05
  - DOS-05

must_haves:
  truths:
    - "Gate bloqueia quando copy está ausente ou tem menos de 100 chars (copy é bloqueante)"
    - "Gate bloqueia quando reviews_count < min_reviews (default 10)"
    - "Gate passa quando copy presente + reviews suficientes, independente de ads ou oferta"
    - "confidence_score é 0 quando copy ausente; escala até 100 quando todos os campos presentes"
    - "structlog emite gate_passed, copy_ok, offer_ok, reviews_count, ads_ok como campos individuais (machine-readable)"
    - "Dossier parcial é permitido: gate pode retornar gate_passed=False mas confidence_score > 0 (para geração de dossier incompleto)"
  artifacts:
    - path: "mis/spies/completeness_gate.py"
      provides: "SpyData dataclass + check_completeness() function"
      exports: ["SpyData", "check_completeness"]
    - path: "mis/tests/test_completeness_gate.py"
      provides: "Testes SPY-05 e DOS-05"
      min_lines: 50
  key_links:
    - from: "mis/spies/completeness_gate.py"
      to: "mis/intelligence/copy_analyzer.py (futuro)"
      via: "check_completeness() retorna (gate_passed, confidence_score) — copy_analyzer só roda se gate_passed=True"
      pattern: "gate_passed"
---

<objective>
Implementar o SpyData dataclass e o data completeness gate — a camada de qualidade que garante que o pipeline LLM só recebe dados suficientes para gerar um dossiê confiável.

Purpose: O gate é a fronteira crítica entre coleta de dados e análise IA. Deve ser simples, testável e auditável via structlog. Implementado antes do pipeline LLM (03-04) para garantir a ordem correta de execução.

Output: mis/spies/completeness_gate.py com SpyData + check_completeness(), testes unitários com todas as combinações relevantes de presença/ausência de dados.
</objective>

<execution_context>
@C:/Users/Gabriel/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Gabriel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-product-espionage-dossiers/03-CONTEXT.md
@.planning/phases/03-product-espionage-dossiers/03-01-SUMMARY.md

<interfaces>
<!-- Regras do gate definidas em 03-CONTEXT.md (decisões travadas): -->

Fontes e regras do gate:
- copy_text: BLOQUEANTE — sem copy, gate_passed=False, confidence=0
- offer_data: não bloqueante — ausência reduz confidence mas não bloqueia
- reviews: BLOQUEANTE — requer len(reviews) >= min_reviews (default 10, configurável)
- ads: não bloqueante — ausência reduz confidence mas não bloqueia

Pesos do confidence_score (soma até 100):
- copy presente: +50
- offer presente: +15
- ads presente: +20
- reviews: escala 0–15 baseada em (reviews_count / min_reviews) * 15

Campos do log structlog (machine-readable — obrigatórios):
  copy_ok, offer_ok, reviews_count, ads_ok, gate_passed

Dossier parcial (incomplete=True): gerado mesmo com gate_passed=False,
  desde que copy esteja presente. Se copy ausente → status=failed, sem dossier.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SpyData dataclass + check_completeness gate (SPY-05, DOS-05)</name>
  <files>
    mis/spies/completeness_gate.py,
    mis/tests/test_completeness_gate.py
  </files>
  <behavior>
    - test_gate_passes: copy presente (>100 chars) + 10 reviews → gate_passed=True, confidence >= 65
    - test_copy_missing_blocks: copy_text=None → gate_passed=False, confidence=0
    - test_copy_too_short_blocks: copy_text de 50 chars → gate_passed=False, confidence=0
    - test_reviews_below_threshold: copy ok mas reviews_count=5 → gate_passed=False, confidence > 0 (copy conta)
    - test_confidence_full: copy + offer + ads + 10 reviews → confidence=100
    - test_confidence_no_ads: copy + offer + 0 ads + 10 reviews → confidence=80 (sem os 20 pontos de ads)
    - test_confidence_no_offer: copy + 0 offer + ads + 10 reviews → confidence=85
    - test_structlog_fields_emitted: structlog capturado tem copy_ok, offer_ok, reviews_count, ads_ok, gate_passed
    - test_partial_dossier_allowed: copy ok + 5 reviews → gate_passed=False mas confidence > 0 (dossier parcial possível)
  </behavior>
  <action>
    Criar mis/spies/completeness_gate.py:

    ```python
    from dataclasses import dataclass, field
    from typing import Optional
    import structlog

    log = structlog.get_logger()

    @dataclass
    class SpyData:
        """Container para todos os dados coletados pela espionagem de um produto."""
        copy_text: Optional[str] = None
        offer_data: Optional[dict] = None
        reviews: list[dict] = field(default_factory=list)
        ads: list[dict] = field(default_factory=list)

    def check_completeness(data: SpyData, min_reviews: int = 10) -> tuple[bool, int]:
        """
        Avalia completude dos dados de espionagem.

        Returns:
            (gate_passed, confidence_score 0-100)

        Regras:
        - copy é BLOQUEANTE: ausente → (False, 0)
        - reviews < min_reviews → gate_passed=False (mas confidence > 0 se copy ok)
        - offer e ads: não bloqueantes — afetam apenas confidence
        """
        copy_ok = bool(data.copy_text and len(data.copy_text) >= 100)
        offer_ok = bool(data.offer_data)
        reviews_count = len(data.reviews)
        ads_ok = bool(data.ads)

        gate_passed = copy_ok and reviews_count >= min_reviews

        # Confidence score: copy tem peso maior
        if not copy_ok:
            confidence = 0
        else:
            confidence = 50  # copy presente
            if offer_ok:
                confidence += 15
            if ads_ok:
                confidence += 20
            # reviews: escala 0 a 15 baseada em 0 a min_reviews
            reviews_score = min(15, int((reviews_count / min_reviews) * 15))
            confidence += reviews_score

        log.info(
            "completeness_gate",
            copy_ok=copy_ok,
            offer_ok=offer_ok,
            reviews_count=reviews_count,
            ads_ok=ads_ok,
            gate_passed=gate_passed,
            confidence_score=confidence,
        )

        return gate_passed, confidence
    ```

    Criar mis/tests/test_completeness_gate.py com todos os casos de teste listados em <behavior>.

    Para test_structlog_fields_emitted: usar pytest com caplog ou structlog.testing.capture_logs() para capturar o evento de log e verificar os campos.

    NOTA: confidence=100 ocorre quando copy(+50) + offer(+15) + ads(+20) + 15 reviews(+15) = 100.
  </action>
  <verify>
    <automated>cd /c/Users/Gabriel/MEGABRAIN && python -m pytest mis/tests/test_completeness_gate.py -x -q</automated>
  </verify>
  <done>9/9 testes GREEN (todos os casos de boundary do gate). confidence_score calculado corretamente para todas as combinações. structlog emite campos machine-readable.</done>
</task>

</tasks>

<verification>
```bash
cd /c/Users/Gabriel/MEGABRAIN && python -m pytest mis/tests/test_completeness_gate.py -x -q -v
```
Todos os casos GREEN incluindo boundary conditions do gate.
</verification>

<success_criteria>
- check_completeness() bloqueia corretamente quando copy ausente ou reviews insuficientes
- confidence_score calculado corretamente para todas as combinações de presença/ausência de dados
- structlog emite campos machine-readable (gate_passed, copy_ok, offer_ok, reviews_count, ads_ok)
- SpyData dataclass exportada e pronta para uso pelo spy_orchestrator (03-05)
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/03-product-espionage-dossiers/03-03-SUMMARY.md`
</output>
