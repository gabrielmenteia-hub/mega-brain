---
phase: 04-pain-radar
plan: "05"
type: execute
wave: 4
depends_on:
  - "04-04"
files_modified:
  - mis/radar/__init__.py
  - mis/scheduler.py
  - mis/__main__.py
  - mis/tests/test_radar_jobs.py
autonomous: true
requirements:
  - RADAR-01
  - RADAR-02
  - RADAR-03
  - RADAR-05
  - RADAR-06

must_haves:
  truths:
    - "register_radar_jobs(config) registra 4 jobs no APScheduler: radar_trends (1h), radar_reddit_quora (1h), radar_youtube (4h), radar_synthesizer (:30)"
    - "Falha de um job de coletor não cancela os demais (jobs independentes)"
    - "CLI 'python -m mis radar --niche <slug>' executa ciclo completo para o nicho e imprime confirmação"
    - "Job de cleanup diário deleta pain_signals com collected_at > 30 dias"
    - "register_radar_jobs usa replace_existing=True — seguro chamar múltiplas vezes"
  artifacts:
    - path: "mis/radar/__init__.py"
      provides: "Expõe register_radar_jobs como import público do package"
      exports: ["register_radar_jobs"]
    - path: "mis/scheduler.py"
      provides: "register_radar_jobs importável e chamável"
    - path: "mis/__main__.py"
      provides: "Subcomando 'radar --niche <slug>' no CLI"
  key_links:
    - from: "mis/scheduler.py"
      to: "mis/radar/__init__.py"
      via: "from mis.radar import register_radar_jobs"
      pattern: "register_radar_jobs"
    - from: "mis/__main__.py"
      to: "mis/radar/synthesizer.py"
      via: "asyncio.run(run_radar_cycle(niche_slug))"
      pattern: "run_radar_cycle|radar"
---

<objective>
Wiring final do Pain Radar: registrar os 4 jobs no APScheduler, expor CLI `mis radar`, e adicionar cleanup job de sinais brutos.

Purpose: Este é o plano de integração — todos os coletores e o synthesizer existem, agora precisam ser ligados ao scheduler e ao CLI para rodar automaticamente e ser invocáveis manualmente.

Output: register_radar_jobs() funcional no scheduler, CLI `python -m mis radar --niche` operacional, 4 testes GREEN.
</objective>

<execution_context>
@C:/Users/Gabriel/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Gabriel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-pain-radar/04-CONTEXT.md
@.planning/phases/04-pain-radar/04-01-SUMMARY.md
@.planning/phases/04-pain-radar/04-04-SUMMARY.md

@mis/scheduler.py
@mis/__main__.py
@mis/radar/__init__.py
@mis/config.py
@mis/tests/test_radar_jobs.py

<interfaces>
<!-- Contratos dos coletores implementados nos plans 02-04 -->

Da mis.radar (todos implementados):
```python
from mis.radar.trends_collector import collect_niche_trends
from mis.radar.reddit_collector import collect_reddit_signals
from mis.radar.quora_collector import collect_quora_signals
from mis.radar.youtube_collector import collect_youtube_signals
from mis.radar.synthesizer import synthesize_niche_pains

# Assinaturas:
async def collect_niche_trends(niche: dict, db_path: str) -> list[dict]: ...
async def collect_reddit_signals(niche: dict, db_path: str | None) -> list[dict]: ...
async def collect_quora_signals(niche: dict, db_path: str | None) -> list[dict]: ...
async def collect_youtube_signals(niche: dict, db_path: str, config: dict) -> list[dict]: ...
async def synthesize_niche_pains(niche_id: int, niche_name: str, niche_slug: str,
                                   cycle_at: str, db_path: str) -> dict | None: ...
```

Do mis.scheduler (padrão existente):
```python
from apscheduler.triggers.cron import CronTrigger
scheduler = get_scheduler()
scheduler.add_job(fn, CronTrigger(...), args=[config], id="job_id", replace_existing=True)
```

Do mis.config (padrão existente):
```python
from mis.config import load_config, settings
# settings.db_path -> str (path do banco)
# config["niches"] -> list[dict] com slug, name, keywords, radar
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: register_radar_jobs() no scheduler + cleanup job</name>
  <files>
    mis/radar/__init__.py,
    mis/scheduler.py,
    mis/tests/test_radar_jobs.py
  </files>
  <behavior>
    - test_register_radar_jobs_adds_four_jobs: chamar register_radar_jobs(config_mock); assert scheduler.get_job("radar_trends") não é None; idem para radar_reddit_quora, radar_youtube, radar_synthesizer
    - test_radar_trends_job_cron_hourly: job radar_trends tem CronTrigger com minute=0
    - test_radar_youtube_job_cron_every_4h: job radar_youtube tem CronTrigger com hour='*/4'
    - test_radar_synthesizer_offset_30min: job radar_synthesizer tem CronTrigger com minute=30
    - test_replace_existing_safe: chamar register_radar_jobs() duas vezes não duplica jobs (len(scheduler.get_jobs()) mesmo)
  </behavior>
  <action>
Atualizar `mis/radar/__init__.py` para expor `register_radar_jobs` e `run_radar_cycle`:

```python
"""mis.radar — Pain Radar: coleta sinais de mercado e sintetiza relatórios horários."""
from .trends_collector import collect_niche_trends
from .reddit_collector import collect_reddit_signals
from .quora_collector import collect_quora_signals
from .youtube_collector import collect_youtube_signals
from .synthesizer import synthesize_niche_pains

import asyncio, os, structlog
from datetime import datetime, timezone

log = structlog.get_logger()


async def run_radar_cycle(niche_slug: str, config: dict, db_path: str) -> dict | None:
    """Executa ciclo completo de coleta + síntese para um nicho."""
    niches = config.get("niches", [])
    niche = next((n for n in niches if n["slug"] == niche_slug), None)
    if not niche:
        log.error("radar.cycle.niche_not_found", niche_slug=niche_slug)
        return None

    cycle_at = datetime.now(tz=timezone.utc).replace(second=0, microsecond=0).isoformat()

    # Coleta paralela — falha de um não cancela os outros
    results = await asyncio.gather(
        collect_niche_trends(niche, db_path),
        collect_reddit_signals(niche, db_path),
        collect_quora_signals(niche, db_path),
        return_exceptions=True,
    )
    log.info("radar.cycle.collection_done", niche=niche_slug, cycle_at=cycle_at,
             signals=[len(r) if isinstance(r, list) else "error" for r in results])

    # niche_id via DB
    import sqlite_utils
    db = sqlite_utils.Database(db_path)
    row = next(db["niches"].rows_where("slug = ?", [niche_slug]), None)
    niche_id = row["id"] if row else None
    if not niche_id:
        log.error("radar.cycle.niche_not_in_db", niche_slug=niche_slug)
        return None

    return await synthesize_niche_pains(niche_id, niche["name"], niche_slug, cycle_at, db_path)


def register_radar_jobs(config: dict) -> None:
    """Registra os 4 jobs do radar no APScheduler singleton."""
    from apscheduler.triggers.cron import CronTrigger
    from mis.scheduler import get_scheduler
    from mis.config import settings as cfg_settings

    scheduler = get_scheduler()
    db_path = cfg_settings.db_path

    # Funções de job (wrappers síncronos que rodam async com asyncio.run)
    def _trends_job():
        asyncio.run(_run_all_trends(config, db_path))

    def _reddit_quora_job():
        asyncio.run(_run_all_reddit_quora(config, db_path))

    def _youtube_job():
        asyncio.run(_run_all_youtube(config, db_path))

    def _synthesizer_job():
        asyncio.run(_run_all_synthesizers(config, db_path))

    def _cleanup_job():
        _run_cleanup(db_path)

    scheduler.add_job(_trends_job, CronTrigger(minute=0),
                      id="radar_trends", replace_existing=True)
    scheduler.add_job(_reddit_quora_job, CronTrigger(minute=0),
                      id="radar_reddit_quora", replace_existing=True)
    scheduler.add_job(_youtube_job, CronTrigger(hour="*/4", minute=0),
                      id="radar_youtube", replace_existing=True)
    scheduler.add_job(_synthesizer_job, CronTrigger(minute=30),
                      id="radar_synthesizer", replace_existing=True)
    scheduler.add_job(_cleanup_job, CronTrigger(hour=3, minute=0),
                      id="radar_cleanup", replace_existing=True)

    log.info("scheduler.radar_jobs_registered", job_count=5)
```

Adicionar funções auxiliares `_run_all_trends`, `_run_all_reddit_quora`, `_run_all_youtube`, `_run_all_synthesizers` em `mis/radar/__init__.py`. Cada uma itera sobre `config["niches"]` e chama o coletor/synthesizer correspondente.

Adicionar `_run_cleanup(db_path)` que deleta registros de `pain_signals` com `collected_at < (now - 30 dias)`:
```python
def _run_cleanup(db_path: str) -> None:
    import sqlite_utils
    from datetime import timedelta
    db = sqlite_utils.Database(db_path)
    cutoff = (datetime.utcnow() - timedelta(days=30)).isoformat()
    db.execute("DELETE FROM pain_signals WHERE collected_at < ?", [cutoff])
    log.info("radar.cleanup.done", cutoff=cutoff)
```

Atualizar `mis/scheduler.py`: adicionar comentário de Phase 4 no docstring.

Atualizar `mis/tests/test_radar_jobs.py` com testes reais (substituir stubs). Usar scheduler isolado por teste (não o singleton global) — criar AsyncIOScheduler local no teste ou usar monkeypatch.
</action>
  <verify>
    <automated>cd /c/Users/Gabriel/MEGABRAIN/mis && python -m pytest tests/test_radar_jobs.py -x -q</automated>
  </verify>
  <done>5 testes GREEN em test_radar_jobs.py; scheduler tem 5 jobs registrados (4 radar + cleanup); replace_existing seguro</done>
</task>

<task type="auto">
  <name>Task 2: CLI subcomando 'radar' em __main__.py</name>
  <files>
    mis/__main__.py
  </files>
  <action>
Atualizar `mis/__main__.py` adicionando o subcomando `radar` ao parser existente.

Adicionar após o bloco do subcomando `spy`:

```python
# ── radar subcommand ────────────────────────────────────────────────────
radar_parser = subparsers.add_parser(
    "radar",
    help="Run a full pain radar cycle for a specific niche",
)
radar_parser.add_argument(
    "--niche",
    required=True,
    metavar="SLUG",
    help="Niche slug as configured in config.yaml (e.g. 'emagrecimento')",
)
```

Adicionar handling no dispatcher:

```python
if args.command == "radar":
    _handle_radar(args)
```

Adicionar função `_handle_radar`:

```python
def _handle_radar(args) -> None:
    """Handle the radar subcommand."""
    from mis.config import load_config, settings
    from mis.db import run_migrations
    from mis.radar import run_radar_cycle

    config = load_config()
    db_path = settings.db_path
    run_migrations(db_path)

    result = asyncio.run(run_radar_cycle(args.niche, config, db_path))
    if result:
        import json
        print(f"\nRadar cycle completed for niche: {args.niche}")
        pains = result.get("pains", [])
        print(f"Top {len(pains)} pains identified:")
        for i, pain in enumerate(pains, 1):
            print(f"  {i}. [{pain.get('interest_level', '?')}] {pain.get('description', '')}")
        print(f"\nSources: {result.get('sources_used', {})}")
        print(f"Cost: ${result.get('cost_usd', 0):.4f}")
    else:
        print(f"No report generated for niche '{args.niche}' (no signals or niche not found)")
        sys.exit(1)
```

Verificação manual (não precisa de teste automatizado pois é glue code que invoca funções já testadas):
`python -m mis radar --help` deve mostrar usage com --niche
`python -m mis --help` deve listar 'radar' como subcomando disponível
</action>
  <verify>
    <automated>cd /c/Users/Gabriel/MEGABRAIN/mis && python -m mis radar --help 2>&1 | grep -c "niche"</automated>
  </verify>
  <done>Output de `python -m mis radar --help` contém '--niche'; `python -m mis --help` lista 'radar' como subcomando; suite completa GREEN</done>
</task>

</tasks>

<verification>
Após ambas as tasks:
1. `cd /c/Users/Gabriel/MEGABRAIN/mis && python -m pytest tests/test_radar_jobs.py -v` — 5 testes GREEN
2. `cd /c/Users/Gabriel/MEGABRAIN/mis && python -m pytest tests/ -q` — suite completa GREEN (zero regressões)
3. `cd /c/Users/Gabriel/MEGABRAIN/mis && python -m mis radar --help` — mostra usage com --niche
4. `cd /c/Users/Gabriel/MEGABRAIN/mis && python -m mis --help` — lista 'spy' e 'radar' como subcomandos
5. `python -c "from mis.radar import register_radar_jobs, run_radar_cycle; print('OK')"` — importa sem erro
</verification>

<success_criteria>
- register_radar_jobs() registra 5 jobs no scheduler (4 radar + cleanup)
- replace_existing=True em todos os jobs
- CLI `python -m mis radar --niche <slug>` disponível e funcional
- Suite completa GREEN (zero regressões em 25+ testes existentes)
- mis.radar exporta register_radar_jobs e run_radar_cycle como API pública
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/04-pain-radar/04-05-SUMMARY.md`
</output>
