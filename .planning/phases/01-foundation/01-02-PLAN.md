---
phase: 01-foundation
plan: "02"
type: execute
wave: 1
depends_on:
  - "01-01"
files_modified:
  - mis/base_scraper.py
  - mis/tests/test_base_scraper.py
autonomous: true
requirements:
  - FOUND-02

must_haves:
  truths:
    - "BaseScraper.fetch() retorna conteudo HTML em resposta 200 mockada"
    - "BaseScraper.fetch() retenta 3 vezes em 429 e levanta ScraperError — nunca retorna None"
    - "rate limiting por dominio: Semaphore criado lazily por domain key, delay aplicado apos cada request"
    - "headers de cada request contem User-Agent real (nao python-httpx, nao vazio)"
    - "AsyncClient e fechado corretamente apos __aexit__ (sem ResourceWarning)"
    - "fetch_spa() existe com Playwright + stealth_async — testavel via import e assinatura"
  artifacts:
    - path: "mis/base_scraper.py"
      provides: "BaseScraper com fetch(), fetch_spa(), __aenter__, __aexit__, _build_headers(), _get_semaphore()"
      exports: ["BaseScraper", "DOMAIN_DELAYS", "DEFAULT_DELAY"]
    - path: "mis/tests/test_base_scraper.py"
      provides: "6 testes cobrindo FOUND-02 com respx para mock httpx"
  key_links:
    - from: "mis/base_scraper.py"
      to: "mis/exceptions.py"
      via: "from .exceptions import ScraperError — levantada quando tenacity esgota retries"
      pattern: "raise ScraperError"
    - from: "mis/tests/test_base_scraper.py"
      to: "mis/base_scraper.py"
      via: "respx.mock context manager mocka httpx.AsyncClient.get()"
      pattern: "respx.get"
---

<objective>
Implementar BaseScraper — a classe-base que todos os scrapers de plataforma das Fases 2-4 vao herdar. Cobre: fetch() via httpx async com retry tenacity e rate limiting por dominio, fetch_spa() via Playwright + playwright-stealth, rotacao de User-Agent via fake-useragent, logging JSON via structlog, ScraperError como unico modo de falha explicita.

Purpose: FOUND-02 exige rate limiting, retry automatico, rotacao de headers anti-bot e ausencia de swallow silencioso. BaseScraper e o contrato que todas as fases seguintes dependem — deve ser implementado corretamente agora.

Output: mis/base_scraper.py com BaseScraper completo e testado. 6 testes unitarios passando via respx mock.
</objective>

<execution_context>
@C:/Users/Gabriel/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Gabriel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
<!-- Contratos dos quais BaseScraper depende — criados em 01-01 -->

De mis/exceptions.py:
```python
class ScraperError(Exception):
    def __init__(self, url: str, attempts: int, cause: Exception) -> None:
        self.url = url
        self.attempts = attempts
        self.cause = cause
```

Stack locked (CONTEXT.md):
- httpx async AsyncClient com http2=True, follow_redirects=True, timeout=30s
- tenacity: stop_after_attempt(3), wait_exponential(min=1, max=10), retry on httpx.HTTPError + httpx.TimeoutException
- asyncio.Semaphore criado lazily por domain key (_get_semaphore padrao)
- DOMAIN_DELAYS dict com hotmart.com, kiwify.com.br, clickbank.com todos 2.0s; DEFAULT_DELAY = 2.0
- playwright-stealth: stealth_async(page) chamado ANTES de page.goto()
- structlog JSON: loga url, status_code, duration_ms, attempt, domain em cada request
- Proxy via PROXY_URL env var — codigo nao sabe se proxy esta ativo ou nao
- fake-useragent: ua.random em cada _build_headers()
- Long-lived: uma instancia por job, AsyncClient aberto durante todo o job
- __aenter__/__aexit__ obrigatorios para fechamento correto
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implementar BaseScraper com fetch() e rate limiting</name>
  <files>
    mis/base_scraper.py,
    mis/tests/test_base_scraper.py
  </files>
  <behavior>
    - test_fetch_success: respx.mock mocka GET https://example.com retornando 200 + text; BaseScraper.fetch() retorna o texto
    - test_fetch_retries_on_429: respx mocka 429 duas vezes depois 200; fetch() retorna texto na terceira tentativa (nao levanta excecao)
    - test_rate_limiting: dois awaits de fetch() para o mesmo dominio; verifica que asyncio.sleep foi chamado (mock de asyncio.sleep); Semaphore nao permite concorrencia simultanea
    - test_headers_not_default: captura headers enviados via respx; User-Agent nao e "python-httpx/..." e nao e vazio
    - test_scraper_error_raised: respx mocka 3x 500; fetch() levanta ScraperError (nao retorna None); exc.url e exc.attempts corretos
    - test_client_closed_on_exit: async with BaseScraper() as s; apos bloco, s._client.is_closed deve ser True
  </behavior>
  <action>
    RED: Substituir stubs de test_base_scraper.py pelos testes reais ANTES da implementacao.

    1. Instalar respx se nao instalado: `pip install respx`

    2. Escrever mis/tests/test_base_scraper.py completo usando respx:
       - Import: `import respx, httpx, pytest` e `from mis.base_scraper import BaseScraper` e `from mis.exceptions import ScraperError`
       - test_fetch_success: `with respx.mock: respx.get("https://example.com").mock(return_value=httpx.Response(200, text="hello")); async with BaseScraper() as s: result = await s.fetch("https://example.com"); assert "hello" in result`
       - test_fetch_retries_on_429: configure respx para retornar 429, 429, 200 em sequencia; assert result contem o texto do 200
       - test_rate_limiting: monkeypatch asyncio.sleep; fazer 2 fetch() sequenciais; assert asyncio.sleep chamado ao menos uma vez
       - test_headers_not_default: capturar request via respx.last_request.headers["user-agent"]; assert "python-httpx" not in ua e len(ua) > 0
       - test_scraper_error_raised: respx mocka 3x status 500 (raise_for_status levanta HTTPStatusError); assert pytest.raises(ScraperError)
       - test_client_closed_on_exit: `async with BaseScraper() as s: pass; assert s._client.is_closed`

    Confirmar RED: pytest mis/tests/test_base_scraper.py -x falha com ImportError.

    GREEN: Implementar mis/base_scraper.py.

    3. Criar mis/base_scraper.py com:
       - Imports: asyncio, httpx, tenacity (retry, stop_after_attempt, wait_exponential, retry_if_exception_type), structlog, fake_useragent, Optional, from .exceptions import ScraperError
       - DOMAIN_DELAYS dict e DEFAULT_DELAY = 2.0
       - _SEMAPHORE: dict[str, asyncio.Semaphore] = {} — module-level dict para semaforos por dominio
       - class BaseScraper com __init__(proxy_url=None), _client=None
       - __aenter__: cria httpx.AsyncClient(http2=True, follow_redirects=True, timeout=httpx.Timeout(30.0), proxy=proxy_url ou None)
       - __aexit__: await self._client.aclose()
       - _get_semaphore(domain): cria Semaphore lazily se nao existe, retorna _SEMAPHORE[domain]
       - _build_headers(): UserAgent().random como User-Agent + headers realistas pt-BR
       - fetch(url): @retry decorator (stop=3, wait_exponential min=1 max=10, retry on httpx.HTTPError+httpx.TimeoutException, reraise=True); extrair domain; async with _get_semaphore(domain); medir t0; await client.get(url, headers=self._build_headers()); raise_for_status(); log.info com url/status/duration_ms; await asyncio.sleep(delay); retorna response.text; capturar excecoes e logar antes de re-raise
       - CRITICO: Apos todas as tentativas de tenacity, capturar excecao e levantar ScraperError(url, attempts, cause). Usar tenacity `after` callback ou try/except apos o retry decorator.
       - fetch_spa(url): importar playwright e stealth_async inline; domain+semaphore+delay iguais ao fetch(); async with async_playwright() as pw; browser=await pw.chromium.launch(proxy=...); page=await browser.new_page(); await stealth_async(page); await page.goto(url, wait_until="networkidle"); content=await page.content(); await browser.close(); await asyncio.sleep(delay); return content

    ANTIPATTERN CRITICO: Nao duplicar `response = await client.get(...)` — a RESEARCH.md aponta que o pattern de referencia tinha duplicata acidental. Apenas UM `await self._client.get()` em fetch().

    ANTIPATTERN: Semaphore nao deve ser criado no __init__ nem no nivel de modulo diretamente — usar _get_semaphore() lazy.

    Configurar structlog no topo de base_scraper.py:
    ```python
    import structlog
    structlog.configure(processors=[
        structlog.stdlib.add_log_level,
        structlog.processors.TimeStamper(fmt="iso"),
        structlog.processors.JSONRenderer(),
    ])
    log = structlog.get_logger()
    ```
  </action>
  <verify>
    <automated>cd /c/Users/Gabriel/MEGABRAIN && python -m pytest mis/tests/test_base_scraper.py -v --timeout=10</automated>
  </verify>
  <done>6 testes test_base_scraper.py passando: fetch_success, retries_on_429, rate_limiting, headers_not_default, scraper_error_raised, client_closed_on_exit. Nenhum ResourceWarning na saida.</done>
</task>

</tasks>

<verification>
Suite completa do BaseScraper:

```
cd /c/Users/Gabriel/MEGABRAIN && python -m pytest mis/tests/test_base_scraper.py -v --timeout=10
```

Verificar ausencia de ResourceWarning:
```
python -m pytest mis/tests/test_base_scraper.py -v -W error::ResourceWarning --timeout=10
```

Verificar que ScraperError nao e None return:
```python
python -c "
import asyncio, respx, httpx
from mis.base_scraper import BaseScraper
from mis.exceptions import ScraperError

async def test():
    with respx.mock:
        respx.get('https://example.com').mock(side_effect=[
            httpx.Response(500), httpx.Response(500), httpx.Response(500)
        ])
        async with BaseScraper() as s:
            try:
                await s.fetch('https://example.com')
                print('FAIL: deveria ter levantado ScraperError')
            except ScraperError as e:
                print(f'OK: ScraperError levantada com url={e.url} attempts={e.attempts}')
            except Exception as e:
                print(f'FAIL: excecao errada: {type(e).__name__}: {e}')

asyncio.run(test())
"
```
</verification>

<success_criteria>
- mis/base_scraper.py existe com BaseScraper, DOMAIN_DELAYS, DEFAULT_DELAY exportados
- BaseScraper implementa __aenter__/__aexit__ com AsyncClient http2=True
- fetch() tem decorator tenacity stop=3, wait_exponential min=1 max=10
- ScraperError levantada (nao None retornado) quando retries esgotam
- _get_semaphore() cria Semaphore lazily por domain key
- _build_headers() usa fake-useragent (User-Agent nunca e "python-httpx")
- fetch_spa() existe com stealth_async() chamado ANTES de page.goto()
- structlog configurado com JSON output
- 6 testes test_base_scraper.py passando
- Nenhum ResourceWarning
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/01-foundation/01-02-SUMMARY.md` com:
- Assinatura completa de BaseScraper (todos os metodos publicos e seus parametros)
- DOMAIN_DELAYS configurados
- Como ScraperError e levantada (pattern exato do tenacity)
- Pitfalls evitados (duplicata de await, Semaphore lazy, stealth antes de goto)
- Status dos 6 testes
</output>
