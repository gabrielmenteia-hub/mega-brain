---
phase: 07-mis-integration-bugfixes
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - mis/mis_agent.py
  - mis/scheduler.py
  - mis/tests/test_mis_agent.py
  - mis/tests/test_scan_and_spy_job.py
autonomous: true
requirements: [INT-01, INT-02]
gap_closure: true

must_haves:
  truths:
    - "export_to_megabrain() retorna exported > 0 quando dossiês com status='done' existem no banco"
    - "get_briefing_data() retorna last_cycle não-nulo quando dossiês existem (coluna generated_at usada)"
    - "get_briefing_data() retorna dossiers_today=True quando há dossiê gerado hoje"
    - "_scan_and_spy_job() salva produtos no banco via save_batch_with_alerts() antes de tentar spy"
    - "Suite completa de testes passa green após os 3 fixes"
  artifacts:
    - path: "mis/mis_agent.py"
      provides: "export_to_megabrain() com filtro status='done'; get_briefing_data() com queries em generated_at"
      contains: "WHERE d.status = 'done'"
    - path: "mis/scheduler.py"
      provides: "_scan_and_spy_job() com save_batch_with_alerts() antes do spy loop"
      contains: "save_batch_with_alerts"
    - path: "mis/tests/test_mis_agent.py"
      provides: "seed corrigido (status='done', generated_at) + test_health_with_dossier_today"
      contains: "test_health_with_dossier_today"
    - path: "mis/tests/test_scan_and_spy_job.py"
      provides: "testes RED→GREEN cobrindo BUG-03"
      contains: "test_scan_and_spy_saves_products"
  key_links:
    - from: "mis/mis_agent.py:export_to_megabrain()"
      to: "dossiers table"
      via: "WHERE d.status = 'done'"
      pattern: "status = 'done'"
    - from: "mis/mis_agent.py:get_briefing_data()"
      to: "dossiers.generated_at"
      via: "SELECT MAX(generated_at)"
      pattern: "generated_at"
    - from: "mis/scheduler.py:_scan_and_spy_job()"
      to: "mis.scanner.save_batch_with_alerts"
      via: "chamada dentro do loop por plataforma"
      pattern: "save_batch_with_alerts"
---

<objective>
Corrige 3 bugs críticos de integração identificados no audit v1.0 do MIS, restaurando funcionalidade real de export, health score e pipeline automático scan→spy.

Purpose: Os bugs fazem o bridge MIS/MEGABRAIN exportar 0 dossiês silenciosamente (BUG-01), calcular health score incorreto (BUG-02) e nunca executar o pipeline automático scan→spy→dossiê (BUG-03). As correções são cirúrgicas — alterações de string literal e adição de uma chamada de função.

Output:
- mis/mis_agent.py com filtro de status e queries de data corrigidas
- mis/scheduler.py com save_batch_with_alerts() inserido antes do spy loop
- mis/tests/test_mis_agent.py com seed corrigido e novo teste de health score
- mis/tests/test_scan_and_spy_job.py com cobertura do pipeline BUG-03
</objective>

<execution_context>
@C:/Users/Gabriel/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Gabriel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-mis-integration-bugfixes/07-RESEARCH.md
@.planning/phases/06-megabrain-integration/06-01-SUMMARY.md

<interfaces>
<!-- Contratos extraídos dos arquivos-fonte. Executor usa diretamente — sem exploração. -->

De mis/scanner.py (assinatura confirmada na pesquisa):
```python
def save_batch_with_alerts(db: sqlite_utils.Database, db_path: str, products: list[Product]) -> None:
    # Faz upsert no DB, cria alertas top-20. db deve estar aberto.
    ...
```

De mis/db.py (padrão já usado em spy_orchestrator.py):
```python
def get_db(db_path: str) -> sqlite_utils.Database:
    # Retorna instância aberta do banco SQLite
    ...
```

De mis/mis_agent.py — linhas com bugs (a corrigir):
```python
# BUG-01 — linha ~308 (export_to_megabrain)
WHERE d.status = 'complete'   # ERRADO — deve ser 'done'

# BUG-02 — linha ~149 (get_briefing_data — last_cycle)
"SELECT MAX(created_at) FROM dossiers"   # ERRADO — deve ser generated_at

# BUG-02 — linha ~229 (get_briefing_data — dossiers_today)
"SELECT COUNT(*) FROM dossiers WHERE created_at >= ?"   # ERRADO — deve ser generated_at
```

De mis/tests/test_mis_agent.py — seed com bug (a corrigir):
```python
# Linha ~54-55 — seed usa status errado e coluna errada
"(product_id, status, opportunity_score, confidence_score, dossier_json, created_at) "
"VALUES (?, 'complete', ?, 0.9, ?, ?)",
# DEVE SER: status='done', coluna=generated_at
```

De mis/spy_orchestrator.py (referência de padrão para IDs):
```python
# Padrão de resolução de DB ID após save (linhas 143-149 de scanner.py)
rows = list(db.execute(
    "SELECT id FROM products WHERE platform_id=? AND external_id=?",
    [p.platform_id, p.external_id],
))
db_id = rows[0][0] if rows else None
```

De mis/scheduler.py — estrutura atual de _scan_and_spy_job (linhas ~89-110):
```python
async def _scan_and_spy_job():
    config = load_config()
    results = await run_all_scanners(config)  # retorna dict[str, list[Product]]
    # BUG: nunca chama save_batch_with_alerts() — products sem DB ID
    # Loop a seguir tenta product.id que não existe → todos skipped
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — stubs RED para BUG-03 e seed corrigido para BUG-01/BUG-02</name>
  <files>mis/tests/test_mis_agent.py, mis/tests/test_scan_and_spy_job.py</files>
  <behavior>
    - test_mis_agent.py/_seed_db: usa status='done' e coluna generated_at (não 'complete' nem created_at)
    - test_mis_agent.py/test_incremental_export: assertion muda para exported > 0 (não >= 0)
    - test_mis_agent.py/test_health_with_dossier_today: novo teste — seed com dossiê de hoje (generated_at = hoje), chama get_briefing_data(), assert result['health']['dossiers_today'] is True e result['last_cycle'] is not None
    - test_scan_and_spy_job.py/test_scan_and_spy_saves_products: mock de run_all_scanners + save_batch_with_alerts, verifica que save_batch_with_alerts foi chamado — RED antes do fix
    - test_scan_and_spy_job.py/test_scan_and_spy_triggers_spy_pipeline: mock de run_all_scanners + save_batch_with_alerts + run_spy_batch, verifica que run_spy_batch recebeu lista não-vazia de IDs — RED antes do fix
  </behavior>
  <action>
    1. Abrir mis/tests/test_mis_agent.py (ler antes de editar).

    2. Em _seed_db(), localizar a INSERT de dossiers (~linha 54-55). Mudar:
       - coluna de `created_at` para `generated_at`
       - valor de `'complete'` para `'done'`
       Manter todos os outros campos idênticos.

    3. Em test_incremental_export, localizar a assertion `result1["exported"] >= 0` e mudar para `result1["exported"] > 0`.

    4. Adicionar no final do arquivo o teste test_health_with_dossier_today:
       - Cria tmp DB, roda _init_db()
       - Insere 1 produto e 1 dossiê com generated_at = hoje (datetime.utcnow().date().isoformat() ou string YYYY-MM-DD)
       - Chama get_briefing_data() com MIS_DB_PATH apontando pro tmp DB
       - Assert result['last_cycle'] is not None
       - Assert result['health']['dossiers_today'] is True (ou result['health'] tem chave dossiers_today com valor True — verificar estrutura existente)

    5. Criar mis/tests/test_scan_and_spy_job.py do zero:
       - Import: pytest, pytest_asyncio, unittest.mock (patch, AsyncMock, MagicMock)
       - Import dos módulos alvo após existirem: `from mis.scheduler import _scan_and_spy_job`
       - test_scan_and_spy_saves_products: usa @patch('mis.scheduler.run_all_scanners', return_value={'niche.platform': [MagicMock(platform_id=1, external_id='X', rank=1)]}) e @patch('mis.scheduler.save_batch_with_alerts') — chama asyncio.run(_scan_and_spy_job()) — verifica save_batch_with_alerts.called is True
       - test_scan_and_spy_triggers_spy_pipeline: similar mas também patcha run_spy_batch — verifica que run_spy_batch foi chamado com lista contendo dicts com chave 'id'

    6. Rodar os testes ANTES dos fixes (devem estar RED para os novos stubs):
       `cd /c/Users/Gabriel/MEGABRAIN && python -m pytest mis/tests/test_mis_agent.py mis/tests/test_scan_and_spy_job.py -v 2>&1 | tail -30`

    Nota: test_incremental_export pode já falhar RED após o seed fix — isso é esperado e correto.
  </action>
  <verify>
    <automated>cd /c/Users/Gabriel/MEGABRAIN && python -m pytest mis/tests/test_mis_agent.py::test_health_with_dossier_today mis/tests/test_scan_and_spy_job.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>Novos testes existem e falham RED (ImportError ou AssertionError) — confirmando que Wave 0 está pronto para os fixes de Wave 1</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wave 1 — fix cirúrgico BUG-01 + BUG-02 em mis_agent.py (GREEN test_mis_agent.py)</name>
  <files>mis/mis_agent.py</files>
  <behavior>
    - export_to_megabrain() filtra dossiês com status='done' (não 'complete')
    - get_briefing_data() usa SELECT MAX(generated_at) para last_cycle
    - get_briefing_data() usa WHERE generated_at >= ? para dossiers_today
    - _render_dossier_markdown usa row.get('generated_at') or row.get('created_at') como fallback seguro
    - test_incremental_export retorna exported > 0 com seed corrigido
    - test_health_with_dossier_today retorna last_cycle não-nulo e dossiers_today=True
  </behavior>
  <action>
    Ler mis/mis_agent.py antes de editar. Aplicar 3 correções cirúrgicas:

    **Fix BUG-01 (~linha 308):**
    Localizar a query em export_to_megabrain() que contém `WHERE d.status = 'complete'`.
    Substituir a string literal `'complete'` por `'done'`.
    Apenas essa string — nenhuma outra mudança na função.

    **Fix BUG-02a (~linha 149):**
    Localizar em get_briefing_data() a query:
    `"SELECT MAX(created_at) FROM dossiers"`
    Substituir por:
    `"SELECT MAX(generated_at) FROM dossiers"`

    **Fix BUG-02b (~linha 229):**
    Localizar em get_briefing_data() a query:
    `"SELECT COUNT(*) FROM dossiers WHERE created_at >= ?"`
    Substituir por:
    `"SELECT COUNT(*) FROM dossiers WHERE generated_at >= ?"`

    **Fix BUG-02c — _render_dossier_markdown (precaução):**
    Localizar onde _render_dossier_markdown usa `row.get("created_at")` (se existir).
    Se existir, mudar para `row.get("generated_at") or row.get("created_at")` para fallback seguro.
    Se não existir, pular esta etapa.

    Após edições, rodar testes para confirmar GREEN:
    `cd /c/Users/Gabriel/MEGABRAIN && python -m pytest mis/tests/test_mis_agent.py -v 2>&1 | tail -20`

    Nenhum refactor além das 3-4 strings trocadas. Não mexer em lógica de negócio.
  </action>
  <verify>
    <automated>cd /c/Users/Gabriel/MEGABRAIN && python -m pytest mis/tests/test_mis_agent.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>Todos os testes em test_mis_agent.py passam GREEN — incluindo test_incremental_export com exported > 0 e test_health_with_dossier_today com last_cycle não-nulo e dossiers_today=True</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wave 1 — fix BUG-03 em scheduler.py + suite completa GREEN</name>
  <files>mis/scheduler.py</files>
  <behavior>
    - _scan_and_spy_job() abre o DB com get_db(db_path) antes do spy loop
    - _scan_and_spy_job() chama save_batch_with_alerts(db, db_path, platform_products) por grupo no loop
    - _scan_and_spy_job() resolve DB IDs reais via SELECT após save
    - test_scan_and_spy_saves_products passa GREEN
    - test_scan_and_spy_triggers_spy_pipeline passa GREEN
    - Suite completa mis/tests/ passa GREEN
  </behavior>
  <action>
    Ler mis/scheduler.py antes de editar. Localizar a função _scan_and_spy_job (linhas ~89-110).

    Inserir, após a chamada `results = await run_all_scanners(config)` e antes do loop de spy, o seguinte padrão:

    1. Adicionar imports necessários no topo de _scan_and_spy_job() ou no nível de módulo:
       - `from mis.db import get_db` (se não importado)
       - `from mis.scanner import save_batch_with_alerts` (se não importado)
       - Verificar se `os` já está importado (para os.environ.get)

    2. Dentro de _scan_and_spy_job(), após `results = await run_all_scanners(config)`:
       ```python
       db_path = os.environ.get("MIS_DB_PATH", "data/mis.db")
       db = get_db(db_path)

       products_to_spy: list[dict] = []
       for platform_key, platform_products in results.items():
           if not platform_products:
               continue
           save_batch_with_alerts(db, db_path, platform_products)
           top_products = platform_products[:SPY_TOP_N]
           for p in top_products:
               rows = list(db.execute(
                   "SELECT id FROM products WHERE platform_id=? AND external_id=?",
                   [p.platform_id, p.external_id],
               ))
               if not rows:
                   log.warning("scan_and_spy_job.product_no_db_id",
                               platform_key=platform_key,
                               external_id=p.external_id)
                   continue
               products_to_spy.append({"id": rows[0][0], "rank": p.rank})
       ```

    3. Remover (ou comentar) o loop antigo que tentava usar `product.id` diretamente sem save — substituí-lo por `products_to_spy` preenchido acima. Verificar como o código existente passa `products_to_spy` para `run_spy_batch` e preservar essa chamada.

    Após edições, rodar suite completa:
    `cd /c/Users/Gabriel/MEGABRAIN && python -m pytest mis/tests/ -v 2>&1 | tail -30`

    SPY_TOP_N já deve estar definido no módulo (verificar import ou definição existente antes de usar).
  </action>
  <verify>
    <automated>cd /c/Users/Gabriel/MEGABRAIN && python -m pytest mis/tests/ -v 2>&1 | tail -30</automated>
  </verify>
  <done>Suite completa mis/tests/ passa GREEN — incluindo test_scan_and_spy_saves_products e test_scan_and_spy_triggers_spy_pipeline. Nenhum teste regressivo quebrado.</done>
</task>

</tasks>

<verification>
Verificação final da fase:

1. BUG-01 corrigido: `grep -n "status = 'done'" mis/mis_agent.py` retorna match na query de export
2. BUG-02 corrigido: `grep -n "generated_at" mis/mis_agent.py` retorna matches nas 2 queries de health
3. BUG-03 corrigido: `grep -n "save_batch_with_alerts" mis/scheduler.py` retorna match dentro de _scan_and_spy_job
4. Suite green: `cd /c/Users/Gabriel/MEGABRAIN && python -m pytest mis/tests/ -v` — todos passam, 0 failures
</verification>

<success_criteria>
1. export_to_megabrain() exporta dossiês com status='done' — test_incremental_export retorna exported > 0
2. get_briefing_data() calcula health score com generated_at — test_health_with_dossier_today retorna last_cycle não-nulo e dossiers_today=True
3. _scan_and_spy_job() persiste produtos antes de espionar — test_scan_and_spy_saves_products e test_scan_and_spy_triggers_spy_pipeline passam GREEN
4. Suite completa mis/tests/ passa sem regressões
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/07-mis-integration-bugfixes/07-01-SUMMARY.md` com:
- Frontmatter: phase, plan, requirements_completed: [INT-01, INT-02], files_modified
- O que foi corrigido (3 bugs, 4 arquivos)
- Resultados dos testes (N passed, 0 failed)
- Desvios do plano, se houver
</output>
