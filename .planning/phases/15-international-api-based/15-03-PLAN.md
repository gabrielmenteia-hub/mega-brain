---
phase: 15-international-api-based
plan: 03
type: execute
wave: 3
depends_on:
  - 15-02
files_modified:
  - mis/scanner.py
  - mis/config.yaml
  - mis/base_scraper.py
  - bin/templates/env.example
autonomous: true
requirements:
  - SCAN-INTL-01
  - SCAN-INTL-02

must_haves:
  truths:
    - "python -m mis scan --platform product_hunt retorna trending products sem erro"
    - "python -m mis scan --platform udemy retorna cursos (ou [] com alert gracioso) sem exception"
    - "run_all_scanners() reconhece 'product_hunt' e 'udemy' nas plataformas do config"
    - "config.yaml tem entradas product_hunt e udemy em todos os 3 nichos"
    - "DOMAIN_DELAYS inclui api.producthunt.com e www.udemy.com"
  artifacts:
    - path: "mis/scanner.py"
      provides: "SCANNER_MAP atualizado com ProductHuntScanner e UdemyScanner"
      contains: "product_hunt.*ProductHuntScanner"
    - path: "mis/config.yaml"
      provides: "Slugs product_hunt e udemy em todos os 3 nichos"
      contains: "product_hunt: trending"
    - path: "mis/base_scraper.py"
      provides: "DOMAIN_DELAYS com entradas para api.producthunt.com e www.udemy.com"
      contains: "api.producthunt.com"
    - path: "bin/templates/env.example"
      provides: "Documentacao das credenciais PRODUCT_HUNT_API_TOKEN, UDEMY_CLIENT_ID, UDEMY_CLIENT_SECRET"
      contains: "PRODUCT_HUNT_API_TOKEN"
  key_links:
    - from: "mis/scanner.py:run_all_scanners()"
      to: "mis/scanners/product_hunt.py:ProductHuntScanner"
      via: "SCANNER_MAP['product_hunt'] = ProductHuntScanner"
      pattern: "product_hunt.*ProductHuntScanner"
    - from: "mis/config.yaml"
      to: "mis/scanner.py:run_all_scanners()"
      via: "platforms.product_hunt e platforms.udemy lidos no loop de niches"
      pattern: "product_hunt: trending"
---

<objective>
Wiring: registrar ProductHuntScanner e UdemyScanner no SCANNER_MAP, atualizar config.yaml com slugs, adicionar DOMAIN_DELAYS, documentar credenciais em env.example.

Purpose: Conectar os scanners implementados ao pipeline de producao — sem este plano os scanners existem mas sao invisíveis para run_all_scanners() e o scheduler.
Output: Phase 15 operacionalmente completa — `python -m mis scan --platform product_hunt` e `--platform udemy` funcionam.
</objective>

<execution_context>
@C:/Users/Gabriel/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Gabriel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-international-api-based/15-CONTEXT.md
@.planning/phases/15-international-api-based/15-02-SUMMARY.md

<interfaces>
<!-- Contratos extraidos do codebase -->

From mis/scanner.py:run_all_scanners() (linhas 183-199):
```python
# Imports lazy dentro de run_all_scanners()
from .scanners.kiwify import KiwifyScanner
from .scanners.hotmart import HotmartScanner
from .scanners.clickbank import ClickBankScanner
from .scanners.eduzz import EduzzScanner
from .scanners.monetizze import MonetizzeScanner
from .scanners.perfectpay import PerfectPayScanner
from .scanners.braip import BraipScanner

SCANNER_MAP = {
    "kiwify": KiwifyScanner,
    "hotmart": HotmartScanner,
    "clickbank": ClickBankScanner,
    "eduzz": EduzzScanner,
    "monetizze": MonetizzeScanner,
    "perfectpay": PerfectPayScanner,
    "braip": BraipScanner,
    # ADICIONAR: "product_hunt": ProductHuntScanner, "udemy": UdemyScanner
}
```

From mis/base_scraper.py (linhas 37-43):
```python
DOMAIN_DELAYS: dict[str, float] = {
    "hotmart.com": 2.0,
    "kiwify.com.br": 2.0,
    "clickbank.com": 2.0,
    "marketplace.braip.com": 2.0,
    # ADICIONAR: "api.producthunt.com": 1.0, "www.udemy.com": 0.5
}
```

From mis/config.yaml (estrutura atual — 3 nichos: marketing-digital, emagrecimento, financas-pessoais):
```yaml
niches:
  - name: "Marketing Digital"
    slug: "marketing-digital"
    platforms:
      hotmart: marketing
      clickbank: marketing
      kiwify: marketing
      braip: cursos-online
      perfectpay: null
      eduzz: null
      monetizze: null
      # ADICIONAR: product_hunt: trending, udemy: "Marketing"
  - name: "Emagrecimento"
    # ADICIONAR: product_hunt: trending, udemy: "Health & Fitness"
  - name: "Financas Pessoais"
    # ADICIONAR: product_hunt: trending, udemy: "Finance & Accounting"
```

Decisao de CONTEXT.md (LOCKED):
- product_hunt: mesmo slug 'trending' para todos os nichos (scanner ignora niche_slug, retorna geral)
- Udemy: Marketing Digital -> "Marketing", Emagrecimento -> "Health & Fitness", Financas -> "Finance & Accounting"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SCANNER_MAP + DOMAIN_DELAYS + config.yaml</name>
  <files>mis/scanner.py, mis/base_scraper.py, mis/config.yaml</files>
  <action>
    1. mis/scanner.py — dentro de run_all_scanners(), adicionar aos imports lazy:
       ```python
       from .scanners.product_hunt import ProductHuntScanner
       from .scanners.udemy import UdemyScanner
       ```
       E adicionar ao SCANNER_MAP:
       ```python
       "product_hunt": ProductHuntScanner,
       "udemy": UdemyScanner,
       ```
       POSICAO: apos "braip": BraipScanner, antes do fechamento do dicionario.

    2. mis/base_scraper.py — adicionar ao DOMAIN_DELAYS:
       ```python
       "api.producthunt.com": 1.0,   # API oficial com rate limit documentado
       "www.udemy.com": 0.5,          # REST API com autenticacao
       ```
       Nao alterar DEFAULT_DELAY nem delays existentes.

    3. mis/config.yaml — adicionar entradas de plataforma em todos os 3 nichos:
       Marketing Digital:
         product_hunt: trending     # scanner ignora slug, retorna trending geral
         udemy: "Marketing"         # categoria oficial Udemy Affiliate API

       Emagrecimento:
         product_hunt: trending
         udemy: "Health & Fitness"  # categoria oficial Udemy

       Financas Pessoais:
         product_hunt: trending
         udemy: "Finance & Accounting"  # categoria oficial Udemy

       Adicionar comentarios inline explicando que product_hunt ignora o slug (retorna trending geral).
  </action>
  <verify>
    <automated>cd /c/Users/Gabriel/MEGABRAIN/mis && python -c "from mis.scanner import run_all_scanners; print('SCANNER_MAP import ok')" && python -c "import yaml; c = yaml.safe_load(open('config.yaml')); niches = c['niches']; assert all('product_hunt' in n['platforms'] for n in niches), 'product_hunt missing'; assert all('udemy' in n['platforms'] for n in niches), 'udemy missing'; print('config ok')" && python -m pytest tests/test_product_hunt_scanner.py tests/test_udemy_scanner.py -x --tb=short 2>&1 | tail -10</automated>
  </verify>
  <done>
    - run_all_scanners() importa ProductHuntScanner e UdemyScanner sem erro
    - config.yaml tem product_hunt e udemy em todos os 3 nichos
    - DOMAIN_DELAYS tem api.producthunt.com=1.0 e www.udemy.com=0.5
    - 12 testes continuam VERDES (sem regressao)
  </done>
</task>

<task type="auto">
  <name>Task 2: env.example — documentar credenciais da Phase 15</name>
  <files>bin/templates/env.example</files>
  <action>
    Adicionar secao MIS — MARKET INTELLIGENCE SYSTEM ao env.example com documentacao das novas credenciais.

    Adicionar apos a secao existente (apos a linha `# ANTHROPIC_API_KEY=`):

    ```
    # === MIS — MARKET INTELLIGENCE SYSTEM ===
    # Required for platform scanners that use official APIs

    # Product Hunt API Token — trending products scanner (Phase 15)
    # Scanner degrades gracefully (returns []) when absent
    # Get yours at: https://www.producthunt.com/v2/oauth/applications
    # Note: Developer token does not expire
    PRODUCT_HUNT_API_TOKEN=

    # Udemy Affiliate API credentials (Phase 15)
    # IMPORTANT: Udemy Affiliate API was deprecated on 2025-01-01
    # Scanner will return [] with alert='api_discontinued' if credentials are invalid
    # Legacy credentials (issued before 2025) may still work
    # Get yours at: https://www.udemy.com/affiliate/tools/api/ (if still accessible)
    UDEMY_CLIENT_ID=
    UDEMY_CLIENT_SECRET=
    ```

    Manter todo o conteudo existente intacto. Apenas adicionar ao final.
  </action>
  <verify>
    <automated>grep -c "PRODUCT_HUNT_API_TOKEN" /c/Users/Gabriel/MEGABRAIN/bin/templates/env.example && grep -c "UDEMY_CLIENT_ID" /c/Users/Gabriel/MEGABRAIN/bin/templates/env.example && grep -c "UDEMY_CLIENT_SECRET" /c/Users/Gabriel/MEGABRAIN/bin/templates/env.example</automated>
  </verify>
  <done>
    - PRODUCT_HUNT_API_TOKEN, UDEMY_CLIENT_ID, UDEMY_CLIENT_SECRET documentados em env.example
    - Cada variavel tem comentario explicando onde obter e nota sobre deprecacao da API Udemy
    - Conteudo existente do env.example preservado
  </done>
</task>

</tasks>

<verification>
Verificacao final da Phase 15 completa:
```bash
# Confirmar imports sem erro
cd /c/Users/Gabriel/MEGABRAIN/mis && python -c "
from mis.scanner import run_all_scanners
from mis.scanners.product_hunt import ProductHuntScanner
from mis.scanners.udemy import UdemyScanner
from mis.platform_ids import PRODUCT_HUNT_PLATFORM_ID, UDEMY_PLATFORM_ID
print('All imports OK')
print(f'PH platform_id={PRODUCT_HUNT_PLATFORM_ID}, Udemy platform_id={UDEMY_PLATFORM_ID}')
"

# Suite completa GREEN
cd /c/Users/Gabriel/MEGABRAIN/mis && python -m pytest tests/ -x --tb=short 2>&1 | tail -15

# Config tem as entradas corretas
cd /c/Users/Gabriel/MEGABRAIN/mis && python -c "
import yaml
c = yaml.safe_load(open('config.yaml'))
for n in c['niches']:
    ph = n['platforms'].get('product_hunt')
    ud = n['platforms'].get('udemy')
    print(f\"{n['slug']}: product_hunt={ph}, udemy={ud}\")
"
</verification>

<success_criteria>
- ProductHuntScanner e UdemyScanner registrados no SCANNER_MAP de run_all_scanners()
- config.yaml tem product_hunt e udemy nos 3 nichos com slugs corretos (decisao LOCKED do CONTEXT.md)
- DOMAIN_DELAYS com delays corretos para as duas novas APIs
- env.example documenta as 3 novas variaveis com instrucoes de onde obter + nota de deprecacao Udemy
- Suite completa (mis/tests/) GREEN sem regressoes
- Phase 15 operacionalmente completa
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/15-international-api-based/15-03-SUMMARY.md`
</output>
