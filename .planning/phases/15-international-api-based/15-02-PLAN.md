---
phase: 15-international-api-based
plan: 02
type: tdd
wave: 2
depends_on:
  - 15-01
files_modified:
  - mis/scanners/product_hunt.py
  - mis/scanners/udemy.py
autonomous: true
requirements:
  - SCAN-INTL-01
  - SCAN-INTL-02

must_haves:
  truths:
    - "ProductHuntScanner retorna trending products com rank posicional (1=top) usando GraphQL v2"
    - "UdemyScanner retorna cursos via REST com fallback gracioso quando API retorna 401/403/404"
    - "Ambos os scanners degradam graciosamente com credenciais ausentes (return [], alert='missing_credentials')"
    - "Todos os 12 testes GREEN apos implementacao completa"
  artifacts:
    - path: "mis/scanners/product_hunt.py"
      provides: "ProductHuntScanner completo com paginacao GraphQL (2 requisicoes, cursor)"
      exports: ["ProductHuntScanner"]
      min_lines: 80
    - path: "mis/scanners/udemy.py"
      provides: "UdemyScanner completo com Basic Auth + fallback api_discontinued"
      exports: ["UdemyScanner"]
      min_lines: 70
  key_links:
    - from: "mis/scanners/product_hunt.py"
      to: "https://api.producthunt.com/v2/api/graphql"
      via: "self._base._client.post() com Authorization Bearer token"
      pattern: "Bearer.*PRODUCT_HUNT_API_TOKEN"
    - from: "mis/scanners/udemy.py"
      to: "https://www.udemy.com/api-2.0/courses/"
      via: "self._base._client.get() com Authorization Basic base64"
      pattern: "Basic.*base64"
    - from: "mis/scanners/product_hunt.py"
      to: "mis/scanner.py:Product"
      via: "_parse_post() retorna Product com external_id=slug, rank=posicao, price=None"
      pattern: "Product\\(external_id=slug"
---

<objective>
GREEN cycle: implementar ProductHuntScanner e UdemyScanner completos para fazer os 12 testes passarem.

Purpose: Entregar os dois scanners internacionais funcionando — ProductHunt com cursor pagination GraphQL e Udemy com Basic Auth REST + fallback gracioso para API descontinuada.
Output: mis/scanners/product_hunt.py e mis/scanners/udemy.py com implementacao completa, todos os 12 testes GREEN.
</objective>

<execution_context>
@C:/Users/Gabriel/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Gabriel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-international-api-based/15-CONTEXT.md
@.planning/phases/15-international-api-based/15-RESEARCH.md
@.planning/phases/15-international-api-based/15-01-SUMMARY.md

<interfaces>
<!-- Contratos — executor usa diretamente -->

From mis/scanner.py:
```python
@dataclass
class Product:
    external_id: str      # slug para PH, str(id) para Udemy
    title: str
    url: str              # URL absoluta (PH: producthunt.com/posts/..., Udemy: https://www.udemy.com + path)
    platform_id: int      # PRODUCT_HUNT_PLATFORM_ID=8, UDEMY_PLATFORM_ID=9
    niche_id: int
    rank: int             # posicao ordinal (1=top) — NAO votesCount bruto
    price: Optional[float] = None   # PH: sempre None; Udemy: price_detail.amount
    commission_pct: Optional[float] = None
    rating: Optional[float] = None  # Udemy: avg_rating; PH: None
    thumbnail_url: Optional[str] = None  # PH: thumbnail["url"]; Udemy: image_480x270
```

From mis/platform_ids.py:
```python
PRODUCT_HUNT_PLATFORM_ID = 8
UDEMY_PLATFORM_ID = 9
```

From mis/scanners/clickbank.py (modelo de POST GraphQL):
```python
async def _post_graphql(self, payload: str) -> str:
    import httpx as _httpx
    client: _httpx.AsyncClient = self._base._client
    headers = {
        "Content-Type": "application/json",
        "Accept": "application/json",
        # DIFERENCA: PH precisa de Authorization Bearer
    }
    response = await client.post(GRAPHQL_URL, content=payload, headers=headers)
    response.raise_for_status()
    return response.text
```

GraphQL query para Product Hunt (RESEARCH.md verificado contra schema oficial):
```
posts(featured: true, postedAfter: "{today_utc_00:00}", postedBefore: "{today_utc_23:59}", order: VOTES, first: 20, after: $after)
  edges { node { id, name, tagline, slug, url, votesCount, thumbnail { url } } }
  pageInfo { endCursor, hasNextPage }
```

CRITICOS (anti-patterns da RESEARCH.md):
- thumbnail["url"] nao thumbnail["imageUrl"] — schema real usa Media.url
- rank = posicao na lista (1, 2, 3...) nao votesCount bruto
- price = None para todos os produtos do ProductHunt
- Udemy url: prefixar "https://www.udemy.com" + course["url"]
- price_detail pode ser null: usar (course.get("price_detail") or {}).get("amount") or None
- thumbnail pode ser null no PH: thumbnail_data = node.get("thumbnail") or {}
- Checar hasNextPage antes de segunda requisicao (pode ser false se < 40 trending hoje)
- Udemy: capturar httpx.HTTPStatusError + Exception para fallback api_discontinued
</interfaces>
</context>

<feature>
  <name>ProductHuntScanner + UdemyScanner — implementacao completa (GREEN cycle)</name>
  <files>mis/scanners/product_hunt.py, mis/scanners/udemy.py</files>
  <behavior>
    ProductHuntScanner.scan_niche():
    - Checa PRODUCT_HUNT_API_TOKEN; se ausente return [] com alert='missing_credentials'
    - Usa datetime.now(timezone.utc) para postedAfter/postedBefore do dia corrente em UTC
    - Primeira requisicao: query com after=None (first: 20)
    - Checa pageInfo.hasNextPage; se True, segunda requisicao com after=endCursor
    - _parse_post() -> Product com external_id=slug, rank=posicao_ordinal (1-based), price=None, thumbnail=thumbnail["url"] ou None
    - Filtra nodes sem slug (retorna None do _parse_post, skip)
    - Loga scan_complete com count ao final
    - Em qualquer excecao de rede/parse: loga schema_drift, retorna []

    UdemyScanner.scan_niche():
    - Checa UDEMY_CLIENT_ID + UDEMY_CLIENT_SECRET; se ausente return [] com alert='missing_credentials'
    - platform_slug = categoria Udemy (ex: "Marketing") — usar como param 'category'
    - Basic Auth: base64.b64encode(f"{client_id}:{client_secret}".encode()).decode()
    - GET https://www.udemy.com/api-2.0/courses/?search=<keyword>&ordering=most-reviewed&category=<cat>&page_size=20&fields[course]=id,title,url,price_detail,avg_rating,image_480x270
    - Nota: platform_slug JA E a categoria (config.yaml mapeia "Marketing", "Health & Fitness")
    - Em httpx.HTTPStatusError (401/403/404): alert='api_discontinued' return []
    - Em qualquer outra excecao: alert='schema_drift' return []
    - _parse_course() -> Product com external_id=str(course["id"]), url="https://www.udemy.com"+course["url"], rank=posicao_ordinal, price=(price_detail or {}).get("amount"), rating=course.get("avg_rating"), thumbnail=course.get("image_480x270")
  </behavior>
  <implementation>
    Substituir stubs do plano 15-01 pelas implementacoes completas. Manter os docstrings e a estrutura de metodos auxiliares (_parse_post, _post_graphql, _parse_course).

    Apos implementacao, rodar testes e confirmar GREEN:
    cd mis && python -m pytest tests/test_product_hunt_scanner.py tests/test_udemy_scanner.py -x -v

    Commit GREEN: `feat(15-02): implement ProductHuntScanner + UdemyScanner — GraphQL cursor pagination + Basic Auth REST`
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ProductHuntScanner — implementacao completa (GREEN)</name>
  <files>mis/scanners/product_hunt.py</files>
  <behavior>
    - scan_niche() com credencial ausente retorna [] + alert='missing_credentials'
    - scan_niche() com fixture trending_today.json retorna 3 produtos (2 paginas se hasNextPage=true na fixture)
    - result[0].rank == 1, result[0].external_id == 'jarvis-ai-assistant'
    - result[0].price == None (PH nao retorna preco monetario)
    - result[0].thumbnail_url is str (nao None) — fixture tem thumbnail.url preenchido
    - result[0].platform_id == PRODUCT_HUNT_PLATFORM_ID (8)
    - Com edges=[] retorna []
  </behavior>
  <action>
    Reescrever mis/scanners/product_hunt.py com implementacao completa:

    1. Constantes:
       - PRODUCT_HUNT_GRAPHQL_URL = "https://api.producthunt.com/v2/api/graphql"
       - TRENDING_TODAY_QUERY com $after: String? — campos: id, name, tagline, slug, url, votesCount, thumbnail { url }, pageInfo { endCursor hasNextPage }
       - postedAfter/postedBefore gerados dinamicamente com datetime.now(timezone.utc)

    2. Funcao auxiliar _parse_post(node: dict, rank: int, niche_id: int) -> Product | None:
       - slug = node.get("slug"); if not slug: return None
       - thumbnail_data = node.get("thumbnail") or {}; thumbnail_url = thumbnail_data.get("url") or None
       - url = node.get("url") or f"https://www.producthunt.com/posts/{slug}"
       - return Product(external_id=slug, title=node.get("name") or slug, url=url, platform_id=PRODUCT_HUNT_PLATFORM_ID, niche_id=niche_id, rank=rank, price=None, thumbnail_url=thumbnail_url)

    3. Metodo _post_graphql(query: str, variables: dict) -> str:
       - Mesmo padrao que ClickBankScanner._post_graphql() mas com header Authorization: Bearer {token}
       - import httpx as _httpx; client = self._base._client
       - headers = {"Authorization": f"Bearer {token}", "Content-Type": "application/json", "Accept": "application/json"}
       - response = await client.post(PRODUCT_HUNT_GRAPHQL_URL, content=json.dumps({"query": query, "variables": variables}), headers=headers)
       - response.raise_for_status(); return response.text

    4. scan_niche() completo:
       - Checa token; se ausente: log.warning(alert='missing_credentials') return []
       - Loop: pagina 1 (after=None) -> parse -> se hasNextPage: pagina 2 (after=endCursor) -> parse
       - Cada requisicao: try/except Exception -> log.error(alert='schema_drift') return []
       - Rank = posicao_global na lista concatenada (1-based)
       - Retorna produtos filtrados (None removidos)
       - log.info('product_hunt_scanner.scan_complete', count=len(products))
  </action>
  <verify>
    <automated>cd /c/Users/Gabriel/MEGABRAIN/mis && python -m pytest tests/test_product_hunt_scanner.py -x -v 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Todos os 6 testes de test_product_hunt_scanner.py VERDES
    - test_happy_path: len(result) >= 1, rank=1, external_id='jarvis-ai-assistant'
    - test_missing_credentials: result == [], alert='missing_credentials' nos logs
    - test_upsert_no_duplicates + test_is_stale: comportamento de upsert verificado
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: UdemyScanner — implementacao completa (GREEN)</name>
  <files>mis/scanners/udemy.py</files>
  <behavior>
    - scan_niche() com credenciais ausentes retorna [] + alert='missing_credentials'
    - scan_niche() com fixture courses_marketing.json retorna 3 produtos
    - result[0].rank == 1, result[0].url.startswith("https://www.udemy.com")
    - result[0].rating is float (avg_rating da fixture)
    - result[0].price is float (price_detail.amount da fixture)
    - result[0].platform_id == UDEMY_PLATFORM_ID (9)
    - HTTP 401 retorna [] + alert='api_discontinued'
    - results=[] retorna []
  </behavior>
  <action>
    Reescrever mis/scanners/udemy.py com implementacao completa:

    1. Constantes:
       - UDEMY_API_URL = "https://www.udemy.com/api-2.0/courses/"

    2. Funcao auxiliar _parse_course(course: dict, rank: int, niche_id: int) -> Product | None:
       - course_id = course.get("id"); if not course_id: return None
       - url_path = course.get("url") or ""; url = "https://www.udemy.com" + url_path
       - price = (course.get("price_detail") or {}).get("amount") or None
       - if price is not None: price = float(price)
       - rating_raw = course.get("avg_rating"); rating = float(rating_raw) if rating_raw is not None else None
       - thumbnail_url = course.get("image_480x270") or None
       - return Product(external_id=str(course_id), title=course.get("title") or str(course_id), url=url, platform_id=UDEMY_PLATFORM_ID, niche_id=niche_id, rank=rank, price=price, rating=rating, thumbnail_url=thumbnail_url)

    3. scan_niche() completo:
       - Checa client_id + client_secret; se ausente: log.warning(alert='missing_credentials') return []
       - credentials = base64.b64encode(f"{client_id}:{client_secret}".encode()).decode()
       - headers = {"Authorization": f"Basic {credentials}", "Accept": "application/json"}
       - params = {"search": platform_slug, "ordering": "most-reviewed", "category": platform_slug, "page_size": 20, "fields[course]": "id,title,url,price_detail,avg_rating,image_480x270"}
       - NOTA: platform_slug em config.yaml JA e a categoria Udemy ("Marketing", "Health & Fitness") — usar diretamente como 'category' e 'search'
       - try: response = await self._base._client.get(UDEMY_API_URL, headers=headers, params=params); response.raise_for_status()
       - except httpx.HTTPStatusError as exc: if exc.response.status_code in (401, 403, 404): log.warning(alert='api_discontinued') return []
       - except Exception as exc: log.warning(alert='schema_drift') return []
       - data = response.json(); results = data.get("results") or []
       - if not results: return []
       - Mapear _parse_course() com rank 1-indexed
       - log.info('udemy_scanner.scan_complete', count=len(products))

    Dependencia necessaria: `import base64, import httpx` no topo do arquivo.
  </action>
  <verify>
    <automated>cd /c/Users/Gabriel/MEGABRAIN/mis && python -m pytest tests/test_product_hunt_scanner.py tests/test_udemy_scanner.py -x -v 2>&1 | tail -25</automated>
  </verify>
  <done>
    - Todos os 12 testes VERDES (6 ProductHunt + 6 Udemy)
    - test_happy_path Udemy: len(result) >= 1, url comeca com 'https://www.udemy.com'
    - test_api_discontinued_fallback: result == [], alert='api_discontinued' nos logs
    - test_field_types: rating is float, price is float, external_id is str
    - Commit GREEN: `feat(15-02): implement ProductHuntScanner + UdemyScanner`
  </done>
</task>

</tasks>

<verification>
Apos ambas as tasks, rodar suite completa para confirmar sem regressoes:
```
cd /c/Users/Gabriel/MEGABRAIN/mis && python -m pytest tests/ -x --tb=short 2>&1 | tail -20
```
</verification>

<success_criteria>
- 12 testes VERDES (test_product_hunt_scanner.py + test_udemy_scanner.py)
- Suite completa (tests/) sem regressoes
- ProductHuntScanner: retorna trending products com rank posicional, price=None, thumbnail.url correto
- UdemyScanner: retorna cursos com url absoluta, rating e price tipados, fallback api_discontinued para HTTP 401
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/15-international-api-based/15-02-SUMMARY.md`
</output>
