---
phase: 03-product-espionage-dossiers
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - mis/migrations/_003_spy_dossiers.py
  - mis/spies/__init__.py
  - mis/spies/sales_page.py
  - mis/prompts/sales_page_extractor.md
  - mis/requirements.txt
  - mis/tests/test_sales_page_spy.py
  - mis/tests/fixtures/sales_page/hotmart_sample.html
  - mis/tests/fixtures/llm_responses/sales_page_extract.json
autonomous: true
requirements:
  - SPY-01
  - SPY-03

must_haves:
  truths:
    - "Migration _003 adiciona colunas status, dossier_json, incomplete, updated_at à tabela dossiers existente sem destruir dados"
    - "Tabelas reviews e llm_calls são criadas pela migration _003"
    - "SalesPageScraper extrai copy (headlines, argumentos, CTAs) e oferta (preço, bônus, garantias) de qualquer URL em uma chamada LLM"
    - "SalesPageScraper usa fetch() com fallback para fetch_spa() para SPAs"
    - "Texto limpo truncado em 50.000 chars antes de enviar ao LLM para controlar tokens"
    - "Fixture HTML commitada + mock LLM: testes rodam sem API real"
  artifacts:
    - path: "mis/migrations/_003_spy_dossiers.py"
      provides: "Migration aditiva para tabelas de espionagem"
      contains: "add_column"
    - path: "mis/spies/sales_page.py"
      provides: "SalesPageScraper platform-agnostic"
      exports: ["SalesPageScraper"]
    - path: "mis/prompts/sales_page_extractor.md"
      provides: "System prompt para extração de copy + oferta"
      contains: "JSON"
    - path: "mis/tests/test_sales_page_spy.py"
      provides: "Testes SPY-01 e SPY-03"
      min_lines: 40
  key_links:
    - from: "mis/spies/sales_page.py"
      to: "mis/base_scraper.py"
      via: "herança de BaseScraper"
      pattern: "class SalesPageScraper.*BaseScraper"
    - from: "mis/spies/sales_page.py"
      to: "Anthropic API (mockada nos testes)"
      via: "AsyncAnthropic().messages.create()"
      pattern: "AsyncAnthropic"
    - from: "mis/migrations/_003_spy_dossiers.py"
      to: "tabela dossiers existente de _001"
      via: "add_column() idempotente"
      pattern: "add_column"
---

<objective>
Criar a fundação de dados (migration _003) e o primeiro spy: SalesPageScraper platform-agnostic que extrai copy completa e estrutura de oferta de qualquer página de vendas usando LLM como parser universal.

Purpose: Desbloquear todos os planos subsequentes — a migration _003 é pré-requisito para persistência de dossiers/reviews/llm_calls; o SalesPageScraper é o spy mais crítico (copy é bloqueante no data completeness gate).

Output: mis/migrations/_003_spy_dossiers.py (schema expandido), mis/spies/sales_page.py (SPY-01 + SPY-03), prompts/sales_page_extractor.md, testes com fixtures commitadas.
</objective>

<execution_context>
@C:/Users/Gabriel/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Gabriel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-product-espionage-dossiers/03-CONTEXT.md
@.planning/phases/03-product-espionage-dossiers/03-RESEARCH.md

<interfaces>
<!-- Interfaces críticas do codebase existente para o executor. -->

De mis/base_scraper.py:
```python
class BaseScraper:
    def __init__(self, proxy_url: Optional[str] = None) -> None: ...
    async def __aenter__(self) -> "BaseScraper": ...
    async def __aexit__(self, *_) -> None: ...
    def _get_semaphore(self, domain: str) -> asyncio.Semaphore: ...
    async def fetch(self, url: str) -> str: ...        # httpx, retry tenacity
    async def fetch_spa(self, url: str) -> str: ...   # Playwright + stealth
```

De mis/exceptions.py:
```python
class ScraperError(Exception): ...
```

De mis/migrations/_001_initial.py — tabela dossiers EXISTENTE (não recriar):
```python
db["dossiers"].create({
    "id": int,
    "product_id": int,
    "analysis": str,           # coluna legada — manter
    "opportunity_score": float,
    "confidence_score": float,
    "generated_at": str,
}, pk="id", foreign_keys=[("product_id", "products", "id")])
```

De mis/db.py:
```python
def get_db(db_path: str) -> sqlite_utils.Database: ...
```

Padrão migration existente (mis/migrations/_002_product_enrichment.py):
```python
def run_migration_002(db_path: str) -> None:
    db = sqlite_utils.Database(db_path)
    existing_cols = {col.name for col in db["products"].columns}
    if "category" not in existing_cols:
        db["products"].add_column("category", str)
    ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration _003 — expandir dossiers + criar reviews e llm_calls</name>
  <files>
    mis/migrations/_003_spy_dossiers.py,
    mis/tests/test_migration_003.py
  </files>
  <behavior>
    - test_migration_adds_columns: após run_migration_003(), tabela dossiers tem colunas status, dossier_json, incomplete, updated_at
    - test_migration_preserves_legacy: colunas legadas (analysis, opportunity_score, confidence_score, generated_at) ainda existem
    - test_migration_idempotent: rodar run_migration_003() duas vezes não levanta exceção
    - test_reviews_table_created: tabela reviews existe com colunas id, product_id, text, valence, rating, source, created_at
    - test_llm_calls_table_created: tabela llm_calls existe com colunas id, dossier_id, model, stage, input_tokens, output_tokens, cost_usd, created_at
  </behavior>
  <action>
    Criar mis/migrations/_003_spy_dossiers.py com função run_migration_003(db_path: str) -> None.

    CRÍTICO — pitfall do _001: a tabela dossiers já existe com schema diferente. NUNCA usar db["dossiers"].create(). Usar padrão add_column() idempotente:

    ```python
    existing_cols = {col.name for col in db["dossiers"].columns}
    new_cols = {
        "status": str,          # 'pending'|'running'|'done'|'failed'
        "dossier_json": str,    # JSON estruturado (campo canônico)
        "incomplete": int,      # BOOL como int (SQLite)
        "updated_at": str,
    }
    for col_name, col_type in new_cols.items():
        if col_name not in existing_cols:
            db["dossiers"].add_column(col_name, col_type)
    ```

    Criar tabela reviews SE não existir:
    ```python
    if "reviews" not in db.table_names():
        db["reviews"].create({
            "id": int, "product_id": int, "text": str,
            "valence": str,  # 'positive'|'negative'
            "rating": float, "source": str, "created_at": str,
        }, pk="id", foreign_keys=[("product_id", "products", "id")])
        db.execute("CREATE INDEX IF NOT EXISTS idx_reviews_product_valence ON reviews(product_id, valence)")
    ```

    Criar tabela llm_calls SE não existir:
    ```python
    if "llm_calls" not in db.table_names():
        db["llm_calls"].create({
            "id": int, "dossier_id": int, "model": str, "stage": str,
            "input_tokens": int, "output_tokens": int,
            "cost_usd": float, "created_at": str,
        }, pk="id", foreign_keys=[("dossier_id", "dossiers", "id")])
    ```

    Atualizar mis/migrations/__init__.py para exportar/registrar run_migration_003.
    Atualizar mis/db.py para chamar run_migration_003 em run_migrations() (seguir padrão existente de encadeamento de migrations).

    Criar mis/tests/test_migration_003.py usando tmp_path (padrão conftest.py existente).
  </action>
  <verify>
    <automated>cd /c/Users/Gabriel/MEGABRAIN && python -m pytest mis/tests/test_migration_003.py -x -q</automated>
  </verify>
  <done>5/5 testes GREEN. Migration idempotente. Tabelas reviews e llm_calls existem. Colunas novas em dossiers adicionadas sem perda das legadas.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SalesPageScraper platform-agnostic (SPY-01 + SPY-03)</name>
  <files>
    mis/spies/__init__.py,
    mis/spies/sales_page.py,
    mis/prompts/sales_page_extractor.md,
    mis/requirements.txt,
    mis/tests/test_sales_page_spy.py,
    mis/tests/fixtures/sales_page/hotmart_sample.html,
    mis/tests/fixtures/llm_responses/sales_page_extract.json
  </files>
  <behavior>
    - test_extract_copy_happy_path: dado fixture HTML de página de vendas, SalesPageScraper.extract() retorna dict com chaves headlines, sub_headlines, arguments, ctas, narrative_structure não vazias
    - test_extract_offer_fields: mesmo fixture retorna dict com price, bonuses, guarantees (SPY-03 — mesma chamada, mesmo fetch)
    - test_spa_fallback: quando fetch() lança ScraperError, fetch_spa() é chamado automaticamente (mock ambos)
    - test_text_truncated: quando HTML resulta em texto > 50.000 chars, texto enviado ao LLM é truncado (verificar via mock)
    - test_json_parse_error_propagates: quando LLM retorna texto livre (não JSON), levanta ValueError com mensagem clara
  </behavior>
  <action>
    Instalar dependências novas: adicionar `anthropic>=0.84.0` e `markdownify>=0.12.0` ao mis/requirements.txt e instalar via pip.

    Criar mis/spies/__init__.py (vazio ou com imports básicos).

    Criar mis/prompts/sales_page_extractor.md com system prompt que instrui o LLM a retornar APENAS JSON válido (sem markdown fencing) com este schema:
    ```json
    {
      "headlines": ["..."],
      "sub_headlines": ["..."],
      "arguments": ["..."],
      "ctas": ["..."],
      "narrative_structure": "AIDA|PAS|Story|Outro",
      "price": "R$ 97",
      "bonuses": ["..."],
      "guarantees": ["..."],
      "upsells": ["..."],
      "downsells": ["..."]
    }
    ```
    O prompt deve: instrução em pt-BR, enfatizar "Responda APENAS com JSON válido, sem texto adicional, sem ```json fencing".

    Criar mis/spies/sales_page.py com SalesPageScraper(BaseScraper):

    ```python
    import json, os
    from pathlib import Path
    import markdownify
    from anthropic import AsyncAnthropic, APIConnectionError, RateLimitError
    from tenacity import retry, stop_after_attempt, wait_exponential, retry_if_exception_type
    from ..base_scraper import BaseScraper
    from ..exceptions import ScraperError
    import structlog

    log = structlog.get_logger()
    MAX_TEXT_CHARS = 50_000
    _PROMPT_PATH = Path(__file__).parent.parent / "prompts" / "sales_page_extractor.md"

    class SalesPageScraper(BaseScraper):
        async def extract(self, url: str) -> dict:
            # 1. Fetch HTML (httpx → SPA fallback)
            try:
                html = await self.fetch(url)
            except ScraperError:
                html = await self.fetch_spa(url)

            # 2. Limpar HTML → Markdown truncado
            clean = markdownify.markdownify(
                html,
                strip=["script","style","noscript","meta","head","aside","footer","nav"],
                heading_style=markdownify.ATX,
            )
            clean = clean[:MAX_TEXT_CHARS]

            # 3. Chamar LLM com retry
            result_text = await self._call_llm(clean)

            # 4. Parse JSON — propagar erro se LLM não retornar JSON válido
            try:
                return json.loads(result_text)
            except json.JSONDecodeError as e:
                raise ValueError(f"LLM retornou texto não-JSON: {result_text[:200]}") from e

        @retry(
            stop=stop_after_attempt(3),
            wait=wait_exponential(multiplier=1, min=2, max=30),
            retry=retry_if_exception_type((APIConnectionError, RateLimitError)),
            reraise=True,
        )
        async def _call_llm(self, content: str) -> str:
            system_prompt = _PROMPT_PATH.read_text(encoding="utf-8")
            client = AsyncAnthropic(api_key=os.environ["ANTHROPIC_API_KEY"])
            response = await client.messages.create(
                model="claude-sonnet-4-6",
                max_tokens=4096,
                system=system_prompt,
                messages=[{"role": "user", "content": content}],
            )
            return response.content[0].text
    ```

    Criar fixture HTML: mis/tests/fixtures/sales_page/hotmart_sample.html — página de vendas realista simplificada com headline, argumentos, preço, bônus, CTA.

    Criar fixture JSON: mis/tests/fixtures/llm_responses/sales_page_extract.json — resposta JSON do LLM correspondente ao fixture HTML (gravada manualmente, representa resposta real esperada).

    Criar mis/tests/test_sales_page_spy.py usando respx para mock do httpx e monkeypatch/mock para AsyncAnthropic.messages.create, carregando fixture JSON como resposta do LLM.

    ANTI-PATTERN a evitar: NÃO criar seletores por plataforma. O LLM é o único parser.
  </action>
  <verify>
    <automated>cd /c/Users/Gabriel/MEGABRAIN && python -m pytest mis/tests/test_sales_page_spy.py -x -q</automated>
  </verify>
  <done>5/5 testes GREEN. SalesPageScraper extrai copy + oferta da fixture. Fallback SPA testado. Truncamento verificado. requirements.txt atualizado com anthropic e markdownify.</done>
</task>

</tasks>

<verification>
```bash
cd /c/Users/Gabriel/MEGABRAIN && python -m pytest mis/tests/test_migration_003.py mis/tests/test_sales_page_spy.py -x -q
```
Ambos os arquivos de teste passam. Migration idempotente verificada. SalesPageScraper extrai copy + oferta com mock LLM.
</verification>

<success_criteria>
- migration _003 cria tabelas reviews e llm_calls; adiciona status/dossier_json/incomplete/updated_at a dossiers sem DROP TABLE
- SalesPageScraper.extract(url) retorna dict com campos de copy (SPY-01) E oferta (SPY-03) em uma chamada
- Testes passam com fixtures — sem dependência de API real
- anthropic e markdownify adicionados ao requirements.txt
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/03-product-espionage-dossiers/03-01-SUMMARY.md`
</output>
