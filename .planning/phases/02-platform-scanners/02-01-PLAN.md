---
phase: 02-platform-scanners
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - mis/scanner.py
  - mis/product_repository.py
  - mis/migrations/_002_product_enrichment.py
  - mis/scanners/__init__.py
  - mis/scanners/kiwify.py
  - mis/config.py
  - mis/config.yaml
  - mis/requirements.txt
  - mis/tests/fixtures/kiwify/catalog_saude.html
  - mis/tests/fixtures/kiwify/catalog_mkt.html
  - mis/tests/test_kiwify_scanner.py
autonomous: true
requirements:
  - SCAN-02

must_haves:
  truths:
    - "KiwifyScanner.scan_niche() retorna pelo menos 10 produtos com todos os campos obrigatórios (external_id, title, url, platform_id, niche_id, rank)"
    - "price é float, rank é int, external_id não é None em nenhum produto retornado"
    - "Quando todos os seletores CSS falham, lista vazia é retornada e structlog emite alert='schema_drift'"
    - "Upsert no DB real: inserir produto e re-rodar com rank diferente resulta em 1 linha com rank atualizado"
    - "Config.yaml aceita bloco platforms por nicho e novos settings (max_products_per_niche, scan_schedule, parallel_scanners)"
  artifacts:
    - path: "mis/scanner.py"
      provides: "Product dataclass + PlatformScanner ABC + run_all_scanners()"
      exports: ["Product", "PlatformScanner", "run_all_scanners"]
    - path: "mis/product_repository.py"
      provides: "upsert_product() com pk composto (platform_id, external_id)"
      exports: ["upsert_product"]
    - path: "mis/migrations/_002_product_enrichment.py"
      provides: "Migration idempotente que adiciona rank, commission_pct, rating, thumbnail_url, updated_at"
      exports: ["run_migration_002"]
    - path: "mis/scanners/kiwify.py"
      provides: "KiwifyScanner(PlatformScanner) com scan_niche(), fallback selectors, drift alert"
      exports: ["KiwifyScanner"]
    - path: "mis/tests/test_kiwify_scanner.py"
      provides: "5 testes cobrindo SCAN-02 (TDD RED primeiro, GREEN após implementação)"
  key_links:
    - from: "mis/scanners/kiwify.py"
      to: "mis/product_repository.py"
      via: "upsert_product(db, product) chamado por consumer — não dentro do scraper"
      pattern: "upsert_product"
    - from: "mis/scanners/kiwify.py"
      to: "mis/scanner.py"
      via: "KiwifyScanner(PlatformScanner) herda PlatformScanner que herda BaseScraper"
      pattern: "class KiwifyScanner\\(PlatformScanner\\)"
    - from: "mis/config.py"
      to: "mis/config.yaml"
      via: "load_config() valida bloco platforms e novos settings"
      pattern: "platforms.*hotmart|clickbank|kiwify"
---

<objective>
Implementar a base arquitetural dos scanners de plataforma e o primeiro scraper concreto (Kiwify), seguindo TDD estrito.

Purpose: Validar o padrão PlatformScanner antes dos scrapers de maior risco (ClickBank, Hotmart). Kiwify é SSR puro — httpx suficiente, sem anti-bot significativo — ideal para estabilizar a arquitetura.

Output:
- mis/scanner.py — Product dataclass + PlatformScanner ABC + run_all_scanners()
- mis/product_repository.py — upsert_product() com pk composto
- mis/migrations/_002_product_enrichment.py — colunas de enriquecimento
- mis/scanners/kiwify.py — KiwifyScanner completo com fallback selectors e drift alert
- mis/config.py (estendido) + mis/config.yaml (atualizado)
- Fixtures HTML de Kiwify gravadas ao vivo
- 5 testes GREEN em test_kiwify_scanner.py
</objective>

<execution_context>
@C:/Users/Gabriel/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Gabriel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-platform-scanners/02-CONTEXT.md
@.planning/phases/02-platform-scanners/02-RESEARCH.md
@.planning/phases/02-platform-scanners/02-VALIDATION.md

<interfaces>
<!-- Contratos existentes que o executor usa diretamente — sem exploração necessária. -->

De mis/base_scraper.py:
```python
DOMAIN_DELAYS: dict[str, float] = {
    "hotmart.com": 2.0,
    "kiwify.com.br": 2.0,
    "clickbank.com": 2.0,
}

class BaseScraper:
    def __init__(self, proxy_url: Optional[str] = None) -> None: ...
    async def __aenter__(self) -> "BaseScraper": ...
    async def __aexit__(self, *_) -> None: ...
    async def fetch(self, url: str) -> str: ...       # httpx + tenacity retry
    async def fetch_spa(self, url: str) -> str: ...   # Playwright + stealth
```

De mis/db.py:
```python
from .migrations._001_initial import run_migrations  # re-exported
def get_db(db_path: str) -> sqlite_utils.Database: ...
```

De mis/scheduler.py:
```python
def get_scheduler() -> AsyncIOScheduler: ...
def start_scheduler() -> None: ...
def stop_scheduler() -> None: ...
```

De mis/tests/conftest.py:
```python
# fixture db_path: tmp_path/mis.db com _001 migration já rodada
# fixture temp_config_yaml: path para config.yaml temporário
```

De mis/pytest.ini:
```ini
asyncio_mode = auto
testpaths = tests
timeout = 10
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — contratos, migration, fixtures Kiwify e testes RED</name>
  <files>
    mis/scanner.py,
    mis/scanners/__init__.py,
    mis/product_repository.py,
    mis/migrations/_002_product_enrichment.py,
    mis/requirements.txt,
    mis/config.yaml,
    mis/config.py,
    mis/tests/fixtures/kiwify/catalog_saude.html,
    mis/tests/fixtures/kiwify/catalog_mkt.html,
    mis/tests/test_kiwify_scanner.py
  </files>
  <action>
    **Passo 1 — mis/scanner.py (contratos):**
    Criar com:
    - `@dataclass class Product` com campos: `external_id: str`, `title: str`, `url: str`, `platform_id: int`, `niche_id: int`, `rank: int`, `price: Optional[float] = None`, `commission_pct: Optional[float] = None`, `rating: Optional[float] = None`, `thumbnail_url: Optional[str] = None`
    - `class PlatformScanner(BaseScraper, ABC)` com método abstrato `async def scan_niche(self, niche_slug: str, platform_slug: str) -> list[Product]`
    - `async def run_all_scanners(config: dict) -> dict[str, list[Product]]` com `asyncio.gather(return_exceptions=True)` — imports internos para evitar circular. Niche sem bloco `platforms`: log.warning("scanner.niche.no_platforms", niche=niche["slug"]) + continue.

    **Passo 2 — mis/scanners/__init__.py:**
    Arquivo vazio (torna subpackage importável).

    **Passo 3 — mis/product_repository.py:**
    Criar `upsert_product(db: sqlite_utils.Database, product: Product) -> None` com:
    - pk=("platform_id", "external_id"), alter=True
    - Campo updated_at como `datetime.now(timezone.utc).isoformat()`
    - Guard: se `not products` em `save_batch()`, retornar sem upsert e logar `zero_products_detected`
    - Criar também `save_batch(db, products: list[Product]) -> None` para uso pelos scrapers

    **Passo 4 — mis/migrations/_002_product_enrichment.py:**
    Criar `run_migration_002(db_path: str) -> None` idempotente:
    - Verifica `{col.name for col in db["products"].columns}` antes de adicionar
    - Adiciona se ausentes: rank (int), commission_pct (float), rating (float), thumbnail_url (str), updated_at (str)

    **Passo 5 — mis/requirements.txt:**
    Adicionar `beautifulsoup4>=4.12.0` e `lxml>=5.0.0` se não presentes.

    **Passo 6 — mis/config.yaml:**
    Atualizar cada nicho com bloco `platforms`:
    ```yaml
    niches:
      - name: "Marketing Digital"
        slug: "marketing-digital"
        platforms:
          hotmart: marketing
          clickbank: marketing
          kiwify: marketing
        keywords: [...]
      - name: "Emagrecimento"
        slug: "emagrecimento"
        platforms:
          hotmart: saude-e-fitness
          clickbank: health
          kiwify: saude
        keywords: [...]
      - name: "Financas Pessoais"
        slug: "financas-pessoais"
        platforms:
          hotmart: financas
          clickbank: investing
          kiwify: financas
        keywords: [...]
    settings:
      proxy_url: ""
      request_delay_s: 2.0
      max_retries: 3
      max_products_per_niche: 50
      scan_schedule: "0 3 * * *"
      parallel_scanners: true
    ```

    **Passo 7 — mis/config.py (estender load_config):**
    Após validação de nichos existente, adicionar:
    - Verificar que `platforms` existe em cada nicho (se não: structlog warning + skip silencioso — backward compatible)
    - Verificar que nomes de plataforma são válidos: `{"hotmart", "clickbank", "kiwify"}` — typos como `hotmrt` lançam `ValueError`
    - Pelo menos uma plataforma mapeada por nicho (ValueError se nenhuma)
    - Ler novos settings: `max_products_per_niche` (default 50), `scan_schedule` (default "0 3 * * *"), `parallel_scanners` (default True)

    **Passo 8 — Gravar fixtures Kiwify ao vivo:**
    Usar httpx para fazer GET em:
    - `https://www.kiwify.com.br/marketplace?category=saude` → gravar conteúdo em `mis/tests/fixtures/kiwify/catalog_saude.html`
    - `https://www.kiwify.com.br/marketplace?category=marketing` → gravar em `mis/tests/fixtures/kiwify/catalog_mkt.html`
    Se a URL exata de categoria for diferente, usar DevTools do browser para identificar a URL real. Gravar o HTML exato sem modificação.

    **Passo 9 — mis/tests/test_kiwify_scanner.py (testes RED):**
    Criar 5 testes que FALHAM pois KiwifyScanner ainda não existe:
    ```python
    import pytest
    import respx
    from pathlib import Path
    from httpx import Response
    from structlog.testing import capture_logs

    FIXTURE_SAUDE = Path(__file__).parent / "fixtures/kiwify/catalog_saude.html"
    KIWIFY_URL = "https://www.kiwify.com.br/marketplace?category=saude"

    async def test_happy_path(tmp_path):
        # testa que scan_niche retorna >= 10 produtos com campos obrigatórios
        ...

    async def test_field_types(tmp_path):
        # testa que price é float, rank é int, external_id não é None
        ...

    async def test_fallback_selector(tmp_path):
        # testa que fallback selector retorna produtos quando primary falha
        ...

    async def test_drift_alert(tmp_path):
        # usa structlog.testing.capture_logs() — NÃO caplog
        # testa que alert='schema_drift' é emitido quando HTML não tem nenhum selector
        with capture_logs() as cap:
            ...
        assert any(e.get("alert") == "schema_drift" for e in cap)

    async def test_upsert_no_duplicates(tmp_path):
        # DB real: upsert do mesmo produto duas vezes → 1 linha com rank atualizado
        ...
    ```
    Todos os testes devem usar `respx.mock` com fixtures HTML. Testes de upsert usam `run_migration_002` no DB real de tmp_path.

    Verificar que os testes falham em RED antes de prosseguir para Task 2.
  </action>
  <verify>
    <automated>cd /c/Users/Gabriel/MEGABRAIN/mis && python -m pytest tests/test_kiwify_scanner.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>
    - mis/scanner.py existe com Product, PlatformScanner, run_all_scanners exportados
    - mis/product_repository.py existe com upsert_product() e save_batch()
    - mis/migrations/_002_product_enrichment.py existe e é idempotente
    - mis/tests/fixtures/kiwify/*.html gravados ao vivo (não vazios)
    - mis/tests/test_kiwify_scanner.py tem 5 testes em RED (ImportError ou AssertionError esperado pois KiwifyScanner não existe ainda)
    - requirements.txt inclui beautifulsoup4 e lxml
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar KiwifyScanner — RED → GREEN</name>
  <files>
    mis/scanners/kiwify.py
  </files>
  <behavior>
    - test_happy_path: scan_niche("emagrecimento", "saude") retorna lista >= 10 produtos, todos com external_id, title, url, platform_id=KIWIFY_PLATFORM_ID, niche_id, rank
    - test_field_types: product.price é float ou None, product.rank é int, product.external_id é str não-vazio
    - test_fallback_selector: quando primary selector ausente no HTML, fallback retorna produtos; resultado ainda é lista não-vazia
    - test_drift_alert: quando HTML não contém nenhum selector reconhecido, capture_logs() captura evento com alert="schema_drift" e products == []
    - test_upsert_no_duplicates: após dois upserts do mesmo external_id com ranks diferentes, DB tem 1 linha com o rank mais recente
  </behavior>
  <action>
    Criar `mis/scanners/kiwify.py` com `KiwifyScanner(PlatformScanner)`:

    ```python
    KIWIFY_PLATFORM_ID = 3  # definir constante — confirmar ID real no DB após migration _001

    SELECTORS_ORDERED = [
        # primary — confirmar após inspecionar fixture HTML
        ".product-card",
        # fallback 1
        "[data-testid='product-item']",
        # fallback 2
        ".marketplace-item",
    ]
    ```

    - `async def scan_niche(self, niche_slug: str, platform_slug: str) -> list[Product]`:
      1. Usa `self.fetch()` herdado do BaseScraper (não criar httpx client direto)
      2. URL: `f"https://www.kiwify.com.br/marketplace?category={platform_slug}"`
      3. Tenta cada selector em SELECTORS_ORDERED via BeautifulSoup4 + lxml
      4. Se nenhum retorna resultados: loga `alert='schema_drift'` com structlog e retorna `[]`
      5. Extrai external_id do slug da URL do produto (ex: `/produto/nome-123` → `nome-123`)
      6. Extrai title, url (absoluta), price (float ou None), rank (posição na lista, int)
      7. Retorna lista de Product dataclasses com platform_id=KIWIFY_PLATFORM_ID

    **Sobre o KIWIFY_PLATFORM_ID:** Verificar no DB `mis.db` qual ID corresponde à plataforma Kiwify:
    `SELECT id FROM platforms WHERE name LIKE '%Kiwify%'`. Se Kiwify não existir na tabela platforms, inserir via migration ou diretamente (mas documentar a decisão).

    Garantir que NÃO lança exceção — erros são logados e retorna `[]`. Usar `async with self` nos testes ou dentro de `run_all_scanners`.

    **Após implementar:** rodar os 5 testes — todos devem passar (GREEN).
  </action>
  <verify>
    <automated>cd /c/Users/Gabriel/MEGABRAIN/mis && python -m pytest tests/test_kiwify_scanner.py -x -q</automated>
  </verify>
  <done>
    - mis/scanners/kiwify.py existe com KiwifyScanner(PlatformScanner)
    - Todos os 5 testes em test_kiwify_scanner.py passam GREEN
    - Suite completa não regride: `python -m pytest tests/ -q` sem novos ERRORs nos testes de Phase 1
  </done>
</task>

</tasks>

<verification>
Suite completa após o plano:

```bash
cd /c/Users/Gabriel/MEGABRAIN/mis && python -m pytest tests/ -q
```

Deve passar todos os testes existentes (Phase 1: 15 testes) + os 5 novos testes de Kiwify.

Verificar migration _002 é idempotente:
```bash
cd /c/Users/Gabriel/MEGABRAIN/mis && python -c "from migrations._002_product_enrichment import run_migration_002; import tempfile, os; d=tempfile.mkdtemp(); path=os.path.join(d,'test.db'); from db import get_db, run_migrations; run_migrations(path); run_migration_002(path); run_migration_002(path); print('OK — idempotent')"
```
</verification>

<success_criteria>
- [ ] Product dataclass exportado de mis/scanner.py com todos os campos (6 obrigatórios + 4 opcionais)
- [ ] PlatformScanner ABC com método abstrato scan_niche() exportado
- [ ] run_all_scanners() com asyncio.gather(return_exceptions=True) exportado
- [ ] upsert_product() usa pk=("platform_id", "external_id"), alter=True
- [ ] save_batch() faz guard de lista vazia antes de iterar
- [ ] migration _002 é idempotente (pode rodar 2x sem erro)
- [ ] config.yaml tem bloco platforms por nicho e 3 novos settings
- [ ] load_config() valida plataformas e lança ValueError em typos
- [ ] KiwifyScanner.scan_niche() retorna >= 10 produtos com campos obrigatórios
- [ ] drift alert usa structlog com alert='schema_drift' (não caplog)
- [ ] Todos 5 testes GREEN em test_kiwify_scanner.py
- [ ] Suite Phase 1 permanece GREEN (sem regressão)
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/02-platform-scanners/02-01-SUMMARY.md` com:
- Artefatos criados e o que cada um exporta
- Decisão sobre KIWIFY_PLATFORM_ID (valor encontrado no DB)
- Estrutura exata dos seletores CSS que funcionaram nas fixtures
- Qual campo do HTML foi usado como external_id do Kiwify
- URL exata do marketplace Kiwify por categoria (confirmada ao vivo)
- Quaisquer ajustes em config.yaml (slugs de plataforma reais por nicho)
</output>
