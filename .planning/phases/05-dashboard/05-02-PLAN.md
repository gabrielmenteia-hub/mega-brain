---
phase: 05-dashboard
plan: "02"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - mis/migrations/_005_alerts.py
  - mis/db.py
  - mis/alert_repository.py
  - mis/dossier_repository.py
  - mis/pain_repository.py
autonomous: true
requirements:
  - DASH-01
  - DASH-04

must_haves:
  truths:
    - "Migration _005 cria tabela alerts de forma idempotente"
    - "db.py encadeia _001.._005 sem erro ao chamar run_migrations()"
    - "alert_repository cria, conta, marca e expira alertas com lógica de idempotência (24h)"
    - "dossier_repository lista produtos com dossiê ordenados por rank, com JOIN em platforms/niches"
    - "pain_repository retorna relatório mais recente por nicho e histórico sem parsear report_json"
  artifacts:
    - path: "mis/migrations/_005_alerts.py"
      provides: "tabela alerts com índices"
      contains: "run_migration_005"
    - path: "mis/db.py"
      provides: "chain de migrations _001..._005"
      contains: "_run_005"
    - path: "mis/alert_repository.py"
      provides: "CRUD de alertas"
      exports: ["create_alert", "get_unseen_count", "mark_seen", "expire_old_alerts"]
    - path: "mis/dossier_repository.py"
      provides: "consultas à tabela dossiers"
      exports: ["get_dossier_by_product_id", "list_dossiers_by_rank"]
    - path: "mis/pain_repository.py"
      provides: "consultas à tabela pain_reports"
      exports: ["get_latest_report", "get_historical_reports"]
  key_links:
    - from: "mis/db.py"
      to: "mis/migrations/_005_alerts.py"
      via: "_run_005(db_path) adicionado ao final de run_migrations()"
      pattern: "_run_005"
    - from: "mis/alert_repository.py"
      to: "mis/db.py"
      via: "get_db(db_path) para obter conexão"
      pattern: "from mis.db import get_db"
---

<objective>
Criar a camada de dados do dashboard: migration _005 (tabela alerts), 3 novos repositórios (alert_repository, dossier_repository, pain_repository) e atualização do db.py para encadear a nova migration.

Purpose: Infra de dados que desbloqueia tanto o web layer (05-03) quanto o trigger de alertas (05-05). Foco único: dados — sem rotas, templates ou CLI.
Output: 5 arquivos de dados, 8 testes de repositório GREEN.
</objective>

<execution_context>
@C:/Users/Gabriel/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Gabriel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-dashboard/05-CONTEXT.md
@.planning/phases/05-dashboard/05-RESEARCH.md
@mis/db.py
@mis/product_repository.py
@mis/migrations/_004_pain_radar.py
@mis/tests/conftest.py

<interfaces>
<!-- Padrões estabelecidos no projeto que DEVEM ser seguidos -->

product_repository.py (padrão de repositório):
```python
def save_batch(db: sqlite_utils.Database, products: list["Product"]) -> None:
    # recebe db instance, não db_path
```

get_db() aceita db_path:
```python
def get_db(db_path: str) -> sqlite_utils.Database:
    db = sqlite_utils.Database(db_path)
    db.execute("PRAGMA journal_mode=WAL")
    db.execute("PRAGMA foreign_keys=ON")
    return db
```

Novos repositórios WEB usam db_path (não db instance):
```python
def get_dossier_by_product_id(db_path: str, product_id: int) -> dict | None:
    db = get_db(db_path)
    ...
```

Tabelas disponíveis após _001.._004:
- products (id, platform_id, external_id, title, url, niche_id, rank, price, commission_pct, rating, thumbnail_url, updated_at)
- platforms (id, name, slug)
- niches (id, name, slug)
- dossiers (id, product_id, dossier_json, ads_json, copy_json, reviews_json, confidence_score, opportunity_score, gate_passed, created_at, updated_at)
- pain_signals (id, url_hash, url, title, source, niche_slug, score, extra_json, collected_at)
- pain_reports (id, niche_id, cycle_at, report_json, created_at)

Tabela a criar em _005:
- alerts (id PK, product_id INT NOT NULL, platform_slug TEXT, niche_slug TEXT, position INT NOT NULL, seen INT DEFAULT 0, created_at TEXT NOT NULL, expires_at TEXT NOT NULL)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration _005 (alerts) + db.py atualizado</name>
  <files>
    mis/migrations/_005_alerts.py
    mis/db.py
  </files>
  <behavior>
    - run_migration_005 cria tabela alerts com IF NOT EXISTS (idempotente)
    - run_migrations() chamado duas vezes no mesmo banco não levanta exceção
    - Após run_migrations(), db.execute("SELECT name FROM sqlite_master WHERE name='alerts'") retorna 1 row
  </behavior>
  <action>
    1. Criar mis/migrations/_005_alerts.py seguindo o padrão de _004_pain_radar.py:
       - Função run_migration_005(db_path: str) -> None
       - Usar sqlite_utils.Database diretamente (não get_db) — padrão das migrations
       - Guard IF NOT EXISTS para tabela alerts
       - Schema: id (int PK autoincrement), product_id (int NOT NULL), platform_slug (text), niche_slug (text), position (int NOT NULL), seen (int DEFAULT 0), created_at (text NOT NULL), expires_at (text NOT NULL)
       - Índices: idx_alerts_created ON alerts(created_at), idx_alerts_seen ON alerts(seen, expires_at)
       - Índices com IF NOT EXISTS (usar raw SQL via db.execute para controle total)

    2. Atualizar mis/db.py:
       - Importar: `from .migrations._005_alerts import run_migration_005 as _run_005`
       - Adicionar `_run_005(db_path)` ao final de run_migrations()
  </action>
  <verify>
    <automated>cd /c/Users/Gabriel/MEGABRAIN/mis && python -c "from mis.db import run_migrations; import tempfile, os; p=os.path.join(tempfile.mkdtemp(),'t.db'); run_migrations(p); run_migrations(p); print('OK idempotente')"</automated>
  </verify>
  <done>Migration _005 criada e encadeada. run_migrations() idempotente. Tabela alerts existente no banco após chamada.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 3 repositórios web (alert_repository, dossier_repository, pain_repository)</name>
  <files>
    mis/alert_repository.py
    mis/dossier_repository.py
    mis/pain_repository.py
  </files>
  <behavior>
    - alert_repository.create_alert: inserir alerta com expires_at = created_at + 7 dias → row existe em alerts com seen=0
    - alert_repository.create_alert segunda chamada para mesmo product_id+position nas últimas 24h → retorna id existente (idempotência)
    - alert_repository.get_unseen_count: 2 alertas (1 seen=0, 1 seen=1) → retorna 1
    - alert_repository.mark_seen: criar alerta, mark_seen(id, db_path) → seen=1
    - alert_repository.expire_old_alerts: alerta com expires_at no passado → removido após expire_old_alerts()
    - dossier_repository.get_dossier_by_product_id: produto sem dossiê → None; com dossiê → dict com campo "dossier" parseado de dossier_json
    - dossier_repository.list_dossiers_by_rank: 3 produtos com ranks 3,1,2 → lista em ordem [1,2,3]
    - pain_repository.get_latest_report: banco vazio → None; com 2 reports para mesmo nicho → retorna o mais recente
    - pain_repository.get_historical_reports: 3 reports para nicho X → lista de 3 dicts com cycle_at (sem report_json)
  </behavior>
  <action>
    1. Criar mis/alert_repository.py:
       - Imports: sqlite_utils, mis.db.get_db, datetime, timezone, timedelta, structlog
       - create_alert(db_path: str, product_id: int, platform_slug: str, niche_slug: str, position: int) -> int
         Calcula expires_at = now + 7 days (ISO format UTC).
         Idempotência: verificar se já existe alerta nas últimas 24h para mesmo product_id + position — se sim, retornar id existente.
         Insere em alerts. Retorna id inserido.
       - get_unseen_count(db_path: str) -> int
         SELECT COUNT(*) FROM alerts WHERE seen=0 AND expires_at > now (ISO UTC string)
       - mark_seen(db_path: str, alert_id: int) -> bool
         UPDATE alerts SET seen=1 WHERE id=alert_id. Retorna True se rowcount>0.
       - expire_old_alerts(db_path: str) -> int
         DELETE FROM alerts WHERE expires_at < now (ISO UTC string). Retorna contagem deletada.

    2. Criar mis/dossier_repository.py:
       - get_dossier_by_product_id(db_path: str, product_id: int) -> dict | None
         Query em dossiers WHERE product_id=?. Se não encontrado → None.
         Se encontrado: json.loads(row["dossier_json"]) → row["dossier"].
         Também parsear ads_json, copy_json, reviews_json se presentes e não-null.
       - list_dossiers_by_rank(db_path: str, platform: str | None = None, niche: str | None = None, order_by: str = "rank", order_dir: str = "asc", per_page: int = 20, page: int = 1) -> list[dict]
         LEFT JOIN products p + dossiers d ON d.product_id = p.id.
         JOIN platforms pl ON pl.id = p.platform_id.
         JOIN niches n ON n.id = p.niche_id.
         Filtros opcionais: WHERE pl.slug = ? AND n.slug = ?.
         ORDER BY p.rank ASC/DESC conforme order_by/order_dir.
         LIMIT per_page OFFSET (page-1)*per_page.
         Retorna lista de dicts: id, title, url, platform_name, platform_slug, niche_name, niche_slug, rank, opportunity_score, confidence_score, has_dossier (bool: d.id IS NOT NULL).

    3. Criar mis/pain_repository.py:
       - get_latest_report(db_path: str, niche_slug: str) -> dict | None
         SELECT pr.* FROM pain_reports pr JOIN niches n ON n.id = pr.niche_id WHERE n.slug = ? ORDER BY pr.cycle_at DESC LIMIT 1.
         Se nenhum → None.
         Se encontrado: json.loads(report_json) → dict["report"].
         Adicionar: signal_count = SELECT COUNT(*) FROM pain_signals WHERE niche_slug=? AND collected_at >= cycle_at.
       - get_historical_reports(db_path: str, niche_slug: str, limit: int = 48) -> list[dict]
         SELECT pr.id, pr.cycle_at FROM pain_reports pr JOIN niches n ON n.id = pr.niche_id WHERE n.slug = ? ORDER BY pr.cycle_at DESC LIMIT limit.
         NÃO parsear report_json (usado apenas para selector de histórico).
         Retornar lista de dicts: {id, cycle_at}.
  </action>
  <verify>
    <automated>cd /c/Users/Gabriel/MEGABRAIN/mis && python -m pytest tests/web/test_alert_repository.py tests/web/test_dossier_repository.py tests/web/test_pain_repository.py -x -q</automated>
  </verify>
  <done>Todos os 8 testes de repositório GREEN. Os 3 repositórios exportam as funções conforme contrato definido nos testes de 05-01.</done>
</task>

</tasks>

<verification>
Migration idempotente: `python -c "from mis.db import run_migrations; ..."` sem exceção na segunda chamada.

Testes de repositório GREEN: `cd mis && python -m pytest tests/web/test_alert_repository.py tests/web/test_dossier_repository.py tests/web/test_pain_repository.py -x -q`

Tabelas: `python -c "from mis.db import run_migrations, get_db; ..."` mostra 'alerts' em sqlite_master.
</verification>

<success_criteria>
8 testes de repositório GREEN. run_migrations() encadeia _001.._005. Os 3 repositórios importáveis sem erro. alert_repository é idempotente para criação duplicada nas 24h.
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/05-dashboard/05-02-SUMMARY.md`
</output>
