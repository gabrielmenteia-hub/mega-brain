---
phase: 20-niche-data-model
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - mis/tests/test_migration_008.py
  - mis/tests/test_niche_repository.py
autonomous: true
requirements:
  - NICHE-01
  - NICHE-02
must_haves:
  truths:
    - "test_migration_008.py existe com testes RED cobrindo niches_v3, subniches e subniche_platform_slugs"
    - "test_niche_repository.py existe com testes RED cobrindo list_niches, list_subniches e get_platform_slug"
    - "pytest mis/tests/test_migration_008.py mis/tests/test_niche_repository.py roda sem ImportError (testes falham por ausência de implementação, não por syntax error)"
  artifacts:
    - path: "mis/tests/test_migration_008.py"
      provides: "Testes de migration: tabelas criadas, 4 nichos, >=40 subnichos, slug lookup, idempotência, dados v1/v2 preservados"
      exports: ["test_migration_008_creates_tables", "test_4_niches_v3_inserted", "test_subniche_count_at_least_40", "test_platform_slug_lookup", "test_migration_idempotent", "test_existing_products_preserved"]
    - path: "mis/tests/test_niche_repository.py"
      provides: "Testes de repository: list_niches, list_subniches(saude), get_platform_slug(hotmart)"
      exports: ["test_list_niches_returns_4", "test_list_subniches_saude", "test_get_platform_slug_hotmart", "test_get_platform_slug_unknown_platform_returns_none"]
  key_links:
    - from: "mis/tests/test_migration_008.py"
      to: "mis/migrations/_008_niche_v3.py"
      via: "from mis.migrations._008_niche_v3 import run_migration_008"
      pattern: "run_migration_008"
    - from: "mis/tests/test_niche_repository.py"
      to: "mis/niche_repository.py"
      via: "from mis.niche_repository import list_niches, list_subniches, get_platform_slug"
      pattern: "from mis.niche_repository import"
---

<objective>
Criar os testes RED para a migration _008 e o niche_repository antes da implementação existir. Os testes definem o contrato que o Plan 02 vai implementar.

Purpose: TDD garante que o seed data e as queries foram especificados com precisão antes de qualquer código de produção ser escrito. Erros de contrato são descobertos aqui, não no Plan 02.
Output: dois arquivos de teste com todos os casos RED (falham com ImportError ou ModuleNotFoundError) prontos para virar GREEN no Plan 02.
</objective>

<execution_context>
@C:/Users/Gabriel/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Gabriel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-niche-data-model/20-RESEARCH.md
@.planning/phases/20-niche-data-model/20-VALIDATION.md

<interfaces>
<!-- Padrão de fixture existente em mis/tests/conftest.py -->
```python
@pytest.fixture
def db_path(tmp_path):
    """Return path to a temporary SQLite database file."""
    return str(tmp_path / "mis.db")
```

<!-- Padrão de migration test existente (_006) -->
```python
from mis.migrations._006_v2_platforms import run_migration_006
from mis.db import run_migrations
import sqlite_utils

def test_platforms_inserted(db_path):
    run_migrations(db_path)
    db = sqlite_utils.Database(db_path)
    count = next(db.execute("SELECT COUNT(*) FROM platforms"))[0]
    assert count == 12
```

<!-- Platform IDs fixos (mis/platform_ids.py) — usados nos asserts de slug -->
```python
HOTMART_PLATFORM_ID = 1
CLICKBANK_PLATFORM_ID = 2
KIWIFY_PLATFORM_ID = 3
BRAIP_PLATFORM_ID = 7
UDEMY_PLATFORM_ID = 9
JVZOO_PLATFORM_ID = 10
GUMROAD_PLATFORM_ID = 11
APPSUMO_PLATFORM_ID = 12
```

<!-- Contratos alvo (ainda não existem — testes vão falhar com ImportError) -->
```python
# mis/migrations/_008_niche_v3.py  (a criar no Plan 02)
def run_migration_008(db_path: str) -> None: ...

# mis/niche_repository.py  (a criar no Plan 02)
def list_niches(db_path: str) -> list[dict]: ...
def list_subniches(db_path: str, niche_slug: str) -> list[dict]: ...
def get_platform_slug(db_path: str, subniche_id: int, platform_slug: str) -> str | None: ...
```
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: RED — testes da migration _008</name>
  <files>mis/tests/test_migration_008.py</files>
  <behavior>
    - test_migration_008_creates_tables: após run_migrations(db_path), as tabelas niches_v3, subniches e subniche_platform_slugs existem em db.table_names()
    - test_4_niches_v3_inserted: COUNT(*) FROM niches_v3 == 4
    - test_subniche_count_at_least_40: COUNT(*) FROM subniches >= 40
    - test_platform_slug_lookup: query JOIN subniches + platforms WHERE s.slug='emagrecimento' AND pl.slug='clickbank' retorna 'health'
    - test_platform_slug_lookup_hotmart: mesma query para slug='emagrecimento' AND pl.slug='hotmart' retorna 'saude-e-fitness'
    - test_migration_idempotent: chamar run_migration_008(db_path) duas vezes (após run_migrations) não levanta exceção e COUNT permanece igual
    - test_existing_products_preserved: inserir produto com niche_id=1 na tabela products antes de run_migrations, depois verificar que o produto ainda existe com niche_id=1 — tabela niches legada intacta
  </behavior>
  <action>
    Criar mis/tests/test_migration_008.py com imports:
      from mis.migrations._008_niche_v3 import run_migration_008
      from mis.db import run_migrations
      import sqlite_utils

    Usar fixture db_path de conftest.py. Todos os testes devem passar o db_path para run_migrations antes de fazer assertions.

    Para test_existing_products_preserved: usar run_migrations(db_path) para criar schema completo, depois inserir um produto manualmente via db.execute INSERT na tabela products com niche_id=1 (FK para tabela niches legada), depois rodar run_migration_008(db_path) novamente e verificar que o produto ainda existe.

    Os testes vão falhar com ImportError/ModuleNotFoundError porque _008_niche_v3.py ainda não existe. Isso é o estado RED esperado.

    NAO criar o arquivo _008_niche_v3.py aqui — apenas os testes.
  </action>
  <verify>
    <automated>cd /c/Users/Gabriel/MEGABRAIN && python -m pytest mis/tests/test_migration_008.py -x -q 2>&amp;1 | head -20</automated>
  </verify>
  <done>test_migration_008.py existe, roda sem SyntaxError, falha com ImportError (não ModuleNotFoundError genérico de pytest) — confirmando que os imports estão corretos e o contrato está definido</done>
</task>

<task type="tdd">
  <name>Task 2: RED — testes do niche_repository</name>
  <files>mis/tests/test_niche_repository.py</files>
  <behavior>
    - test_list_niches_returns_4: list_niches(db_path) retorna lista com len == 4 após run_migrations
    - test_list_niches_structure: cada item tem chaves 'id', 'name', 'slug'
    - test_list_subniches_saude: list_subniches(db_path, 'saude') retorna lista com len >= 10
    - test_list_subniches_unknown_niche: list_subniches(db_path, 'nicho-inexistente') retorna lista vazia []
    - test_get_platform_slug_hotmart: get_platform_slug(db_path, 201, 'hotmart') retorna 'saude-e-fitness' (subniche_id=201 é Emagrecimento)
    - test_get_platform_slug_clickbank: get_platform_slug(db_path, 201, 'clickbank') retorna 'health'
    - test_get_platform_slug_unknown_platform_returns_none: get_platform_slug(db_path, 201, 'perfectpay') retorna None (perfectpay não tem slug mapeado)
    - test_get_platform_slug_unknown_subniche_returns_none: get_platform_slug(db_path, 9999, 'hotmart') retorna None
  </behavior>
  <action>
    Criar mis/tests/test_niche_repository.py com imports:
      from mis.niche_repository import list_niches, list_subniches, get_platform_slug
      from mis.db import run_migrations

    Usar fixture db_path de conftest.py. Chamar run_migrations(db_path) em cada test (ou usar fixture com autouse).

    Os testes vão falhar com ImportError porque mis/niche_repository.py ainda não existe. Isso é o estado RED esperado.

    NAO criar niche_repository.py aqui — apenas os testes.
  </action>
  <verify>
    <automated>cd /c/Users/Gabriel/MEGABRAIN && python -m pytest mis/tests/test_niche_repository.py -x -q 2>&amp;1 | head -20</automated>
  </verify>
  <done>test_niche_repository.py existe, roda sem SyntaxError, falha com ImportError — confirmando que o contrato do repository está definido antes da implementação</done>
</task>

</tasks>

<verification>
Ambos os arquivos de teste existem e rodam com pytest sem SyntaxError.
Os testes falham com ImportError (estado RED correto).
Nenhum arquivo de implementação foi criado neste plano.
</verification>

<success_criteria>
- mis/tests/test_migration_008.py existe com 7 testes cobrindo NICHE-01 e NICHE-02
- mis/tests/test_niche_repository.py existe com 8 testes cobrindo as 3 funções do repository
- python -m pytest mis/tests/test_migration_008.py mis/tests/test_niche_repository.py --co -q lista todos os testes sem erro de coleta
- Todos os testes estão RED (falham por ImportError, não por syntax)
</success_criteria>

<output>
Após conclusão, criar .planning/phases/20-niche-data-model/20-01-SUMMARY.md
</output>
