---
phase: 19-code-quality-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mis/scanner.py
  - mis/web/templates/unified_table.html
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "scanner.py tem guard `if platform_slug is None: continue` logo após o bloco `scanner_cls is None` no dispatch loop"
    - "unified_table.html exibe platform_name dentro de `<span class=\"platform-badge ...\">` em vez de texto puro"
    - "REQUIREMENTS.md linha INFRA-03 diz `tabela \`platforms\`` (não `tabela \`products\``)"
    - "scanner.py module docstring descreve o Fallback Scanner Pattern com referência a `marketplace_unavailable`"
  artifacts:
    - path: "mis/scanner.py"
      provides: "Guard clause + module docstring atualizado"
      contains: "platform_slug is None"
    - path: "mis/web/templates/unified_table.html"
      provides: "Platform badge estilizado"
      contains: "platform-badge"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Correção documental INFRA-03"
      contains: "tabela `platforms`"
  key_links:
    - from: "mis/scanner.py dispatch loop"
      to: "guard platform_slug is None"
      via: "posição: após scanner_cls is None, antes de key = f\"{niche_slug}.{platform_name}\""
      pattern: "platform_slug is None"
    - from: "mis/web/templates/unified_table.html td Plataforma"
      to: "span.platform-badge"
      via: "substituição de texto puro por elemento HTML"
      pattern: "platform-badge"
---

<objective>
Fechar os 4 itens de qualidade de código acumulados no audit v2.0: (1) null slug guard em scanner.py, (2) platform badges estilizados em unified_table.html, (3) correção documental INFRA-03 em REQUIREMENTS.md, e (4) documentação do Fallback Scanner Pattern no module docstring de scanner.py.

Purpose: Liquidar tech debt cosmético/defensivo antes do sign-off v2.0. Nenhuma mudança comportamental — só qualidade, documentação e robustez defensiva.
Output: scanner.py com guard clause + docstring expandida, unified_table.html com badges HTML, REQUIREMENTS.md corrigido.
</objective>

<execution_context>
@C:/Users/Gabriel/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Gabriel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-code-quality-cleanup/19-RESEARCH.md
</context>

<interfaces>
<!-- Interfaces extraídas diretamente dos arquivos-alvo para o executor. Não é necessário explorar o codebase. -->

De mis/scanner.py (linhas 1-7 — module docstring atual):
```python
"""Platform scanner abstractions for MIS.

Provides:
- Product dataclass with all required and optional fields
- PlatformScanner ABC extending BaseScraper with abstract scan_niche()
- run_all_scanners() coroutine that runs all configured scanners in parallel
"""
```

De mis/scanner.py (linhas 260-273 — dispatch loop atual):
```python
for platform_name, platform_slug in platforms.items():
    scanner_cls = SCANNER_MAP.get(platform_name)
    if scanner_cls is None:
        log.warning(
            "scanner.platform.not_implemented",
            platform=platform_name,
            niche=niche_slug,
        )
        continue
    key = f"{niche_slug}.{platform_name}"
    keys.append(key)
    coroutines.append(
        _run_one(scanner_cls, niche_slug, platform_slug, key)
    )
```

De mis/web/templates/unified_table.html (linhas 23-25 — celula Plataforma atual):
```html
<td class="px-4 py-3 text-gray-300">
  {{ product.platform_name }}{% if product.is_stale %} <span title="Dados desatualizados">&#9888;&#65039;</span>{% endif %}
</td>
```

De .planning/REQUIREMENTS.md (linha 13 — INFRA-03 atual):
```
- [x] **INFRA-03**: Campo `rank_type` adicionado à tabela `products` para identificar a semântica do rank por plataforma (posição, gravity, EPC, upvotes, enrollment, etc.)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Null slug guard + Fallback Scanner Pattern docstring em scanner.py</name>
  <files>mis/scanner.py</files>
  <action>
Fazer dois edits atômicos em mis/scanner.py:

**Edit A — Module docstring (linhas 1-7):**
Substituir o docstring atual pela versão expandida com seção "Fallback Scanner Pattern":

```python
"""Platform scanner abstractions for MIS.

Provides:
- Product dataclass with all required and optional fields
- PlatformScanner ABC extending BaseScraper with abstract scan_niche()
- run_all_scanners() coroutine that runs all configured scanners in parallel

Fallback Scanner Pattern (BR Platforms):
    Eduzz, Monetizze, and PerfectPay require authentication — their public
    marketplaces are not accessible. These scanners implement the fallback
    pattern: scan_niche() always returns [] and emits a structured warning
    log with alert='marketplace_unavailable'. run_all_scanners() detects the
    empty return and calls mark_stale() to flag existing DB records.

    When a marketplace becomes publicly accessible:
        1. Re-implement scan_niche() in the scanner class.
        2. Remove the marketplace_unavailable log.warning() call.
        3. Add fixtures and tests following the Phase 14 pattern.
"""
```

**Edit B — Guard clause no dispatch loop (após linha 268, antes de `key = f"..."`)**
Inserir imediatamente após o bloco `if scanner_cls is None: ... continue` e ANTES de `key = f"{niche_slug}.{platform_name}"`:

```python
            if platform_slug is None:
                # Null slug: platform opted in to config.yaml but no category mapping
                # provided. Passing None to scan_niche() is not supported — skip.
                continue
```

A ordem correta do loop após os dois edits:
```python
for platform_name, platform_slug in platforms.items():
    scanner_cls = SCANNER_MAP.get(platform_name)
    if scanner_cls is None:
        log.warning(
            "scanner.platform.not_implemented",
            platform=platform_name,
            niche=niche_slug,
        )
        continue
    if platform_slug is None:
        # Null slug: platform opted in to config.yaml but no category mapping
        # provided. Passing None to scan_niche() is not supported — skip.
        continue
    key = f"{niche_slug}.{platform_name}"
    keys.append(key)
    coroutines.append(
        _run_one(scanner_cls, niche_slug, platform_slug, key)
    )
```

Não alterar nenhuma outra linha do arquivo.
  </action>
  <verify>
    <automated>grep -n "platform_slug is None" /c/Users/Gabriel/MEGABRAIN/mis/scanner.py && grep -n "marketplace_unavailable" /c/Users/Gabriel/MEGABRAIN/mis/scanner.py</automated>
  </verify>
  <done>
    - `grep -n "platform_slug is None" mis/scanner.py` retorna exatamente 1 match, posicionado entre o bloco `scanner_cls is None` e a linha `key = f"..."`
    - `grep -n "marketplace_unavailable" mis/scanner.py` retorna 1 match dentro do module docstring
    - `pytest mis/tests/test_scanner_niche_id.py mis/tests/test_scanner_alerts.py -q` passa sem novos erros
  </done>
</task>

<task type="auto">
  <name>Task 2: Platform badge estilizado em unified_table.html + correção INFRA-03 em REQUIREMENTS.md</name>
  <files>mis/web/templates/unified_table.html, .planning/REQUIREMENTS.md</files>
  <action>
**Edit A — unified_table.html (célula Plataforma, linhas 23-25):**

Substituir a célula atual:
```html
<td class="px-4 py-3 text-gray-300">
  {{ product.platform_name }}{% if product.is_stale %} <span title="Dados desatualizados">&#9888;&#65039;</span>{% endif %}
</td>
```

Pela versão com badge estilizado:
```html
<td class="px-4 py-3">
  <span class="platform-badge bg-gray-700 text-gray-200 text-xs px-2 py-0.5 rounded">{{ product.platform_name }}</span>{% if product.is_stale %} <span title="Dados desatualizados">&#9888;&#65039;</span>{% endif %}
</td>
```

Nota: remover `text-gray-300` da `<td>` (a cor agora está no `<span>`). Não alterar nenhuma outra linha do template.

**Edit B — .planning/REQUIREMENTS.md (linha 13 — INFRA-03):**

Verificar antes de editar: `mis/migrations/_006_v2_platforms.py` para confirmar que `rank_type` foi adicionado à tabela `platforms` (não `products`). O ROADMAP.md já confirma: "Cada plataforma no DB tem campo `rank_type` preenchido" — é a tabela `platforms`.

Substituir na linha 13:
```
- [x] **INFRA-03**: Campo `rank_type` adicionado à tabela `products` para identificar a semântica do rank por plataforma (posição, gravity, EPC, upvotes, enrollment, etc.)
```

Por:
```
- [x] **INFRA-03**: Campo `rank_type` adicionado à tabela `platforms` para identificar a semântica do rank por plataforma (posição, gravity, EPC, upvotes, enrollment, etc.)
```

Não alterar nenhuma outra linha do arquivo.
  </action>
  <verify>
    <automated>grep -n "platform-badge" /c/Users/Gabriel/MEGABRAIN/mis/web/templates/unified_table.html && grep "INFRA-03" /c/Users/Gabriel/MEGABRAIN/.planning/REQUIREMENTS.md</automated>
  </verify>
  <done>
    - `grep -n "platform-badge" mis/web/templates/unified_table.html` retorna 1 match na célula Plataforma
    - `grep "INFRA-03" .planning/REQUIREMENTS.md` mostra "tabela `platforms`" (não "tabela `products`")
    - O restante do template está intacto (as outras células não foram alteradas)
  </done>
</task>

</tasks>

<verification>
Após ambas as tasks:

```bash
# Item 1: null slug guard
grep -n "platform_slug is None" mis/scanner.py

# Item 2: fallback pattern docstring
grep -n "marketplace_unavailable" mis/scanner.py

# Item 3: platform badge
grep -n "platform-badge" mis/web/templates/unified_table.html

# Item 4: REQUIREMENTS.md correction
grep "INFRA-03" .planning/REQUIREMENTS.md

# Regressão: testes de scanner existentes devem permanecer GREEN
pytest mis/tests/test_scanner_niche_id.py mis/tests/test_scanner_alerts.py -q
```

Todos os 5 comandos devem retornar resultados positivos (matches) ou zero falhas (pytest).
</verification>

<success_criteria>
1. `grep -n "platform_slug is None" mis/scanner.py` retorna match após o bloco `scanner_cls is None`, antes de `key = f"..."`
2. `grep -n "marketplace_unavailable" mis/scanner.py` retorna match no module docstring
3. `grep -n "platform-badge" mis/web/templates/unified_table.html` retorna match na célula Plataforma
4. `grep "INFRA-03" .planning/REQUIREMENTS.md` mostra "tabela \`platforms\`"
5. `pytest mis/tests/test_scanner_niche_id.py mis/tests/test_scanner_alerts.py -q` — zero falhas novas
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/19-code-quality-cleanup/19-01-SUMMARY.md` com:
- Frontmatter: phase, plan, status, files_modified, requirements
- Seção "What Was Done": bullet points por item fechado
- Seção "Decisions": confirmar tabela correta do INFRA-03 após inspeção do migration
- Seção "Verification": output dos 5 grep/pytest checks
</output>
