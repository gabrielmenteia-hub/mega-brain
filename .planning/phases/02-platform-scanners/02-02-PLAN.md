---
phase: 02-platform-scanners
plan: 02
type: tdd
wave: 2
depends_on:
  - 02-01
files_modified:
  - mis/scanners/clickbank.py
  - mis/tests/fixtures/clickbank/marketplace_health.html
  - mis/tests/fixtures/clickbank/marketplace_mktg.html
  - mis/tests/test_clickbank_scanner.py
autonomous: true
requirements:
  - SCAN-03

must_haves:
  truths:
    - "ClickBankScanner.scan_niche() retorna produtos com gravity score (como rank int ou float) por nicho configurado"
    - "Campos obrigatórios presentes em todos os produtos: external_id, title, url, platform_id, niche_id, rank"
    - "Quando todos os seletores falham, retorna lista vazia e emite structlog alert='schema_drift'"
    - "Upsert no DB real: re-run do mesmo external_id atualiza rank sem duplicar linha"
    - "ClickBankScanner usa httpx (SSR marketplace) — sem Playwright"
  artifacts:
    - path: "mis/scanners/clickbank.py"
      provides: "ClickBankScanner(PlatformScanner) com scan_niche(), fallback selectors, drift alert"
      exports: ["ClickBankScanner"]
    - path: "mis/tests/fixtures/clickbank/marketplace_health.html"
      provides: "Fixture HTML real do marketplace ClickBank categoria health"
    - path: "mis/tests/fixtures/clickbank/marketplace_mktg.html"
      provides: "Fixture HTML real do marketplace ClickBank categoria marketing"
    - path: "mis/tests/test_clickbank_scanner.py"
      provides: "5 testes cobrindo SCAN-03 (TDD)"
  key_links:
    - from: "mis/scanners/clickbank.py"
      to: "mis/scanner.py"
      via: "ClickBankScanner(PlatformScanner) — herança via mis/scanner.py"
      pattern: "class ClickBankScanner\\(PlatformScanner\\)"
    - from: "mis/scanners/clickbank.py"
      to: "structlog"
      via: "alert='schema_drift' quando todos selectors falham"
      pattern: "alert.*schema_drift"
---

<objective>
Implementar o scraper ClickBank via scraping do marketplace web (SSR/Next.js), seguindo TDD estrito.

Purpose: ClickBank não tem API pública de marketplace por categoria com gravity score. O marketplace `https://www.clickbank.com/view-marketplace/` é Next.js SSR — dados iniciais estão no HTML. httpx suficiente (mesmo padrão do Kiwify, validado no Plan 02-01).

Output:
- mis/scanners/clickbank.py — ClickBankScanner com fallback selectors e drift alert
- Fixtures HTML reais gravadas ao vivo
- 5 testes GREEN em test_clickbank_scanner.py
</objective>

<execution_context>
@C:/Users/Gabriel/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Gabriel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-platform-scanners/02-CONTEXT.md
@.planning/phases/02-platform-scanners/02-RESEARCH.md
@.planning/phases/02-platform-scanners/02-VALIDATION.md
@.planning/phases/02-platform-scanners/02-01-SUMMARY.md

<interfaces>
<!-- Contratos criados em 02-01 que este plano usa. -->

De mis/scanner.py (criado em 02-01):
```python
@dataclass
class Product:
    external_id: str
    title: str
    url: str
    platform_id: int
    niche_id: int
    rank: int
    price: Optional[float] = None
    commission_pct: Optional[float] = None
    rating: Optional[float] = None
    thumbnail_url: Optional[str] = None

class PlatformScanner(BaseScraper, ABC):
    @abstractmethod
    async def scan_niche(self, niche_slug: str, platform_slug: str) -> list[Product]: ...
```

De mis/product_repository.py (criado em 02-01):
```python
def upsert_product(db: sqlite_utils.Database, product: Product) -> None: ...
def save_batch(db, products: list[Product]) -> None: ...  # guard vazio incluido
```

De mis/migrations/_002_product_enrichment.py (criado em 02-01):
```python
def run_migration_002(db_path: str) -> None: ...  # idempotente
```

De mis/base_scraper.py:
```python
class BaseScraper:
    async def fetch(self, url: str) -> str: ...  # httpx + tenacity retry
```

Padrão de teste para drift alert (OBRIGATÓRIO — não usar caplog):
```python
from structlog.testing import capture_logs

def test_drift_alert():
    with capture_logs() as cap:
        products = ...  # chamar scraper com HTML sem selectors
    assert products == []
    assert any(e.get("alert") == "schema_drift" for e in cap)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — gravar fixtures ClickBank ao vivo e criar testes RED</name>
  <files>
    mis/tests/fixtures/clickbank/marketplace_health.html,
    mis/tests/fixtures/clickbank/marketplace_mktg.html,
    mis/tests/test_clickbank_scanner.py
  </files>
  <action>
    **Passo 1 — Investigar estrutura do marketplace ClickBank:**
    Executar GET com httpx no marketplace ClickBank e inspecionar o HTML retornado:
    ```python
    import httpx, asyncio

    async def inspect():
        headers = {"User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36"}
        async with httpx.AsyncClient(headers=headers, follow_redirects=True) as client:
            r = await client.get("https://www.clickbank.com/view-marketplace/")
            print(r.status_code, len(r.text))
            # Salvar e inspecionar HTML
            with open("cb_test.html", "w") as f:
                f.write(r.text)

    asyncio.run(inspect())
    ```

    Verificar:
    - Se gravity score aparece no HTML SSR sem autenticação
    - Formato do URL para filtrar por categoria (ex: `?cat=health`, `/health/`, ou query param diferente)
    - Quais elementos HTML contêm os dados de produto (nome, URL, gravity/rank)

    Se gravity NÃO aparecer sem autenticação: usar rank posicional (posição na lista de "top products" por categoria) como `rank` — documentar no SUMMARY.

    **Passo 2 — Gravar fixtures ao vivo:**
    Após confirmar URLs de categoria:
    - GET na URL de categoria "health" → gravar em `mis/tests/fixtures/clickbank/marketplace_health.html`
    - GET na URL de categoria "marketing" → gravar em `mis/tests/fixtures/clickbank/marketplace_mktg.html`
    Gravar o HTML exato, sem modificação.

    **Passo 3 — Definir seletores HTML (baseado nas fixtures gravadas):**
    Inspecionar o HTML das fixtures e identificar:
    - Primary selector para o elemento de cada produto
    - Fallback selector alternativo (segunda estrutura possível)
    - Campo que será o `external_id` (slug da URL ou ID de atributo data-)
    - Campo gravity score (se disponível) ou posição na lista

    **Passo 4 — Criar mis/tests/test_clickbank_scanner.py (5 testes RED):**
    Estrutura dos testes — todos usando `respx.mock` e `structlog.testing.capture_logs()`:

    ```python
    import pytest
    import respx
    from pathlib import Path
    from httpx import Response
    from structlog.testing import capture_logs

    FIXTURE_HEALTH = Path(__file__).parent / "fixtures/clickbank/marketplace_health.html"
    CB_URL = "https://www.clickbank.com/view-marketplace/?cat=health"  # ajustar URL real

    async def test_happy_path(tmp_path):
        # respx.mock retorna fixture HTML, verifica >= 5 produtos com campos obrigatórios
        ...

    async def test_field_types(tmp_path):
        # rank é int, external_id não-None, price é float ou None
        ...

    async def test_fallback_selector(tmp_path):
        # primary selector ausente, fallback retorna produtos
        ...

    async def test_drift_alert(tmp_path):
        # HTML sem nenhum selector → capture_logs captura alert='schema_drift'
        with capture_logs() as cap:
            ...
        assert any(e.get("alert") == "schema_drift" for e in cap)

    async def test_upsert_no_duplicates(tmp_path):
        # dois upserts do mesmo external_id com rank diferente → 1 linha, rank atualizado
        ...
    ```

    Executar testes — devem falhar em RED (ClickBankScanner não existe ainda).
  </action>
  <verify>
    <automated>cd /c/Users/Gabriel/MEGABRAIN/mis && python -m pytest tests/test_clickbank_scanner.py -x -q 2>&1 | grep -E "ERROR|FAILED|passed|error" | tail -5</automated>
  </verify>
  <done>
    - mis/tests/fixtures/clickbank/marketplace_health.html existe e não está vazio (HTML real do marketplace)
    - mis/tests/fixtures/clickbank/marketplace_mktg.html existe e não está vazio
    - mis/tests/test_clickbank_scanner.py tem 5 testes em RED (ImportError ou fixture-based failure esperado)
    - Estrutura HTML documentada (quais seletores usar, campo external_id, gravity disponível ou não)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar ClickBankScanner — RED → GREEN</name>
  <files>
    mis/scanners/clickbank.py
  </files>
  <behavior>
    - test_happy_path: scan_niche("marketing-digital", "marketing") retorna lista >= 5 produtos com todos os campos obrigatórios (external_id, title, url, platform_id, niche_id, rank não-None)
    - test_field_types: product.rank é int, product.external_id é str não-vazio, product.price é float ou None
    - test_fallback_selector: quando primary selector ausente no HTML de fixture, fallback selector retorna produtos — lista não-vazia
    - test_drift_alert: HTML sem nenhum selector reconhecido → capture_logs captura evento com alert="schema_drift" e products == []
    - test_upsert_no_duplicates: após dois upserts do mesmo produto (mesmo external_id) com ranks diferentes, DB tem 1 linha com rank mais recente
  </behavior>
  <action>
    Criar `mis/scanners/clickbank.py` com `ClickBankScanner(PlatformScanner)`:

    ```python
    CLICKBANK_PLATFORM_ID = ?  # verificar no DB: SELECT id FROM platforms WHERE name LIKE '%ClickBank%'

    # Seletores baseados nas fixtures gravadas em Task 1 — ajustar conforme HTML real
    SELECTORS_ORDERED = [
        # primary — substituir pelo selector real encontrado na fixture
        ".product-container",
        # fallback 1
        "[data-product]",
        # fallback 2
        ".marketplace-product",
    ]

    MARKETPLACE_BASE_URL = "https://www.clickbank.com/view-marketplace/"
    ```

    - `async def scan_niche(self, niche_slug: str, platform_slug: str) -> list[Product]`:
      1. Usa `self.fetch()` herdado do BaseScraper
      2. Constrói URL com parâmetro de categoria identificado em Task 1 (ex: `?cat={platform_slug}`)
      3. Parseia com BeautifulSoup4/lxml
      4. Tenta cada selector em SELECTORS_ORDERED
      5. Se nenhum retorna resultados: loga `alert='schema_drift'` e retorna `[]`
      6. Extrai external_id (slug da URL ou ID de atributo — conforme Task 1)
      7. Extrai rank (gravity score como int se disponível, posição na lista caso contrário)
      8. Retorna lista de Product dataclasses com platform_id=CLICKBANK_PLATFORM_ID

    **Comportamento em edge cases:**
    - HTML com 0 resultados: retorna `[]` sem lançar exceção
    - Gravity score ausente sem autenticação: usar posição na lista como `rank` (1-indexed), logar `log.info("clickbank.gravity_not_available_using_rank_position")`
    - URL de categoria inválida (ex: 404): BaseScraper.fetch() lança ScraperError, catch e retorna `[]` com log de erro

    **Após implementar:** rodar os 5 testes — todos devem passar (GREEN).
    Em seguida rodar suite completa para garantir sem regressão.
  </action>
  <verify>
    <automated>cd /c/Users/Gabriel/MEGABRAIN/mis && python -m pytest tests/test_clickbank_scanner.py tests/test_kiwify_scanner.py -q</automated>
  </verify>
  <done>
    - mis/scanners/clickbank.py existe com ClickBankScanner(PlatformScanner)
    - Todos os 5 testes em test_clickbank_scanner.py passam GREEN
    - Testes de Kiwify ainda passam (sem regressão)
    - Suite completa não regride: `python -m pytest tests/ -q` limpo
  </done>
</task>

</tasks>

<verification>
Suite completa após o plano:

```bash
cd /c/Users/Gabriel/MEGABRAIN/mis && python -m pytest tests/ -q
```

Deve passar todos os testes existentes (Phase 1: 15 + Kiwify: 5) + os 5 novos testes de ClickBank.
Total esperado: 25 testes GREEN.
</verification>

<success_criteria>
- [ ] Fixtures HTML de ClickBank gravadas ao vivo (não vazias, HTML real)
- [ ] ClickBankScanner.scan_niche() usa httpx via self.fetch() (sem Playwright)
- [ ] external_id usa campo estável (slug ou ID de atributo — documentado no SUMMARY)
- [ ] rank usa gravity score se disponível, posição na lista caso contrário
- [ ] drift alert usa structlog com alert='schema_drift' (não caplog)
- [ ] upsert usa pk=("platform_id", "external_id") — sem duplicatas cross-platform
- [ ] Todos 5 testes GREEN em test_clickbank_scanner.py
- [ ] Suite Phase 1 + Phase 2 Plan 01 permanece GREEN
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/02-platform-scanners/02-02-SUMMARY.md` com:
- Artefatos criados
- CLICKBANK_PLATFORM_ID (valor do DB)
- Gravity score disponível no SSR sem autenticação? (sim/não + evidência)
- URL exata do marketplace por categoria (ex: `?cat=health` ou outra)
- Seletores CSS que funcionaram (primary e fallbacks)
- Campo usado como external_id
- Decisão sobre rank (gravity vs posição)
</output>
