---
phase: 02-platform-scanners
plan: 03
type: tdd
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - mis/scanners/hotmart.py
  - mis/scheduler.py
  - mis/health_monitor.py
  - mis/tests/fixtures/hotmart/response_saude_page1.json
  - mis/tests/fixtures/hotmart/response_mkt_page1.json
  - mis/tests/test_hotmart_scanner.py
  - mis/tests/test_scanner_jobs.py
autonomous: false
requirements:
  - SCAN-01
  - SCAN-04

must_haves:
  truths:
    - "HotmartScanner.scan_niche() retorna pelo menos 10 produtos com campos obrigatórios via XHR intercept do Playwright"
    - "Quando todos os endpoints XHR falham, retorna lista vazia e emite structlog alert='schema_drift'"
    - "register_scanner_jobs() registra 3 jobs (hotmart, clickbank, kiwify) com CronTrigger baseado em scan_schedule do config"
    - "Canary DB-based alerta quando updated_at do produto mais recente de uma plataforma é > 25 horas atrás"
    - "run_all_scanners() com asyncio.gather(return_exceptions=True): falha de uma plataforma não cancela as outras"
  artifacts:
    - path: "mis/scanners/hotmart.py"
      provides: "HotmartScanner(PlatformScanner) com XHR intercept Playwright, fallback endpoints, drift alert"
      exports: ["HotmartScanner"]
    - path: "mis/scheduler.py"
      provides: "register_scanner_jobs() adicionado ao scheduler existente"
      exports: ["register_scanner_jobs"]
    - path: "mis/health_monitor.py"
      provides: "run_platform_canary() DB-based (sem request live) adicionado ao health_monitor existente"
      exports: ["run_platform_canary"]
    - path: "mis/tests/fixtures/hotmart/response_saude_page1.json"
      provides: "JSON real do XHR Hotmart para categoria saude-e-fitness"
    - path: "mis/tests/test_hotmart_scanner.py"
      provides: "5 testes cobrindo SCAN-01 (TDD)"
    - path: "mis/tests/test_scanner_jobs.py"
      provides: "4 testes cobrindo SCAN-04 (jobs, cron trigger, canary, partial failure)"
  key_links:
    - from: "mis/scanners/hotmart.py"
      to: "mis/base_scraper.py"
      via: "HotmartScanner usa fetch_spa() herdado do BaseScraper + page.on('response') para capturar XHR"
      pattern: "fetch_spa|page\\.on"
    - from: "mis/scheduler.py"
      to: "mis/scanners/hotmart.py,mis/scanners/clickbank.py,mis/scanners/kiwify.py"
      via: "register_scanner_jobs() registra run_hotmart_scan, run_clickbank_scan, run_kiwify_scan"
      pattern: "register_scanner_jobs"
    - from: "mis/health_monitor.py"
      to: "mis/db.py"
      via: "run_platform_canary() faz SELECT MAX(updated_at) FROM products WHERE platform_id = ?"
      pattern: "SELECT MAX\\(updated_at\\)"
---

<objective>
Implementar HotmartScanner via Playwright XHR intercept, jobs APScheduler por plataforma e canary DB-based de plataforma.

Purpose: Hotmart é o maior risco técnico da fase (endpoint XHR não documentado). A Wave 0 deste plano requer inspeção live obrigatória via DevTools para identificar o endpoint real antes de qualquer código ser escrito. Após confirmar o endpoint, o scraper e os jobs de agendamento completam a cobertura de SCAN-01 e SCAN-04.

Output:
- Fixture JSON real do XHR Hotmart (gravada ao vivo após inspeção DevTools)
- mis/scanners/hotmart.py — HotmartScanner com XHR intercept e fallback endpoints
- mis/scheduler.py — register_scanner_jobs() adicionado
- mis/health_monitor.py — run_platform_canary() adicionado
- 5 testes GREEN em test_hotmart_scanner.py + 4 testes GREEN em test_scanner_jobs.py
</objective>

<execution_context>
@C:/Users/Gabriel/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Gabriel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-platform-scanners/02-CONTEXT.md
@.planning/phases/02-platform-scanners/02-RESEARCH.md
@.planning/phases/02-platform-scanners/02-VALIDATION.md
@.planning/phases/02-platform-scanners/02-01-SUMMARY.md
@.planning/phases/02-platform-scanners/02-02-SUMMARY.md

<interfaces>
<!-- Contratos existentes que o executor usa diretamente. -->

De mis/scanner.py (criado em 02-01):
```python
@dataclass
class Product:
    external_id: str; title: str; url: str
    platform_id: int; niche_id: int; rank: int
    price: Optional[float] = None
    commission_pct: Optional[float] = None
    rating: Optional[float] = None
    thumbnail_url: Optional[str] = None

class PlatformScanner(BaseScraper, ABC):
    @abstractmethod
    async def scan_niche(self, niche_slug: str, platform_slug: str) -> list[Product]: ...

async def run_all_scanners(config: dict) -> dict[str, list[Product]]: ...
```

De mis/base_scraper.py:
```python
class BaseScraper:
    async def fetch_spa(self, url: str) -> str: ...  # Playwright + stealth_async
```

De mis/scheduler.py (existente — ESTENDER, não reescrever):
```python
def get_scheduler() -> AsyncIOScheduler: ...  # singleton
def start_scheduler() -> None: ...
def stop_scheduler() -> None: ...
```

De mis/health_monitor.py (existente — ESTENDER, não reescrever):
```python
async def run_canary_check() -> bool: ...
def register_canary_job() -> None: ...
```

De mis/product_repository.py (criado em 02-01):
```python
def save_batch(db, products: list[Product]) -> None: ...  # guard vazio incluído
```

Padrão de XHR intercept (hipótese — confirmar na Task 0/checkpoint):
```python
async def _intercept_hotmart(niche_slug, category_slug):
    captured = []
    async with async_playwright() as pw:
        browser = await pw.chromium.launch()
        page = await browser.new_page()
        await stealth_async(page)

        async def handle_response(response):
            for endpoint in HOTMART_ENDPOINTS:
                if endpoint in response.url:
                    try:
                        body = await response.json()
                        captured.append(body)
                    except Exception:
                        pass

        page.on("response", handle_response)
        url = f"https://hotmart.com/pt-br/marketplace/produtos?category={category_slug}"
        await page.goto(url, wait_until="networkidle")
        await browser.close()
    return captured
```

Endpoints hipotéticos (NÃO usar sem confirmar na Task 0):
```python
HOTMART_ENDPOINTS = [
    "api.hotmart.com/product/v3/search",   # hipótese — verificar ao vivo
    "api.hotmart.com/marketplace/products", # fallback — verificar ao vivo
]
```

Padrão de teste para drift alert (OBRIGATÓRIO — não usar caplog):
```python
from structlog.testing import capture_logs

def test_drift_alert():
    with capture_logs() as cap:
        ...
    assert any(e.get("alert") == "schema_drift" for e in cap)
```

Pattern de APScheduler (Phase 1, manter exato):
```python
scheduler.add_job(func, trigger, args=[config], id=job_id, replace_existing=True)
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 0 (Wave 0 obrigatório): Inspeção live Hotmart via DevTools — gravar endpoint e fixture JSON</name>
  <what-built>
    Nenhum código ainda. Esta é a etapa de audit obrigatória antes de qualquer implementação de HotmartScanner.
    Os endpoints XHR do Hotmart não são documentados publicamente — implementar sem esta etapa produz código que nunca funciona (ver Pitfall 2 no RESEARCH.md).
  </what-built>
  <how-to-verify>
    Execute os seguintes passos:

    1. Abrir Chrome/Edge com DevTools (F12) → aba Network → filtrar por XHR/Fetch
    2. Navegar para: `https://hotmart.com/pt-br/marketplace/produtos?category=saude-e-fitness`
    3. Aguardar a página carregar completamente (lista de produtos deve aparecer)
    4. Na aba Network, identificar o request que retorna a lista de produtos:
       - Procurar por requests para `api.hotmart.com` ou similar
       - O response deve ser JSON com lista de produtos (nome, preço, URL, etc.)
    5. Anotar:
       - URL exata do endpoint (ex: `https://api.hotmart.com/product/v3/search?...`)
       - Headers necessários (Authorization? X-Api-Key? cookies?)
       - Formato do JSON de response (campos: name, url, price, rank/score, external_id candidato)
       - Se há paginação (param page, offset, limit)
    6. Salvar o JSON do response como `mis/tests/fixtures/hotmart/response_saude_page1.json`
    7. Repetir para categoria `marketing` → salvar como `mis/tests/fixtures/hotmart/response_mkt_page1.json`
    8. Se a página usa GraphQL em vez de REST: anotar a query e o endpoint GraphQL

    **Se a página não faz nenhum XHR identificável:**
    - Verificar se os dados estão embutidos no HTML SSR (inspecionar `__NUXT_DATA__` ou `window.__STATE__`)
    - Se for SSR: documentar a estrutura e HotmartScanner pode usar httpx em vez de Playwright

    **Se Hotmart bloquear a sessão (Cloudflare 403):**
    - Tentar com VPN ou proxy
    - Documentar o blocker — este é o Pre-Phase 3 blocker já anotado em STATE.md
  </how-to-verify>
  <resume-signal>
    Após completar a inspeção, responder com:
    - URL exata do endpoint XHR (ou confirmação de SSR)
    - JSON schema do response (campos do produto no JSON)
    - Headers necessários (se algum)
    - Caminho dos arquivos de fixture gravados
    - Qualquer blocker encontrado (Cloudflare, login obrigatório, etc.)

    Ex: "Endpoint: https://api.hotmart.com/product/v3/search?category=saude-e-fitness&page=0&limit=50
    Campos: {items: [{productId, productName, price, salesPageUrl, ...}]}
    Fixtures gravadas em mis/tests/fixtures/hotmart/
    Proceed."
  </resume-signal>
</task>

<task type="auto">
  <name>Task 1: Testes RED para HotmartScanner e scanner jobs</name>
  <files>
    mis/tests/test_hotmart_scanner.py,
    mis/tests/test_scanner_jobs.py
  </files>
  <action>
    **Usar a informação da Task 0 (endpoint real + JSON schema) para criar testes realistas.**

    **Parte A — mis/tests/test_hotmart_scanner.py (5 testes RED):**

    Mock strategy para Hotmart (Playwright): monkeypatchar `BaseScraper.fetch_spa()` para retornar o HTML fixture, e injetar o JSON da fixture via mock do `page.on('response')`. Alternativa mais simples: mockar o método interno `_intercept_hotmart()` para retornar a fixture JSON diretamente — isola o parser do Playwright.

    ```python
    import pytest
    from pathlib import Path
    from unittest.mock import AsyncMock, patch
    from structlog.testing import capture_logs

    FIXTURE_JSON = Path(__file__).parent / "fixtures/hotmart/response_saude_page1.json"

    async def test_happy_path(tmp_path):
        # mock _intercept_hotmart para retornar fixture JSON parsed
        # verifica >= 10 produtos com campos obrigatórios
        ...

    async def test_field_types(tmp_path):
        # rank é int, external_id não-None, price é float ou None
        ...

    async def test_fallback_endpoint(tmp_path):
        # primary endpoint retorna vazio, fallback retorna produtos
        ...

    async def test_drift_alert(tmp_path):
        # todos os endpoints retornam vazio → capture_logs captura alert='schema_drift'
        with capture_logs() as cap:
            ...
        assert any(e.get("alert") == "schema_drift" for e in cap)

    async def test_upsert_no_duplicates(tmp_path):
        # dois upserts do mesmo produto → 1 linha, rank atualizado
        ...
    ```

    **Parte B — mis/tests/test_scanner_jobs.py (4 testes RED):**

    ```python
    import pytest
    from mis.config import load_config
    from pathlib import Path

    def test_jobs_registered(temp_config_yaml):
        from mis.scheduler import get_scheduler, stop_scheduler, register_scanner_jobs
        config = load_config(Path(temp_config_yaml))
        register_scanner_jobs(config)
        scheduler = get_scheduler()
        job_ids = {job.id for job in scheduler.get_jobs()}
        assert "scanner_hotmart" in job_ids
        assert "scanner_clickbank" in job_ids
        assert "scanner_kiwify" in job_ids
        stop_scheduler()

    def test_cron_trigger(temp_config_yaml):
        # verifica que job usa CronTrigger com schedule correto do config
        ...

    async def test_platform_canary_stale(tmp_path):
        # DB com produto cujo updated_at é 30h atrás
        # run_platform_canary() retorna False e emite alert='platform_data_stale'
        from structlog.testing import capture_logs
        with capture_logs() as cap:
            result = await run_platform_canary(str(tmp_path / "mis.db"), platform_id=1, platform_name="hotmart")
        assert result == False
        assert any(e.get("alert") == "platform_data_stale" for e in cap)

    async def test_partial_failure(tmp_path):
        # run_all_scanners() com um scanner falhando — outros ainda retornam resultados
        # verifica que a exceção de uma plataforma não cancela as demais
        ...
    ```

    Executar testes — devem falhar em RED (HotmartScanner e register_scanner_jobs não existem ainda).
  </action>
  <verify>
    <automated>cd /c/Users/Gabriel/MEGABRAIN/mis && python -m pytest tests/test_hotmart_scanner.py tests/test_scanner_jobs.py -x -q 2>&1 | grep -E "ERROR|FAILED|passed|error" | tail -5</automated>
  </verify>
  <done>
    - mis/tests/test_hotmart_scanner.py tem 5 testes em RED
    - mis/tests/test_scanner_jobs.py tem 4 testes em RED
    - Todos os testes de Kiwify e ClickBank ainda passam (sem regressão)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar HotmartScanner, register_scanner_jobs e run_platform_canary — RED → GREEN</name>
  <files>
    mis/scanners/hotmart.py,
    mis/scheduler.py,
    mis/health_monitor.py
  </files>
  <behavior>
    HotmartScanner:
    - test_happy_path: scan_niche() retorna >= 10 produtos com campos obrigatórios usando fixture JSON do XHR
    - test_field_types: rank é int, external_id str não-vazio, price float ou None
    - test_fallback_endpoint: quando primary endpoint retorna vazio, fallback retorna produtos
    - test_drift_alert: capture_logs captura alert='schema_drift' quando todos endpoints retornam vazio
    - test_upsert_no_duplicates: 1 linha no DB após dois upserts do mesmo produto com rank diferente

    Scanner jobs (SCAN-04):
    - test_jobs_registered: após register_scanner_jobs(), scheduler.get_jobs() contém scanner_hotmart, scanner_clickbank, scanner_kiwify
    - test_cron_trigger: trigger do job é CronTrigger configurado com scan_schedule do config (default "0 3 * * *")
    - test_platform_canary_stale: run_platform_canary() retorna False e emite alert='platform_data_stale' quando updated_at > 25h
    - test_partial_failure: run_all_scanners() com um scanner levantando exceção retorna resultados das plataformas saudáveis
  </behavior>
  <action>
    **A — mis/scanners/hotmart.py:**

    Usando o endpoint real confirmado na Task 0 (não as hipóteses):

    ```python
    HOTMART_PLATFORM_ID = ?  # verificar no DB: SELECT id FROM platforms WHERE name LIKE '%Hotmart%'

    # Endpoints confirmados na inspeção live (Task 0) — substituir hipóteses por valores reais
    HOTMART_ENDPOINTS = [
        "ENDPOINT_REAL_TASK_0",     # primary — URL exata do XHR identificado
        "ENDPOINT_FALLBACK_TASK_0", # fallback — segundo endpoint tentado
    ]
    ```

    `class HotmartScanner(PlatformScanner)`:
    - `async def scan_niche(self, niche_slug: str, platform_slug: str) -> list[Product]`:
      1. Usa `self.fetch_spa()` herdado do BaseScraper (que já aplica stealth_async)
      2. Registra handler `page.on("response")` antes de navegar
      3. Handler captura JSON dos endpoints em HOTMART_ENDPOINTS
      4. Se nenhum endpoint retorna dados: loga `alert='schema_drift'` + retorna `[]`
      5. Parseia JSON usando schema confirmado na Task 0
      6. external_id: productId do JSON ou slug da URL (conforme Task 0)
      7. commission_pct: extrair se presente no JSON
      8. thumbnail_url: extrair URL remota se presente — NÃO fazer download

    Mock nos testes: monkeypatchar o método privado `_fetch_xhr_data()` (extrair a lógica de Playwright para método separado) para retornar o JSON da fixture — evita instanciar Playwright nos testes.

    **B — mis/scheduler.py (ESTENDER — adicionar ao final do arquivo existente):**

    Adicionar função `register_scanner_jobs(config: dict) -> None`:
    ```python
    from apscheduler.triggers.cron import CronTrigger

    def register_scanner_jobs(config: dict) -> None:
        """Register one APScheduler job per platform scanner. Call before start_scheduler()."""
        from .scanners.hotmart import run_hotmart_scan
        from .scanners.clickbank import run_clickbank_scan
        from .scanners.kiwify import run_kiwify_scan

        scheduler = get_scheduler()
        scan_schedule = config.get("settings", {}).get("scan_schedule", "0 3 * * *")
        trigger = CronTrigger.from_crontab(scan_schedule)

        for job_id, func in [
            ("scanner_hotmart", run_hotmart_scan),
            ("scanner_clickbank", run_clickbank_scan),
            ("scanner_kiwify", run_kiwify_scan),
        ]:
            scheduler.add_job(func, trigger, args=[config], id=job_id, replace_existing=True)
            log.info("scanner.job.registered", job_id=job_id, schedule=scan_schedule)
    ```

    Adicionar também funções `run_hotmart_scan(config)`, `run_clickbank_scan(config)`, `run_kiwify_scan(config)` em seus respectivos arquivos de scraper — funções top-level que instanciam o scanner, carregam o DB e chamam save_batch().

    **C — mis/health_monitor.py (ESTENDER — adicionar ao final do arquivo existente):**

    Adicionar `async def run_platform_canary(db_path: str, platform_id: int, platform_name: str) -> bool`:
    - Query: `SELECT MAX(updated_at) as last_update FROM products WHERE platform_id = ?`
    - Threshold: 25 horas
    - Se last_update None ou < threshold: loga `alert='platform_data_stale'` com platform_name, last_update, threshold_hours=25
    - Retorna bool (True=ok, False=stale)
    - Nunca lança exceção (mesmo padrão de run_canary_check())

    **Após implementar todos os três arquivos:** rodar os 9 testes novos — todos devem passar GREEN.
  </action>
  <verify>
    <automated>cd /c/Users/Gabriel/MEGABRAIN/mis && python -m pytest tests/test_hotmart_scanner.py tests/test_scanner_jobs.py -q</automated>
  </verify>
  <done>
    - mis/scanners/hotmart.py existe com HotmartScanner(PlatformScanner) usando endpoint real do Task 0
    - mis/scheduler.py tem register_scanner_jobs() adicionado (sem quebrar funções existentes)
    - mis/health_monitor.py tem run_platform_canary() adicionado (sem quebrar run_canary_check)
    - Todos os 5 testes em test_hotmart_scanner.py passam GREEN
    - Todos os 4 testes em test_scanner_jobs.py passam GREEN
    - Suite completa GREEN: `python -m pytest tests/ -q` (Phase 1 + Kiwify + ClickBank + Hotmart + jobs)
  </done>
</task>

</tasks>

<verification>
Suite completa após o plano:

```bash
cd /c/Users/Gabriel/MEGABRAIN/mis && python -m pytest tests/ -q
```

Resultado esperado: 34 testes GREEN (15 Phase 1 + 5 Kiwify + 5 ClickBank + 5 Hotmart + 4 scanner jobs).

Verificar que scanner_jobs estão no scheduler:
```bash
cd /c/Users/Gabriel/MEGABRAIN/mis && python -c "
from config import load_config
from scheduler import get_scheduler, register_scanner_jobs, stop_scheduler
from pathlib import Path
config = load_config(Path('config.yaml'))
register_scanner_jobs(config)
jobs = {j.id for j in get_scheduler().get_jobs()}
assert 'scanner_hotmart' in jobs
assert 'scanner_clickbank' in jobs
assert 'scanner_kiwify' in jobs
print('OK — 3 scanner jobs registered:', jobs)
stop_scheduler()
"
```
</verification>

<success_criteria>
- [ ] Inspeção live Hotmart completa (checkpoint aprovado) — endpoint real documentado
- [ ] mis/scanners/hotmart.py usa endpoint real (não hipótese) com fallback
- [ ] XHR intercept extraído em método `_fetch_xhr_data()` para testabilidade
- [ ] register_scanner_jobs() usa replace_existing=True (padrão Phase 1)
- [ ] run_platform_canary() consulta apenas o DB (sem request live)
- [ ] run_platform_canary() emite alert='platform_data_stale' via structlog
- [ ] Todos 5 testes GREEN em test_hotmart_scanner.py
- [ ] Todos 4 testes GREEN em test_scanner_jobs.py
- [ ] Suite completa (34 testes) GREEN sem regressão
- [ ] Fixtures JSON do Hotmart commitadas no repo
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/02-platform-scanners/02-03-SUMMARY.md` com:
- Endpoint real do Hotmart identificado (URL completa)
- JSON schema do response (campos usados: nome do campo → campo do Product)
- HOTMART_PLATFORM_ID (valor do DB)
- Fallback endpoint (se identificado)
- Headers necessários (se algum)
- Total de testes: X/34 GREEN
- Tempo de execução da suite completa
- Qualquer blocker encontrado com Cloudflare/anti-bot
</output>
