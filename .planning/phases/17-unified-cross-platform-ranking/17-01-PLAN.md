---
phase: 17-unified-cross-platform-ranking
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - mis/tests/test_unified_ranking.py
  - mis/product_repository.py
  - mis/web/routes/ranking.py
  - mis/web/templates/ranking.html
  - mis/web/templates/ranking_tabs.html
  - mis/web/templates/unified.html
  - mis/web/templates/unified_table.html
autonomous: true
requirements:
  - DASH-V2-01
  - DASH-V2-02
  - DASH-V2-03

must_haves:
  truths:
    - "Usuário acessa /ranking/unified e vê produtos ordenados por unified_score DESC"
    - "Filtro de nicho retorna apenas produtos daquele nicho de todas as plataformas"
    - "Toggle multi-platform only filtra para produtos com título em 2+ plataformas"
    - "Cada produto exibe badge de plataforma, unified_score (float 1 decimal) e rank bruto"
    - "Produtos stale aparecem com texto dimmed (text-gray-500) e ícone ⚠️"
    - "Tabs Por Plataforma | Unificado visíveis em /ranking e /ranking/unified"
    - "Plataformas com < 5 produtos no nicho são excluídas do cálculo de percentile"
  artifacts:
    - path: "mis/tests/test_unified_ranking.py"
      provides: "12 testes cobrindo DASH-V2-01/02/03"
      exports: ["test_unified_score_order", "test_percentile_positional", "test_percentile_gravity", "test_null_rank_excluded", "test_min_products_threshold", "test_niche_filter", "test_multi_platform_filter", "test_title_normalization", "test_result_fields", "test_stale_included", "test_single_platform_warning", "test_pagination"]
    - path: "mis/product_repository.py"
      provides: "list_unified_ranking() com percentile normalization"
      contains: "def list_unified_ranking"
    - path: "mis/web/routes/ranking.py"
      provides: "GET /ranking/unified + GET /ranking/unified/table"
      exports: ["unified_ranking", "unified_ranking_table"]
    - path: "mis/web/templates/unified.html"
      provides: "Página completa /ranking/unified com filtros e toggle"
    - path: "mis/web/templates/unified_table.html"
      provides: "Partial HTMX com tabela unificada (5 colunas)"
    - path: "mis/web/templates/ranking_tabs.html"
      provides: "Tabs Por Plataforma | Unificado (partial reutilizado)"
  key_links:
    - from: "mis/web/routes/ranking.py"
      to: "mis/product_repository.py"
      via: "list_unified_ranking(db_path, niche, multi_platform_only, per_page, page)"
      pattern: "list_unified_ranking"
    - from: "mis/web/templates/unified.html"
      to: "/ranking/unified/table"
      via: "hx-get nos selects de filtro"
      pattern: "hx-get=\"/ranking/unified/table\""
    - from: "mis/web/templates/ranking.html"
      to: "mis/web/templates/ranking_tabs.html"
      via: "{% include 'ranking_tabs.html' %}"
      pattern: "include.*ranking_tabs"
---

<objective>
Entregar a view `/ranking/unified` com percentile normalization cross-platform — entrega principal do v2.0.

Purpose: Consolidar produtos de todas as 9 plataformas em um ranking unificado normalizado por nicho, permitindo ao usuário identificar os produtos mais dominantes independente de plataforma de origem.

Output: `list_unified_ranking()` em product_repository.py + 2 rotas FastAPI + 2 templates Jinja2/HTMX + tabs de navegação + 12 testes GREEN.
</objective>

<execution_context>
@C:/Users/Gabriel/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Gabriel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-unified-cross-platform-ranking/17-CONTEXT.md
@.planning/phases/17-unified-cross-platform-ranking/17-RESEARCH.md
@.planning/phases/17-unified-cross-platform-ranking/17-VALIDATION.md

<interfaces>
<!-- Contratos existentes que o executor precisa. Extraídos do codebase. -->

From mis/product_repository.py (arquivo que receberá list_unified_ranking):
```python
# Padrão de query parametrizada (mesmo arquivo, adaptar para unified)
def mark_stale(db: sqlite_utils.Database, platform_id: int, niche_id: int) -> None: ...
def upsert_product(db: sqlite_utils.Database, product: "Product") -> None: ...
def save_batch(db: sqlite_utils.Database, products: list["Product"]) -> None: ...
# Nova função a adicionar:
# def list_unified_ranking(db_path: str, niche: str | None, multi_platform_only: int,
#                          per_page: int, page: int) -> dict: ...
```

From mis/web/routes/ranking.py (arquivo que receberá as 2 novas rotas):
```python
def is_htmx(request: Request) -> bool:
    return request.headers.get("HX-Request") == "true"

# Padrão de rota a replicar:
@router.get("/ranking", response_class=HTMLResponse)
async def ranking(request: Request, platform: str | None = None,
                  niche: str | None = None, order_by: str = "rank",
                  order_dir: str = "asc", per_page: int = 20, page: int = 1) -> HTMLResponse:
    db_path = request.app.state.db_path
    templates = request.app.state.templates
    context = _get_ranking_context(...)
    if is_htmx(request): return templates.TemplateResponse(request=request, name="ranking_table.html", context=context)
    return templates.TemplateResponse(request=request, name="ranking.html", context=context)
```

From mis/tests/conftest.py:
```python
@pytest.fixture
def db_path(tmp_path):
    return str(tmp_path / "mis.db")
```

From mis/dossier_repository.py (padrão SQL com join a replicar):
```python
params: list = []
where_clauses: list[str] = []
if niche:
    where_clauses.append("n.slug = ?")
    params.append(niche)
where_sql = ("WHERE " + " AND ".join(where_clauses)) if where_clauses else ""
sql = f"""
    SELECT p.id, p.title, pl.name AS platform_name, pl.slug AS platform_slug,
           pl.rank_type, n.name AS niche_name, p.rank, p.is_stale,
           CASE WHEN d.id IS NOT NULL THEN 1 ELSE 0 END AS has_dossier
    FROM products p
    JOIN platforms pl ON pl.id = p.platform_id
    JOIN niches    n  ON n.id  = p.niche_id
    LEFT JOIN dossiers d ON d.product_id = p.id
    {where_sql}
    ORDER BY pl.slug, p.rank ASC
"""
cursor = db.execute(sql, params)
columns = [desc[0] for desc in cursor.description]
rows = cursor.fetchall()
results = [dict(zip(columns, row)) for row in rows]
```

Retorno esperado de list_unified_ranking():
```python
{
    "products": [
        {
            "id": int,
            "title": str,
            "platform_name": str,
            "platform_slug": str,
            "niche_name": str,
            "unified_score": float,  # 1 decimal, ex: 87.4
            "rank": float | int | None,
            "rank_type": str,        # positional | gravity | upvotes | enrollment
            "is_stale": bool,
            "has_dossier": bool,
        }
    ],
    "total": int,
    "warning_single_platform": bool,
    "niches": list[dict],            # para o select de filtro
    "last_updated": str | None,
    "selected_niche": str | None,
    "multi_platform_only": int,
    "per_page": int,
    "page": int,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — 12 test stubs para list_unified_ranking()</name>
  <files>mis/tests/test_unified_ranking.py</files>
  <behavior>
    - test_unified_score_order: 2 plataformas x 5 produtos cada (positional) → resultado ordenado por unified_score DESC
    - test_percentile_positional: rank #1 em total=5 → unified_score = (1 - 1/5) * 100 = 80.0
    - test_percentile_gravity: produto com gravity=100.0 em grupo onde max=100.0 → unified_score = 100.0
    - test_null_rank_excluded: produto com rank=NULL não aparece no resultado
    - test_min_products_threshold: plataforma com 3 produtos (< MIN=5) excluída; plataforma com 5 produtos incluída
    - test_niche_filter: produtos de 2 nichos no DB → filtro por nicho A retorna apenas nicho A
    - test_multi_platform_filter: títulos normalizados idênticos em 2 plataformas → toggle ON retorna ambos; toggle OFF retorna todos
    - test_title_normalization: "Café Marketing" e "cafe marketing" → mesmo título normalizado
    - test_result_fields: cada dict tem chaves: id, title, platform_name, platform_slug, niche_name, unified_score, rank, rank_type, is_stale, has_dossier
    - test_stale_included: produto com is_stale=1 aparece no resultado com is_stale=True
    - test_single_platform_warning: nicho com dados de apenas 1 plataforma → warning_single_platform=True no retorno
    - test_pagination: 10 produtos no total, per_page=5, page=2 → retorna produtos 6-10

    TODOS os testes devem ser RED após a criação (list_unified_ranking não existe ainda).
    Cada teste usa a fixture db_path(tmp_path) do conftest.py existente.
    Setup do DB em cada teste: usar sqlite_utils.Database(db_path) + executar migrations ou inserir diretamente nas tabelas products/platforms/niches (verificar se migrations estão disponíveis — se não, inserir rows diretamente).
    Não usar pytest.mark.asyncio (list_unified_ranking é síncrona).
  </behavior>
  <action>
    Criar mis/tests/test_unified_ranking.py com 12 funções de teste.

    IMPORTANTE — setup de fixtures nos testes:
    1. Importar sqlite_utils e mis.product_repository (após criação em Task 2)
    2. Para setup do DB nos testes: verificar se existe mis.db.get_db() ou similar. Se não, usar sqlite_utils.Database(db_path) e criar tabelas manualmente com CREATE TABLE IF NOT EXISTS para products, platforms, niches, dossiers.
    3. Verificar estrutura das tabelas lendo mis/migrations/_001_initial.py e _006_v2_platforms.py antes de escrever o setup.

    Estrutura mínima das tabelas para os testes:
    - platforms: id INTEGER PK, name TEXT, slug TEXT UNIQUE, rank_type TEXT
    - niches: id INTEGER PK, name TEXT, slug TEXT UNIQUE
    - products: id INTEGER PK AUTOINCREMENT, platform_id INTEGER, external_id TEXT, title TEXT, rank REAL, is_stale INTEGER DEFAULT 0, niche_id INTEGER
    - dossiers: id INTEGER PK, product_id INTEGER

    Helper de setup sugerido (pode ser função local ou fixture):
    ```python
    def _setup_db(db_path: str) -> sqlite_utils.Database:
        db = sqlite_utils.Database(db_path)
        db.execute("CREATE TABLE IF NOT EXISTS platforms (id INTEGER PRIMARY KEY, name TEXT, slug TEXT UNIQUE, rank_type TEXT)")
        db.execute("CREATE TABLE IF NOT EXISTS niches (id INTEGER PRIMARY KEY, name TEXT, slug TEXT UNIQUE)")
        db.execute("CREATE TABLE IF NOT EXISTS products (id INTEGER PRIMARY KEY AUTOINCREMENT, platform_id INTEGER, external_id TEXT, title TEXT, url TEXT, rank REAL, is_stale INTEGER DEFAULT 0, niche_id INTEGER)")
        db.execute("CREATE TABLE IF NOT EXISTS dossiers (id INTEGER PRIMARY KEY, product_id INTEGER)")
        db.conn.commit()
        return db
    ```

    Para test_pagination: verificar que o retorno inclui "total" com a contagem total (não apenas a página atual).
    Para test_single_platform_warning: retorno deve ter chave "warning_single_platform".

    Todos os testes devem importar de: `from mis.product_repository import list_unified_ranking`
    O import vai falhar até Task 2 — isso é esperado no ciclo RED.
  </action>
  <verify>
    <automated>cd /c/Users/Gabriel/MEGABRAIN && python -m pytest mis/tests/test_unified_ranking.py -x -q 2>&1 | head -20</automated>
  </verify>
  <done>Arquivo mis/tests/test_unified_ranking.py existe com 12 funções de teste. Todos falham com ImportError ou AssertionError (RED confirmado). Nenhum teste passa.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — list_unified_ranking() em product_repository.py</name>
  <files>mis/product_repository.py</files>
  <behavior>
    - test_percentile_positional passa: rank #1 / total=5 → score = (1 - 1/5) * 100 = 80.0
    - test_percentile_gravity passa: gravity=100 / max=100 → score = 100.0
    - test_null_rank_excluded passa: produtos com rank=None/NULL não aparecem
    - test_min_products_threshold passa: grupo com < 5 produtos excluído do cálculo
    - test_niche_filter passa: WHERE n.slug = ? aplicado corretamente
    - test_multi_platform_filter passa: toggle ON filtra títulos normalizados presentes em 2+ plataformas
    - test_title_normalization passa: unicodedata NFKD + encode ascii + lower + strip
    - test_result_fields passa: todos os campos obrigatórios presentes em cada dict
    - test_stale_included passa: is_stale=1 no DB → is_stale=True no resultado
    - test_single_platform_warning passa: warning_single_platform=True quando 1 plataforma
    - test_pagination passa: slice correto para page=2, per_page=5
    - test_unified_score_order passa: ordenação DESC por unified_score
  </behavior>
  <action>
    Adicionar ao final de mis/product_repository.py:

    1. Constante no topo do módulo (após imports): `MIN_PRODUCTS_PER_PLATFORM = 5`

    2. Funções auxiliares privadas:
       - `_normalize_title(title: str) -> str` — unicodedata.normalize("NFKD", title).encode("ascii", "ignore").decode("ascii").lower().strip()
       - `_compute_unified_scores(platform_rows: list[dict]) -> list[dict]` — calcula unified_score por rank_type:
         - positional: round((1 - row["rank"] / total) * 100, 1)
         - gravity/upvotes/enrollment: round((row["rank"] / max_val) * 100, 1) if max_val > 0 else 0.0
       - `_format_rank(rank, rank_type: str) -> str` — formatação visual (usada apenas pelas rotas/templates, opcional aqui)

    3. Função principal `list_unified_ranking(db_path, niche, multi_platform_only, per_page, page)`:

    ALGORITMO (seguir exatamente, Pitfall 1 crítico):
    ```
    Step 1: SQL query — TODOS os produtos do nicho com rank IS NOT NULL
      - JOIN platforms (rank_type, name, slug), JOIN niches (name, slug), LEFT JOIN dossiers (has_dossier)
      - WHERE n.slug = ? (se niche fornecido) AND p.rank IS NOT NULL
      - ORDER BY pl.slug, p.rank ASC
      - SEM LIMIT — buscar tudo (Pitfall 1: paginação NUNCA no SQL)

    Step 2: Agrupar por platform_slug
      - from collections import defaultdict
      - groups: dict[str, list[dict]]

    Step 3: Filtrar grupos com < MIN_PRODUCTS_PER_PLATFORM
      - Descartar silenciosamente grupos com len(group) < MIN_PRODUCTS_PER_PLATFORM

    Step 4: Calcular unified_score por grupo usando _compute_unified_scores()

    Step 5: Achatar todos os grupos numa lista flat
      - Detectar warning_single_platform: len(grupos_incluídos) == 1

    Step 6: Se multi_platform_only (bool(multi_platform_only)):
      - normalized_titles: dict[str, set[str]] mapeando titulo_normalizado → set de platform_slug
      - Manter apenas produtos cujo titulo_normalizado aparece em 2+ plataformas

    Step 7: Ordenar por unified_score DESC, tiebreaker rank ASC (menor rank vence)
      - sorted(all_products, key=lambda r: (-r["unified_score"], r["rank"] or 999999))

    Step 8: Contar total (para paginação)

    Step 9: Paginar em Python
      - offset = (page - 1) * per_page
      - paginated = all_products[offset : offset + per_page]

    Step 10: Buscar niches e last_updated para o contexto do template
      - SELECT slug, name FROM niches ORDER BY name
      - SELECT MAX(updated_at) FROM products

    Step 11: Retornar dict com:
      {
        "products": paginated,
        "total": total,
        "warning_single_platform": warning_single_platform,
        "niches": niches_list,
        "last_updated": last_updated,
        "selected_niche": niche,
        "multi_platform_only": multi_platform_only,
        "per_page": per_page,
        "page": page,
      }
    ```

    PITFALLS a evitar:
    - Pitfall 1: Nunca colocar LIMIT no SQL principal
    - Pitfall 3: WHERE p.rank IS NOT NULL no SQL (já no Step 1)
    - Pitfall 4: guard `if max_val > 0 else 0.0` no score gravity/upvotes/enrollment
    - Pitfall 2: filtrar grupos com len < MIN_PRODUCTS_PER_PLATFORM ANTES de calcular scores

    Usar get_db() de mis.db para obter db instance: `from mis.db import get_db` (já no padrão do arquivo).
    Adicionar import `import unicodedata` e `from collections import defaultdict` no topo do arquivo.
  </action>
  <verify>
    <automated>cd /c/Users/Gabriel/MEGABRAIN && python -m pytest mis/tests/test_unified_ranking.py -x -q</automated>
  </verify>
  <done>Todos os 12 testes de test_unified_ranking.py passam (GREEN). pytest mis/tests/test_unified_ranking.py -x -q reporta "12 passed".</done>
</task>

<task type="auto">
  <name>Task 3: Rotas FastAPI + Templates HTMX + Tabs de Navegação</name>
  <files>mis/web/routes/ranking.py, mis/web/templates/ranking_tabs.html, mis/web/templates/ranking.html, mis/web/templates/unified.html, mis/web/templates/unified_table.html</files>
  <action>
    ARQUIVO 1: mis/web/templates/ranking_tabs.html (novo — partial compartilhado)
    Criar partial com as duas tabs:
    ```html
    <div class="flex gap-1 mb-4 border-b border-gray-700">
      <a href="/ranking"
         class="px-4 py-2 text-sm {% if '/ranking/unified' not in request.url.path %}text-white border-b-2 border-blue-500{% else %}text-gray-400 hover:text-white{% endif %}">
        Por Plataforma
      </a>
      <a href="/ranking/unified"
         class="px-4 py-2 text-sm {% if '/ranking/unified' in request.url.path %}text-white border-b-2 border-blue-500{% else %}text-gray-400 hover:text-white{% endif %}">
        Unificado
      </a>
    </div>
    ```

    ARQUIVO 2: mis/web/templates/ranking.html (modificar — adicionar tabs)
    Inserir `{% include "ranking_tabs.html" %}` ANTES do `<h1 class="text-2xl font-bold...">` existente.
    NÃO alterar nenhum outro conteúdo do arquivo.

    ARQUIVO 3: mis/web/templates/unified.html (novo — extends base.html)
    Estrutura baseada em ranking.html com estas diferenças:
    - Título: "Ranking Unificado — MIS Dashboard"
    - h1: "Ranking Unificado"
    - Incluir ranking_tabs.html no topo
    - SEM filtro de plataforma (apenas nicho + per_page + toggle)
    - Toggle multi-platform: `<input type="checkbox" name="multi_platform_only" value="1" {% if multi_platform_only %}checked{% endif %}>` com label "Multi-plataforma"
    - Banner condicional: `{% if warning_single_platform %}<div class="mb-3 p-3 bg-yellow-900 text-yellow-200 text-sm rounded">Dados de 1 plataforma — ranking reflete apenas uma fonte</div>{% endif %}`
    - hx-get nos selects aponta para "/ranking/unified/table"
    - hx-include de TODOS os selects deve incluir: `[name='niche'],[name='multi_platform_only'],[name='per_page'],[name='page']`
    - div wrapper: `id="unified-table-wrapper"`
    - Usar `{% include "unified_table.html" %}` dentro do wrapper

    ARQUIVO 4: mis/web/templates/unified_table.html (novo — partial HTMX)
    Tabela com 5 colunas: #, Produto, Plataforma, Score, Rank Original
    - Linha com classe `{% if product.is_stale %}text-gray-500{% endif %}`
    - Coluna #: `{{ loop.index + (page - 1) * per_page }}` (posição unificada real)
    - Coluna Produto: link `/dossier/{{ product.id }}` + badge Pendente se not has_dossier
    - Coluna Plataforma: `{{ product.platform_name }}{% if product.is_stale %} <span title="Dados desatualizados">⚠️</span>{% endif %}`
    - Coluna Score: `{{ "%.1f"|format(product.unified_score) }}`
    - Coluna Rank Original: formatação por rank_type:
      ```
      {% if product.rank_type == 'positional' %}#{{ product.rank|int }}
      {% elif product.rank_type == 'gravity' %}{{ "%.1f"|format(product.rank) }} gravity
      {% elif product.rank_type == 'upvotes' %}#{{ product.rank|int }} upvotes
      {% elif product.rank_type == 'enrollment' %}{{ product.rank|int }} alunos
      {% else %}{{ product.rank }}{% endif %}
      ```
    - Rodapé: última atualização (padrão ranking_table.html)
    - Estado vazio: `<td colspan="5" ...>Sem dados ainda</td>`

    ARQUIVO 5: mis/web/routes/ranking.py (modificar — adicionar 2 rotas + _get_unified_context)
    Adicionar ao final do arquivo (após as rotas existentes):

    ```python
    def _get_unified_context(
        db_path: str,
        niche: str | None,
        multi_platform_only: int,
        per_page: int,
        page: int,
    ) -> dict:
        """Build context dict for unified ranking routes."""
        from mis.product_repository import list_unified_ranking
        return list_unified_ranking(db_path, niche=niche,
                                    multi_platform_only=multi_platform_only,
                                    per_page=per_page, page=page)


    @router.get("/ranking/unified", response_class=HTMLResponse)
    async def unified_ranking(
        request: Request,
        niche: str | None = None,
        multi_platform_only: int = 0,
        per_page: int = 20,
        page: int = 1,
    ) -> HTMLResponse:
        """Return the unified ranking page or unified_table partial for HTMX."""
        db_path = request.app.state.db_path
        templates = request.app.state.templates
        context = _get_unified_context(db_path, niche, multi_platform_only, per_page, page)
        if is_htmx(request):
            return templates.TemplateResponse(
                request=request, name="unified_table.html", context=context
            )
        return templates.TemplateResponse(
            request=request, name="unified.html", context=context
        )


    @router.get("/ranking/unified/table", response_class=HTMLResponse)
    async def unified_ranking_table(
        request: Request,
        niche: str | None = None,
        multi_platform_only: int = 0,
        per_page: int = 20,
        page: int = 1,
    ) -> HTMLResponse:
        """Return unified_table partial — always without base layout."""
        db_path = request.app.state.db_path
        templates = request.app.state.templates
        context = _get_unified_context(db_path, niche, multi_platform_only, per_page, page)
        return templates.TemplateResponse(
            request=request, name="unified_table.html", context=context
        )
    ```

    ATENÇÃO: multi_platform_only usa `int = 0` (não bool) porque checkbox HTML unchecked não envia o campo. FastAPI recebe ausência como 0, presença com value="1" como 1. A função list_unified_ranking converte internamente com bool().
  </action>
  <verify>
    <automated>cd /c/Users/Gabriel/MEGABRAIN && python -m pytest mis/tests/ -x -q</automated>
  </verify>
  <done>Suite completa passa. Templates unified.html + unified_table.html + ranking_tabs.html existem. ranking.html inclui ranking_tabs.html. ranking.py tem rotas /ranking/unified e /ranking/unified/table. python -m mis dashboard sobe sem ImportError.</done>
</task>

</tasks>

<verification>
Suite completa verde:
```
pytest mis/tests/ -x -q
```

Verificação de importação limpa:
```
python -c "from mis.product_repository import list_unified_ranking; print('OK')"
```

Verificação de existência dos templates:
```
ls mis/web/templates/unified.html mis/web/templates/unified_table.html mis/web/templates/ranking_tabs.html
```

Verificação das rotas registradas:
```
python -c "from mis.web.routes.ranking import router; paths = [r.path for r in router.routes]; assert '/ranking/unified' in paths and '/ranking/unified/table' in paths; print('rotas OK')"
```
</verification>

<success_criteria>
- [ ] 12 testes em test_unified_ranking.py passam (GREEN)
- [ ] list_unified_ranking() retorna products ordenados por unified_score DESC
- [ ] Percentile positional: rank #1 / total=5 → 80.0
- [ ] Percentile gravity: valor/max * 100
- [ ] Plataformas com < 5 produtos excluídas silenciosamente
- [ ] rank=NULL excluídos do resultado
- [ ] Toggle multi_platform_only filtra por título normalizado em 2+ plataformas
- [ ] warning_single_platform=True quando nicho tem dados de 1 plataforma
- [ ] Paginação correta (page=2 retorna slice correto)
- [ ] GET /ranking/unified responde 200 com template unified.html
- [ ] GET /ranking/unified/table responde 200 com partial unified_table.html
- [ ] Tabs visíveis em /ranking e /ranking/unified
- [ ] pytest mis/tests/ -x -q : all green
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/17-unified-cross-platform-ranking/17-01-SUMMARY.md` com:
- Artifacts criados e modificados
- Decisões tomadas (Claude's discretion)
- Resultado dos testes
- Qualquer desvio do plano
</output>
