---
phase: 04-pain-radar
plan: "04"
type: execute
wave: 3
depends_on:
  - "04-02"
  - "04-03"
files_modified:
  - mis/radar/synthesizer.py
  - mis/prompts/pain_synthesis_prompt.txt
  - mis/tests/test_synthesizer.py
autonomous: true
requirements:
  - RADAR-05
  - RADAR-06

must_haves:
  truths:
    - "synthesize_niche_pains() chama LLM e retorna dict com chave 'pains' contendo lista de top 5 dores"
    - "Cada dor tem: description (str), evidence (str com fonte), interest_level ('Alto'|'Médio'|'Baixo')"
    - "Synthesizer não chama LLM quando count de sinais do ciclo == 0 — emite alert='radar_synthesizer_no_signals'"
    - "pain_reports é upsertado com (niche_id, cycle_at) como chave — re-execução substitui, não duplica"
    - "Relatório armazenado em pain_reports como JSON válido parseable"
  artifacts:
    - path: "mis/radar/synthesizer.py"
      provides: "synthesize_niche_pains(), fetch_cycle_signals()"
      exports: ["synthesize_niche_pains", "fetch_cycle_signals"]
    - path: "mis/prompts/pain_synthesis_prompt.txt"
      provides: "Template de prompt para síntese de dores"
  key_links:
    - from: "mis/radar/synthesizer.py"
      to: "mis/migrations/_004_pain_radar.py"
      via: "fetch de pain_signals e upsert em pain_reports"
      pattern: "pain_signals.*rows_where|pain_reports.*upsert"
    - from: "mis/radar/synthesizer.py"
      to: "anthropic.AsyncAnthropic"
      via: "LLM claude-sonnet-4-6 com mesmo padrão do dossier_generator.py"
      pattern: "AsyncAnthropic|claude-sonnet-4-6"
---

<objective>
Implementar o synthesizer LLM que consolida sinais brutos em relatórios horários de dores por nicho.

Purpose: O synthesizer é o produto final do Pain Radar — transforma centenas de posts/trends/comentários em 5 dores acionáveis com evidências e nível de interesse. É o equivalente do dossier_generator.py para sinais de mercado.

Output: mis/radar/synthesizer.py funcional com 4+ testes GREEN, relatórios idempotentes em pain_reports.
</objective>

<execution_context>
@C:/Users/Gabriel/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Gabriel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-pain-radar/04-CONTEXT.md
@.planning/phases/04-pain-radar/04-RESEARCH.md
@.planning/phases/04-pain-radar/04-01-SUMMARY.md
@.planning/phases/04-pain-radar/04-02-SUMMARY.md
@.planning/phases/04-pain-radar/04-03-SUMMARY.md

@mis/intelligence/dossier_generator.py
@mis/prompts/
@mis/migrations/_004_pain_radar.py
@mis/tests/conftest.py
@mis/tests/test_synthesizer.py

<interfaces>
<!-- Contratos existentes e novos que este plano implementa -->

Do dossier_generator.py (padrão a replicar):
```python
import anthropic
client = anthropic.AsyncAnthropic()  # usa ANTHROPIC_API_KEY do env

response = await client.messages.create(
    model="claude-sonnet-4-6",
    max_tokens=2048,
    messages=[{"role": "user", "content": prompt}],
)
text = response.content[0].text
cost_usd = (response.usage.input_tokens * 0.000003 +
            response.usage.output_tokens * 0.000015)
```

Da migration _004 — pain_signals (leitura):
```python
# Buscar sinais de um ciclo por niche_slug e intervalo de tempo
signals = list(db["pain_signals"].rows_where(
    "niche_slug = ? AND collected_at > ?",
    [niche_slug, cycle_start_iso]
))
```

Da migration _004 — pain_reports (upsert idempotente):
```python
# unique constraint em (niche_id, cycle_at)
# Upsert: se já existe para este ciclo, substitui
db.execute("""
    INSERT INTO pain_reports (niche_id, cycle_at, report_json, created_at)
    VALUES (?, ?, ?, ?)
    ON CONFLICT(niche_id, cycle_at) DO UPDATE SET
        report_json = excluded.report_json,
        created_at = excluded.created_at
""", [niche_id, cycle_at, json.dumps(report), datetime.utcnow().isoformat()])
```

Formato esperado do relatório (output do LLM):
```json
{
  "pains": [
    {"description": "...", "evidence": "Reddit: 23 posts, YouTube: alto view_count", "interest_level": "Alto"},
    ...
  ],
  "niche": "emagrecimento",
  "cycle_at": "2026-03-15T14:00:00",
  "sources_used": {"reddit": 18, "youtube": 5, "google_trends": 3, "quora": 2}
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prompt de síntese + Synthesizer LLM com idempotência</name>
  <files>
    mis/prompts/pain_synthesis_prompt.txt,
    mis/radar/synthesizer.py,
    mis/tests/test_synthesizer.py
  </files>
  <behavior>
    - test_synthesize_returns_report_with_top_pains: popular pain_signals com 5 sinais mock; mockar AsyncAnthropic.messages.create retornando JSON válido; assert report["pains"] tem 5 items; cada pain tem description, evidence, interest_level
    - test_no_signals_skip_llm_call: pain_signals vazio para o nicho; assert LLM NÃO é chamado (mock verify never called); assert retorna None; assert structlog tem alert='radar_synthesizer_no_signals'
    - test_report_idempotent_upsert: chamar synthesize_niche_pains() duas vezes para mesmo ciclo; assert COUNT(*) em pain_reports == 1 (não 2)
    - test_report_has_evidence_fields: report_json tem chaves 'pains', 'niche', 'cycle_at', 'sources_used'
    - test_fetch_cycle_signals_returns_recent_only: inserir sinais com collected_at de 2h atrás e de 25h atrás; assert fetch_cycle_signals retorna apenas os de 2h (dentro do ciclo de 2h)
  </behavior>
  <action>
Criar `mis/prompts/pain_synthesis_prompt.txt` com template de síntese em pt-BR:

```
Você é um analista especialista em marketing digital. Analise os seguintes sinais coletados do mercado para o nicho "{niche_name}" e identifique as TOP 5 DORES E DESEJOS mais relevantes.

SINAIS COLETADOS ({total_signals} sinais):

{signals_text}

INSTRUÇÕES:
1. Identifique exatamente 5 dores/desejos principais que emergem destes dados
2. Para cada dor: escreva uma descrição clara e acionável (1-2 frases)
3. Inclua evidência específica (ex: "Reddit: 18 posts com alto upvote, YouTube: vídeos com 500K+ views")
4. Classifique o nível de interesse: Alto (múltiplas fontes + alto engajamento), Médio (uma fonte ou baixo engajamento), Baixo (sinal fraco ou único)

RESPONDA APENAS com JSON válido neste formato:
{
  "pains": [
    {"description": "...", "evidence": "...", "interest_level": "Alto|Médio|Baixo"},
    ...5 items...
  ]
}
```

Criar `mis/radar/synthesizer.py`:

```python
def fetch_cycle_signals(db_path: str, niche_slug: str, cycle_start_iso: str) -> list[dict]:
    """Busca sinais coletados a partir de cycle_start_iso para o nicho."""
    db = sqlite_utils.Database(db_path)
    return list(db["pain_signals"].rows_where(
        "niche_slug = ? AND collected_at >= ?",
        [niche_slug, cycle_start_iso]
    ))

def _format_signals_for_prompt(signals: list[dict]) -> str:
    """Formata sinais brutos como texto estruturado para o LLM."""
    lines = []
    for s in signals:
        extra = json.loads(s.get("extra_json", "{}"))
        line = f"[{s['source'].upper()}] {s['title']}"
        if s.get("score"):
            line += f" (score: {s['score']})"
        if "top_comments" in extra and extra["top_comments"]:
            line += f"\n  Comentários: {'; '.join(extra['top_comments'][:3])}"
        lines.append(line)
    return "\n".join(lines)

async def synthesize_niche_pains(
    niche_id: int,
    niche_name: str,
    niche_slug: str,
    cycle_at: str,
    db_path: str,
) -> dict | None:
    """
    Sintetiza sinais do ciclo atual em relatório de dores via LLM.
    Idempotente: re-execução substitui via upsert em (niche_id, cycle_at).
    Retorna None se não há sinais suficientes.
    """
    # Janela do ciclo: sinais das últimas 2 horas antes de cycle_at
    cycle_dt = datetime.fromisoformat(cycle_at)
    cycle_start = (cycle_dt - timedelta(hours=2)).isoformat()
    signals = fetch_cycle_signals(db_path, niche_slug, cycle_start)

    if not signals:
        log.warning("radar.synthesizer.no_signals",
                    niche=niche_slug, cycle_at=cycle_at,
                    alert="radar_synthesizer_no_signals")
        return None

    # Montar prompt
    prompt_template = Path(__file__).parent.parent / "prompts" / "pain_synthesis_prompt.txt"
    template = prompt_template.read_text(encoding="utf-8")
    signals_text = _format_signals_for_prompt(signals)
    sources_used = {}
    for s in signals:
        sources_used[s["source"]] = sources_used.get(s["source"], 0) + 1

    prompt = template.format(
        niche_name=niche_name,
        total_signals=len(signals),
        signals_text=signals_text,
    )

    # Chamar LLM
    client = anthropic.AsyncAnthropic()
    response = await client.messages.create(
        model="claude-sonnet-4-6",
        max_tokens=1024,
        messages=[{"role": "user", "content": prompt}],
    )
    raw_text = response.content[0].text
    cost_usd = (response.usage.input_tokens * 0.000003 +
                response.usage.output_tokens * 0.000015)

    # Parse JSON do LLM
    try:
        llm_data = json.loads(raw_text)
    except json.JSONDecodeError:
        # Tentar extrair JSON do texto se LLM adicionou texto extra
        import re
        match = re.search(r'\{.*\}', raw_text, re.DOTALL)
        llm_data = json.loads(match.group()) if match else {"pains": []}

    report = {
        "pains": llm_data.get("pains", []),
        "niche": niche_slug,
        "cycle_at": cycle_at,
        "sources_used": sources_used,
        "cost_usd": cost_usd,
        "signals_count": len(signals),
    }

    # Upsert idempotente via SQL raw (ON CONFLICT)
    db = sqlite_utils.Database(db_path)
    db.execute(
        """
        INSERT INTO pain_reports (niche_id, cycle_at, report_json, created_at)
        VALUES (?, ?, ?, ?)
        ON CONFLICT(niche_id, cycle_at) DO UPDATE SET
            report_json = excluded.report_json,
            created_at = excluded.created_at
        """,
        [niche_id, cycle_at, json.dumps(report), datetime.utcnow().isoformat()],
    )

    log.info("radar.synthesizer.report_generated",
             niche=niche_slug, cycle_at=cycle_at,
             signals_count=len(signals), cost_usd=cost_usd,
             pains_count=len(report["pains"]))
    return report
```

Atualizar `mis/tests/test_synthesizer.py` com testes reais. Monkeypatch ANTHROPIC_API_KEY (padrão da Phase 3) e mockar AsyncAnthropic.messages.create. Usar tmp_path DB real (padrão conftest.py).
</action>
  <verify>
    <automated>cd /c/Users/Gabriel/MEGABRAIN/mis && python -m pytest tests/test_synthesizer.py -x -q</automated>
  </verify>
  <done>5 testes GREEN em test_synthesizer.py; synthesize_niche_pains retorna dict com pains[]; retorna None sem chamar LLM quando sem sinais; upsert idempotente verificado</done>
</task>

</tasks>

<verification>
Após conclusão:
1. `cd /c/Users/Gabriel/MEGABRAIN/mis && python -m pytest tests/test_synthesizer.py -v` — todos GREEN
2. `cd /c/Users/Gabriel/MEGABRAIN/mis && python -m pytest tests/ -q` — suite completa GREEN
3. `python -c "from mis.radar.synthesizer import synthesize_niche_pains, fetch_cycle_signals; print('OK')"` — importa sem erro
4. `cat mis/prompts/pain_synthesis_prompt.txt | head -5` — arquivo existe
</verification>

<success_criteria>
- mis/radar/synthesizer.py existe com synthesize_niche_pains() e fetch_cycle_signals()
- mis/prompts/pain_synthesis_prompt.txt existe com template em pt-BR
- 5 testes GREEN: happy path, no-signals skip, idempotência, campos do relatório, janela temporal
- LLM não chamado quando signals == 0
- pain_reports upsertado com ON CONFLICT — re-execução não duplica
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/04-pain-radar/04-04-SUMMARY.md`
</output>
