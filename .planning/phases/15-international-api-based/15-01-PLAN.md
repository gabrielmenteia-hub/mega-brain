---
phase: 15-international-api-based
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - mis/tests/fixtures/product_hunt/trending_today.json
  - mis/tests/fixtures/udemy/courses_marketing.json
  - mis/tests/test_product_hunt_scanner.py
  - mis/tests/test_udemy_scanner.py
  - mis/scanners/product_hunt.py
  - mis/scanners/udemy.py
autonomous: true
requirements:
  - SCAN-INTL-01
  - SCAN-INTL-02

must_haves:
  truths:
    - "Fixtures JSON existem em mis/tests/fixtures/product_hunt/ e mis/tests/fixtures/udemy/"
    - "Testes RED confirmados — falham porque as implementações são stubs"
    - "Stubs de scanner implementam o contrato PlatformScanner ABC (scan_niche retorna [])"
  artifacts:
    - path: "mis/tests/fixtures/product_hunt/trending_today.json"
      provides: "Fixture JSON com estrutura real da GraphQL API do Product Hunt"
      contains: "data.posts.edges[].node com slug, name, thumbnail.url, votesCount"
    - path: "mis/tests/fixtures/udemy/courses_marketing.json"
      provides: "Fixture JSON com estrutura da Udemy REST API (arquivada)"
      contains: "results[].id, title, url, price_detail.amount, avg_rating, image_480x270"
    - path: "mis/tests/test_product_hunt_scanner.py"
      provides: "6 testes para SCAN-INTL-01: happy_path, field_types, missing_credentials, empty_results, upsert_no_duplicates, is_stale"
      exports: ["test_happy_path", "test_field_types", "test_missing_credentials", "test_empty_results", "test_upsert_no_duplicates", "test_is_stale"]
    - path: "mis/tests/test_udemy_scanner.py"
      provides: "6 testes para SCAN-INTL-02: mesmo padrão"
      exports: ["test_happy_path", "test_field_types", "test_missing_credentials", "test_empty_results", "test_api_discontinued_fallback", "test_upsert_no_duplicates"]
    - path: "mis/scanners/product_hunt.py"
      provides: "Stub ProductHuntScanner — classe válida com scan_niche() retornando []"
      exports: ["ProductHuntScanner"]
    - path: "mis/scanners/udemy.py"
      provides: "Stub UdemyScanner — classe válida com scan_niche() retornando []"
      exports: ["UdemyScanner"]
  key_links:
    - from: "mis/tests/test_product_hunt_scanner.py"
      to: "mis/tests/fixtures/product_hunt/trending_today.json"
      via: "Path(__file__).parent / 'fixtures/product_hunt/trending_today.json'"
      pattern: "trending_today\\.json"
    - from: "mis/tests/test_udemy_scanner.py"
      to: "mis/tests/fixtures/udemy/courses_marketing.json"
      via: "Path(__file__).parent / 'fixtures/udemy/courses_marketing.json'"
      pattern: "courses_marketing\\.json"
---

<objective>
Wave 0 TDD: criar fixtures JSON, scaffolds de teste (RED) e stubs de scanner para ProductHunt e Udemy.

Purpose: Estabelecer o contrato de teste antes da implementação — ciclo RED obrigatório do TDD. Os testes devem falhar porque os stubs retornam [] (correto para um stub); os testes de happy_path falharão por ausência de lógica de parse.
Output: 6 fixtures/stubs e 2 arquivos de teste com 12 testes totais, todos em estado RED adequado para o ciclo GREEN no plano 15-02.
</objective>

<execution_context>
@C:/Users/Gabriel/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Gabriel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-international-api-based/15-CONTEXT.md
@.planning/phases/15-international-api-based/15-RESEARCH.md
@.planning/phases/15-international-api-based/15-VALIDATION.md

<interfaces>
<!-- Contratos extraídos do codebase — executor usa diretamente sem explorar -->

From mis/scanner.py:
```python
@dataclass
class Product:
    external_id: str
    title: str
    url: str
    platform_id: int
    niche_id: int
    rank: int
    price: Optional[float] = None
    commission_pct: Optional[float] = None
    rating: Optional[float] = None
    thumbnail_url: Optional[str] = None

class PlatformScanner(ABC):
    async def scan_niche(self, niche_slug: str, platform_slug: str, niche_id: int = 0) -> list[Product]: ...
```

From mis/platform_ids.py:
```python
PRODUCT_HUNT_PLATFORM_ID = 8
UDEMY_PLATFORM_ID = 9
```

From mis/tests/test_braip_scanner.py (modelo de teste — seguir este padrão):
```python
import pytest, respx
from pathlib import Path
from httpx import Response
import structlog.testing

FIXTURE_PATH = Path(__file__).parent / "fixtures/braip/catalog_cursos-online.html"
SCAN_URL = "https://marketplace.braip.com/search"

@pytest.mark.asyncio
@respx.mock
async def test_happy_path():
    content = FIXTURE_PATH.read_text(encoding="utf-8")
    respx.get(SCAN_URL).mock(return_value=Response(200, text=content))
    async with BraipScanner() as scanner:
        result = await scanner.scan_niche("marketing-digital", "cursos-online", niche_id=2)
    assert len(result) >= 1
    assert result[0].external_id != ""
    assert result[0].rank == 1
```

From mis/tests/ conftest.py fixture pattern for sqlite tmp:
```python
# Tests that need DB use tmp_path fixture from pytest:
import sqlite_utils
def get_tmp_db(tmp_path):
    db_path = str(tmp_path / "test.db")
    # run migrations inline or use mis.db.create_db
    return db_path
```
</interfaces>
</context>

<feature>
  <name>Wave 0 — Fixtures + Test Scaffolds + Stubs</name>
  <files>
    mis/tests/fixtures/product_hunt/trending_today.json,
    mis/tests/fixtures/udemy/courses_marketing.json,
    mis/tests/test_product_hunt_scanner.py,
    mis/tests/test_udemy_scanner.py,
    mis/scanners/product_hunt.py,
    mis/scanners/udemy.py
  </files>
  <behavior>
    ProductHuntScanner tests:
    - test_happy_path: respx.mock para https://api.producthunt.com/v2/api/graphql retornando trending_today.json — espera len(result) >= 1, result[0].rank == 1, result[0].platform_id == PRODUCT_HUNT_PLATFORM_ID, result[0].external_id == 'jarvis-ai-assistant'
    - test_field_types: rank is int, external_id is non-empty str, thumbnail_url is str or None, price is None (PH não retorna preço)
    - test_missing_credentials: sem PRODUCT_HUNT_API_TOKEN no os.environ -> result == [], log contém alert='missing_credentials'
    - test_empty_results: respx retorna {"data": {"posts": {"edges": [], "pageInfo": {"hasNextPage": false, "endCursor": null}}}} -> result == []
    - test_upsert_no_duplicates: criar produto via upsert_product() -> reinserir mesmo external_id com rank diferente -> SELECT COUNT(*) == 1, rank atualizado
    - test_is_stale: upsert produto -> mark_stale(platform_id, niche_id) -> is_stale==1; upsert novamente -> is_stale==0

    UdemyScanner tests:
    - test_happy_path: respx.mock para https://www.udemy.com/api-2.0/courses/ retornando courses_marketing.json — espera len(result) >= 1, result[0].rank == 1, result[0].platform_id == UDEMY_PLATFORM_ID, result[0].url.startswith("https://www.udemy.com")
    - test_field_types: rank is int, rating is float or None, price is float or None, thumbnail_url is str or None
    - test_missing_credentials: sem UDEMY_CLIENT_ID ou UDEMY_CLIENT_SECRET -> result == [], log contém alert='missing_credentials'
    - test_empty_results: respx retorna {"count": 0, "results": []} -> result == []
    - test_api_discontinued_fallback: respx retorna HTTP 401 -> result == [], log contém alert='api_discontinued'
    - test_upsert_no_duplicates: mesma verificação do ProductHunt mas com UDEMY_PLATFORM_ID
  </behavior>
  <implementation>
    RED cycle: Criar stubs mínimos que satisfazem o contrato ABC mas NÃO implementam a lógica de parse. Os testes test_happy_path e test_field_types devem falhar (resultado [] vindo do stub). Os testes test_missing_credentials passam porque o stub pode implementar apenas a checagem de credencial.

    Stubs:
    - mis/scanners/product_hunt.py: classe ProductHuntScanner(PlatformScanner) com scan_niche() que apenas checa se PRODUCT_HUNT_API_TOKEN está presente; se ausente, return [] com log.warning(alert='missing_credentials'); caso contrário também return [] (sem parse)
    - mis/scanners/udemy.py: UdemyScanner(PlatformScanner) com scan_niche() que checa UDEMY_CLIENT_ID + UDEMY_CLIENT_SECRET; se ausente, return [] com alert='missing_credentials'; tenta GET com Basic Auth; em HTTP 401/403/404 retorna [] com alert='api_discontinued'; caso contrário return [] (sem parse real)

    Fixtures: Criar diretórios mis/tests/fixtures/product_hunt/ e mis/tests/fixtures/udemy/ com arquivos JSON seguindo estrutura da RESEARCH.md (seção "Fixture JSON — estrutura esperada"). Fixtures devem ter >= 2 produtos para que testes de paginação possam ser escritos.

    Commit RED: `test(15-01): add failing test scaffolds for ProductHuntScanner + UdemyScanner`
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fixtures JSON + Stubs de scanner</name>
  <files>
    mis/tests/fixtures/product_hunt/trending_today.json,
    mis/tests/fixtures/udemy/courses_marketing.json,
    mis/scanners/product_hunt.py,
    mis/scanners/udemy.py
  </files>
  <behavior>
    - Fixture product_hunt/trending_today.json: estrutura GraphQL com edges[] contendo 3 produtos, pageInfo com endCursor e hasNextPage=true para primeira page; cada node tem id, name, tagline, slug, url (producthunt.com/posts/...), votesCount, thumbnail.url (nao imageUrl)
    - Fixture udemy/courses_marketing.json: results[] com 3 cursos, cada um com id (int), title, url (/course/slug/), price_detail.amount, avg_rating, image_480x270
    - mis/scanners/product_hunt.py: stub ProductHuntScanner(PlatformScanner) — scan_niche() checa PRODUCT_HUNT_API_TOKEN; se ausente log.warning(alert='missing_credentials') + return []; se presente faz POST GraphQL para PRODUCT_HUNT_GRAPHQL_URL mas retorna [] (sem parse) — stub intencional para TDD RED
    - mis/scanners/udemy.py: stub UdemyScanner(PlatformScanner) — scan_niche() checa UDEMY_CLIENT_ID + UDEMY_CLIENT_SECRET; se ausente return [] + alert='missing_credentials'; faz GET https://www.udemy.com/api-2.0/courses/ com Basic Auth; captura httpx.HTTPStatusError e retorna [] com alert='api_discontinued' para 401/403/404; caso contrario return [] (sem parse) — stub intencional
    - CRITICO: thumbnail deve usar `thumbnail["url"]` nao `thumbnail["imageUrl"]` (schema real do Product Hunt usa Media.url)
    - CRITICO: price=None para todos os produtos do ProductHunt (API nao retorna valor monetario)
    - CRITICO: url da Udemy: sempre prefixar "https://www.udemy.com" + course["url"] (API retorna path relativo)
  </behavior>
  <action>
    1. Criar diretorio mis/tests/fixtures/product_hunt/ com trending_today.json seguindo estrutura da secao "Fixture JSON" da RESEARCH.md. Incluir 3 produtos: slugs 'jarvis-ai-assistant', 'notion-ai-plus', 'cursor-ide'; hasNextPage=true; endCursor base64 valido.

    2. Criar diretorio mis/tests/fixtures/udemy/ com courses_marketing.json: 3 cursos com IDs 1234567, 7654321, 9876543; urls como /course/complete-digital-marketing-course/; price_detail.amount entre 12.99 e 29.99; avg_rating entre 4.2 e 4.8.

    3. Criar mis/scanners/product_hunt.py:
       - PRODUCT_HUNT_GRAPHQL_URL = "https://api.producthunt.com/v2/api/graphql"
       - TRENDING_TODAY_QUERY: query GraphQL completa conforme RESEARCH.md (com thumbnail { url } nao imageUrl)
       - Classe ProductHuntScanner(PlatformScanner)
       - scan_niche() stub: checa PRODUCT_HUNT_API_TOKEN via os.environ.get; se ausente log.warning(alert='missing_credentials') return []; faz POST graphql (mas sem parse — return [] intencional)
       - Metodo _post_graphql() copiando padrao do ClickBankScanner mas com Authorization Bearer token

    4. Criar mis/scanners/udemy.py:
       - UDEMY_API_URL = "https://www.udemy.com/api-2.0/courses/"
       - Classe UdemyScanner(PlatformScanner)
       - scan_niche() stub: checa UDEMY_CLIENT_ID + UDEMY_CLIENT_SECRET; se ausente alert='missing_credentials' return []; monta Basic Auth com base64.b64encode(f"{id}:{secret}".encode()).decode(); GET via self._base._client; captura httpx.HTTPStatusError: se status 401/403/404 -> log.warning(alert='api_discontinued') return []; else return [] (sem parse)
  </action>
  <verify>
    <automated>MISSING — Wave 0 deve criar mis/tests/test_product_hunt_scanner.py e mis/tests/test_udemy_scanner.py primeiro</automated>
  </verify>
  <done>
    - mis/tests/fixtures/product_hunt/trending_today.json existe com JSON valido e 3 produtos
    - mis/tests/fixtures/udemy/courses_marketing.json existe com JSON valido e 3 cursos
    - mis/scanners/product_hunt.py: ProductHuntScanner importavel, scan_niche() retorna [] para credenciais ausentes
    - mis/scanners/udemy.py: UdemyScanner importavel, scan_niche() retorna [] para credenciais ausentes e HTTP 401
    - `python -c "from mis.scanners.product_hunt import ProductHuntScanner; from mis.scanners.udemy import UdemyScanner; print('ok')"` passa sem erro
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test scaffolds RED — test_product_hunt_scanner.py + test_udemy_scanner.py</name>
  <files>
    mis/tests/test_product_hunt_scanner.py,
    mis/tests/test_udemy_scanner.py
  </files>
  <behavior>
    ProductHuntScanner (6 tests):
    - test_happy_path: respx.mock POST para PRODUCT_HUNT_GRAPHQL_URL retornando trending_today.json -> espera len(result) >= 1, result[0].rank == 1, result[0].platform_id == PRODUCT_HUNT_PLATFORM_ID, result[0].external_id == 'jarvis-ai-assistant' — DEVE FALHAR com stubs (retornam [])
    - test_field_types: rank is int, price is None, thumbnail_url is str or None — DEVE FALHAR com stubs
    - test_missing_credentials: monkeypatch.delenv("PRODUCT_HUNT_API_TOKEN", raising=False) -> result == [], logs contem alert='missing_credentials' — DEVE PASSAR com stubs
    - test_empty_results: respx retorna edges=[], hasNextPage=false -> result == [] — comportamento dependera de implementacao
    - test_upsert_no_duplicates: usa tmp_path, cria DB real, upsert produto PRODUCT_HUNT_PLATFORM_ID niche_id=1 rank=1; upsert novamente rank=2; SELECT COUNT(*)==1, rank==2
    - test_is_stale: upsert -> mark_stale(PRODUCT_HUNT_PLATFORM_ID, niche_id=1) -> is_stale==1; upsert novamente -> is_stale==0

    UdemyScanner (6 tests):
    - test_happy_path: respx.mock GET UDEMY_API_URL retornando courses_marketing.json + env UDEMY_CLIENT_ID/SECRET setados -> len(result) >= 1, result[0].rank == 1, result[0].platform_id == UDEMY_PLATFORM_ID, result[0].url.startswith("https://www.udemy.com") — DEVE FALHAR com stubs
    - test_field_types: rating is float or None, price is float or None, external_id is str — DEVE FALHAR com stubs
    - test_missing_credentials: sem UDEMY_CLIENT_ID/SECRET -> result == [], alert='missing_credentials'
    - test_empty_results: respx retorna {"count": 0, "results": []} -> result == []
    - test_api_discontinued_fallback: respx retorna HTTP 401 -> result == [], log contem alert='api_discontinued' — DEVE PASSAR com stubs
    - test_upsert_no_duplicates: mesma verificacao de upsert com UDEMY_PLATFORM_ID
  </behavior>
  <action>
    Escrever os dois arquivos de teste ANTES das implementacoes completas (TDD RED). Seguir exatamente o padrao de test_braip_scanner.py:
    - import pytest, respx, structlog.testing
    - from pathlib import Path
    - from httpx import Response
    - FIXTURE_PATH aponta para os JSON criados na Task 1
    - respx.mock para POST (product_hunt) e GET (udemy)
    - Para testes de credenciais: usar monkeypatch.delenv() ou monkeypatch.setenv()
    - Para testes de upsert/is_stale: importar mis.db, mis.product_repository

    Estrutura do mock respx para POST GraphQL:
    ```python
    PRODUCT_HUNT_GRAPHQL_URL = "https://api.producthunt.com/v2/api/graphql"
    respx.post(PRODUCT_HUNT_GRAPHQL_URL).mock(
        return_value=Response(200, json=json.loads(fixture_path.read_text()))
    )
    ```

    Estrutura do mock respx para GET Udemy:
    ```python
    UDEMY_API_URL = "https://www.udemy.com/api-2.0/courses/"
    respx.get(UDEMY_API_URL).mock(
        return_value=Response(200, json=json.loads(fixture_path.read_text()))
    )
    ```

    Para test_api_discontinued_fallback (Udemy):
    ```python
    respx.get(UDEMY_API_URL).mock(return_value=Response(401, json={"detail": "Authentication failed"}))
    ```

    Para testes de upsert/is_stale: usar tmp_path para criar DB temporario via mis.db.create_db() ou aplicar migrations minimas.

    Confirmar que test_missing_credentials e test_api_discontinued_fallback passam (stubs ja implementam esses paths). Os outros testes de happy_path/field_types ficam RED.
  </action>
  <verify>
    <automated>cd /c/Users/Gabriel/MEGABRAIN/mis && python -m pytest tests/test_product_hunt_scanner.py tests/test_udemy_scanner.py -x --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>
    - mis/tests/test_product_hunt_scanner.py existe com 6 testes
    - mis/tests/test_udemy_scanner.py existe com 6 testes
    - test_missing_credentials (ambos) VERDE — stubs ja retornam [] com alert correto
    - test_api_discontinued_fallback (udemy) VERDE — stub captura HTTP 401
    - test_happy_path (ambos) VERMELHO — stubs retornam [] mas testes esperam produtos
    - Commit RED: `test(15-01): add failing test scaffolds for ProductHuntScanner + UdemyScanner`
  </done>
</task>

</tasks>

<verification>
Verificar que os stubs sao importaveis e os arquivos de fixture sao JSON validos:
```
python -c "from mis.scanners.product_hunt import ProductHuntScanner; from mis.scanners.udemy import UdemyScanner; print('imports ok')"
python -c "import json; json.load(open('mis/tests/fixtures/product_hunt/trending_today.json')); json.load(open('mis/tests/fixtures/udemy/courses_marketing.json')); print('fixtures ok')"
cd mis && python -m pytest tests/test_product_hunt_scanner.py::test_missing_credentials tests/test_udemy_scanner.py::test_missing_credentials tests/test_udemy_scanner.py::test_api_discontinued_fallback -v
```
</verification>

<success_criteria>
- Fixtures JSON validos existem para ambas as plataformas
- 12 testes escritos (6 por scanner)
- Testes de credenciais ausentes e api_discontinued VERDES com stubs
- Testes de happy_path VERMELHOS (confirmando ciclo RED do TDD)
- Sem erros de importacao em mis/scanners/product_hunt.py e mis/scanners/udemy.py
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/15-international-api-based/15-01-SUMMARY.md`
</output>
