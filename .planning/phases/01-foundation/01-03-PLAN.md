---
phase: 01-foundation
plan: "03"
type: execute
wave: 2
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - mis/config.yaml
  - mis/config.py
  - mis/scheduler.py
  - mis/tests/test_config.py
autonomous: true
requirements:
  - FOUND-03

must_haves:
  truths:
    - "config.yaml com 3 nichos carrega sem erro via load_config()"
    - "config.yaml com 6 nichos levanta ValueError com mensagem legivel"
    - "PROXY_URL em .env sobrescreve settings.proxy_url do config.yaml"
    - "APScheduler skeleton inicializa sem erro e aceita jobs registrados"
    - "config.py expoe load_config() que retorna dict com niches e settings"
  artifacts:
    - path: "mis/config.yaml"
      provides: "configuracao editavel pelo usuario com 3 nichos exemplo e settings"
      contains: "niches, settings"
    - path: "mis/config.py"
      provides: "load_config() com validacao de 3-5 nichos e override de .env"
      exports: ["load_config"]
    - path: "mis/scheduler.py"
      provides: "APScheduler AsyncIOScheduler skeleton com funcoes start/stop e registro de jobs"
      exports: ["get_scheduler", "start_scheduler", "stop_scheduler"]
  key_links:
    - from: "mis/config.py"
      to: "mis/config.yaml"
      via: "Path(__file__).parent / 'config.yaml' — caminho relativo ao modulo"
      pattern: "CONFIG_PATH"
    - from: "mis/scheduler.py"
      to: "mis/config.py"
      via: "load_config() para ler request_delay_s e max_retries do scheduler"
      pattern: "load_config"
---

<objective>
Implementar o sistema de configuracao de nichos (config.yaml + config.py) e o skeleton do APScheduler. O usuario edita config.yaml para definir 3-5 nichos alvo. O scheduler skeleton sera expandido na Fase 2 com jobs reais — aqui so precisa inicializar corretamente e aceitar registros de job.

Purpose: FOUND-03 exige que nichos sejam configurados por arquivo e referenciados consistentemente por todos os modulos. O scheduler skeleton e necessario para a Fase 2 sem retrabalho.

Output: mis/config.yaml editavel com 3 nichos exemplo, mis/config.py com validacao, mis/scheduler.py como skeleton funcional. 3 testes test_config.py passando.
</objective>

<execution_context>
@C:/Users/Gabriel/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Gabriel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md

<interfaces>
<!-- Contratos disponiveis para este plano -->

De mis/exceptions.py (criado em 01-01):
```python
class ScraperError(Exception):
    def __init__(self, url: str, attempts: int, cause: Exception) -> None: ...
```

De mis/base_scraper.py (criado em 01-02):
```python
DOMAIN_DELAYS: dict[str, float]  # {'hotmart.com': 2.0, ...}
DEFAULT_DELAY: float  # 2.0

class BaseScraper:
    def __init__(self, proxy_url: Optional[str] = None) -> None: ...
    async def __aenter__(self) -> "BaseScraper": ...
    async def __aexit__(self, *_) -> None: ...
    async def fetch(self, url: str) -> str: ...
    async def fetch_spa(self, url: str) -> str: ...
```

Estrutura esperada de config.yaml (da RESEARCH.md):
```yaml
niches:
  - name: "Marketing Digital"
    slug: "marketing-digital"
    keywords: [...]
settings:
  proxy_url: ""
  request_delay_s: 2.0
  max_retries: 3
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Config loader com validacao de nichos</name>
  <files>
    mis/config.yaml,
    mis/config.py,
    mis/tests/test_config.py
  </files>
  <behavior>
    - test_load_3_niches: cria config.yaml temporario (fixture temp_config_yaml do conftest) com 3 nichos; load_config() retorna dict com key "niches" de len=3 sem levantar excecao
    - test_too_many_niches: config.yaml com 6 nichos; load_config() levanta ValueError com mensagem contendo "3-5 niches" ou similar
    - test_proxy_env_override: config.yaml com proxy_url="" e PROXY_URL="http://proxy:8080" em os.environ; load_config() retorna cfg["settings"]["proxy_url"] == "http://proxy:8080"
    - load_config() valida que cada niche tem pelo menos "name" e "slug" (nao aceita niches sem slug)
    - load_config() usa CONFIG_PATH = Path(__file__).parent / "config.yaml" por padrao, mas aceita parametro opcional config_path para facilitar testes
  </behavior>
  <action>
    RED: Substituir stubs de test_config.py pelos testes reais.

    1. Escrever mis/tests/test_config.py:
       - Usar fixture temp_config_yaml do conftest que cria config.yaml temporario
       - test_load_3_niches: chamar load_config(config_path=temp_yaml_path); assert len(result["niches"]) == 3
       - test_too_many_niches: criar yaml com 6 nichos em tmp; pytest.raises(ValueError, match="3-5")
       - test_proxy_env_override: monkeypatch.setenv("PROXY_URL", "http://proxy:8080"); chamar load_config(); assert result["settings"]["proxy_url"] == "http://proxy:8080"

    Confirmar RED: pytest mis/tests/test_config.py -x falha com ImportError.

    GREEN: Implementar.

    2. Criar mis/config.yaml com 3 nichos exemplo (Marketing Digital, Emagrecimento, Financas Pessoais) e settings completos. IMPORTANTE: config.yaml contem APENAS dados de configuracao, NUNCA credenciais ou PROXY_URL real.

    3. Criar mis/config.py:
       ```python
       from pathlib import Path
       import os
       import yaml
       from dotenv import load_dotenv

       load_dotenv()

       CONFIG_PATH = Path(__file__).parent / "config.yaml"

       def load_config(config_path: Path | None = None) -> dict:
           path = config_path or CONFIG_PATH
           with open(path, encoding="utf-8") as f:
               cfg = yaml.safe_load(f)
           niches = cfg.get("niches", [])
           if not (3 <= len(niches) <= 5):
               raise ValueError(f"config.yaml must define 3-5 niches, got {len(niches)}")
           for niche in niches:
               if not niche.get("slug"):
                   raise ValueError(f"Each niche must have a 'slug' field. Missing in: {niche}")
           proxy = os.getenv("PROXY_URL", "").strip()
           if proxy:
               cfg.setdefault("settings", {})["proxy_url"] = proxy
           return cfg
       ```
  </action>
  <verify>
    <automated>cd /c/Users/Gabriel/MEGABRAIN && python -m pytest mis/tests/test_config.py -v --timeout=10</automated>
  </verify>
  <done>3 testes test_config.py passando: test_load_3_niches, test_too_many_niches, test_proxy_env_override.</done>
</task>

<task type="auto">
  <name>Task 2: APScheduler skeleton</name>
  <files>
    mis/scheduler.py
  </files>
  <action>
    Criar mis/scheduler.py com skeleton do APScheduler. Nao precisa de testes unitarios proprios — sera testado integralmente na Fase 2. O objetivo aqui e ter uma interface limpa que o health monitor (01-04) possa usar sem retrabalho.

    Implementar:
    ```python
    # mis/scheduler.py
    """APScheduler skeleton for MIS.

    Phase 1: Only the health check job is registered.
    Phase 2+: Platform scraper jobs are added here.
    """
    from apscheduler.schedulers.asyncio import AsyncIOScheduler
    from apscheduler.events import EVENT_JOB_ERROR, EVENT_JOB_EXECUTED
    import structlog

    log = structlog.get_logger()

    _scheduler: AsyncIOScheduler | None = None


    def get_scheduler() -> AsyncIOScheduler:
        """Return the global scheduler instance, creating it if needed."""
        global _scheduler
        if _scheduler is None:
            _scheduler = AsyncIOScheduler()
            _scheduler.add_listener(_on_job_event, EVENT_JOB_ERROR | EVENT_JOB_EXECUTED)
        return _scheduler


    def start_scheduler() -> None:
        """Start the scheduler. Safe to call multiple times."""
        scheduler = get_scheduler()
        if not scheduler.running:
            scheduler.start()
            log.info("scheduler.started")


    def stop_scheduler() -> None:
        """Stop the scheduler gracefully."""
        global _scheduler
        if _scheduler and _scheduler.running:
            _scheduler.shutdown(wait=False)
            log.info("scheduler.stopped")
        _scheduler = None


    def _on_job_event(event) -> None:
        if hasattr(event, "exception") and event.exception:
            log.error("scheduler.job.error",
                      job_id=event.job_id,
                      exception=str(event.exception))
        else:
            log.info("scheduler.job.executed", job_id=event.job_id)
    ```

    Verificar que o modulo importa sem erro e que AsyncIOScheduler pode ser instanciado:
    `python -c "from mis.scheduler import get_scheduler; s = get_scheduler(); print('scheduler OK:', type(s).__name__)"`
  </action>
  <verify>
    <automated>cd /c/Users/Gabriel/MEGABRAIN && python -c "from mis.scheduler import get_scheduler, start_scheduler, stop_scheduler; s = get_scheduler(); print('OK:', type(s).__name__)"</automated>
  </verify>
  <done>mis/scheduler.py importa sem erro. get_scheduler() retorna AsyncIOScheduler. start_scheduler() e stop_scheduler() existem e sao chamáveis. Modulo logado com structlog.</done>
</task>

</tasks>

<verification>
Suite completa de config e scheduler:

```
cd /c/Users/Gabriel/MEGABRAIN && python -m pytest mis/tests/test_config.py -v --timeout=10
```

Verificar que config.yaml tem exatamente 3 nichos e carrega via caminho padrao:
```
python -c "from mis.config import load_config; cfg = load_config(); print('nichos:', [n['slug'] for n in cfg['niches']])"
```

Verificar que scheduler inicia e para sem erro:
```
python -c "
import asyncio
from mis.scheduler import start_scheduler, stop_scheduler

async def test():
    start_scheduler()
    print('started: OK')
    stop_scheduler()
    print('stopped: OK')

asyncio.run(test())
"
```
</verification>

<success_criteria>
- mis/config.yaml com 3 nichos exemplo (Marketing Digital, Emagrecimento, Financas Pessoais) e settings completos
- mis/config.py exporta load_config(config_path=None) com validacao 3-5 nichos e override PROXY_URL
- load_config() aceita parametro config_path opcional para facilitar testes sem mockar PATH
- mis/scheduler.py exporta get_scheduler(), start_scheduler(), stop_scheduler()
- Scheduler usa APScheduler 3.x AsyncIOScheduler
- Listener de eventos loga erros de job via structlog
- 3 testes test_config.py passando
- python import sem erro em todos os modulos novos
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/01-foundation/01-03-SUMMARY.md` com:
- Assinatura de load_config() e o que valida
- Estrutura do config.yaml (fields obrigatorios por niche)
- Interface do scheduler (3 funcoes exportadas)
- Como adicionar jobs na Fase 2 (exemplo de scheduler.add_job)
- Status dos 3 testes test_config.py
</output>
