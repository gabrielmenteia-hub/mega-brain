---
phase: 11-health-monitor-wiring-tech-debt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mis/web/app.py
  - mis/health_monitor.py
  - mis/base_scraper.py
  - mis/mis_agent.py
  - mis/scheduler.py
  - mis/scanners/hotmart.py
  - mis/scanners/clickbank.py
  - mis/scanners/kiwify.py
  - mis/intelligence/dossier_generator.py
  - mis/radar/quora_collector.py
  - mis/radar/reddit_collector.py
  - mis/radar/synthesizer.py
  - mis/radar/trends_collector.py
  - mis/radar/youtube_collector.py
  - mis/radar/__init__.py
  - mis/tests/test_lifespan.py
  - mis/tests/test_scanner_jobs.py
autonomous: true
requirements:
  - FOUND-02
  - FOUND-04

must_haves:
  truths:
    - "run_schema_integrity_check() é chamado no startup do servidor (lifespan) e alerta quando tabelas estão faltando"
    - "run_platform_canary() dispara automaticamente via APScheduler para as 3 plataformas"
    - "fetch_spa() usa _select_proxy() para selecionar proxy da lista (igual ao fetch() httpx)"
    - "_compute_health() em mis_agent.py não usa asyncio.run() — é async def segura para contexto async"
    - "register_scanner_jobs() e run_*_scan helpers removidos de scheduler.py e scanners/"
    - "datetime.utcnow() substituído por datetime.now(timezone.utc) nos 7 arquivos MIS afetados"
    - "Suite completa de testes continua green após todas as mudanças"
  artifacts:
    - path: "mis/health_monitor.py"
      provides: "register_platform_canary_jobs(db_path) — nova função que registra 3 jobs no APScheduler"
      exports: ["register_platform_canary_jobs", "run_schema_integrity_check", "run_platform_canary", "register_canary_job"]
    - path: "mis/web/app.py"
      provides: "lifespan com run_schema_integrity_check + register_platform_canary_jobs"
      contains: "await run_schema_integrity_check(db_path)"
    - path: "mis/mis_agent.py"
      provides: "_compute_health como async def com await run_canary_check()"
      contains: "async def _compute_health"
    - path: "mis/base_scraper.py"
      provides: "fetch_spa() usando _select_proxy() em vez de self._proxy"
      contains: "_select_proxy()"
  key_links:
    - from: "mis/web/app.py lifespan"
      to: "mis/health_monitor.run_schema_integrity_check"
      via: "await direto no bloco try/except separado"
    - from: "mis/web/app.py lifespan for-loop"
      to: "mis/health_monitor.register_platform_canary_jobs"
      via: "tupla no for loop existente de job registrations"
    - from: "mis/mis_agent.get_briefing_data"
      to: "mis/mis_agent._compute_health"
      via: "asyncio.run(_compute_health(...)) — um único asyncio.run em vez de dois aninhados"
---

<objective>
Fechar todos os gaps de observabilidade identificados no audit v1.0 — nenhum módulo novo é construído, apenas código existente é conectado, corrigido e limpo.

Purpose: FOUND-02 (proxy rotation em Playwright) e FOUND-04 (health monitor em produção) permanecem pending nos requirements. Esta fase os fecha definitivamente, além de eliminar 3 bugs latentes e remover dead code.

Output:
- run_schema_integrity_check() e run_platform_canary() no caminho de execução de produção
- fetch_spa() com proxy rotation real (igual ao fetch() httpx)
- _compute_health() async-safe (sem asyncio.run aninhado)
- register_scanner_jobs() e run_*_scan helpers removidos
- datetime.utcnow() eliminado dos 7 arquivos MIS
- Suite de testes green
</objective>

<execution_context>
@C:/Users/Gabriel/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Gabriel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-health-monitor-wiring-tech-debt/11-CONTEXT.md

<interfaces>
<!-- Interfaces extraídas do código existente — executor usa diretamente sem explorar codebase -->

De mis/health_monitor.py:
```python
async def run_canary_check() -> bool: ...
def register_canary_job() -> None: ...  # usa interval=15min, id="health_canary", replace_existing=True
async def run_platform_canary(db_path: str, platform_id: int, platform_name: str, threshold_hours: int = 25) -> bool: ...
async def run_schema_integrity_check(db_path: str) -> bool: ...  # já existe, nunca propaga exceções
```

De mis/base_scraper.py:
```python
def _select_proxy(self) -> Optional[str]: ...  # já existe, usado em fetch() linha 131
self._proxy: Optional[str]  # usado em fetch_spa() linha 193 — ERRADO, trocar por _select_proxy()
# fetch_spa linha 193 atual: proxy={"server": self._proxy} if self._proxy else None
# fetch_spa linha 193 novo: selected = self._select_proxy(); proxy={"server": selected} if selected else None
```

De mis/web/app.py (lifespan atual):
```python
# Imports atuais na linha 13-15:
from mis.health_monitor import register_canary_job
from mis.radar import register_radar_jobs
from mis.scheduler import get_scheduler, register_scan_and_spy_job

# For loop atual (linhas 50-52):
for name, fn, args in [
    ("register_scan_and_spy_job", register_scan_and_spy_job, [config]),
    ("register_radar_jobs", register_radar_jobs, [config]),
    ("register_canary_job", register_canary_job, []),
]:

# Schema check deve ser ANTES do for loop, depois de run_migrations() (se houver)
# Import a adicionar: run_schema_integrity_check, register_platform_canary_jobs
```

De mis/mis_agent.py:
```python
# Atual (linha 195):
def _compute_health(db_path, last_cycle, data_stale, unseen_alerts, run_canary_check) -> dict: ...
    scraper_ok = asyncio.run(run_canary_check())  # linha 216 — PROBLEMA

# Novo:
async def _compute_health(db_path, last_cycle, data_stale, unseen_alerts, run_canary_check) -> dict: ...
    scraper_ok = await run_canary_check()  # sem asyncio.run

# get_briefing_data() (sync) continua usando:
health = asyncio.run(_compute_health(...))  # um único asyncio.run no nível certo
```

De mis/scheduler.py (dead code a remover):
```python
# Linhas 145-175: register_scanner_jobs(config) — remover função inteira
# Linhas 158-160: imports de run_hotmart_scan, run_clickbank_scan, run_kiwify_scan — remover
# Linha 4 docstring: "Phase 2: Platform scanner jobs (register_scanner_jobs)." — atualizar
```

De mis/scanners/hotmart.py linha 192, clickbank.py linha 301, kiwify.py linha 302:
```python
async def run_hotmart_scan(config: dict) -> None: ...   # remover
async def run_clickbank_scan(config: dict) -> None: ... # remover
async def run_kiwify_scan(config: dict) -> None: ...    # remover
```

datetime.utcnow() nos 7 arquivos afetados:
```
mis/intelligence/dossier_generator.py:83  — from datetime import datetime → add timezone
mis/radar/quora_collector.py:93
mis/radar/reddit_collector.py:70          — já tem timezone no import
mis/radar/synthesizer.py:160              — já tem timedelta, adicionar timezone
mis/radar/trends_collector.py:78
mis/radar/youtube_collector.py:41,65,145  — já tem timedelta, adicionar timezone
mis/radar/__init__.py:112                 — já tem timezone no import
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire observability into production (lifespan + APScheduler)</name>
  <files>
    mis/health_monitor.py,
    mis/web/app.py,
    mis/tests/test_lifespan.py
  </files>
  <behavior>
    - register_platform_canary_jobs(db_path) registra 3 jobs: canary_hotmart (platform_id=1), canary_clickbank (platform_id=2), canary_kiwify (platform_id=3), intervalo 25h, replace_existing=True
    - scheduler.get_jobs() retorna len==3 depois de chamar register_platform_canary_jobs(db_path)
    - lifespan chama await run_schema_integrity_check(db_path) ANTES do for loop de job registrations
    - falha em run_schema_integrity_check loga warning mas não aborta startup (soft-fail)
    - test_lifespan_registers_jobs_on_startup verifica chamada de run_schema_integrity_check E register_platform_canary_jobs
    - mock de run_schema_integrity_check como AsyncMock (é async def)
  </behavior>
  <action>
    1. Em mis/health_monitor.py — adicionar função register_platform_canary_jobs(db_path: str) -> None:
       - Importa get_scheduler() (lazy import igual a register_canary_job)
       - Plataformas: [(1, "hotmart"), (2, "clickbank"), (3, "kiwify")]
       - Para cada plataforma: scheduler.add_job(run_platform_canary, trigger="interval", hours=25, id=f"canary_{name}", kwargs={"db_path": db_path, "platform_id": pid, "platform_name": name, "threshold_hours": 25}, replace_existing=True)
       - log.info("health.platform_canary.registered", platforms=3)
       - Não exportar em mis/__init__.py (consistente com register_canary_job)

    2. Em mis/web/app.py — atualizar import e lifespan:
       - Linha 13: from mis.health_monitor import register_canary_job, register_platform_canary_jobs, run_schema_integrity_check
       - Antes do for loop de job registrations, adicionar bloco separado:
         ```python
         try:
             await run_schema_integrity_check(db_path)
         except Exception as exc:
             _log.warning("lifespan.schema_check_failed", error=str(exc))
         ```
       - No for loop existente, adicionar tupla:
         ("register_platform_canary_jobs", register_platform_canary_jobs, [db_path])
       - db_path vem do closure de create_app(db_path)

    3. Em mis/tests/test_lifespan.py — atualizar test_lifespan_registers_jobs_on_startup:
       - Adicionar patches: patch("mis.web.app.run_schema_integrity_check") como AsyncMock e patch("mis.web.app.register_platform_canary_jobs")
       - Adicionar asserts: mock_schema_check.assert_called_once_with(db_path) e mock_platform_canary.assert_called_once()
       - Adicionar novo teste test_register_platform_canary_jobs_registers_3_jobs: usar APScheduler fresh (BackgroundScheduler ou mock), chamar register_platform_canary_jobs(db_path), assert len(jobs)==3, assert job IDs incluem canary_hotmart, canary_clickbank, canary_kiwify
  </action>
  <verify>
    <automated>cd C:/Users/Gabriel/MEGABRAIN/mis && python -m pytest tests/test_lifespan.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>
    - register_platform_canary_jobs() existe em health_monitor.py e registra 3 jobs
    - Lifespan chama run_schema_integrity_check com await antes dos job registrations
    - test_lifespan_registers_jobs_on_startup passa verificando ambas as novas funções
    - Novo teste de register_platform_canary_jobs green
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix latent bugs (fetch_spa proxy + _compute_health async)</name>
  <files>
    mis/base_scraper.py,
    mis/mis_agent.py,
    mis/tests/test_base_scraper.py,
    mis/tests/test_mis_agent.py
  </files>
  <behavior>
    - fetch_spa() chama _select_proxy() no início e usa o resultado no proxy kwarg do Playwright (não self._proxy)
    - Quando _select_proxy() retorna None (proxy_list vazio): proxy=None (comportamento atual mantido)
    - Quando _select_proxy() retorna uma string: proxy={"server": selected}
    - _compute_health() é async def e usa await run_canary_check() (sem asyncio.run interno)
    - get_briefing_data() continua sync, usa asyncio.run(_compute_health(...)) — um único asyncio.run no nível correto
    - Testes de _compute_health usam AsyncMock para run_canary_check (não MagicMock)
  </behavior>
  <action>
    1. Em mis/base_scraper.py — corrigir fetch_spa():
       - Na linha 176 (def fetch_spa), antes de abrir o browser, adicionar:
         selected = self._select_proxy()
       - Na linha 193, trocar:
         proxy={"server": self._proxy} if self._proxy else None
         por:
         proxy={"server": selected} if selected else None
       - Nenhuma outra mudança em fetch_spa()

    2. Em mis/mis_agent.py — corrigir _compute_health():
       - Trocar "def _compute_health(" por "async def _compute_health("
       - Trocar "scraper_ok = asyncio.run(run_canary_check())" por "scraper_ok = await run_canary_check()"
       - Manter try/except em volta do await para capturar exceções
       - Na função get_briefing_data() onde _compute_health é chamado (linha ~167):
         trocar "health = _compute_health(...)" por "health = asyncio.run(_compute_health(...))"
       - Verificar que asyncio import ainda existe no topo do arquivo

    3. Em mis/tests/test_base_scraper.py — adicionar/atualizar teste de fetch_spa proxy:
       - Novo teste test_fetch_spa_uses_select_proxy_not_self_proxy:
         - Instanciar BaseScraper(proxy_list=["http://p1:8080", "http://p2:8080"])
         - Mock playwright.async_api.async_playwright como AsyncMock context manager
         - Capturar kwargs de chromium.launch()
         - Assert que proxy kwarg contém {"server": <string da lista>} (não self._proxy que seria None)

    4. Em mis/tests/test_mis_agent.py — atualizar testes de _compute_health:
       - Trocar MagicMock() por AsyncMock() para o parâmetro run_canary_check
       - Adicionar await / usar asyncio.run() nos calls de teste para _compute_health (é async agora)
       - Verificar que os asserts existentes continuam válidos com a nova assinatura async
  </action>
  <verify>
    <automated>cd C:/Users/Gabriel/MEGABRAIN/mis && python -m pytest tests/test_base_scraper.py tests/test_mis_agent.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>
    - fetch_spa() usa _select_proxy() — proxy rotation funciona para Playwright igual ao httpx
    - _compute_health() é async def com await run_canary_check() sem asyncio.run aninhado
    - Testes de fetch_spa proxy e _compute_health async passam
  </done>
</task>

<task type="auto">
  <name>Task 3: Remove dead code and replace deprecated datetime</name>
  <files>
    mis/scheduler.py,
    mis/scanners/hotmart.py,
    mis/scanners/clickbank.py,
    mis/scanners/kiwify.py,
    mis/intelligence/dossier_generator.py,
    mis/radar/quora_collector.py,
    mis/radar/reddit_collector.py,
    mis/radar/synthesizer.py,
    mis/radar/trends_collector.py,
    mis/radar/youtube_collector.py,
    mis/radar/__init__.py,
    mis/tests/test_scanner_jobs.py
  </files>
  <action>
    PARTE A — Remover dead code:

    1. Em mis/scheduler.py:
       - Remover função register_scanner_jobs(config) inteira (linhas ~145-175)
       - Remover imports órfãos das linhas ~158-160 (from mis.scanners.hotmart import run_hotmart_scan, etc.)
         NOTA: esses imports estão DENTRO da função — serão removidos junto com ela
       - Atualizar docstring do módulo (linha 4): remover "Phase 2: Platform scanner jobs (register_scanner_jobs)."
         substituir por texto que reflita realidade atual (register_scan_and_spy_job + register_radar_jobs)

    2. Em mis/scanners/hotmart.py:
       - Remover função async def run_hotmart_scan(config: dict) -> None (linha ~192 até o fim do arquivo)

    3. Em mis/scanners/clickbank.py:
       - Remover função async def run_clickbank_scan(config: dict) -> None (linha ~301 até o fim)

    4. Em mis/scanners/kiwify.py:
       - Remover função async def run_kiwify_scan(config: dict) -> None (linha ~302 até o fim)

    5. Deletar mis/tests/test_scanner_jobs.py completamente (testa código que foi removido)

    PARTE B — Substituir datetime.utcnow():

    Para cada arquivo, substituir datetime.utcnow() por datetime.now(timezone.utc) e garantir que timezone está no import:

    - mis/intelligence/dossier_generator.py linha 12: from datetime import datetime → from datetime import datetime, timezone
      linha 83: datetime.utcnow().isoformat() → datetime.now(timezone.utc).isoformat()

    - mis/radar/quora_collector.py linha 11: from datetime import datetime → from datetime import datetime, timezone
      linha 93: datetime.utcnow().isoformat() → datetime.now(timezone.utc).isoformat()

    - mis/radar/reddit_collector.py linha 13: já tem timezone no import
      linha 70: datetime.utcnow().isoformat() → datetime.now(timezone.utc).isoformat()

    - mis/radar/synthesizer.py linha 11: from datetime import datetime, timedelta → from datetime import datetime, timedelta, timezone
      linha 160: datetime.utcnow().isoformat() → datetime.now(timezone.utc).isoformat()

    - mis/radar/trends_collector.py linha 13: from datetime import datetime → from datetime import datetime, timezone
      linha 78: datetime.utcnow().isoformat() → datetime.now(timezone.utc).isoformat()

    - mis/radar/youtube_collector.py linha 11: from datetime import datetime, timedelta → from datetime import datetime, timedelta, timezone
      linhas 41, 65, 145: datetime.utcnow() → datetime.now(timezone.utc)

    - mis/radar/__init__.py linha 7: já tem timezone no import
      linha 112: datetime.utcnow() → datetime.now(timezone.utc)

    PARTE C — Atualizar testes que assertam timestamp strings:
    Procurar testes que fazem assert em strings de timestamp format '2026-...' sem '+00:00'
    e atualizar para aceitar o novo formato com timezone offset.
    Comando de busca: grep -rn "utcnow\|isoformat" mis/tests/ para encontrar casos afetados.
  </action>
  <verify>
    <automated>cd C:/Users/Gabriel/MEGABRAIN/mis && python -m pytest -x -q 2>&1 | tail -30</automated>
  </verify>
  <done>
    - register_scanner_jobs() removido de scheduler.py
    - run_hotmart_scan, run_clickbank_scan, run_kiwify_scan removidos dos scanners
    - test_scanner_jobs.py deletado
    - datetime.utcnow() substituído por datetime.now(timezone.utc) nos 7 arquivos
    - Suite completa de testes green (zero failures)
  </done>
</task>

</tasks>

<verification>
Verificação final da fase completa:

```bash
cd C:/Users/Gabriel/MEGABRAIN/mis && python -m pytest -x -q 2>&1 | tail -20
```

Checklist manual:
- grep -n "utcnow" mis/**/*.py — deve retornar zero resultados dentro de mis/ (exceto core/)
- grep -n "register_scanner_jobs\|run_hotmart_scan\|run_clickbank_scan\|run_kiwify_scan" mis/scheduler.py mis/scanners/*.py — deve retornar zero resultados
- grep -n "asyncio.run(run_canary_check" mis/mis_agent.py — deve retornar zero resultados
- grep -n "self._proxy" mis/base_scraper.py — deve aparecer apenas no __aenter__ (httpx client), não em fetch_spa()
- grep -n "run_schema_integrity_check\|register_platform_canary_jobs" mis/web/app.py — deve encontrar ambos
</verification>

<success_criteria>
1. run_schema_integrity_check(db_path) é chamado no lifespan do servidor com await em bloco try/except separado — log warning se falhar, continua startup
2. register_platform_canary_jobs(db_path) está no for loop do lifespan — 3 jobs APScheduler criados (canary_hotmart, canary_clickbank, canary_kiwify) com intervalo de 25h
3. fetch_spa() usa selected = self._select_proxy() no início e proxy={"server": selected} if selected else None — proxy rotation funciona em Playwright igual ao httpx
4. _compute_health() é async def com scraper_ok = await run_canary_check() — sem asyncio.run aninhado; get_briefing_data() usa asyncio.run(_compute_health(...)) uma única vez
5. register_scanner_jobs() e as 3 funções run_*_scan removidas; test_scanner_jobs.py deletado
6. datetime.utcnow() substituído por datetime.now(timezone.utc) nos 7 arquivos MIS; timezone adicionado aos imports onde faltava
7. python -m pytest retorna 0 failures
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/11-health-monitor-wiring-tech-debt/11-01-SUMMARY.md`
</output>
