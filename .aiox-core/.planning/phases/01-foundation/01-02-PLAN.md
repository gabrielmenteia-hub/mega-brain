---
phase: 01-foundation
plan: 02
type: tdd
wave: 2
depends_on: ["01-01"]
files_modified:
  - nexus/rubrics/__init__.py
  - nexus/rubrics/copy_rubric.py
  - nexus/rubrics/tech_rubric.py
  - nexus/rubrics/compliance_rubric.py
  - nexus/rubrics/performance_rubric.py
  - tests/test_rubrics.py
autonomous: true
requirements: [FND-03]
must_haves:
  truths:
    - "Cada uma das 4 rubricas tem dimension, criteria e few_shot_examples"
    - "Todo criterio tem name, weight e levels com ancoras numericas (10 e 2 no minimo)"
    - "Pesos dos criterios somam 1.0 em cada rubrica"
    - "Cada rubrica tem pelo menos 2 few-shot examples com input, expected_score, expected_total, expected_approved e reasoning"
    - "Pelo menos 1 example aprovado e 1 reprovado por rubrica"
    - "expected_approved e consistente com expected_total vs threshold do NexusConfig"
  artifacts:
    - path: "nexus/rubrics/__init__.py"
      provides: "Export central das 4 rubricas e ALL_RUBRICS list"
      exports: ["COPY_RUBRIC", "TECH_RUBRIC", "COMPLIANCE_RUBRIC", "PERFORMANCE_RUBRIC", "ALL_RUBRICS"]
    - path: "nexus/rubrics/copy_rubric.py"
      provides: "Rubrica de qualidade de copy com 4 criterios (hook, cta, length_fit, persuasion)"
      exports: ["COPY_RUBRIC"]
    - path: "nexus/rubrics/tech_rubric.py"
      provides: "Rubrica de qualidade tecnica com criterios visuais e de audio"
      exports: ["TECH_RUBRIC"]
    - path: "nexus/rubrics/compliance_rubric.py"
      provides: "Rubrica de compliance Meta Ads com politicas e restricoes"
      exports: ["COMPLIANCE_RUBRIC"]
    - path: "nexus/rubrics/performance_rubric.py"
      provides: "Rubrica de predicao de performance com metricas de engagement"
      exports: ["PERFORMANCE_RUBRIC"]
    - path: "tests/test_rubrics.py"
      provides: "Testes estruturais para todas as rubricas"
      min_lines: 60
  key_links:
    - from: "nexus/rubrics/__init__.py"
      to: "nexus/rubrics/*_rubric.py"
      via: "import e re-export de ALL_RUBRICS"
      pattern: "ALL_RUBRICS.*=.*\\["
    - from: "nexus/rubrics/*_rubric.py"
      to: "nexus/config.py"
      via: "few-shot expected_approved alinhado com thresholds do NexusConfig"
      pattern: "expected_approved"
---

<objective>
Criar as 4 rubricas de revisao com criterios objetivos e exemplos few-shot para calibracao dos agentes.

Purpose: FND-03 garante que os agentes revisores da Phase 2 tenham ancoras claras para scoring. Sem rubricas estruturadas com exemplos, os agentes produzem scores inconsistentes (scoring drift). Cada rubrica e a "prova" que o agente segue para julgar um criativo.

Output: 4 rubric files em nexus/rubrics/ + test suite + export central via __init__.py
</objective>

<execution_context>
@C:/Users/Gabriel/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Gabriel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<interfaces>
<!-- Tipos criados no Plan 01-01 que este plan usa para alinhar thresholds -->

From nexus/config.py:
```python
class NexusConfig(BaseSettings):
    min_copy_score: int = 7
    min_tech_score: int = 6
    min_compliance_score: int = 8
    min_performance_score: int = 6
```

From nexus/models.py:
```python
class AgentScore(BaseModel):
    dimension: str = Field(description="Review dimension: copy|tech|compliance|performance")
    score: int = Field(ge=1, le=10)
    approved: bool
    feedback: str = Field(min_length=10)
    strengths: list[str]
    issues: list[str]
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED tests para estrutura das rubricas + stub files</name>
  <files>tests/test_rubrics.py, nexus/rubrics/__init__.py, nexus/rubrics/copy_rubric.py, nexus/rubrics/tech_rubric.py, nexus/rubrics/compliance_rubric.py, nexus/rubrics/performance_rubric.py</files>
  <behavior>
    - test_all_rubrics_have_required_fields: cada rubrica em ALL_RUBRICS tem "dimension", "criteria", "few_shot_examples" e len(few_shot_examples) >= 2
    - test_all_criteria_have_numeric_levels: cada criterio tem "name", "weight", "levels" e levels tem keys int incluindo 10 e 2
    - test_criteria_weights_sum_to_one: soma dos weights de cada rubrica == 1.0 (tolerancia 0.01)
    - test_few_shot_examples_have_expected_fields: cada example tem "input", "expected_score", "expected_total", "expected_approved", "reasoning"
    - test_at_least_one_approved_and_one_rejected_per_rubric: cada rubrica tem pelo menos 1 example com expected_approved=True e 1 com expected_approved=False
    - test_expected_approved_consistent_with_threshold: para copy rubric, example com expected_total >= 7 tem expected_approved=True e vice-versa (usar thresholds do NexusConfig defaults)
  </behavior>
  <action>
    1. Criar tests/test_rubrics.py com os 6 testes acima. Usar exatamente os padroes da RESEARCH.md secao "Rubric Structure Test Pattern" como base, ACRESCENDO os 2 testes extras (approved/rejected coverage e threshold consistency).

    Para o teste de threshold consistency:
    ```python
    THRESHOLDS = {"copy": 7, "tech": 6, "compliance": 8, "performance": 6}
    def test_expected_approved_consistent_with_threshold():
        for rubric in ALL_RUBRICS:
            threshold = THRESHOLDS[rubric["dimension"]]
            for ex in rubric["few_shot_examples"]:
                if ex["expected_total"] >= threshold:
                    assert ex["expected_approved"] is True, ...
                else:
                    assert ex["expected_approved"] is False, ...
    ```

    2. Criar nexus/rubrics/__init__.py com stubs VAZIOS que farao os testes FALHAREM:
    ```python
    from nexus.rubrics.copy_rubric import COPY_RUBRIC
    from nexus.rubrics.tech_rubric import TECH_RUBRIC
    from nexus.rubrics.compliance_rubric import COMPLIANCE_RUBRIC
    from nexus.rubrics.performance_rubric import PERFORMANCE_RUBRIC

    ALL_RUBRICS = [COPY_RUBRIC, TECH_RUBRIC, COMPLIANCE_RUBRIC, PERFORMANCE_RUBRIC]
    ```

    3. Criar 4 stub files (copy_rubric.py, tech_rubric.py, compliance_rubric.py, performance_rubric.py), cada um com:
    ```python
    # Stub - to be implemented
    COPY_RUBRIC = {}  # (nome correspondente em cada arquivo)
    ```

    4. Rodar pytest tests/test_rubrics.py -- TODOS devem FALHAR (RED) com KeyError ou AssertionError (rubrics vazias)
  </action>
  <verify>
    <automated>cd c:/Users/Gabriel/MEGABRAIN/.aiox-core && python -m pytest tests/test_rubrics.py -x -q 2>&1 | head -20</automated>
  </verify>
  <done>tests/test_rubrics.py tem 6 testes. nexus/rubrics/ tem 5 arquivos (__init__ + 4 stubs). Todos os testes falham (RED).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — Implementar as 4 rubricas completas</name>
  <files>nexus/rubrics/copy_rubric.py, nexus/rubrics/tech_rubric.py, nexus/rubrics/compliance_rubric.py, nexus/rubrics/performance_rubric.py</files>
  <behavior>
    - Todos os 6 testes RED da Task 1 devem passar (GREEN)
    - Suite completa (models + config + rubrics) deve passar
  </behavior>
  <action>
    1. Implementar nexus/rubrics/copy_rubric.py — COPIAR a estrutura completa da RESEARCH.md Pattern 3 (COPY_RUBRIC). Ja tem 4 criterios (hook, cta, length_fit, persuasion) com weights somando 1.0 e 2 few-shot examples. Verificar que expected_approved e consistente com threshold 7.

    2. Implementar nexus/rubrics/tech_rubric.py — TECH_RUBRIC com:
       - dimension: "tech"
       - 4 criterios relevantes para qualidade tecnica de video publicitario:
         - visual_quality (weight=0.3): clareza visual, resolucao, composicao
         - audio_quality (weight=0.3): clareza de voz, volume adequado, sem ruido
         - sync (weight=0.2): sincronizacao audio-video, lip sync, timing
         - format_compliance (weight=0.2): duracao correta, aspect ratio, resolucao minima
       - Levels com ancoras numericas (10, 8, 6, 4, 2) para cada criterio
       - 2+ few-shot examples (1 aprovado com total >= 6, 1 reprovado com total < 6)
       - Threshold do NexusConfig para tech: 6

    3. Implementar nexus/rubrics/compliance_rubric.py — COMPLIANCE_RUBRIC com:
       - dimension: "compliance"
       - 4 criterios para conformidade com politicas Meta Ads:
         - prohibited_content (weight=0.3): sem claims medicos, financeiros ou enganosos
         - targeting_alignment (weight=0.2): linguagem adequada ao publico-alvo
         - disclosure (weight=0.2): disclaimers quando necessario
         - community_standards (weight=0.3): respeita normas da comunidade Meta
       - Levels com ancoras numericas (10, 8, 6, 4, 2)
       - 2+ few-shot examples (1 aprovado com total >= 8, 1 reprovado com total < 8)
       - Threshold do NexusConfig para compliance: 8 (MAIS RIGOROSO)

    4. Implementar nexus/rubrics/performance_rubric.py — PERFORMANCE_RUBRIC com:
       - dimension: "performance"
       - 4 criterios para predicao de performance no Meta Ads:
         - scroll_stopping (weight=0.3): probabilidade de parar o scroll nos primeiros 3s
         - engagement_potential (weight=0.25): probabilidade de gerar comentarios/shares
         - click_intent (weight=0.25): forca do CTA e motivacao para clicar
         - audience_match (weight=0.2): aderencia ao avatar/nicho especificado
       - Levels com ancoras numericas (10, 8, 6, 4, 2)
       - 2+ few-shot examples (1 aprovado com total >= 6, 1 reprovado com total < 6)
       - Threshold do NexusConfig para performance: 6

    5. REGRAS PARA TODAS AS RUBRICAS:
       - Weights DEVEM somar exatamente 1.0
       - Levels DEVEM incluir pelo menos 10 e 2
       - few_shot_examples DEVEM ter pelo menos 2 (1 aprovado, 1 reprovado)
       - expected_approved DEVE ser consistente com expected_total vs threshold
       - Textos dos levels devem ser descritivos e especificos ao dominio (Meta Ads, video publicitario)
       - Input dos few-shot deve ser realista (scripts de anuncio em PT-BR)

    6. Rodar suite completa: pytest tests/ -v --tb=short — TODOS devem PASSAR
  </action>
  <verify>
    <automated>cd c:/Users/Gabriel/MEGABRAIN/.aiox-core && python -m pytest tests/ -v --tb=short</automated>
  </verify>
  <done>4 rubricas completas em nexus/rubrics/. Cada uma tem dimension, criteria (weights=1.0), levels (10 e 2 minimo), 2+ few-shot examples (1 aprovado, 1 reprovado). Thresholds alinhados com NexusConfig defaults. Suite completa (models + config + rubrics) GREEN.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/ -v --tb=short` — suite completa GREEN (models + config + rubrics)
- `python -c "from nexus.rubrics import ALL_RUBRICS; print(f'{len(ALL_RUBRICS)} rubrics loaded')"` — imprime "4 rubrics loaded"
- `python -c "from nexus.rubrics import ALL_RUBRICS; assert all(sum(c['weight'] for c in r['criteria']) == 1.0 for r in ALL_RUBRICS); print('weights OK')"` — weights somam 1.0
- `python -c "from nexus.rubrics import ALL_RUBRICS; assert all(len(r['few_shot_examples']) >= 2 for r in ALL_RUBRICS); print('few-shot OK')"` — 2+ examples cada
</verification>

<success_criteria>
- 4 rubricas (copy, tech, compliance, performance) com criterios objetivos e ancoras numericas
- Cada rubrica tem 2+ few-shot examples com 1 aprovado e 1 reprovado
- Weights de criterios somam 1.0 em cada rubrica
- expected_approved alinhado com thresholds do NexusConfig (copy=7, tech=6, compliance=8, performance=6)
- Suite completa de testes (models + config + rubrics) passa em menos de 5 segundos
- Imports funcionam: from nexus.rubrics import ALL_RUBRICS
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
