---
phase: 01-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - mis/__init__.py
  - mis/exceptions.py
  - mis/db.py
  - mis/migrations/001_initial.py
  - mis/pytest.ini
  - mis/requirements.txt
  - mis/requirements-dev.txt
  - mis/tests/__init__.py
  - mis/tests/conftest.py
  - mis/tests/test_db.py
  - mis/tests/test_base_scraper.py
  - mis/tests/test_config.py
  - mis/tests/test_health_monitor.py
autonomous: true
requirements:
  - FOUND-01

must_haves:
  truths:
    - "mis.db existe com 5 tabelas: products, platforms, niches, pains, dossiers"
    - "migrations rodam duas vezes sem erro (idempotencia)"
    - "chave estrangeira invalida em products.platform_id levanta IntegrityError"
    - "suite de testes completa (stubs + conftest) existe e passa no pytest -x"
  artifacts:
    - path: "mis/db.py"
      provides: "funcao run_migrations() e get_db() usando sqlite-utils"
      exports: ["run_migrations", "get_db"]
    - path: "mis/migrations/001_initial.py"
      provides: "script de migration idempotente com 5 tabelas"
      contains: "platforms, niches, products, pains, dossiers"
    - path: "mis/exceptions.py"
      provides: "ScraperError custom exception com url, attempts, cause"
      exports: ["ScraperError"]
    - path: "mis/pytest.ini"
      provides: "configuracao pytest com asyncio_mode = auto"
      contains: "asyncio_mode = auto"
    - path: "mis/tests/conftest.py"
      provides: "fixtures compartilhadas: db_path, mock_scraper, temp_config"
    - path: "mis/tests/test_db.py"
      provides: "testes FOUND-01: tabelas, idempotencia, foreign keys"
  key_links:
    - from: "mis/db.py"
      to: "mis/migrations/001_initial.py"
      via: "import e chamada de run_migrations()"
      pattern: "from .migrations"
    - from: "mis/tests/conftest.py"
      to: "mis/db.py"
      via: "fixture db_path passa path in-memory para get_db()"
      pattern: ":memory:"
---

<objective>
Criar a fundacao de dados do MIS: schema do banco de dados SQLite com 5 tabelas, sistema de migration idempotente via sqlite-utils, ScraperError custom exception, e toda a infraestrutura de testes (pytest.ini, conftest, stubs de teste) que os planos subsequentes precisam para ter cobertura automatizada.

Purpose: FOUND-01 exige que o schema exista e seja migravel. Os planos 01-02, 01-03 e 01-04 dependem dos stubs de teste e fixtures do conftest para ter <verify><automated> funcional desde o Wave 1.

Output: mis/ como modulo Python instalavel com schema funcional, mis/tests/ com stubs prontos para serem preenchidos pelos planos seguintes.
</objective>

<execution_context>
@C:/Users/Gabriel/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Gabriel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0 (Wave 0): Criar infraestrutura de testes e requirements</name>
  <files>
    mis/pytest.ini,
    mis/requirements.txt,
    mis/requirements-dev.txt,
    mis/__init__.py,
    mis/tests/__init__.py,
    mis/tests/conftest.py,
    mis/tests/test_db.py,
    mis/tests/test_base_scraper.py,
    mis/tests/test_config.py,
    mis/tests/test_health_monitor.py
  </files>
  <behavior>
    - pytest.ini: asyncio_mode = auto, testpaths = tests, timeout padrao = 10
    - requirements.txt: httpx[http2]>=0.28.1, playwright>=1.58.0, playwright-stealth>=2.0.2, tenacity>=9.1.4, structlog>=25.5.0, fake-useragent>=2.2.0, sqlite-utils>=3.39, apscheduler>=3.11.2, PyYAML>=6.0.3, python-dotenv>=1.2.1
    - requirements-dev.txt: pytest>=8.4.2, pytest-asyncio>=0.24,<1.0, respx>=0.21.0
    - conftest.py: fixture db_path retorna path para DB in-memory (":memory:" ou tmp_path/mis.db), fixture temp_config_yaml cria config.yaml temporario com 3 nichos validos
    - test_db.py: stubs FAILING com pytest.fail("not implemented") para test_all_tables_exist, test_migration_idempotent, test_foreign_key_constraint
    - test_base_scraper.py: stubs FAILING para test_fetch_success, test_fetch_retries_on_429, test_rate_limiting, test_headers_not_default, test_scraper_error_raised, test_client_closed_on_exit
    - test_config.py: stubs FAILING para test_load_3_niches, test_too_many_niches, test_proxy_env_override
    - test_health_monitor.py: stubs FAILING para test_canary_empty_response, test_canary_scraper_error, test_canary_healthy
  </behavior>
  <action>
    Criar toda a estrutura de arquivos ANTES de qualquer implementacao.

    1. Criar mis/__init__.py vazio.
    2. Criar mis/tests/__init__.py vazio.
    3. Criar mis/pytest.ini:
       ```ini
       [pytest]
       asyncio_mode = auto
       testpaths = tests
       timeout = 10
       ```
    4. Criar mis/requirements.txt com dependencias listadas em behavior.
    5. Criar mis/requirements-dev.txt com deps de dev.
    6. Criar mis/tests/conftest.py com fixtures db_path (usando tmp_path) e temp_config_yaml.
    7. Criar mis/tests/test_db.py com 3 stubs que executam pytest.fail("not implemented — implement after mis/db.py exists").
    8. Criar mis/tests/test_base_scraper.py com 6 stubs idem.
    9. Criar mis/tests/test_config.py com 3 stubs idem.
    10. Criar mis/tests/test_health_monitor.py com 3 stubs idem.

    IMPORTANTE: stubs devem FALHAR (nao SKIP) para que o TDD cycle RED seja verificavel.
    Use `pytest.fail("not implemented")` dentro de cada funcao de stub.
  </action>
  <verify>
    <automated>cd /c/Users/Gabriel/MEGABRAIN && python -m pytest mis/tests/ -x -q --timeout=10 2>&1 | head -30</automated>
  </verify>
  <done>pytest coleta todos os arquivos de teste sem ImportError. Todos os stubs estao RED (failed, nao error de import). pytest.ini presente e lido.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: ScraperError e schema do banco de dados</name>
  <files>
    mis/exceptions.py,
    mis/db.py,
    mis/migrations/001_initial.py,
    mis/tests/test_db.py
  </files>
  <behavior>
    - ScraperError(url: str, attempts: int, cause: Exception) — herda de Exception, armazena os 3 atributos, __str__ legivel
    - run_migrations(db_path: str) — idempotente: cria tabelas somente se nao existem; safe to run multiplas vezes
    - Tabelas: platforms (id, name, slug UNIQUE, base_url, created_at), niches (id, name, slug UNIQUE, created_at), products (id, platform_id FK->platforms, niche_id FK->niches, external_id, title, url, rank_score, price, currency, scraped_at, raw_data), pains (id, niche_id FK->niches, source, content, sentiment, detected_at), dossiers (id, product_id FK->products, analysis, opportunity_score, confidence_score, generated_at)
    - PRAGMA journal_mode=WAL e PRAGMA foreign_keys=ON aplicados em cada conexao
    - get_db(db_path: str) -> sqlite_utils.Database — helper de conexao que aplica os PRAGMAs
    - test_all_tables_exist: chama run_migrations() em db in-memory, verifica 5 tabelas presentes
    - test_migration_idempotent: chama run_migrations() duas vezes, nao levanta excecao
    - test_foreign_key_constraint: tenta inserir produto com platform_id=9999 (inexistente), verifica IntegrityError
  </behavior>
  <action>
    RED: Os stubs em test_db.py ja estao falhando. Substituir pytest.fail() pelos testes reais antes de implementar.

    1. Escrever tests em mis/tests/test_db.py (substituindo stubs):
       - test_all_tables_exist: usa fixture db_path (tmp_path/"mis.db"), chama run_migrations(), assert set(db.table_names()) == {"platforms","niches","products","pains","dossiers"}
       - test_migration_idempotent: chama run_migrations() 2x, nao levanta excecao, tabelas ainda existem
       - test_foreign_key_constraint: run_migrations(); tenta db["products"].insert({platform_id: 9999, ...}); espera sqlite3.IntegrityError

    2. Confirmar RED: pytest mis/tests/test_db.py -x deve falhar com ImportError ou NameError.

    GREEN: Implementar producao.

    3. Criar mis/exceptions.py:
       ```python
       class ScraperError(Exception):
           def __init__(self, url: str, attempts: int, cause: Exception) -> None:
               self.url = url
               self.attempts = attempts
               self.cause = cause
               super().__init__(f"ScraperError after {attempts} attempts on {url}: {cause}")
       ```

    4. Criar mis/migrations/__init__.py vazio.

    5. Criar mis/migrations/001_initial.py com funcao run_migrations(db_path) usando o pattern sqlite-utils da RESEARCH.md. Idempotencia via `if "table_name" not in db.table_names()`. Aplicar WAL e foreign_keys ON.

    6. Criar mis/db.py:
       ```python
       from pathlib import Path
       import sqlite_utils
       from .migrations.001_initial import run_migrations

       def get_db(db_path: str) -> sqlite_utils.Database:
           db = sqlite_utils.Database(db_path)
           db.execute("PRAGMA journal_mode=WAL")
           db.execute("PRAGMA foreign_keys=ON")
           return db
       ```
       Exportar run_migrations e get_db de mis/db.py.

    REFACTOR: Verificar que ScraperError.__str__ e legivel antes de finalizar.
  </action>
  <verify>
    <automated>cd /c/Users/Gabriel/MEGABRAIN && python -m pytest mis/tests/test_db.py -v --timeout=10</automated>
  </verify>
  <done>3 testes test_db.py passando: test_all_tables_exist GREEN, test_migration_idempotent GREEN, test_foreign_key_constraint GREEN. ScraperError importavel de mis.exceptions.</done>
</task>

</tasks>

<verification>
Suite completa de DB e infraestrutura de testes:

```
pytest mis/tests/test_db.py -v --timeout=10
```

Deve mostrar 3 passed. Os stubs dos outros modulos (test_base_scraper, test_config, test_health_monitor) devem estar FAILED (nao ERROR) — isso confirma que a infraestrutura de coleta funciona e os modulos seguintes podem comecar em RED.

Verificar que mis/pytest.ini e lido:
```
cd mis && python -m pytest --co -q
```

Verificar que migration e idempotente:
```python
python -c "from mis.db import get_db; from mis.migrations._001_initial import run_migrations; run_migrations('/tmp/test.db'); run_migrations('/tmp/test.db'); print('idempotent OK')"
```
</verification>

<success_criteria>
- mis/ e um modulo Python valido (mis/__init__.py existe)
- mis/pytest.ini configura asyncio_mode=auto e testpaths=tests
- mis/requirements.txt e mis/requirements-dev.txt listam todas as dependencias corretas
- mis/exceptions.py exporta ScraperError com url, attempts, cause
- mis/db.py exporta run_migrations() e get_db()
- mis/migrations/001_initial.py cria 5 tabelas (platforms, niches, products, pains, dossiers) com FKs e indices
- run_migrations() e idempotente (segunda execucao nao falha)
- 3 testes test_db.py passando, stubs dos outros modulos em estado RED (pytest.fail)
- Nenhum ResourceWarning ou ImportError em pytest
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/01-foundation/01-01-SUMMARY.md` com:
- Arquivos criados e seus exports principais
- Schema das 5 tabelas (nome, PK, FKs, indices)
- Decisoes tomadas (ex: localizacao do DB, nomes de colunas)
- Estado dos stubs de teste (quantos RED aguardando implementacao)
</output>
