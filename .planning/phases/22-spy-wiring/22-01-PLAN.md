---
phase: 22-spy-wiring
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - mis/tests/test_spy_wiring.py
  - mis/tests/test_search_repository.py
  - mis/tests/test_dossier_routes.py
autonomous: true
requirements:
  - SPY-V3-01
  - SPY-V3-02
  - SPY-V3-03

must_haves:
  truths:
    - "test_spy_triggered_after_scan falha com AttributeError ou ImportError (run_spy_batch não chamado)"
    - "test_session_status_transitions falha porque status 'spying'/'spy_done' nunca são gravados"
    - "test_results_page_triggers_respy falha porque GET /results não dispara re-spy"
    - "test_fallback_platforms_excluded falha porque a lógica de exclusão não existe ainda"
    - "test_cancel_session_cancels_spy falha porque spy batch não está no _TASK_REGISTRY"
    - "test_list_session_products_with_dossier falha porque product_id e dossier_status não estão no SELECT"
    - "test_from_search_param falha porque dossier.py não aceita ?from_search"
    - "test_oferta_tab_renders falha porque dossier_tab_oferta.html não existe"
  artifacts:
    - path: "mis/tests/test_spy_wiring.py"
      provides: "Testes RED para SPY-V3-01 (spy trigger, status, re-spy, fallback, cancel)"
    - path: "mis/tests/test_search_repository.py"
      provides: "Teste RED para list_session_products_with_dossier"
    - path: "mis/tests/test_dossier_routes.py"
      provides: "Testes RED para ?from_search e tab Oferta"
  key_links:
    - from: "mis/tests/test_spy_wiring.py"
      to: "mis/search_orchestrator.py"
      via: "monkeypatch de run_spy_batch"
      pattern: "run_spy_batch"
    - from: "mis/tests/test_dossier_routes.py"
      to: "mis/web/routes/dossier.py"
      via: "TestClient GET /dossier/{id}?from_search={sid}"
      pattern: "from_search"
---

<objective>
Escrever todos os testes que devem FALHAR (RED) antes da implementação da Phase 22.

Purpose: TDD garante que o wiring de spy, as transições de status, o re-spy na página de resultados, a coluna de dossiê e a tab Oferta sejam todos implementados corretamente e verificáveis automaticamente.
Output: 3 arquivos de teste com 8 testes RED que falham por razões corretas (funcionalidade ausente, não erro de configuração).
</objective>

<execution_context>
@C:/Users/Gabriel/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Gabriel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/22-spy-wiring/22-CONTEXT.md
@.planning/phases/22-spy-wiring/22-RESEARCH.md
@mis/tests/test_spy_orchestrator.py
@mis/tests/test_search_orchestrator.py
@mis/tests/test_search_repository.py
</context>

<feature>
  <name>RED tests — Phase 22 spy wiring</name>
  <files>mis/tests/test_spy_wiring.py, mis/tests/test_search_repository.py, mis/tests/test_dossier_routes.py</files>
  <behavior>
    test_spy_triggered_after_scan: run_manual_search() deve chamar run_spy_batch() com top-5 produtos por plataforma após scan concluir → falha porque spy_batch não está wired ainda.

    test_session_status_transitions: após run_manual_search() concluir, session.status deve ser 'spy_done' (passando por 'spying') → falha porque apenas 'done' é gravado.

    test_results_page_triggers_respy: GET /search/{id}/results com produtos sem dossier deve disparar asyncio.create_task(run_spy_batch(...)) → falha porque não existe esse código em search.py.

    test_fallback_platforms_excluded: produtos de plataformas fallback-only (eduzz, monetizze, perfectpay) não devem entrar na lista de top-5 para spy → falha porque lógica de filtragem não existe.

    test_cancel_session_cancels_spy: DELETE /search/{id} deve cancelar o spy batch se estiver rodando → falha porque spy batch não é registrado no _TASK_REGISTRY.

    test_list_session_products_with_dossier: list_session_products() deve retornar product_id e dossier_status por produto → falha porque essas colunas não estão no SELECT.

    test_from_search_param: GET /dossier/{id}?from_search=42 deve incluir back_url="/search/42/results" e back_label="← Voltar aos resultados" no contexto do template → falha porque dossier.py não lê from_search.

    test_oferta_tab_renders: GET /dossier/{id}/tab/oferta deve retornar 200 e renderizar dossier_tab_oferta.html → falha porque 'oferta' não está em _VALID_TABS e o template não existe.
  </behavior>
  <implementation>
    Criar testes que falham por razão CORRETA:
    - Falhas de comportamento ausente (AssertionError, 404, KeyError) — não ImportError de módulo inexistente.
    - Usar fixtures existentes de conftest.py (app_client, tmp_db_path, etc.).
    - Monkeypatchar run_spy_batch para verificar se foi chamado com os produtos certos.
    - Para test_list_session_products_with_dossier: estender test_search_repository.py existente (não substituir).
    - Para test_dossier_routes.py: criar arquivo novo se não existir, ou estender se existir.
    - Verificar que TODOS os testes falham com `pytest mis/tests/test_spy_wiring.py mis/tests/test_search_repository.py::test_list_session_products_with_dossier mis/tests/test_dossier_routes.py::test_from_search_param mis/tests/test_dossier_routes.py::test_oferta_tab_renders -x` antes de commitar.
    - Commit RED: `test(22-01): add failing RED tests for spy wiring`
  </implementation>
</feature>

<verification>
`pytest mis/tests/test_spy_wiring.py mis/tests/test_search_repository.py::test_list_session_products_with_dossier mis/tests/test_dossier_routes.py::test_from_search_param mis/tests/test_dossier_routes.py::test_oferta_tab_renders -v 2>&1 | tail -20`

Resultado esperado: TODOS os 8 testes FALHAM (FAILED ou ERROR). Nenhum deve passar (PASSED seria sinal de teste mal escrito).

Suite completa não deve quebrar: `pytest mis/tests/ -x --ignore=mis/tests/test_spy_wiring.py` deve continuar verde.
</verification>

<success_criteria>
- 8 testes existem nos 3 arquivos
- Todos os 8 falham por razão correta (ausência de funcionalidade, não erro de importação)
- Suite existente permanece verde
- Commit RED gravado
</success_criteria>

<output>
After completion, create `.planning/phases/22-spy-wiring/22-01-SUMMARY.md`
</output>
