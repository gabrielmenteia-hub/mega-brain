---
phase: 14-br-scanners
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - mis/migrations/_007_is_stale.py
  - mis/db.py
  - mis/product_repository.py
  - mis/scanners/eduzz.py
  - mis/scanners/monetizze.py
  - mis/scanner.py
  - mis/tests/test_migration_007.py
  - mis/tests/test_eduzz_scanner.py
  - mis/tests/test_monetizze_scanner.py
  - mis/tests/fixtures/eduzz/.gitkeep
  - mis/tests/fixtures/monetizze/.gitkeep
autonomous: true
requirements:
  - SCAN-BR-01
  - SCAN-BR-02

must_haves:
  truths:
    - "EduzzScanner.scan_niche() retorna [] e emite alert='marketplace_unavailable' sem lançar exceção"
    - "MonetizzeScanner.scan_niche() retorna [] e emite alert='marketplace_unavailable' sem lançar exceção"
    - "Coluna is_stale existe na tabela products após run_migrations()"
    - "upsert_product() seta is_stale=False em cada produto persistido"
    - "mark_stale() seta is_stale=True em batch por (platform_id, niche_id)"
    - "EduzzScanner e MonetizzeScanner registrados em SCANNER_MAP em scanner.py"
    - "run_all_scanners() chama mark_stale() quando scan_niche() retorna [] para um scanner de fallback"
    - "Comando 'python -m mis scan --platform eduzz' executa sem exception (retorna [] com alert log emitido)"
  artifacts:
    - path: "mis/migrations/_007_is_stale.py"
      provides: "Migration idempotente que adiciona is_stale BOOLEAN DEFAULT FALSE"
      exports: ["run_migration_007"]
    - path: "mis/scanners/eduzz.py"
      provides: "EduzzScanner subclass de PlatformScanner"
      exports: ["EduzzScanner"]
    - path: "mis/scanners/monetizze.py"
      provides: "MonetizzeScanner subclass de PlatformScanner"
      exports: ["MonetizzeScanner"]
    - path: "mis/product_repository.py"
      provides: "upsert_product() + mark_stale() com is_stale support"
      exports: ["upsert_product", "mark_stale"]
    - path: "mis/tests/test_migration_007.py"
      provides: "5 testes de migration _007"
    - path: "mis/tests/test_eduzz_scanner.py"
      provides: "6 testes SCAN-BR-01"
    - path: "mis/tests/test_monetizze_scanner.py"
      provides: "6 testes SCAN-BR-02"
  key_links:
    - from: "mis/db.py"
      to: "mis/migrations/_007_is_stale.py"
      via: "import + chamada _run_007 em run_migrations()"
      pattern: "_run_007\\(db_path\\)"
    - from: "mis/scanner.py SCANNER_MAP"
      to: "mis/scanners/eduzz.py + mis/scanners/monetizze.py"
      via: "import + registro no dict SCANNER_MAP"
      pattern: "EduzzScanner|MonetizzeScanner"
    - from: "mis/scanners/eduzz.py"
      to: "mis/platform_ids.py"
      via: "from mis.platform_ids import EDUZZ_PLATFORM_ID"
      pattern: "EDUZZ_PLATFORM_ID"
    - from: "mis/scanner.py run_all_scanners() ou _run_one()"
      to: "mis/product_repository.mark_stale()"
      via: "chamada após scan_niche() retornar [] — necessário para is_stale ser setado em produção"
      pattern: "mark_stale\\(db,"
---

<objective>
Implementar EduzzScanner e MonetizzeScanner (fallback-only — sem marketplace público), migration _007_is_stale, e atualizar product_repository com is_stale support. Wiring completo de mark_stale() no pipeline de scan.

Purpose: Eduzz e Monetizze não possuem marketplace público acessível sem autenticação. Os scanners implementam o padrão de fallback estabelecido no CONTEXT.md: retornam [] + alert='marketplace_unavailable'. A migration _007 adiciona a coluna is_stale que permite ao sistema rastrear quais produtos ficaram sem atualização após um scan vazio. CRÍTICO: mark_stale() deve ser chamada pelo pipeline (run_all_scanners/_run_one) quando scan retorna [] — sem isso, is_stale só funciona nos testes unitários, não em produção.

Output: 2 scanners de fallback registrados, migration _007 fiada em db.py, product_repository com mark_stale() e upsert_product() atualizado, pipeline de scan atualizado para chamar mark_stale(), 17 testes GREEN (5 migration + 6 eduzz + 6 monetizze).
</objective>

<execution_context>
@C:/Users/Gabriel/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Gabriel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-br-scanners/14-CONTEXT.md
@.planning/phases/14-br-scanners/14-RESEARCH.md
@.planning/phases/14-br-scanners/14-VALIDATION.md

<interfaces>
<!-- Contratos que o executor precisa. Extraídos do código existente. -->
<!-- Não explorar o codebase — usar diretamente. -->

De mis/scanner.py:
```python
@dataclass
class Product:
    external_id: str
    title: str
    url: str
    platform_id: int
    niche_id: int
    rank: int
    price: Optional[float] = None
    commission_pct: Optional[float] = None
    rating: Optional[float] = None
    thumbnail_url: Optional[str] = None

class PlatformScanner(ABC):
    def __init__(self, proxy_url=None, proxy_list=None): ...
    async def __aenter__(self) -> "PlatformScanner": ...
    async def __aexit__(self, *args) -> None: ...
    async def fetch(self, url: str) -> str: ...  # delegado ao BaseScraper

    @abstractmethod
    async def scan_niche(
        self,
        niche_slug: str,
        platform_slug: str,
        niche_id: int = 0,
    ) -> list[Product]: ...
```

De mis/platform_ids.py (constantes confirmadas — não alterar):
```python
HOTMART_PLATFORM_ID    = 1
CLICKBANK_PLATFORM_ID  = 2
KIWIFY_PLATFORM_ID     = 3
EDUZZ_PLATFORM_ID      = 4
MONETIZZE_PLATFORM_ID  = 5
PERFECTPAY_PLATFORM_ID = 6
BRAIP_PLATFORM_ID      = 7
```

De mis/product_repository.py (estado ATUAL — antes desta phase):
```python
def upsert_product(db: sqlite_utils.Database, product: "Product") -> None:
    # UPDATE SET title, url, niche_id, rank, price, commission_pct, rating, thumbnail_url, updated_at
    # WHERE platform_id=? AND external_id=?
    # Se rowcount==0 → INSERT
    # ATENÇÃO: is_stale NÃO existe nesta versão — deve ser adicionado nesta phase
    ...

def save_batch(db: sqlite_utils.Database, products: list["Product"]) -> None:
    # guarda cada produto via upsert_product()
    ...
# mark_stale() NÃO existe — deve ser criada nesta phase
```

De mis/db.py (run_migrations atual — antes desta phase):
```python
from .migrations._006_v2_platforms import run_migration_006 as _run_006

def run_migrations(db_path: str) -> None:
    _run_001(db_path)
    _run_002(db_path)
    _run_003(db_path)
    _run_004(db_path)
    _run_005(db_path)
    _run_006(db_path)
    # _run_007 deve ser adicionado aqui
```

De mis/migrations/_006_v2_platforms.py (padrão a replicar):
```python
def run_migration_006(db_path: str) -> None:
    db = sqlite_utils.Database(db_path)
    if "platforms" in db.table_names():
        existing_cols = {col.name for col in db["platforms"].columns}
        if "rank_type" not in existing_cols:
            db["platforms"].add_column("rank_type", str)
    db.conn.commit()
```

De mis/tests/conftest.py:
```python
@pytest.fixture
def db_path(tmp_path):
    return str(tmp_path / "mis.db")
```

Padrão de log de fallback (de mis/scanners/hotmart.py):
```python
log = structlog.get_logger(__name__)
# Para marketplace_unavailable:
log.warning(
    "eduzz_scanner.marketplace_unavailable",
    alert="marketplace_unavailable",
    niche=niche_slug,
    platform_slug=platform_slug,
    reason="...",
)
```

SCANNER_MAP em mis/scanner.py (onde registrar):
```python
# Buscar por SCANNER_MAP no arquivo — adicionar EduzzScanner e MonetizzeScanner
SCANNER_MAP = {
    "kiwify": KiwifyScanner,
    "hotmart": HotmartScanner,
    "clickbank": ClickBankScanner,
    # "eduzz": EduzzScanner,       ← adicionar
    # "monetizze": MonetizzeScanner, ← adicionar
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration _007 + is_stale em product_repository + wiring em db.py</name>
  <files>
    mis/migrations/_007_is_stale.py,
    mis/product_repository.py,
    mis/db.py,
    mis/tests/test_migration_007.py
  </files>
  <behavior>
    - test_is_stale_column_added: após run_migrations(), SELECT is_stale FROM products WHERE 1=0 não levanta OperationalError (coluna existe)
    - test_migration_idempotent: chamar run_migration_007() duas vezes não levanta exceção
    - test_is_stale_default_false: produto inserido via upsert_product() tem is_stale=0 (False)
    - test_mark_stale_sets_true: mark_stale(db, platform_id, niche_id) seta is_stale=1 em todos os produtos daquela plataforma+nicho
    - test_upsert_resets_stale: produto com is_stale=1 recebe upsert → is_stale volta para 0
  </behavior>
  <action>
    1. CRIAR mis/migrations/_007_is_stale.py:
       - Função run_migration_007(db_path: str) -> None
       - sqlite_utils.Database(db_path)
       - Verificar "products" in db.table_names()
       - Verificar "is_stale" not in {col.name for col in db["products"].columns}
       - Se ausente: db["products"].add_column("is_stale", bool, not_null_default=False)
       - Sempre chamar db.conn.commit() no final (obrigatório — não auto-commitado pelo sqlite_utils)

    2. ATUALIZAR mis/product_repository.py (ler o arquivo antes de editar):
       a) Em upsert_product() — bloco UPDATE: adicionar "is_stale = 0" no SET (coluna booleana, 0 = False)
       b) Em upsert_product() — bloco INSERT: adicionar "is_stale": 0 no dict
       c) ADICIONAR nova função mark_stale() após save_batch():
          ```python
          def mark_stale(db: sqlite_utils.Database, platform_id: int, niche_id: int) -> None:
              """Mark all products for a platform+niche as is_stale=True.

              Called by fallback scanners when scan_niche() returns [].
              Preserves existing data — apenas marca como desatualizado.

              Args:
                  db:          sqlite-utils Database instance.
                  platform_id: FK da plataforma.
                  niche_id:    FK do nicho.
              """
              db.execute(
                  "UPDATE products SET is_stale = 1 WHERE platform_id = ? AND niche_id = ?",
                  [platform_id, niche_id],
              )
              log.info(
                  "product_repository.mark_stale",
                  platform_id=platform_id,
                  niche_id=niche_id,
              )
          ```

    3. ATUALIZAR mis/db.py (ler antes de editar):
       - Adicionar import: from .migrations._007_is_stale import run_migration_007 as _run_007
       - Em run_migrations(): adicionar _run_007(db_path) após _run_006(db_path)

    4. CRIAR mis/tests/test_migration_007.py com os testes do bloco <behavior>.
       Usar fixture db_path de conftest.py.
       Importar: from mis.migrations._007_is_stale import run_migration_007
       Importar: from mis.db import run_migrations, get_db
       Importar: from mis.product_repository import upsert_product, mark_stale
       Importar: from mis.scanner import Product
       Usar platform_id=7 (Braip) e niche_id=1 nos testes de upsert/mark_stale
       (inserir row em platforms e niches com INSERT OR IGNORE antes de usar FK).
  </action>
  <verify>
    <automated>python -m pytest mis/tests/test_migration_007.py -x -q</automated>
  </verify>
  <done>
    5 testes GREEN em test_migration_007.py. Coluna is_stale existe no schema. upsert_product seta is_stale=0. mark_stale seta is_stale=1. db.py chama _run_007 em run_migrations().
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: EduzzScanner + MonetizzeScanner (fallback-only) + registro em SCANNER_MAP + mark_stale wiring em run_all_scanners</name>
  <files>
    mis/scanners/eduzz.py,
    mis/scanners/monetizze.py,
    mis/scanner.py,
    mis/tests/test_eduzz_scanner.py,
    mis/tests/test_monetizze_scanner.py,
    mis/tests/fixtures/eduzz/.gitkeep,
    mis/tests/fixtures/monetizze/.gitkeep
  </files>
  <behavior>
    Para EduzzScanner (6 testes — fallback-only):
    - test_happy_path: scan_niche() retorna []
    - test_field_types: retorno é list (pode ser vazia) — sem TypeError
    - test_fallback_selector: N/A para fallback-only — testar que [] retornado é mesmo [] (type check)
    - test_drift_alert: capture_logs() mostra alert='marketplace_unavailable' após scan_niche()
    - test_upsert_no_duplicates: scan_niche() retorna [] → mark_stale() chamado → nenhuma row nova inserida no DB
    - test_is_stale: upsert produto → mark_stale() → is_stale=True; upsert produto novamente → is_stale=False

    Para MonetizzeScanner: estrutura idêntica ao EduzzScanner (6 testes com MONETIZZE_PLATFORM_ID=5).
  </behavior>
  <action>
    1. CRIAR mis/scanners/eduzz.py:
       ```python
       """Eduzz platform scanner — fallback only.

       Eduzz migrou para orbita.eduzz.com (SPA React). Vitrine de afiliados
       requer autenticação. Não há marketplace público acessível.

       Implementa fallback puro: scan_niche() retorna [] com alert='marketplace_unavailable'.
       Dados existentes no DB são marcados is_stale=True via mark_stale().
       Quando marketplace for liberado: apenas reimplementar scan_niche().
       """
       from __future__ import annotations
       import structlog
       from mis.scanner import PlatformScanner, Product
       from mis.platform_ids import EDUZZ_PLATFORM_ID

       log = structlog.get_logger(__name__)

       class EduzzScanner(PlatformScanner):
           """Eduzz scanner — marketplace requer autenticação. Implementa fallback."""

           async def scan_niche(
               self,
               niche_slug: str,
               platform_slug: str,
               niche_id: int = 0,
           ) -> list[Product]:
               log.warning(
                   "eduzz_scanner.marketplace_unavailable",
                   alert="marketplace_unavailable",
                   niche=niche_slug,
                   platform_slug=platform_slug,
                   reason="Eduzz marketplace requires authentication — orbita.eduzz.com is a React SPA with no public product listing",
               )
               return []
       ```

    2. CRIAR mis/scanners/monetizze.py: estrutura idêntica ao eduzz.py mas com:
       - Importar MONETIZZE_PLATFORM_ID
       - Classe MonetizzeScanner
       - Log key: "monetizze_scanner.marketplace_unavailable"
       - reason: "Monetizze affiliate storefront requires authentication — app.monetizze.com.br returns 403 without login"

    3. ATUALIZAR mis/scanner.py (ler antes de editar):
       a) Adicionar imports de EduzzScanner e MonetizzeScanner
       b) Adicionar "eduzz": EduzzScanner e "monetizze": MonetizzeScanner ao SCANNER_MAP
       c) CRÍTICO — Atualizar run_all_scanners() ou o helper _run_one() para chamar mark_stale()
          quando scan_niche() retornar lista vazia:

          Ler mis/scanner.py para localizar run_all_scanners() e/ou _run_one().
          Após a chamada await scanner.scan_niche(...), verificar se products == []:
          ```python
          from mis.product_repository import mark_stale

          # Dentro de _run_one() ou run_all_scanners(), após await scanner.scan_niche():
          if not products:
              # Scanner retornou vazio — marcar produtos existentes como stale
              db = get_db(db_path)
              mark_stale(db, platform_id=platform_id, niche_id=niche_id)
          else:
              save_batch(db, products)
          ```

          Adaptar ao padrão existente (o executor deve ler o arquivo para identificar
          a estrutura exata de run_all_scanners — pode ser função isolada ou método).
          Garantir que mark_stale recebe db (sqlite-utils Database), não db_path (str).
          Se get_db() não estiver disponível onde necessário, importar de mis.db.

    4. CRIAR mis/tests/fixtures/eduzz/.gitkeep (arquivo vazio — pasta obrigatória, scanner é fallback sem fixture HTML)
    5. CRIAR mis/tests/fixtures/monetizze/.gitkeep (idem)

    6. CRIAR mis/tests/test_eduzz_scanner.py com os 6 testes do bloco <behavior>.
       Padrão de test_is_stale (reutilizável para todos 4 scanners):
       ```python
       @pytest.mark.asyncio
       async def test_is_stale(tmp_path):
           from mis.db import get_db, run_migrations
           from mis.product_repository import upsert_product, mark_stale
           from mis.scanner import Product
           from mis.platform_ids import EDUZZ_PLATFORM_ID

           db_path = str(tmp_path / "mis.db")
           run_migrations(db_path)
           db = get_db(db_path)

           # Garantir FK: platform e nicho existem
           db.execute("INSERT OR IGNORE INTO platforms (id, name, slug, base_url, created_at) VALUES (4, 'Eduzz', 'eduzz', 'https://eduzz.com', '2026-01-01T00:00:00Z')")
           db.execute("INSERT OR IGNORE INTO niches (id, name, slug, created_at) VALUES (1, 'Saude', 'saude', '2026-01-01T00:00:00Z')")

           product = Product(
               external_id="eduzz-test-001",
               title="Produto Eduzz Teste",
               url="https://eduzz.com/produto-teste",
               platform_id=EDUZZ_PLATFORM_ID,
               niche_id=1,
               rank=1,
               price=97.0,
           )
           upsert_product(db, product)
           row = list(db.execute(
               "SELECT is_stale FROM products WHERE platform_id=? AND external_id=?",
               [EDUZZ_PLATFORM_ID, "eduzz-test-001"],
           ))[0]
           assert row[0] == 0, "is_stale deve ser 0 após upsert"

           mark_stale(db, EDUZZ_PLATFORM_ID, 1)
           row = list(db.execute(
               "SELECT is_stale FROM products WHERE platform_id=? AND external_id=?",
               [EDUZZ_PLATFORM_ID, "eduzz-test-001"],
           ))[0]
           assert row[0] == 1, "is_stale deve ser 1 após mark_stale()"

           upsert_product(db, product)
           row = list(db.execute(
               "SELECT is_stale FROM products WHERE platform_id=? AND external_id=?",
               [EDUZZ_PLATFORM_ID, "eduzz-test-001"],
           ))[0]
           assert row[0] == 0, "is_stale deve ser resetado para 0 após upsert"
       ```

    7. CRIAR mis/tests/test_monetizze_scanner.py: estrutura idêntica com MONETIZZE_PLATFORM_ID=5 e "monetizze-test-001".
  </action>
  <verify>
    <automated>python -m pytest mis/tests/test_eduzz_scanner.py mis/tests/test_monetizze_scanner.py -x -q</automated>
  </verify>
  <done>
    12 testes GREEN (6 eduzz + 6 monetizze). EduzzScanner e MonetizzeScanner em SCANNER_MAP. alert='marketplace_unavailable' emitido em cada scan_niche(). is_stale comporta-se corretamente para ambos scanners. run_all_scanners() chama mark_stale() quando scan retorna [] — is_stale funciona em produção, não só nos testes unitários.
  </done>
</task>

</tasks>

<verification>
Suite completa do plano 14-01:

```bash
python -m pytest mis/tests/test_migration_007.py mis/tests/test_eduzz_scanner.py mis/tests/test_monetizze_scanner.py -v -q
```

Esperado: 17 testes GREEN (5 migration + 6 eduzz + 6 monetizze).

Verificação de integração:
```bash
python -c "from mis.migrations._007_is_stale import run_migration_007; print('_007 importa OK')"
python -c "from mis.scanners.eduzz import EduzzScanner; print('EduzzScanner importa OK')"
python -c "from mis.scanners.monetizze import MonetizzeScanner; print('MonetizzeScanner importa OK')"
python -c "from mis.product_repository import upsert_product, mark_stale; print('mark_stale exportada OK')"
```

Verificação CLI (truth observável):
```bash
python -m mis scan --platform eduzz 2>&1 | grep -E "marketplace_unavailable|error" || true
# Deve executar sem exception — retorna [] com log de alert emitido
```
</verification>

<success_criteria>
- [ ] mis/migrations/_007_is_stale.py existe com run_migration_007() idempotente
- [ ] mis/db.py chama _run_007 em run_migrations()
- [ ] mis/product_repository.py exporta mark_stale() e upsert_product() seta is_stale=0
- [ ] mis/scanners/eduzz.py e monetizze.py existem e emitem alert='marketplace_unavailable'
- [ ] EduzzScanner e MonetizzeScanner registrados em SCANNER_MAP
- [ ] run_all_scanners() (ou _run_one()) chama mark_stale() quando scan_niche() retorna []
- [ ] 17 testes GREEN sem skips ou xfails
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/14-br-scanners/14-01-SUMMARY.md`
</output>
