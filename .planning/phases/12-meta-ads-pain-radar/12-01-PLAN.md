---
phase: 12-meta-ads-pain-radar
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mis/radar/meta_ads_collector.py
  - mis/radar/__init__.py
  - mis/tests/test_meta_ads_radar.py
  - mis/tests/fixtures/meta_ads/ads_archive_radar_response.json
  - mis/tests/test_radar_jobs.py
autonomous: true
requirements:
  - RADAR-04
must_haves:
  truths:
    - "collect_ad_comments() com token válido retorna lista de pain_signals com source='meta_ads'"
    - "collect_ad_comments() sem META_ACCESS_TOKEN retorna [] sem propagar exceção"
    - "Segunda execução com mesma fixture não aumenta o COUNT em pain_signals (INSERT OR IGNORE via url_hash)"
    - "Anúncio com ad_creative_bodies vazio é silenciosamente ignorado"
    - "Job radar_meta_ads está registrado no APScheduler com CronTrigger(minute=0)"
    - "Total de 6 jobs após register_radar_jobs() (era 5)"
  artifacts:
    - path: "mis/radar/meta_ads_collector.py"
      provides: "async def collect_ad_comments(niche, db_path) — coletor de radar Meta Ads"
      exports: ["collect_ad_comments", "META_API_URL"]
    - path: "mis/radar/__init__.py"
      provides: "register_radar_jobs() atualizado com _run_all_meta_ads + 6º job"
      contains: "radar_meta_ads"
    - path: "mis/tests/test_meta_ads_radar.py"
      provides: "4 cenários TDD para o coletor"
    - path: "mis/tests/fixtures/meta_ads/ads_archive_radar_response.json"
      provides: "fixture com 3 anúncios para testes"
    - path: "mis/tests/test_radar_jobs.py"
      provides: "asserts de radar_meta_ads e len(jobs)==6"
  key_links:
    - from: "mis/radar/__init__.py"
      to: "mis/radar/meta_ads_collector.py"
      via: "from .meta_ads_collector import collect_ad_comments"
      pattern: "collect_ad_comments"
    - from: "mis/radar/meta_ads_collector.py"
      to: "pain_signals (SQLite)"
      via: "db[\"pain_signals\"].insert(signal, ignore=True)"
      pattern: "INSERT OR IGNORE"
    - from: "mis/radar/__init__.py"
      to: "APScheduler singleton"
      via: "_job_specs append + remove-before-add pattern"
      pattern: "radar_meta_ads"
---

<objective>
Implementar MetaAdsRadarCollector — o 6º coletor do Pain Radar — fechando RADAR-04, único
requisito v1 ainda pendente. O coletor busca criativos de anúncios ativos na Meta Ad Library
API por keyword/nicho, persiste em pain_signals com source='meta_ads', e se integra ao
APScheduler via register_radar_jobs().

Purpose: Fechar RADAR-04 — sinal de dor via copy de anúncios patrocinados completa a
         cobertura de fontes do radar (Trends + Reddit/Quora + YouTube + Meta Ads).
Output: meta_ads_collector.py funcional, job registrado, 4+2 testes GREEN, suite completa GREEN.
</objective>

<execution_context>
@C:/Users/Gabriel/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Gabriel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

# Arquivos de contexto da fase
@.planning/phases/12-meta-ads-pain-radar/12-CONTEXT.md
@.planning/phases/12-meta-ads-pain-radar/12-RESEARCH.md
</context>

<interfaces>
<!-- Interfaces extraídas do codebase. Executor usa diretamente — sem exploração. -->

<!-- mis/radar/__init__.py — estrutura atual de _job_specs (5 jobs, adicionar 6º) -->
```python
# Padrão estabelecido — _job_specs atual:
_job_specs = [
    ("radar_trends",       _trends_job,       CronTrigger(minute=0)),
    ("radar_reddit_quora", _reddit_quora_job,  CronTrigger(minute=0)),
    ("radar_youtube",      _youtube_job,       CronTrigger(hour="*/4", minute=0)),
    ("radar_synthesizer",  _synthesizer_job,   CronTrigger(minute=30)),
    ("radar_cleanup",      _cleanup_job,       CronTrigger(hour=3, minute=0)),
    # ADICIONAR: ("radar_meta_ads", _meta_ads_job, CronTrigger(minute=0)),
]
# log.info("scheduler.radar_jobs_registered", job_count=5)  → atualizar para 6
```

<!-- mis/radar/__init__.py — padrão de _run_all_* e imports (replicar exatamente) -->
```python
# imports no topo (adicionar):
from .meta_ads_collector import collect_ad_comments

# função helper (adicionar após _run_all_youtube):
async def _run_all_meta_ads(config: dict, db_path: str) -> None:
    """Run Meta Ads collection for all niches. Errors per niche are logged, not raised."""
    niches = config.get("niches", [])
    for niche in niches:
        try:
            signals = await collect_ad_comments(niche, db_path)
            log.info("radar.meta_ads.done", niche=niche.get("slug"), count=len(signals))
        except Exception as e:
            log.error("radar.meta_ads.error", niche=niche.get("slug"), error=str(e))
```

<!-- Schema de pain_signals (migration _004) — campos que o coletor insere -->
```python
# Colunas de pain_signals usadas por meta_ads_collector:
{
    "url_hash":     str,   # UNIQUE — SHA-256 de ad_snapshot_url
    "url":          str,   # ad_snapshot_url (URL do anúncio no Facebook)
    "title":        str,   # texto completo do criativo (sem truncamento — SQLite TEXT sem limite)
    "source":       str,   # 'meta_ads' (literal)
    "niche_slug":   str,   # niche["slug"]
    "score":        int,   # 0 (sem score para anúncios)
    "extra_json":   str,   # JSON: {"page_name": ..., "ad_delivery_start_time": ...}
    "collected_at": str,   # datetime.now(timezone.utc).isoformat()
}
# NOTA: "title" é o campo TEXT para o texto completo do criativo.
# Não há coluna "content" separada no schema.
```

<!-- mis/spies/meta_ads.py — endpoint e params da API (código-modelo para o radar) -->
```python
META_API_URL = "https://graph.facebook.com/v25.0/ads_archive"

params = {
    "access_token": token,
    "search_terms": product_title[:100],
    "ad_reached_countries": "BR",    # OBRIGATÓRIO — sem isso API retorna HTTP 400
    "ad_active_status": "ACTIVE",
    "ad_type": "ALL",
    "fields": "page_name,ad_snapshot_url,ad_creative_bodies,ad_delivery_start_time",
    "limit": 25,
}
# Para o radar: search_terms = keyword (do config.yaml), não product_title
# ad_reached_countries = ",".join(niche.get("ad_countries", ["BR"]))
```

<!-- Padrão de idempotência em pain_signals (trends_collector.py / reddit_collector.py) -->
```python
import sqlite_utils
db = sqlite_utils.Database(db_path)
db["pain_signals"].insert(signal_dict, ignore=True)
# ignore=True → INSERT OR IGNORE — UNIQUE index em url_hash resolve duplicatas silenciosamente
```

<!-- Padrão remove-before-add no APScheduler (register_radar_jobs atual) -->
```python
for job_id, func, trigger in _job_specs:
    existing = scheduler.get_job(job_id)
    if existing is not None:
        scheduler.remove_job(job_id)
    scheduler.add_job(func, trigger, id=job_id, replace_existing=True)
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — Scaffolds de teste e fixture (RED)</name>
  <files>mis/tests/test_meta_ads_radar.py, mis/tests/fixtures/meta_ads/ads_archive_radar_response.json, mis/tests/test_radar_jobs.py</files>
  <behavior>
    - test_happy_path_inserts_3_signals: fixture com 3 anúncios + token → collect_ad_comments retorna 3 signals + COUNT=3 em pain_signals
    - test_no_token_returns_empty: sem META_ACCESS_TOKEN → retorna [], nenhuma exceção, COUNT=0
    - test_idempotency_url_hash: rodar collect duas vezes com mesma fixture → COUNT não muda na segunda execução
    - test_ad_without_creative_body_skipped: fixture com ad_creative_bodies=[] → retorna [], COUNT=0
    - test_radar_jobs.py: adicionar test_radar_meta_ads_job_registered (radar_meta_ads em job_ids) e test_six_jobs_registered (len(jobs)==6)
    - Monkeypatch asyncio.sleep em TODOS os testes de meta_ads_radar para evitar delay real na suite
  </behavior>
  <action>
    1. Criar mis/tests/fixtures/meta_ads/ads_archive_radar_response.json com 3 anúncios, cada um com page_name, ad_snapshot_url único, ad_creative_bodies (lista com 1-2 strings), ad_delivery_start_time. Usar IDs radar001, radar002, radar003 nas URLs.

    2. Criar mis/tests/test_meta_ads_radar.py com:
       - Import: `from mis.radar.meta_ads_collector import collect_ad_comments, META_API_URL`
       - Fixture `radar_fixture` que lê ads_archive_radar_response.json
       - Os 4 testes com @pytest.mark.asyncio
       - Em todos os testes: `monkeypatch.setattr("asyncio.sleep", AsyncMock())` para neutralizar sleeps
       - Padrão respx.mock exatamente como test_meta_ads_spy.py: `respx.get(META_API_URL).mock(return_value=httpx.Response(200, json=...))`
       - run_migrations(db_path) para criar pain_signals antes de cada teste
       - Verificação via sqlite3.connect direto após collect (padrão dos outros testes de radar)

    3. Adicionar ao final de mis/tests/test_radar_jobs.py:
       ```python
       def test_radar_meta_ads_job_registered(isolated_scheduler):
           """radar_meta_ads job is registered after Phase 12."""
           config = _make_mock_config()
           with patch("mis.radar.get_scheduler", return_value=isolated_scheduler):
               from mis.radar import register_radar_jobs
               register_radar_jobs(config)
           job_ids = {job.id for job in isolated_scheduler.get_jobs()}
           assert "radar_meta_ads" in job_ids

       def test_six_jobs_registered(isolated_scheduler):
           """register_radar_jobs() registers exactly 6 jobs after Phase 12."""
           config = _make_mock_config()
           with patch("mis.radar.get_scheduler", return_value=isolated_scheduler):
               from mis.radar import register_radar_jobs
               register_radar_jobs(config)
           assert len(isolated_scheduler.get_jobs()) == 6
       ```

    Confirmar que os testes falham com ImportError (mis.radar.meta_ads_collector não existe) — esse é o RED esperado.
  </action>
  <verify>
    <automated>cd /c/Users/Gabriel/MEGABRAIN/mis && python -m pytest tests/test_meta_ads_radar.py tests/test_radar_jobs.py::test_radar_meta_ads_job_registered tests/test_radar_jobs.py::test_six_jobs_registered -x -q 2>&1 | head -30</automated>
  </verify>
  <done>Testes importam corretamente, falham com ImportError ou AssertionError (RED confirmado). Fixture JSON válida com 3 anúncios.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementação — meta_ads_collector.py + wiring no __init__.py (GREEN)</name>
  <files>mis/radar/meta_ads_collector.py, mis/radar/__init__.py</files>
  <behavior>
    - collect_ad_comments(niche, db_path): token check → loop keywords → GET Meta API → INSERT OR IGNORE → retorna lista de signals inseridos
    - Graceful degradation: sem token retorna [] + log.warning
    - Idempotência: db["pain_signals"].insert(signal, ignore=True) via url_hash UNIQUE
    - ad_creative_bodies vazio ou None: `bodies = ad.get("ad_creative_bodies") or []` → skip silencioso
    - HTTPStatusError por keyword: log.error + continue (não propagar, não abortar o niche)
    - Sleep entre keywords: asyncio.sleep(random.uniform(1, 2))
    - __init__.py: import collect_ad_comments, _run_all_meta_ads, 6º job em _job_specs, job_count=6
  </behavior>
  <action>
    1. Criar mis/radar/meta_ads_collector.py com a implementação completa:

       ```python
       """mis.radar.meta_ads_collector — Meta Ad Library radar collector (RADAR-04).

       Coleta criativos de anúncios ativos por keyword/nicho via Meta Ad Library API.
       Persiste em pain_signals com source='meta_ads'. Retorna [] graciosamente sem token.
       """
       import asyncio
       import hashlib
       import json
       import os
       import random
       from datetime import datetime, timezone

       import httpx
       import sqlite_utils
       import structlog

       log = structlog.get_logger()
       META_API_URL = "https://graph.facebook.com/v25.0/ads_archive"


       def _url_hash(url: str) -> str:
           return hashlib.sha256(url.encode()).hexdigest()


       async def collect_ad_comments(niche: dict, db_path: str) -> list[dict]:
           """Collect Meta Ads creative bodies as pain signals for a niche.
           ...
           """
           token = os.getenv("META_ACCESS_TOKEN", "").strip()
           if not token:
               log.warning("meta_ads.skipped", reason="no_META_ACCESS_TOKEN")
               return []

           niche_slug = niche["slug"]
           ad_countries = ",".join(niche.get("ad_countries", ["BR"]))
           db = sqlite_utils.Database(db_path)
           collected = []

           for keyword in niche.get("keywords", []):
               params = {
                   "access_token": token,
                   "search_terms": keyword,
                   "ad_reached_countries": ad_countries,
                   "ad_active_status": "ACTIVE",
                   "ad_type": "ALL",
                   "fields": "page_name,ad_snapshot_url,ad_creative_bodies,ad_delivery_start_time",
                   "limit": 25,
               }
               try:
                   async with httpx.AsyncClient(timeout=30.0) as client:
                       resp = await client.get(META_API_URL, params=params)
                       resp.raise_for_status()
                       ads = resp.json().get("data", [])
               except httpx.HTTPStatusError as e:
                   log.error("meta_ads_radar.fetch_error", keyword=keyword, niche=niche_slug, error=str(e))
                   await asyncio.sleep(random.uniform(1, 2))
                   continue

               for ad in ads:
                   bodies = ad.get("ad_creative_bodies") or []
                   if not bodies:
                       log.debug("meta_ads_radar.skipped_no_body", keyword=keyword)
                       continue

                   content = "\n\n".join(bodies)
                   url = ad["ad_snapshot_url"]
                   signal = {
                       "url_hash": _url_hash(url),
                       "url": url,
                       "title": content,
                       "source": "meta_ads",
                       "niche_slug": niche_slug,
                       "score": 0,
                       "extra_json": json.dumps({
                           "page_name": ad.get("page_name"),
                           "ad_delivery_start_time": ad.get("ad_delivery_start_time"),
                       }),
                       "collected_at": datetime.now(timezone.utc).isoformat(),
                   }
                   try:
                       db["pain_signals"].insert(signal, ignore=True)
                       collected.append(signal)
                   except Exception as exc:
                       log.warning("meta_ads_radar.persist_failed", error=str(exc))

               log.info("meta_ads_radar.fetched", keyword=keyword, niche=niche_slug, count=len(ads))
               await asyncio.sleep(random.uniform(1, 2))

           log.info("meta_ads_radar.done", niche=niche_slug, count=len(collected))
           return collected
       ```

    2. Editar mis/radar/__init__.py — TRÊS mudanças pontuais:

       a) Adicionar import no bloco de imports existente (após .synthesizer import):
          ```python
          from .meta_ads_collector import collect_ad_comments
          ```

       b) Adicionar função _run_all_meta_ads após a função _run_all_youtube (antes de _run_cleanup):
          ```python
          async def _run_all_meta_ads(config: dict, db_path: str) -> None:
              """Run Meta Ads collection for all niches. Errors per niche are logged, not raised."""
              niches = config.get("niches", [])
              for niche in niches:
                  try:
                      signals = await collect_ad_comments(niche, db_path)
                      log.info("radar.meta_ads.done", niche=niche.get("slug"), count=len(signals))
                  except Exception as e:
                      log.error("radar.meta_ads.error", niche=niche.get("slug"), error=str(e))
          ```

       c) Dentro de register_radar_jobs(): adicionar _meta_ads_job e entrada no _job_specs, e atualizar job_count:
          ```python
          # Adicionar job wrapper (junto com os outros async def _*_job()):
          async def _meta_ads_job():
              await _run_all_meta_ads(config, db_path)

          # Adicionar ao _job_specs list:
          ("radar_meta_ads", _meta_ads_job, CronTrigger(minute=0)),

          # Atualizar log no final:
          log.info("scheduler.radar_jobs_registered", job_count=6)
          ```

    NOTA: Ler mis/radar/__init__.py completamente antes de editar para não perder nenhuma linha existente.
  </action>
  <verify>
    <automated>cd /c/Users/Gabriel/MEGABRAIN/mis && python -m pytest tests/test_meta_ads_radar.py tests/test_radar_jobs.py -x -q && python -m pytest -x -q 2>&1 | tail -10</automated>
  </verify>
  <done>
    - 4 testes test_meta_ads_radar.py GREEN
    - test_radar_meta_ads_job_registered GREEN
    - test_six_jobs_registered GREEN
    - Suite completa: todos os testes pré-existentes continuam GREEN
  </done>
</task>

</tasks>

<verification>
Suite completa green: `cd /c/Users/Gabriel/MEGABRAIN/mis && python -m pytest -x -q`

Verificar que:
1. mis/radar/meta_ads_collector.py existe e importa sem erro: `python -c "from mis.radar.meta_ads_collector import collect_ad_comments; print('OK')"`
2. register_radar_jobs() registra 6 jobs (test_six_jobs_registered)
3. radar_meta_ads está nos job_ids (test_radar_meta_ads_job_registered)
4. source='meta_ads' salvo em pain_signals (test_happy_path_inserts_3_signals)
5. INSERT OR IGNORE funciona (test_idempotency_url_hash)
</verification>

<success_criteria>
- mis/radar/meta_ads_collector.py existe com collect_ad_comments() exportado
- mis/radar/__init__.py registra 6 jobs (era 5), inclui radar_meta_ads com CronTrigger(minute=0)
- 4 testes test_meta_ads_radar.py GREEN (happy path, no token, idempotência, skip sem body)
- test_six_jobs_registered GREEN
- test_radar_meta_ads_job_registered GREEN
- Suite completa mis/ continua 100% GREEN
- RADAR-04 fechado: criativos de anúncios Meta entram automaticamente no radar horário
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/12-meta-ads-pain-radar/12-01-SUMMARY.md`
</output>
