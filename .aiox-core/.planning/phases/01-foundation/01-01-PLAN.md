---
phase: 01-foundation
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - nexus/__init__.py
  - nexus/models.py
  - nexus/config.py
  - tests/__init__.py
  - tests/test_models.py
  - tests/test_config.py
  - pyproject.toml
  - .env.example
autonomous: true
requirements: [FND-01, FND-02]
must_haves:
  truths:
    - "AgentScore rejeita score fora de 1-10 com ValidationError"
    - "AgentScore rejeita feedback com menos de 10 caracteres"
    - "CreativeBundle aceita dados validos e rejeita campos obrigatorios ausentes"
    - "NexusConfig carrega valores do .env com prefixo NEXUS_"
    - "NexusConfig usa defaults quando variavel opcional nao existe no .env"
    - "NexusConfig falha com erro claro quando NEXUS_ANTHROPIC_API_KEY esta ausente"
    - "Variavel de ambiente do sistema tem prioridade sobre valor no .env"
  artifacts:
    - path: "nexus/models.py"
      provides: "AgentScore e CreativeBundle Pydantic models"
      exports: ["AgentScore", "CreativeBundle"]
    - path: "nexus/config.py"
      provides: "NexusConfig BaseSettings com env_prefix NEXUS_"
      exports: ["NexusConfig"]
    - path: "tests/test_models.py"
      provides: "Testes unitarios para modelos Pydantic"
      min_lines: 40
    - path: "tests/test_config.py"
      provides: "Testes unitarios para config loader"
      min_lines: 30
    - path: "pyproject.toml"
      provides: "Configuracao de projeto e pytest"
      contains: "[tool.pytest.ini_options]"
    - path: ".env.example"
      provides: "Template de variaveis NEXUS_"
      contains: "NEXUS_ANTHROPIC_API_KEY"
  key_links:
    - from: "nexus/config.py"
      to: ".env"
      via: "pydantic-settings BaseSettings com SettingsConfigDict(env_prefix='NEXUS_')"
      pattern: "env_prefix.*NEXUS_"
    - from: "nexus/models.py"
      to: "instructor (Phase 2)"
      via: "json_schema_extra com few-shot examples no AgentScore"
      pattern: "json_schema_extra"
---

<objective>
Criar a infraestrutura de dados tipados e configuracao externalizavel do NEXUS.

Purpose: FND-01 e FND-02 sao a fundacao de todo o pipeline — cada fase posterior importa estes modelos e config. Sem tipos validos e config de .env, agentes nao podem retornar dados estruturados nem ser configurados sem editar codigo.

Output: nexus/models.py (AgentScore, CreativeBundle), nexus/config.py (NexusConfig), test suite completa, pyproject.toml, .env.example
</objective>

<execution_context>
@C:/Users/Gabriel/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Gabriel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold do projeto + RED tests para models e config</name>
  <files>pyproject.toml, nexus/__init__.py, tests/__init__.py, tests/test_models.py, tests/test_config.py, .env.example</files>
  <behavior>
    - test_valid_score: AgentScore(dimension="copy", score=8, approved=True, feedback="Hook forte com urgencia. CTA claro e direto.", strengths=["Hook urgente"], issues=[]) cria instancia com score==8
    - test_score_below_minimum: AgentScore(score=0, ...) levanta ValidationError com "greater than or equal to 1"
    - test_score_above_maximum: AgentScore(score=11, ...) levanta ValidationError com "less than or equal to 10"
    - test_feedback_too_short: AgentScore(feedback="short", ...) levanta ValidationError (min_length=10)
    - test_valid_creative_bundle: CreativeBundle com niche, script_text, status cria instancia valida
    - test_creative_bundle_missing_required: CreativeBundle sem niche levanta ValidationError
    - test_config_loads_from_env: NexusConfig com .env contendo NEXUS_ANTHROPIC_API_KEY=test-key carrega corretamente
    - test_config_uses_defaults_when_env_missing: NexusConfig sem NEXUS_MIN_COPY_SCORE usa default 7
    - test_config_rejects_missing_required_key: NexusConfig com .env vazio levanta ValidationError
    - test_config_env_override: env var NEXUS_MIN_COPY_SCORE=9 sobrepoe .env com valor 5
  </behavior>
  <action>
    1. Criar pyproject.toml com:
       - [project] name="nexus", version="0.1.0", requires-python=">=3.11"
       - [project.dependencies]: pydantic>=2.10.0, pydantic-settings>=2.7.0, python-dotenv>=1.0.0
       - [project.optional-dependencies] dev: pytest>=8.0.0, ruff>=0.8.0, mypy>=1.13.0
       - [tool.pytest.ini_options] testpaths=["tests"], pythonpath=["."]

    2. Criar nexus/__init__.py vazio e tests/__init__.py vazio

    3. Criar tests/test_models.py com os testes RED acima. Importar de nexus.models (que ainda nao existe). Usar exatamente os padroes da RESEARCH.md secao "Model Validation Test Pattern". Incluir:
       - AgentScore: 4 testes (valid, score baixo, score alto, feedback curto)
       - CreativeBundle: 2 testes (valido, campo obrigatorio ausente)
       - Usar pytest.raises(ValidationError) para testes de rejeicao

    4. Criar tests/test_config.py com os testes RED acima. Importar de nexus.config. Usar exatamente os padroes da RESEARCH.md secao "Config Loading Test Pattern". Incluir:
       - 4 testes: loads from env, uses defaults, rejects missing key, env override
       - Usar tmp_path e monkeypatch fixtures do pytest

    5. Criar .env.example documentando TODAS as variaveis NEXUS_ com comentarios explicativos:
       ```
       # Required
       NEXUS_ANTHROPIC_API_KEY=sk-ant-your-key-here

       # Review thresholds (defaults shown)
       NEXUS_MIN_COPY_SCORE=7
       NEXUS_MIN_TECH_SCORE=6
       NEXUS_MIN_COMPLIANCE_SCORE=8
       NEXUS_MIN_PERFORMANCE_SCORE=6

       # Pipeline control
       NEXUS_MAX_REVIEW_ATTEMPTS=3
       NEXUS_REVIEW_MODEL=claude-sonnet-4-20250514

       # Paths
       NEXUS_OUTPUT_DIR=output
       ```

    6. Instalar dependencias: pip install pydantic pydantic-settings python-dotenv pytest
    7. Rodar pytest -- TODOS os testes DEVEM FALHAR (RED) com ImportError
  </action>
  <verify>
    <automated>cd c:/Users/Gabriel/MEGABRAIN/.aiox-core && pip install pydantic pydantic-settings python-dotenv pytest -q && python -m pytest tests/ -x -q 2>&1 | head -20</automated>
  </verify>
  <done>pyproject.toml existe com deps e pytest config. tests/test_models.py tem 6 testes. tests/test_config.py tem 4 testes. Todos falham com ImportError (RED). .env.example documenta todas variaveis NEXUS_.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — Implementar models.py e config.py</name>
  <files>nexus/models.py, nexus/config.py</files>
  <behavior>
    - Todos os 10 testes RED da Task 1 devem passar (GREEN)
  </behavior>
  <action>
    1. Criar nexus/models.py com Pydantic v2 (NUNCA usar sintaxe v1):
       - AgentScore(BaseModel):
         - dimension: str = Field(description="Review dimension: copy|tech|compliance|performance")
         - score: int = Field(ge=1, le=10, description="Quality score 1-10")
         - approved: bool = Field(description="Meets minimum threshold")
         - feedback: str = Field(min_length=10, description="Specific actionable feedback")
         - strengths: list[str] = Field(default_factory=list)
         - issues: list[str] = Field(default_factory=list)
         - model_config = ConfigDict(json_schema_extra={"examples": [...]}) com 2 exemplos few-shot (1 aprovado, 1 reprovado) copiados da RESEARCH.md Pattern 1

       - CreativeBundle(BaseModel):
         - id: str = Field(default_factory=lambda: str(uuid4()), description="Unique creative ID")
         - niche: str = Field(min_length=1, description="Target niche")
         - script_text: str = Field(min_length=1, description="Ad script content")
         - audio_path: str | None = Field(default=None, description="Path to generated audio")
         - video_path: str | None = Field(default=None, description="Path to generated video")
         - image_path: str | None = Field(default=None, description="Path to source image")
         - status: str = Field(default="pending", description="Pipeline status: pending|reviewing|approved|rejected|quarantined")
         - attempt: int = Field(default=1, ge=1, description="Current attempt number")
         - reviews: list[AgentScore] = Field(default_factory=list, description="Review scores from agents")

    2. Criar nexus/config.py com pydantic-settings v2:
       - NexusConfig(BaseSettings):
         - anthropic_api_key: str (required, sem default)
         - min_copy_score: int = 7
         - min_tech_score: int = 6
         - min_compliance_score: int = 8
         - min_performance_score: int = 6
         - max_review_attempts: int = 3
         - review_model: str = "claude-sonnet-4-20250514"
         - output_dir: str = "output"
         - model_config = SettingsConfigDict(env_file=".env", env_prefix="NEXUS_", env_file_encoding="utf-8")

    3. IMPORTANTE — usar APENAS sintaxe Pydantic v2:
       - ConfigDict (NAO class Config:)
       - Field() com named args (NAO schema_extra, usar json_schema_extra)
       - SettingsConfigDict (NAO class Config: no BaseSettings)

    4. Rodar pytest -- TODOS os testes DEVEM PASSAR (GREEN)
  </action>
  <verify>
    <automated>cd c:/Users/Gabriel/MEGABRAIN/.aiox-core && python -m pytest tests/test_models.py tests/test_config.py -v --tb=short</automated>
  </verify>
  <done>nexus/models.py exporta AgentScore e CreativeBundle com Field constraints. nexus/config.py exporta NexusConfig que carrega .env com prefixo NEXUS_. Todos os 10 testes passam (GREEN). Zero warnings de Pydantic v1 deprecation.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/ -v --tb=short` — todos os testes GREEN
- `python -c "from nexus.models import AgentScore, CreativeBundle; print('models OK')"` — import funciona
- `python -c "from nexus.config import NexusConfig; print('config OK')"` — import funciona
- `grep -r 'NEXUS_' .env.example | wc -l` — pelo menos 8 variaveis documentadas
</verification>

<success_criteria>
- AgentScore rejeita score=0, score=11, feedback curto com ValidationError
- CreativeBundle aceita dados validos, rejeita niche ausente
- NexusConfig carrega de .env com prefixo NEXUS_, usa defaults, falha sem API key
- Env var do sistema sobrepoe valor do .env
- Todos os testes passam em menos de 5 segundos
- .env.example documenta todas variaveis com comentarios
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
