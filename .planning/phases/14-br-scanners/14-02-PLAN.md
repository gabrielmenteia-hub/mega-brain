---
phase: 14-br-scanners
plan: 02
type: tdd
wave: 2
depends_on:
  - 14-01
files_modified:
  - mis/scanners/perfectpay.py
  - mis/scanners/braip.py
  - mis/scanner.py
  - mis/config.yaml
  - mis/tests/test_perfectpay_scanner.py
  - mis/tests/test_braip_scanner.py
  - mis/tests/fixtures/perfectpay/.gitkeep
  - mis/tests/fixtures/braip/catalog_cursos-online.html
autonomous: true
requirements:
  - SCAN-BR-03
  - SCAN-BR-04

must_haves:
  truths:
    - "PerfectPayScanner.scan_niche() retorna [] e emite alert='marketplace_unavailable' sem exceção"
    - "BraipScanner.scan_niche() retorna lista de produtos com external_id=hash, price=float (centavos/100), rank=int"
    - "BraipScanner emite alert='schema_drift' quando window.__NUXT__ não é encontrado no HTML"
    - "BraipScanner e PerfectPayScanner registrados em SCANNER_MAP"
    - "config.yaml tem entradas para braip e perfectpay nos 3 nichos"
    - "config.yaml tem entradas explícitas eduzz: null e monetizze: null nos 3 nichos (opt-in declarativo)"
  artifacts:
    - path: "mis/scanners/perfectpay.py"
      provides: "PerfectPayScanner subclass de PlatformScanner (fallback-only)"
      exports: ["PerfectPayScanner"]
    - path: "mis/scanners/braip.py"
      provides: "BraipScanner com parsing window.__NUXT__"
      exports: ["BraipScanner", "_parse_nuxt_products"]
    - path: "mis/tests/fixtures/braip/catalog_cursos-online.html"
      provides: "Fixture HTML real de marketplace.braip.com capturada ao vivo"
      contains: "window.__NUXT__ ou window.__NUXT_DATA__"
    - path: "mis/tests/test_perfectpay_scanner.py"
      provides: "6 testes SCAN-BR-03"
    - path: "mis/tests/test_braip_scanner.py"
      provides: "6 testes SCAN-BR-04"
  key_links:
    - from: "mis/scanners/braip.py _parse_nuxt_products()"
      to: "window.__NUXT__ JSON blob"
      via: "re.search + json.loads → state.search.products"
      pattern: "window\\.__NUXT__"
    - from: "mis/scanner.py SCANNER_MAP"
      to: "mis/scanners/perfectpay.py + mis/scanners/braip.py"
      via: "import + registro no dict SCANNER_MAP"
      pattern: "PerfectPayScanner|BraipScanner"
    - from: "mis/scanners/braip.py"
      to: "mis/platform_ids.py"
      via: "from mis.platform_ids import BRAIP_PLATFORM_ID"
      pattern: "BRAIP_PLATFORM_ID"
---

<objective>
Implementar PerfectPayScanner (fallback-only) e BraipScanner (parsing window.__NUXT__), registrar ambos no SCANNER_MAP, e adicionar slugs de categoria no config.yaml com entradas explícitas para todas as 4 plataformas BR.

Purpose: PerfectPay é plataforma de checkout sem marketplace público — segue o mesmo padrão de fallback do plano 14-01. Braip é o único scanner BR com marketplace real: marketplace.braip.com expõe produtos via Nuxt.js SSR com dados embutidos em window.__NUXT__. O hash do produto serve como external_id estável. Config.yaml deve registrar TODAS as plataformas BR explicitamente (braip com slug, eduzz/monetizze/perfectpay: null) — ausência de entrada significa scanner ignorado pelo scheduler.

Output: 1 scanner de fallback + 1 scanner SSR funcional, 12 testes GREEN (6 perfectpay + 6 braip), config.yaml atualizado com slugs Braip e entradas null para as demais plataformas BR nos 3 nichos.
</objective>

<execution_context>
@C:/Users/Gabriel/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Gabriel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-br-scanners/14-CONTEXT.md
@.planning/phases/14-br-scanners/14-RESEARCH.md
@.planning/phases/14-br-scanners/14-VALIDATION.md
@.planning/phases/14-br-scanners/14-01-SUMMARY.md

<interfaces>
<!-- Contratos que o executor precisa. Extraídos do código existente e do plano 14-01. -->
<!-- Não explorar o codebase — usar diretamente. -->

De mis/scanner.py (Product e PlatformScanner — inalterados desde 14-01):
```python
@dataclass
class Product:
    external_id: str        # Para Braip: campo "hash" do __NUXT__ (e.g. "proown9j")
    title: str
    url: str
    platform_id: int
    niche_id: int
    rank: int               # 1-based, posicional (ordem de listagem)
    price: Optional[float] = None  # Para Braip: price_cents / 100.0
    commission_pct: Optional[float] = None
    rating: Optional[float] = None  # Para Braip: campo "star"
    thumbnail_url: Optional[str] = None  # Para Braip: campo "src"

class PlatformScanner(ABC):
    async def scan_niche(
        self,
        niche_slug: str,
        platform_slug: str,
        niche_id: int = 0,
    ) -> list[Product]: ...
```

De mis/platform_ids.py (constantes — não alterar):
```python
PERFECTPAY_PLATFORM_ID = 6
BRAIP_PLATFORM_ID      = 7
```

De mis/product_repository.py (adicionado em 14-01):
```python
def upsert_product(db, product) -> None:
    # UPDATE SET ... is_stale=0 ... WHERE platform_id=? AND external_id=?
    # Se rowcount==0 → INSERT com is_stale=0

def mark_stale(db, platform_id: int, niche_id: int) -> None:
    # UPDATE products SET is_stale=1 WHERE platform_id=? AND niche_id=?
```

Estrutura window.__NUXT__ confirmada em marketplace.braip.com (2026-03-17):
```
URL de categoria: https://marketplace.braip.com/search?categorySlug={slug}
JSON path: state -> search -> products (lista)

Por produto:
  hash          → external_id (e.g. "proown9j") — campo mais estável
  slug          → URL path (e.g. "braip-pages")
  title         → nome do produto
  price         → centavos inteiros (2990 = R$29.90) → dividir por 100
  src           → thumbnail_url
  star          → rating float
  totalRatings  → count de reviews
```

Atenção a pitfall do Nuxt 3:
```python
# Braip pode usar __NUXT__ (Nuxt 2) ou __NUXT_DATA__ (Nuxt 3 array-format)
# Implementar regex para AMBOS, retornar [] com alert='schema_drift' se nenhum funcionar
# Fixture HTML capturada ao vivo determina qual formato está ativo
```

Slugs de categoria Braip confirmados:
```
encapsulados       → nicho emagrecimento/saude
cursos-online      → nicho marketing digital / geral
mentorias          → nicho negócios
livros             → nicho educação
cosmeticos         → nicho beleza
```

Padrão de SCANNER_MAP em scanner.py (após 14-01):
```python
SCANNER_MAP = {
    "hotmart": HotmartScanner,
    "kiwify": KiwifyScanner,
    "clickbank": ClickBankScanner,
    "eduzz": EduzzScanner,
    "monetizze": MonetizzeScanner,
    # "perfectpay": PerfectPayScanner,  ← adicionar neste plano
    # "braip": BraipScanner,             ← adicionar neste plano
}
```

Padrão de config.yaml com plataformas (estrutura ALVO após este plano):
```yaml
# Regra: ausência de entrada = scanner ignorado pelo scheduler.
# Plataformas sem marketplace público recebem null explicitamente.
# Isso documenta o opt-in intencional e previne ambiguidade.
niches:
  - name: "Emagrecimento"
    slug: "emagrecimento"
    platforms:
      hotmart: saude-e-fitness
      kiwify: saude
      braip: encapsulados    # ← marketplace público disponível
      perfectpay: null        # ← sem marketplace público (checkout-only)
      eduzz: null             # ← sem marketplace público (SPA autenticado)
      monetizze: null         # ← sem marketplace público (403 sem login)
  - name: "Marketing Digital"
    slug: "marketing-digital"
    platforms:
      hotmart: marketing-digital
      kiwify: marketing
      braip: cursos-online   # ← marketplace público disponível
      perfectpay: null
      eduzz: null
      monetizze: null
  - name: "Financas Pessoais"
    slug: "financas-pessoais"
    platforms:
      hotmart: financas-e-investimentos
      kiwify: financas
      braip: cursos-online   # proxy — verificar se existe slug mais específico
      perfectpay: null
      eduzz: null
      monetizze: null
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PerfectPayScanner (fallback-only) + 6 testes</name>
  <files>
    mis/scanners/perfectpay.py,
    mis/scanner.py,
    mis/tests/test_perfectpay_scanner.py,
    mis/tests/fixtures/perfectpay/.gitkeep
  </files>
  <behavior>
    - test_happy_path: scan_niche() retorna [] (lista vazia)
    - test_field_types: retorno é list — sem TypeError, sem AttributeError
    - test_fallback_selector: scan_niche() sempre retorna [] independente do platform_slug — fallback puro
    - test_drift_alert: capture_logs() mostra alert='marketplace_unavailable' após scan_niche()
    - test_upsert_no_duplicates: scan_niche() retorna [] → mark_stale() chamado com (PERFECTPAY_PLATFORM_ID, niche_id) → produtos existentes marcados is_stale=True, nenhuma row nova inserida
    - test_is_stale: (idêntico ao padrão Eduzz do 14-01) upsert produto → mark_stale → is_stale=1; upsert novamente → is_stale=0
  </behavior>
  <action>
    1. CRIAR mis/scanners/perfectpay.py:
       ```python
       """PerfectPay platform scanner — fallback only.

       PerfectPay é plataforma de checkout e gestão de pagamentos.
       Não possui marketplace centralizado público — vitrine de afiliados
       disponível apenas no painel autenticado (app.perfectpay.com.br).

       Implementa fallback puro: scan_niche() retorna [] com alert='marketplace_unavailable'.
       Dados existentes no DB são preservados e marcados is_stale=True via mark_stale().
       """
       from __future__ import annotations
       import structlog
       from mis.scanner import PlatformScanner, Product
       from mis.platform_ids import PERFECTPAY_PLATFORM_ID

       log = structlog.get_logger(__name__)

       class PerfectPayScanner(PlatformScanner):
           """PerfectPay scanner — plataforma de checkout sem marketplace público."""

           async def scan_niche(
               self,
               niche_slug: str,
               platform_slug: str,
               niche_id: int = 0,
           ) -> list[Product]:
               log.warning(
                   "perfectpay_scanner.marketplace_unavailable",
                   alert="marketplace_unavailable",
                   niche=niche_slug,
                   platform_slug=platform_slug,
                   reason="PerfectPay is a checkout platform with no public marketplace — affiliate store requires authentication at app.perfectpay.com.br",
               )
               return []
       ```

    2. ATUALIZAR mis/scanner.py (ler antes de editar):
       - Adicionar import de PerfectPayScanner
       - Adicionar "perfectpay": PerfectPayScanner ao SCANNER_MAP

    3. CRIAR mis/tests/fixtures/perfectpay/.gitkeep (arquivo vazio)

    4. CRIAR mis/tests/test_perfectpay_scanner.py com os 6 testes descritos em <behavior>.
       Usar PERFECTPAY_PLATFORM_ID=6 nos testes de is_stale.
       Seguir exatamente o mesmo padrão dos testes Eduzz/Monetizze do plano 14-01.
  </action>
  <verify>
    <automated>python -m pytest mis/tests/test_perfectpay_scanner.py -x -q</automated>
  </verify>
  <done>
    6 testes GREEN. PerfectPayScanner em SCANNER_MAP. alert='marketplace_unavailable' emitido. is_stale comporta-se corretamente.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: BraipScanner (window.__NUXT__ SSR) + fixture HTML ao vivo + 6 testes + config.yaml</name>
  <files>
    mis/scanners/braip.py,
    mis/scanner.py,
    mis/config.yaml,
    mis/tests/test_braip_scanner.py,
    mis/tests/fixtures/braip/catalog_cursos-online.html
  </files>
  <behavior>
    - test_happy_path: scan_niche() com fixture HTML retorna >= 1 produto com todos campos obrigatórios; external_id == hash do produto (string não-vazia)
    - test_field_types: rank é int, price é float (centavos/100), external_id é str não-vazia
    - test_fallback_selector: HTML sem window.__NUXT__ → retorna [] (nenhum produto) — graceful degradation
    - test_drift_alert: HTML sem window.__NUXT__ E sem window.__NUXT_DATA__ → capture_logs() mostra alert='schema_drift'
    - test_upsert_no_duplicates: inserir produto → reinserir com rank diferente → 1 row no DB, rank atualizado
    - test_is_stale: upsert produto → mark_stale → is_stale=1; upsert novamente → is_stale=0
  </behavior>
  <action>
    PASSO 0 — CAPTURAR FIXTURE HTML AO VIVO (obrigatório antes de escrever testes):
    Execute o WebFetch da URL https://marketplace.braip.com/search?categorySlug=cursos-online
    e salve o HTML bruto como mis/tests/fixtures/braip/catalog_cursos-online.html.
    Verificar que o arquivo contém "window.__NUXT__" ou "window.__NUXT_DATA__" antes de prosseguir.
    Se não contiver nenhum dos dois, emitir erro e parar — fixture inválida.

    1. CRIAR mis/scanners/braip.py:

       Constantes:
       ```python
       BRAIP_MARKETPLACE_URL = "https://marketplace.braip.com/search"
       # Braip usa Nuxt.js SSR — dados embutidos via window.__NUXT__ (Nuxt 2)
       # ou window.__NUXT_DATA__ (Nuxt 3). Checar fixture para confirmar formato ativo.
       ```

       Funções de parsing:
       ```python
       import json, re
       from typing import Optional

       def _parse_nuxt_products(html: str, niche_id: int) -> list[Product]:
           """Parse products from Braip window.__NUXT__ state.

           Tenta window.__NUXT__ (Nuxt 2 — objeto JSON com state.search.products).
           Fallback para window.__NUXT_DATA__ (Nuxt 3 — array de arrays).
           Retorna [] se nenhum formato for reconhecido.
           """
           # Tentativa 1: Nuxt 2 — window.__NUXT__ = { state: { search: { products: [...] } } }
           match = re.search(r'window\.__NUXT__\s*=\s*(\{.+?\});\s*</script>', html, re.DOTALL)
           if match:
               try:
                   nuxt_data = json.loads(match.group(1))
                   raw = nuxt_data.get("state", {}).get("search", {}).get("products", [])
                   if raw:
                       return _build_products(raw, niche_id)
               except (json.JSONDecodeError, KeyError, ValueError):
                   pass

           # Tentativa 2: Nuxt 3 — window.__NUXT_DATA__ = [[...]]
           # Formato diferente — se fixture confirmar Nuxt 3, implementar aqui
           # Por ora: retornar [] (schema_drift será emitido pelo caller)
           return []

       def _build_products(raw: list[dict], niche_id: int) -> list[Product]:
           """Convert raw Nuxt product dicts to Product dataclasses."""
           from mis.platform_ids import BRAIP_PLATFORM_ID
           products = []
           for rank, item in enumerate(raw, start=1):
               hash_id = item.get("hash")
               if not hash_id:
                   continue
               price_cents = item.get("price")
               products.append(Product(
                   external_id=hash_id,
                   title=item.get("title", hash_id),
                   url=f"https://marketplace.braip.com/{item.get('slug', hash_id)}",
                   platform_id=BRAIP_PLATFORM_ID,
                   niche_id=niche_id,
                   rank=rank,
                   price=price_cents / 100.0 if price_cents else None,
                   rating=item.get("star"),
                   thumbnail_url=item.get("src"),
               ))
           return products
       ```

       Classe BraipScanner:
       ```python
       class BraipScanner(PlatformScanner):
           """Braip marketplace scanner via Nuxt.js SSR parsing.

           URL: https://marketplace.braip.com/search?categorySlug={slug}
           Dados em window.__NUXT__ state.search.products.
           external_id = campo "hash" (mais estável — não muda com renomeação).
           Apenas página 1 (top 12 produtos) — paginação é enhancement futuro.
           """

           async def scan_niche(
               self,
               niche_slug: str,
               platform_slug: str,
               niche_id: int = 0,
           ) -> list[Product]:
               url = f"{BRAIP_MARKETPLACE_URL}?categorySlug={platform_slug}"
               try:
                   html = await self.fetch(url)
               except Exception as exc:
                   log.error(
                       "braip_scanner.fetch_failed",
                       niche=niche_slug,
                       platform_slug=platform_slug,
                       error=str(exc),
                   )
                   return []

               products = _parse_nuxt_products(html, niche_id)

               if not products:
                   log.warning(
                       "braip_scanner.schema_drift",
                       alert="schema_drift",
                       niche=niche_slug,
                       platform_slug=platform_slug,
                       url=url,
                       reason="window.__NUXT__ not found or state.search.products empty",
                   )
                   return []

               log.info(
                   "braip_scanner.scan_complete",
                   niche=niche_slug,
                   platform_slug=platform_slug,
                   count=len(products),
               )
               return products
       ```

       Adicionar entrada de rate limiting para Braip em base_scraper.py DOMAIN_DELAYS:
       Ler base_scraper.py, adicionar `"marketplace.braip.com": 2.0` ao dict DOMAIN_DELAYS.

    2. ATUALIZAR mis/scanner.py (ler antes de editar):
       - Adicionar import de BraipScanner
       - Adicionar "braip": BraipScanner ao SCANNER_MAP

    3. ATUALIZAR mis/config.yaml (ler o arquivo antes de editar):
       Para CADA um dos 3 nichos configurados, garantir que existam entradas para TODAS as 4 plataformas BR:
       - braip: <slug-confirmado-pela-fixture>  (encapsulados / cursos-online conforme nicho)
       - perfectpay: null   (sem marketplace público — entrada explícita documenta intenção)
       - eduzz: null        (sem marketplace público — entrada explícita documenta intenção)
       - monetizze: null    (sem marketplace público — entrada explícita documenta intenção)

       REGRA: ausência de entrada = scanner ignorado pelo scheduler. O valor null significa
       "plataforma registrada, sem slug disponível". Plataformas sem entrada não existem para o scheduler.

       Se eduzz/monetizze já tiverem entradas (criadas em 14-01), preservar os valores existentes.
       Se não tiverem, criar as entradas com valor null.

    4. CRIAR mis/tests/test_braip_scanner.py com os 6 testes:

       Imports necessários:
       ```python
       import respx
       from pathlib import Path
       from httpx import Response
       from structlog.testing import capture_logs
       from mis.platform_ids import BRAIP_PLATFORM_ID

       FIXTURE_HTML = Path(__file__).parent / "fixtures/braip/catalog_cursos-online.html"
       BRAIP_SEARCH_URL = "https://marketplace.braip.com/search"
       ```

       test_happy_path e test_field_types: usar @respx.mock, servir FIXTURE_HTML, verificar campos.
       test_fallback_selector: HTML sem window.__NUXT__ → BraipScanner retorna [].
       test_drift_alert: HTML sem NUXT → capture_logs mostra alert='schema_drift'.
       test_upsert_no_duplicates: upsert mesmo produto 2x com ranks diferentes → 1 row, rank atualizado.
       test_is_stale: padrão idêntico aos scanners anteriores com BRAIP_PLATFORM_ID=7.
  </action>
  <verify>
    <automated>
      python -c "from pathlib import Path; c=Path('mis/tests/fixtures/braip/catalog_cursos-online.html').read_text(encoding='utf-8'); assert 'window.__NUXT__' in c or 'window.__NUXT_DATA__' in c, 'Fixture invalida — NUXT data ausente'; print('Fixture OK')"
      python -m pytest mis/tests/test_braip_scanner.py -x -q
    </automated>
  </verify>
  <done>
    Fixture mis/tests/fixtures/braip/catalog_cursos-online.html existe e contém window.__NUXT__ ou window.__NUXT_DATA__. 6 testes GREEN. BraipScanner retorna produtos com hash como external_id e price=float. alert='schema_drift' emitido quando NUXT ausente. BraipScanner em SCANNER_MAP. config.yaml atualizado com slugs Braip e entradas null explícitas para eduzz/monetizze/perfectpay nos 3 nichos.
  </done>
</task>

</tasks>

<verification>
Suite completa do plano 14-02:

```bash
python -m pytest mis/tests/test_perfectpay_scanner.py mis/tests/test_braip_scanner.py -v -q
```

Esperado: 12 testes GREEN (6 perfectpay + 6 braip).

Suite completa da Phase 14 (todos os planos):
```bash
python -m pytest mis/tests/test_migration_007.py mis/tests/test_eduzz_scanner.py mis/tests/test_monetizze_scanner.py mis/tests/test_perfectpay_scanner.py mis/tests/test_braip_scanner.py -v -q
```

Esperado: 29 testes GREEN.

Verificação de integração — todos os 4 scanners no SCANNER_MAP:
```bash
python -c "
from mis.scanner import SCANNER_MAP
required = {'eduzz', 'monetizze', 'perfectpay', 'braip'}
missing = required - set(SCANNER_MAP.keys())
print('OK' if not missing else f'MISSING: {missing}')
"
```

Verificação de fixture Braip:
```bash
python -c "
from pathlib import Path
fixture = Path('mis/tests/fixtures/braip/catalog_cursos-online.html')
assert fixture.exists(), 'Fixture não encontrada'
content = fixture.read_text(encoding='utf-8')
assert 'window.__NUXT__' in content or 'window.__NUXT_DATA__' in content, 'NUXT data não encontrada na fixture'
print('Fixture Braip OK')
"
```

Verificação de config.yaml — entradas BR completas:
```bash
python -c "
import yaml
from pathlib import Path
cfg = yaml.safe_load(Path('mis/config.yaml').read_text())
br_platforms = {'braip', 'perfectpay', 'eduzz', 'monetizze'}
for niche in cfg.get('niches', []):
    platforms = niche.get('platforms', {})
    missing = br_platforms - set(platforms.keys())
    print(f\"{niche['slug']}: {'OK' if not missing else 'MISSING: ' + str(missing)}\")
"
```
</verification>

<success_criteria>
- [ ] mis/scanners/perfectpay.py existe e emite alert='marketplace_unavailable'
- [ ] mis/scanners/braip.py existe e parseia window.__NUXT__ corretamente
- [ ] mis/tests/fixtures/braip/catalog_cursos-online.html existe com window.__NUXT__ ou window.__NUXT_DATA__
- [ ] PerfectPayScanner e BraipScanner registrados em SCANNER_MAP
- [ ] config.yaml tem slugs braip para os 3 nichos
- [ ] config.yaml tem entradas explícitas eduzz: null, monetizze: null, perfectpay: null nos 3 nichos
- [ ] 12 testes GREEN (6 perfectpay + 6 braip)
- [ ] Suite completa 14-01 + 14-02 = 29 testes GREEN
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/14-br-scanners/14-02-SUMMARY.md`
</output>
